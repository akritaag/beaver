{
  "dw_104": [
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE repeats ~166.7 rows per value (10000 rows, 60 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~166.7 times after this join.",
    "- Join CIS_COURSE_CATALOG.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: CIS_COURSE_CATALOG.DEPARTMENT_CODE repeats ~212.8 rows per value (10000 rows, 47 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~212.8 times after this join.",
    "- \"subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subject title\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"Mathematics department\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE: already repeats ~166.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"Mathematics department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"Course 18\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE: already repeats ~166.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"academic year 2022\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"printed in the bulletin\" -> COURSE_CATALOG_SUBJECT_OFFERED.IS_PRINTED_IN_BULLETIN: already repeats ~3333.3 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows."
  ],
  "dw_1133": [
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE repeats ~166.7 rows per value (10000 rows, 60 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~166.7 times after this join.",
    "- \"department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"department\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"Mathematics (Course 18)\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics (Course 18), use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"Mathematics (Course 18)\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics (Course 18), use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"subject grade type\" -> COURSE_CATALOG_SUBJECT_OFFERED.GRADE_TYPE: already repeats ~5000.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"grade type\" -> COURSE_CATALOG_SUBJECT_OFFERED.GRADE_TYPE: already repeats ~5000.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"letter graded\" -> COURSE_CATALOG_SUBJECT_OFFERED.GRADE_TYPE: already repeats ~5000.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subject is offered in the current academic year\" -> COURSE_CATALOG_SUBJECT_OFFERED.IS_OFFERED_THIS_YEAR: already repeats ~5000.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"number of such subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"minimum total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"top 10 departments by number of subjects offered in the 2022 academic year\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"top 10 departments by number of subjects offered in the 2022 academic year\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct top 10 departments by number of subjects offered in the 2022 academic year, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"top 10 departments by number of subjects offered in the 2022 academic year\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"excluding Political Science\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct excluding Political Science, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"degree-granting departments\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct degree-granting departments, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"number of subjects offered by that department in 2022\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"number of subjects offered by that department in 2022\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows."
  ],
  "dw_1191": [
    "- Join CIS_COURSE_CATALOG.SUBJECT_ID = COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: CIS_COURSE_CATALOG.SUBJECT_ID repeats ~4.4 rows per value (10000 rows, 2269 values); COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID repeats ~2.0 rows per value (10000 rows, 4969 values).",
    "  -> each CIS_COURSE_CATALOG row appears ~2.0 times after this join.",
    "  -> each COURSE_CATALOG_SUBJECT_OFFERED row appears ~4.4 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SIS_SUBJECT_CODE.DEPARTMENT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SIS_SUBJECT_CODE.DEPARTMENT_CODE repeats ~3.6 rows per value (221 rows, 61 values).",
    "  -> each SIS_DEPARTMENT row appears ~3.6 times after this join.",
    "- \"subjects offered in the academic year 2024\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered in the academic year 2024, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR).",
    "- \"CIS course catalog\" -> CIS_COURSE_CATALOG.SUBJECT_ID: already repeats ~4.4 rows per value in its own table; the hinted joins multiply CIS_COURSE_CATALOG rows ~x2.0, so a plain COUNT over the joined result counts duplicates. To count distinct CIS course catalog, use COUNT(DISTINCT CIS_COURSE_CATALOG.SUBJECT_ID).",
    "- \"Mathematics department (Course 18)\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics department (Course 18), use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME).",
    "- \"Mathematics department (Course 18)\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE: already repeats ~166.7 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics department (Course 18), use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE).",
    "- \"Mathematics department (Course 18)\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x3.6, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics department (Course 18), use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"Mathematics department (Course 18)\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x3.6, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics department (Course 18), use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"subject ID\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct subject ID, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID).",
    "- \"subject code\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE: already repeats ~181.8 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct subject code, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE).",
    "- \"subject code\" -> SIS_SUBJECT_CODE.SUBJECT_CODE: unique per row in its own table; the hinted joins do not multiply SIS_SUBJECT_CODE rows.",
    "- \"subject number\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_NUMBER: already repeats ~6.9 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct subject number, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_NUMBER).",
    "- \"department name\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME).",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x3.6, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"subject short title\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_SHORT_TITLE: already repeats ~2.6 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct subject short title, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_SHORT_TITLE).",
    "- \"subject title\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.7 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct subject title, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_TITLE).",
    "- \"lecture units\" -> COURSE_CATALOG_SUBJECT_OFFERED.LECTURE_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~4.4 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"lab units\" -> COURSE_CATALOG_SUBJECT_OFFERED.LAB_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~4.4 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~4.4 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"grade type\" -> COURSE_CATALOG_SUBJECT_OFFERED.GRADE_TYPE: already repeats ~5000.0 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct grade type, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.GRADE_TYPE).",
    "- \"grade rule\" -> COURSE_CATALOG_SUBJECT_OFFERED.GRADE_RULE: already repeats ~2500.0 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct grade rule, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.GRADE_RULE).",
    "- \"minimum total units value over each department code group\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~4.4 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"minimum total units value over each department code group\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE: already repeats ~166.7 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct minimum total units value over each department code group, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE).",
    "- \"number of subject codes for each department and subject code where the department is degree granting and is Mathematics (Course 18)\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x3.6, so a plain COUNT over the joined result counts duplicates. To count distinct number of subject codes for each department and subject code where the department is degree granting and is Mathematics (Course 18), use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"number of subject codes for each department and subject code where the department is degree granting and is Mathematics (Course 18)\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x3.6, so a plain COUNT over the joined result counts duplicates. To count distinct number of subject codes for each department and subject code where the department is degree granting and is Mathematics (Course 18), use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"number of subject codes for each department and subject code where the department is degree granting and is Mathematics (Course 18)\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x3.6, so a plain COUNT over the joined result counts duplicates. To count distinct number of subject codes for each department and subject code where the department is degree granting and is Mathematics (Course 18), use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"number of subject codes for each department and subject code where the department is degree granting and is Mathematics (Course 18)\" -> SIS_SUBJECT_CODE.SUBJECT_CODE: unique per row in its own table; the hinted joins do not multiply SIS_SUBJECT_CODE rows.",
    "- \"minimum department budget code\" -> SIS_DEPARTMENT.DEPT_BUDGET_CODE: already repeats ~2.2 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x3.6, so a plain COUNT over the joined result counts duplicates. To count distinct minimum department budget code, use COUNT(DISTINCT SIS_DEPARTMENT.DEPT_BUDGET_CODE).",
    "- \"ordered by department code, subject code, and subject number\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE: already repeats ~166.7 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct ordered by department code, subject code, and subject number, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE).",
    "- \"ordered by department code, subject code, and subject number\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE: already repeats ~181.8 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct ordered by department code, subject code, and subject number, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE).",
    "- \"ordered by department code, subject code, and subject number\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_NUMBER: already repeats ~6.9 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct ordered by department code, subject code, and subject number, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_NUMBER)."
  ],
  "dw_1207": [
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~105.3 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = LIBRARY_RESERVE_MATRL_DETAIL.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); LIBRARY_RESERVE_MATRL_DETAIL.TERM_CODE repeats ~1250.0 rows per value (10000 rows, 8 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~1250.0 times after this join.",
    "- \"academic year\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x1250.0, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR).",
    "- \"academic year\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x1250.0, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC).",
    "- \"academic year description\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x1250.0, so a plain COUNT over the joined result counts duplicates. To count distinct academic year description, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC).",
    "- \"coefficient of variation of the total units for subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"Mathematics department\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"coefficient of variation of the total units for all library reserve materials\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY: already repeats ~2000.0 rows per value in its own table; the hinted joins do not multiply LIBRARY_RESERVE_MATRL_DETAIL rows.",
    "- \"coefficient of variation of the total units for all library reserve materials\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"material status 'U'\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY: already repeats ~2000.0 rows per value in its own table; the hinted joins do not multiply LIBRARY_RESERVE_MATRL_DETAIL rows."
  ],
  "dw_140": [
    "- Join ACADEMIC_TERMS.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.EFFECTIVE_TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.EFFECTIVE_TERM_CODE repeats ~200.0 rows per value (10000 rows, 50 values).",
    "  -> each ACADEMIC_TERMS row appears ~200.0 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = SUBJECT_SUMMARY.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); SUBJECT_SUMMARY.TERM_CODE repeats ~83.3 rows per value (10000 rows, 120 values).",
    "  -> each ACADEMIC_TERMS row appears ~83.3 times after this join.",
    "- \"department\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"department\" -> SUBJECT_SUMMARY.DEPARTMENT_NAME: already repeats ~181.8 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x200.0, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR).",
    "- \"subject enrollment number\" -> SUBJECT_SUMMARY.SUBJECT_ENROLLMENT_NUMBER: already repeats ~57.8 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"Mathematics\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"number of unique subjects\" -> SUBJECT_SUMMARY.SUBJECT_ID: already repeats ~1.9 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"minimum total units offered\" -> SUBJECT_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_SUMMARY; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"latest term end date\" -> ACADEMIC_TERMS.TERM_END_DATE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x200.0, so a plain COUNT over the joined result counts duplicates. To count distinct latest term end date, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_END_DATE)."
  ],
  "dw_1414": [
    "- Join SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE is unique per row (179 rows); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "- \"degree-granting department\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins do not multiply SIS_DEPARTMENT rows.",
    "- \"department budget code\" -> SIS_DEPARTMENT.DEPT_BUDGET_CODE: already repeats ~2.2 rows per value in its own table; the hinted joins do not multiply SIS_DEPARTMENT rows.",
    "- \"Mathematics\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins do not multiply SIS_DEPARTMENT rows.",
    "- \"Physics\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins do not multiply SIS_DEPARTMENT rows.",
    "- \"total clearing cost collector\" -> SIS_ADMIN_DEPARTMENT.CLEARING_COST_COLLECTOR: already repeats ~3.7 rows per value in its own table; the hinted joins do not multiply SIS_ADMIN_DEPARTMENT rows.",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins do not multiply SIS_DEPARTMENT rows.",
    "- \"school name\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins do not multiply SIS_DEPARTMENT rows."
  ],
  "dw_1498": [
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SIS_SUBJECT_CODE.DEPARTMENT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SIS_SUBJECT_CODE.DEPARTMENT_CODE repeats ~3.6 rows per value (221 rows, 61 values).",
    "  -> each SIS_DEPARTMENT row appears ~3.6 times after this join.",
    "- Join MIT_STUDENT_DIRECTORY.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: MIT_STUDENT_DIRECTORY.DEPARTMENT repeats ~204.1 rows per value (10000 rows, 49 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~204.1 times after this join.",
    "- Join SIS_COURSE_DESCRIPTION.COURSE = SIS_SUBJECT_CODE.COURSE_NUMBER: SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values); SIS_SUBJECT_CODE.COURSE_NUMBER is unique per row (221 rows).",
    "  -> each SIS_SUBJECT_CODE row appears ~1.1 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE = SIS_COURSE_DESCRIPTION.COURSE: COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE repeats ~181.8 rows per value (10000 rows, 55 values); SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values).",
    "  -> each COURSE_CATALOG_SUBJECT_OFFERED row appears ~1.1 times after this join.",
    "  -> each SIS_COURSE_DESCRIPTION row appears ~181.8 times after this join.",
    "- \"subject code\" -> SIS_SUBJECT_CODE.SUBJECT_CODE: unique per row in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct subject code, use COUNT(DISTINCT SIS_SUBJECT_CODE.SUBJECT_CODE).",
    "- \"subject code description\" -> SIS_SUBJECT_CODE.SUBJECT_CODE_DESC: already repeats ~1.2 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct subject code description, use COUNT(DISTINCT SIS_SUBJECT_CODE.SUBJECT_CODE_DESC).",
    "- \"Political Science department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct Political Science department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"degree-granting\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct degree-granting, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"degree-granting\" -> SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING: already repeats ~347.5 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct degree-granting, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING).",
    "- \"number of students in each degree-granting department\" -> MIT_STUDENT_DIRECTORY.DEPARTMENT: already repeats ~204.1 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"total number of students\" -> MIT_STUDENT_DIRECTORY.DEPARTMENT: already repeats ~204.1 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"department name\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME).",
    "- \"school name\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_DEPARTMENT.SCHOOL_NAME).",
    "- \"average of the total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~1.1 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"range of the total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~1.1 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"variance of the total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~1.1 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"subjects offered in the 2022 academic year\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered in the 2022 academic year, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR).",
    "- \"excluding 'Mathematics' departments\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct excluding 'Mathematics' departments, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME).",
    "- \"excluding 'Mathematics' departments\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT: already repeats ~9.7 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct excluding 'Mathematics' departments, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT).",
    "- \"department phone number\" -> SIS_ADMIN_DEPARTMENT.DEPARTMENT_PHONE_NUMBER: already repeats ~2.8 rows per value in its own table; the hinted joins do not multiply SIS_ADMIN_DEPARTMENT rows."
  ],
  "dw_1566": [
    "- Join FCLT_BUILDING_HIST.FCLT_BUILDING_KEY = FCLT_ROOMS.FCLT_BUILDING_KEY: FCLT_BUILDING_HIST.FCLT_BUILDING_KEY repeats ~39.2 rows per value (10000 rows, 255 values); FCLT_ROOMS.FCLT_BUILDING_KEY repeats ~49.5 rows per value (10000 rows, 202 values).",
    "  -> each FCLT_BUILDING_HIST row appears ~49.5 times after this join.",
    "  -> each FCLT_ROOMS row appears ~39.2 times after this join.",
    "- Join FCLT_BUILDING.FCLT_BUILDING_KEY = FCLT_BUILDING_HIST.FCLT_BUILDING_KEY: FCLT_BUILDING.FCLT_BUILDING_KEY is unique per row (242 rows); FCLT_BUILDING_HIST.FCLT_BUILDING_KEY repeats ~39.2 rows per value (10000 rows, 255 values).",
    "  -> each FCLT_BUILDING row appears ~39.2 times after this join.",
    "- \"academic building\" -> FCLT_BUILDING.BUILDING_TYPE: already repeats ~80.7 rows per value in its own table; the hinted joins multiply FCLT_BUILDING rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct academic building, use COUNT(DISTINCT FCLT_BUILDING.BUILDING_TYPE).",
    "- \"owned by MIT\" -> FCLT_BUILDING.OWNERSHIP_TYPE: already repeats ~121.0 rows per value in its own table; the hinted joins multiply FCLT_BUILDING rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct owned by MIT, use COUNT(DISTINCT FCLT_BUILDING.OWNERSHIP_TYPE).",
    "- \"owned by MIT\" -> FCLT_BUILDING.SITE: already repeats ~24.2 rows per value in its own table; the hinted joins multiply FCLT_BUILDING rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct owned by MIT, use COUNT(DISTINCT FCLT_BUILDING.SITE).",
    "- \"total area of rooms\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins multiply FCLT_ROOMS rows ~x39.2, so SUM/AVG over the joined result would count each FCLT_ROOMS row ~39.2 times. Aggregate it at FCLT_ROOMS's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"rooms used for \"RESIDENT\" or \"LABS\" purposes\" -> FCLT_ROOMS.MAJOR_USE_DESC: already repeats ~769.2 rows per value in its own table; the hinted joins multiply FCLT_ROOMS rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct rooms used for \"RESIDENT\" or \"LABS\" purposes, use COUNT(DISTINCT FCLT_ROOMS.MAJOR_USE_DESC).",
    "- \"building name\" -> FCLT_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins multiply FCLT_BUILDING rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct building name, use COUNT(DISTINCT FCLT_BUILDING.BUILDING_NAME).",
    "- \"fiscal period\" -> FCLT_BUILDING_HIST.FISCAL_PERIOD: already repeats ~208.3 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_HIST rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct fiscal period, use COUNT(DISTINCT FCLT_BUILDING_HIST.FISCAL_PERIOD).",
    "- \"running sum of assignable area within each campus sector\" -> FCLT_BUILDING_HIST.ASSIGNABLE_AREA is a numeric measure of FCLT_BUILDING_HIST; the hinted joins multiply FCLT_BUILDING_HIST rows ~x49.5, so SUM/AVG over the joined result would count each FCLT_BUILDING_HIST row ~49.5 times. Aggregate it at FCLT_BUILDING_HIST's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"running sum of assignable area within each campus sector\" -> FCLT_BUILDING.CAMPUS_SECTOR: already repeats ~30.2 rows per value in its own table; the hinted joins multiply FCLT_BUILDING rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct running sum of assignable area within each campus sector, use COUNT(DISTINCT FCLT_BUILDING.CAMPUS_SECTOR).",
    "- \"building's external gross area\" -> FCLT_BUILDING.EXT_GROSS_AREA is a numeric measure of FCLT_BUILDING; the hinted joins multiply FCLT_BUILDING rows ~x39.2, so SUM/AVG over the joined result would count each FCLT_BUILDING row ~39.2 times. Aggregate it at FCLT_BUILDING's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"not the Stata building\" -> FCLT_BUILDING.FCLT_BUILDING_KEY: unique per row in its own table; the hinted joins multiply FCLT_BUILDING rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct not the Stata building, use COUNT(DISTINCT FCLT_BUILDING.FCLT_BUILDING_KEY).",
    "- \"order by fiscal period and building name\" -> FCLT_BUILDING_HIST.FISCAL_PERIOD: already repeats ~208.3 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_HIST rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct order by fiscal period and building name, use COUNT(DISTINCT FCLT_BUILDING_HIST.FISCAL_PERIOD).",
    "- \"order by fiscal period and building name\" -> FCLT_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins multiply FCLT_BUILDING rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct order by fiscal period and building name, use COUNT(DISTINCT FCLT_BUILDING.BUILDING_NAME)."
  ],
  "dw_1570": [
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SIS_SUBJECT_CODE.DEPARTMENT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SIS_SUBJECT_CODE.DEPARTMENT_CODE repeats ~3.6 rows per value (221 rows, 61 values).",
    "  -> each SIS_DEPARTMENT row appears ~3.6 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE repeats ~196.1 rows per value (10000 rows, 51 values).",
    "  -> each SIS_DEPARTMENT row appears ~196.1 times after this join.",
    "- Join SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE is unique per row (179 rows); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "- Join CIS_COURSE_CATALOG.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: CIS_COURSE_CATALOG.DEPARTMENT_CODE repeats ~212.8 rows per value (10000 rows, 47 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~212.8 times after this join.",
    "- \"departments\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct departments, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"departments\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct departments, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"Mathematics department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"total number of enrolled students\" -> SUBJECT_OFFERED_SUMMARY.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"average total enrolled students across all departments\" -> SUBJECT_OFFERED_SUMMARY.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"average total enrolled students across all departments\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct average total enrolled students across all departments, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"degree-granting departments\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct degree-granting departments, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"Electrical Engineering and Computer Science\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct Electrical Engineering and Computer Science, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"courses listed in the course catalog\" -> CIS_COURSE_CATALOG.DEPARTMENT_CODE: already repeats ~212.8 rows per value in its own table; the hinted joins do not multiply CIS_COURSE_CATALOG rows.",
    "- \"unique course titles\" -> CIS_COURSE_CATALOG.SUBJECT_TITLE: already repeats ~6.5 rows per value in its own table; the hinted joins do not multiply CIS_COURSE_CATALOG rows.",
    "- \"department code\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct department code, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME)."
  ],
  "dw_1582": [
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE repeats ~166.7 rows per value (10000 rows, 60 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~166.7 times after this join.",
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- Join CIS_COURSE_CATALOG.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: CIS_COURSE_CATALOG.DEPARTMENT_CODE repeats ~212.8 rows per value (10000 rows, 47 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~212.8 times after this join.",
    "- \"subjects offered in the academic year 2024\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"total of 12 units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"Mathematics department (Course 18)\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subject code\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE: already repeats ~181.8 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subject title\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"department name\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"school name\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_DEPARTMENT.SCHOOL_NAME).",
    "- \"number of such subjects offered in that school\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct number of such subjects offered in that school, use COUNT(DISTINCT SIS_DEPARTMENT.SCHOOL_NAME).",
    "- \"number of such subjects offered in that school\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE: already repeats ~181.8 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"variance of the total units of courses offered by that department\" -> CIS_COURSE_CATALOG.TOTAL_UNITS is a numeric measure of CIS_COURSE_CATALOG; the hinted joins do not multiply CIS_COURSE_CATALOG rows.",
    "- \"variance of the total units of courses offered by that department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct variance of the total units of courses offered by that department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"standard deviation of the total units of courses offered by that department\" -> CIS_COURSE_CATALOG.TOTAL_UNITS is a numeric measure of CIS_COURSE_CATALOG; the hinted joins do not multiply CIS_COURSE_CATALOG rows.",
    "- \"standard deviation of the total units of courses offered by that department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct standard deviation of the total units of courses offered by that department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"degree-granting departments\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct degree-granting departments, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"courses with a non-null total units value\" -> CIS_COURSE_CATALOG.TOTAL_UNITS is a numeric measure of CIS_COURSE_CATALOG; the hinted joins do not multiply CIS_COURSE_CATALOG rows."
  ],
  "dw_1616": [
    "- Join LIBRARY_SUBJECT_OFFERED.TERM_CODE = TIP_SUBJECT_OFFERED.TERM_CODE: LIBRARY_SUBJECT_OFFERED.TERM_CODE repeats ~192.3 rows per value (10000 rows, 52 values); TIP_SUBJECT_OFFERED.TERM_CODE repeats ~79.4 rows per value (10000 rows, 126 values).",
    "  -> each LIBRARY_SUBJECT_OFFERED row appears ~79.4 times after this join.",
    "  -> each TIP_SUBJECT_OFFERED row appears ~192.3 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = IAP_SUBJECT_DETAIL.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); IAP_SUBJECT_DETAIL.TERM_CODE repeats ~465.0 rows per value (465 rows, 1 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~465.0 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~105.3 times after this join.",
    "- \"subjects offered in the term '2014FA'\" -> LIBRARY_SUBJECT_OFFERED.TERM_CODE: already repeats ~192.3 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered in the term '2014FA', use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.TERM_CODE).",
    "- \"subjects offered in the term '2014FA'\" -> TIP_SUBJECT_OFFERED.TERM_CODE: already repeats ~79.4 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered in the term '2014FA', use COUNT(DISTINCT TIP_SUBJECT_OFFERED.TERM_CODE).",
    "- \"Chemistry department\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct Chemistry department, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"subject title from the library\" -> LIBRARY_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~5.6 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct subject title from the library, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.SUBJECT_TITLE).",
    "- \"number of enrolled students in the library subject\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x79.4, so SUM/AVG over the joined result would count each LIBRARY_SUBJECT_OFFERED row ~79.4 times. Aggregate it at LIBRARY_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"responsible faculty name from the TIP subject\" -> TIP_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_NAME: already repeats ~4.1 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct responsible faculty name from the TIP subject, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_NAME).",
    "- \"offer department name from the TIP subject\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct offer department name from the TIP subject, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"window of 3 preceding and 1 following row ordered by the TIP subject's responsible faculty MIT ID\" -> TIP_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_MIT_ID: already repeats ~4.1 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct window of 3 preceding and 1 following row ordered by the TIP subject's responsible faculty MIT ID, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_MIT_ID).",
    "- \"minimum number of enrolled students in the library subject within window\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x79.4, so SUM/AVG over the joined result would count each LIBRARY_SUBJECT_OFFERED row ~79.4 times. Aggregate it at LIBRARY_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"total number of Chemistry subjects offered in '2014FA'\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct total number of Chemistry subjects offered in '2014FA', use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"total number of Chemistry subjects offered in '2014FA'\" -> TIP_SUBJECT_OFFERED.TERM_CODE: already repeats ~79.4 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct total number of Chemistry subjects offered in '2014FA', use COUNT(DISTINCT TIP_SUBJECT_OFFERED.TERM_CODE).",
    "- \"academic year\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x465.0, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR).",
    "- \"department name\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"academic year description\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x465.0, so a plain COUNT over the joined result counts duplicates. To count distinct academic year description, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC).",
    "- \"maximum enrollment for IAP activities\" -> IAP_SUBJECT_DETAIL.MAX_ENROLLMENT is a numeric measure of IAP_SUBJECT_DETAIL; the hinted joins do not multiply IAP_SUBJECT_DETAIL rows.",
    "- \"IAP activities\" -> IAP_SUBJECT_DETAIL.MAX_ENROLLMENT is a numeric measure of IAP_SUBJECT_DETAIL; the hinted joins do not multiply IAP_SUBJECT_DETAIL rows.",
    "- \"Mathematics department\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"advance sign-up required\" -> IAP_SUBJECT_DETAIL.ENROLLMENT_TYPE: already repeats ~116.2 rows per value in its own table; the hinted joins do not multiply IAP_SUBJECT_DETAIL rows.",
    "- \"not cancelled\" -> IAP_SUBJECT_DETAIL.IS_CANCELLED: already repeats ~232.5 rows per value in its own table; the hinted joins do not multiply IAP_SUBJECT_DETAIL rows."
  ],
  "dw_1639": [
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~105.3 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = ACADEMIC_TERM_PARAMETER.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); ACADEMIC_TERM_PARAMETER.TERM_CODE is unique per row (3 rows).",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE repeats ~196.1 rows per value (10000 rows, 51 values).",
    "  -> each SIS_DEPARTMENT row appears ~196.1 times after this join.",
    "- Join CIS_COURSE_CATALOG.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: CIS_COURSE_CATALOG.DEPARTMENT_CODE repeats ~212.8 rows per value (10000 rows, 47 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~212.8 times after this join.",
    "- \"academic year\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR).",
    "- \"academic year description\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct academic year description, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC).",
    "- \"department name\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"total coefficient of variation\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"current term\" -> ACADEMIC_TERM_PARAMETER.IS_CURRENT_TERM: already repeats ~1.5 rows per value in its own table; the hinted joins do not multiply ACADEMIC_TERM_PARAMETER rows.",
    "- \"total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"total units\" -> CIS_COURSE_CATALOG.TOTAL_UNITS is a numeric measure of CIS_COURSE_CATALOG; the hinted joins do not multiply CIS_COURSE_CATALOG rows.",
    "- \"degree-granting departments\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct degree-granting departments, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"Chemistry (Course 5)\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct Chemistry (Course 5), use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"average total units\" -> CIS_COURSE_CATALOG.TOTAL_UNITS is a numeric measure of CIS_COURSE_CATALOG; the hinted joins do not multiply CIS_COURSE_CATALOG rows.",
    "- \"range\" -> CIS_COURSE_CATALOG.TOTAL_UNITS is a numeric measure of CIS_COURSE_CATALOG; the hinted joins do not multiply CIS_COURSE_CATALOG rows.",
    "- \"variance\" -> CIS_COURSE_CATALOG.TOTAL_UNITS is a numeric measure of CIS_COURSE_CATALOG; the hinted joins do not multiply CIS_COURSE_CATALOG rows.",
    "- \"2008 academic year\" -> CIS_COURSE_CATALOG.ACADEMIC_YEAR: already repeats ~1250.0 rows per value in its own table; the hinted joins do not multiply CIS_COURSE_CATALOG rows."
  ],
  "dw_1679": [
    "- Join SIS_COURSE_DESCRIPTION.(COURSE,COURSE) = SIS_SUBJECT_CODE.(SUBJECT_CODE,COURSE_NUMBER): SIS_COURSE_DESCRIPTION.(COURSE,COURSE) repeats ~1.1 rows per value (695 rows, 630 values); SIS_SUBJECT_CODE.(SUBJECT_CODE,COURSE_NUMBER) is unique per row (221 rows).",
    "  -> each SIS_SUBJECT_CODE row appears ~1.1 times after this join.",
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE repeats ~166.7 rows per value (10000 rows, 60 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~166.7 times after this join.",
    "- \"school name\" -> SIS_SUBJECT_CODE.SCHOOL_NAME: already repeats ~24.6 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_SUBJECT_CODE.SCHOOL_NAME).",
    "- \"school name\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_DEPARTMENT.SCHOOL_NAME).",
    "- \"department name\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"variance of the CIP program code\" -> SIS_COURSE_DESCRIPTION.CIP_PROGRAM_CODE: already repeats ~10.9 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"degree-granting courses\" -> SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING: already repeats ~347.5 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"Mathematics department (Course 18)\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subjects offered by departments in that school\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE: already repeats ~166.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subjects offered by departments in that school\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered by departments in that school, use COUNT(DISTINCT SIS_DEPARTMENT.SCHOOL_NAME).",
    "- \"offered this year\" -> COURSE_CATALOG_SUBJECT_OFFERED.IS_OFFERED_THIS_YEAR: already repeats ~5000.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"departments that are degree-granting\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct departments that are degree-granting, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"Department of Management\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE: already repeats ~166.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"geometric mean\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"overall average geometric mean\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows."
  ],
  "dw_1737": [
    "- Join ACADEMIC_TERMS.TERM_CODE = SUBJECT_SUMMARY.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); SUBJECT_SUMMARY.TERM_CODE repeats ~83.3 rows per value (10000 rows, 120 values).",
    "  -> each ACADEMIC_TERMS row appears ~83.3 times after this join.",
    "- Join LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER = SIS_SUBJECT_CODE.COURSE_NUMBER: LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER repeats ~625.0 rows per value (10000 rows, 16 values); SIS_SUBJECT_CODE.COURSE_NUMBER is unique per row (221 rows).",
    "  -> each SIS_SUBJECT_CODE row appears ~625.0 times after this join.",
    "- \"department\" -> SUBJECT_SUMMARY.DEPARTMENT_NAME: already repeats ~181.8 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"department\" -> SIS_SUBJECT_CODE.DEPARTMENT_NAME: already repeats ~3.7 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x625.0, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SIS_SUBJECT_CODE.DEPARTMENT_NAME).",
    "- \"department name\" -> SUBJECT_SUMMARY.DEPARTMENT_NAME: already repeats ~181.8 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"department name\" -> SIS_SUBJECT_CODE.DEPARTMENT_NAME: already repeats ~3.7 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x625.0, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_SUBJECT_CODE.DEPARTMENT_NAME).",
    "- \"term code\" -> ACADEMIC_TERMS.TERM_CODE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x83.3, so a plain COUNT over the joined result counts duplicates. To count distinct term code, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_CODE).",
    "- \"term code\" -> LIBRARY_SUBJECT_OFFERED.TERM_CODE: already repeats ~192.3 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"2019FA\" -> ACADEMIC_TERMS.TERM_CODE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x83.3, so a plain COUNT over the joined result counts duplicates. To count distinct 2019FA, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_CODE).",
    "- \"Mathematics\" -> SUBJECT_SUMMARY.DEPARTMENT_NAME: already repeats ~181.8 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"Mathematics\" -> SIS_SUBJECT_CODE.DEPARTMENT_NAME: already repeats ~3.7 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x625.0, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics, use COUNT(DISTINCT SIS_SUBJECT_CODE.DEPARTMENT_NAME).",
    "- \"subjects\" -> SUBJECT_SUMMARY.SUBJECT_ID: already repeats ~1.9 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"total number of subjects\" -> SUBJECT_SUMMARY.SUBJECT_ID: already repeats ~1.9 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"total units\" -> SUBJECT_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_SUMMARY; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"average total units\" -> SUBJECT_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_SUMMARY; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"average number of enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"highest average number of enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows."
  ],
  "dw_1795": [
    "- Join SIS_COURSE_DESCRIPTION.COURSE = SIS_SUBJECT_CODE.COURSE_NUMBER: SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values); SIS_SUBJECT_CODE.COURSE_NUMBER is unique per row (221 rows).",
    "  -> each SIS_SUBJECT_CODE row appears ~1.1 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE = SIS_COURSE_DESCRIPTION.COURSE: COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE repeats ~181.8 rows per value (10000 rows, 55 values); SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values).",
    "  -> each COURSE_CATALOG_SUBJECT_OFFERED row appears ~1.1 times after this join.",
    "  -> each SIS_COURSE_DESCRIPTION row appears ~181.8 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each SIS_DEPARTMENT row appears ~161.3 times after this join.",
    "- Join STUDENT_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: STUDENT_DEPARTMENT.DEPARTMENT_CODE is unique per row (79 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each STUDENT_DEPARTMENT row appears ~161.3 times after this join.",
    "- \"school name\" -> SIS_COURSE_DESCRIPTION.SCHOOL_NAME: already repeats ~77.2 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.SCHOOL_NAME).",
    "- \"school name\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_DEPARTMENT.SCHOOL_NAME).",
    "- \"department name\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME).",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"average total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~1.1 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"total units range\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~1.1 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"total units variance\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~1.1 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"degree-granting subjects\" -> SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING: already repeats ~347.5 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct degree-granting subjects, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING).",
    "- \"degree-granting subjects\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct degree-granting subjects, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"2022 academic year\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct 2022 academic year, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR).",
    "- \"Mathematics department (Course 18)\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics department (Course 18), use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME).",
    "- \"Mathematics department (Course 18)\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT: already repeats ~9.7 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics department (Course 18), use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT).",
    "- \"School of Engineering\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct School of Engineering, use COUNT(DISTINCT SIS_DEPARTMENT.SCHOOL_NAME).",
    "- \"Management\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct Management, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"total student enrollment\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"most popular course\" -> TIP_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"course enrollment\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows."
  ],
  "dw_1848": [
    "- Join FCLT_BUILDING_HIST.FCLT_BUILDING_KEY = FCLT_ROOMS.FCLT_BUILDING_KEY: FCLT_BUILDING_HIST.FCLT_BUILDING_KEY repeats ~39.2 rows per value (10000 rows, 255 values); FCLT_ROOMS.FCLT_BUILDING_KEY repeats ~49.5 rows per value (10000 rows, 202 values).",
    "  -> each FCLT_BUILDING_HIST row appears ~49.5 times after this join.",
    "  -> each FCLT_ROOMS row appears ~39.2 times after this join.",
    "- Join FCLT_BUILDING.FCLT_BUILDING_KEY = FCLT_BUILDING_HIST.FCLT_BUILDING_KEY: FCLT_BUILDING.FCLT_BUILDING_KEY is unique per row (242 rows); FCLT_BUILDING_HIST.FCLT_BUILDING_KEY repeats ~39.2 rows per value (10000 rows, 255 values).",
    "  -> each FCLT_BUILDING row appears ~39.2 times after this join.",
    "- \"academic building\" -> FCLT_BUILDING.BUILDING_TYPE: already repeats ~80.7 rows per value in its own table; the hinted joins multiply FCLT_BUILDING rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct academic building, use COUNT(DISTINCT FCLT_BUILDING.BUILDING_TYPE).",
    "- \"owned by MIT\" -> FCLT_BUILDING.OWNERSHIP_TYPE: already repeats ~121.0 rows per value in its own table; the hinted joins multiply FCLT_BUILDING rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct owned by MIT, use COUNT(DISTINCT FCLT_BUILDING.OWNERSHIP_TYPE).",
    "- \"owned by MIT\" -> FCLT_BUILDING.SITE: already repeats ~24.2 rows per value in its own table; the hinted joins multiply FCLT_BUILDING rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct owned by MIT, use COUNT(DISTINCT FCLT_BUILDING.SITE).",
    "- \"excluding the Stata building\" -> FCLT_BUILDING.FCLT_BUILDING_KEY: unique per row in its own table; the hinted joins multiply FCLT_BUILDING rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct excluding the Stata building, use COUNT(DISTINCT FCLT_BUILDING.FCLT_BUILDING_KEY).",
    "- \"building name\" -> FCLT_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins multiply FCLT_BUILDING rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct building name, use COUNT(DISTINCT FCLT_BUILDING.BUILDING_NAME).",
    "- \"fiscal period\" -> FCLT_BUILDING_HIST.FISCAL_PERIOD: already repeats ~208.3 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_HIST rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct fiscal period, use COUNT(DISTINCT FCLT_BUILDING_HIST.FISCAL_PERIOD).",
    "- \"total area of rooms used for \"RESIDENT\" or \"LABS\" purposes\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins multiply FCLT_ROOMS rows ~x39.2, so SUM/AVG over the joined result would count each FCLT_ROOMS row ~39.2 times. Aggregate it at FCLT_ROOMS's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"total area of rooms used for \"RESIDENT\" or \"LABS\" purposes\" -> FCLT_ROOMS.MAJOR_USE_DESC: already repeats ~769.2 rows per value in its own table; the hinted joins multiply FCLT_ROOMS rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct total area of rooms used for \"RESIDENT\" or \"LABS\" purposes, use COUNT(DISTINCT FCLT_ROOMS.MAJOR_USE_DESC).",
    "- \"total room area greater than 1000\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins multiply FCLT_ROOMS rows ~x39.2, so SUM/AVG over the joined result would count each FCLT_ROOMS row ~39.2 times. Aggregate it at FCLT_ROOMS's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"assignable area\" -> FCLT_BUILDING_HIST.ASSIGNABLE_AREA is a numeric measure of FCLT_BUILDING_HIST; the hinted joins multiply FCLT_BUILDING_HIST rows ~x49.5, so SUM/AVG over the joined result would count each FCLT_BUILDING_HIST row ~49.5 times. Aggregate it at FCLT_BUILDING_HIST's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"campus sector\" -> FCLT_BUILDING.CAMPUS_SECTOR: already repeats ~30.2 rows per value in its own table; the hinted joins multiply FCLT_BUILDING rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct campus sector, use COUNT(DISTINCT FCLT_BUILDING.CAMPUS_SECTOR).",
    "- \"external gross area\" -> FCLT_BUILDING.EXT_GROSS_AREA is a numeric measure of FCLT_BUILDING; the hinted joins multiply FCLT_BUILDING rows ~x39.2, so SUM/AVG over the joined result would count each FCLT_BUILDING row ~39.2 times. Aggregate it at FCLT_BUILDING's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_19": [
    "- Join SIS_COURSE_DESCRIPTION.COURSE = SIS_SUBJECT_CODE.COURSE_NUMBER: SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values); SIS_SUBJECT_CODE.COURSE_NUMBER is unique per row (221 rows).",
    "  -> each SIS_SUBJECT_CODE row appears ~1.1 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE = SIS_COURSE_DESCRIPTION.COURSE: COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE repeats ~181.8 rows per value (10000 rows, 55 values); SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values).",
    "  -> each COURSE_CATALOG_SUBJECT_OFFERED row appears ~1.1 times after this join.",
    "  -> each SIS_COURSE_DESCRIPTION row appears ~181.8 times after this join.",
    "- \"department\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME).",
    "- \"department\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT: already repeats ~9.7 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT).",
    "- \"department name\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME).",
    "- \"average of the total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~1.1 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"range of the total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~1.1 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"variance of the total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~1.1 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"subjects offered\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE: already repeats ~181.8 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE).",
    "- \"2022 academic year\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct 2022 academic year, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR).",
    "- \"degree granting\" -> SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING: already repeats ~347.5 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct degree granting, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING).",
    "- \"more than 9 total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~1.1 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"not 'Mathematics' (Course 18)\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct not 'Mathematics' (Course 18), use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME).",
    "- \"not 'Mathematics' (Course 18)\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT: already repeats ~9.7 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct not 'Mathematics' (Course 18), use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT)."
  ],
  "dw_1948": [
    "- Join SIS_COURSE_DESCRIPTION.COURSE = SUBJECT_OFFERED_SUMMARY.COURSE_NUMBER: SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values); SUBJECT_OFFERED_SUMMARY.COURSE_NUMBER repeats ~90.9 rows per value (10000 rows, 110 values).",
    "  -> each SIS_COURSE_DESCRIPTION row appears ~90.9 times after this join.",
    "  -> each SUBJECT_OFFERED_SUMMARY row appears ~1.1 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE repeats ~196.1 rows per value (10000 rows, 51 values).",
    "  -> each SIS_DEPARTMENT row appears ~196.1 times after this join.",
    "- \"department\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x90.9, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME).",
    "- \"department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"department name\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x90.9, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME).",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"average enrollment\" -> SUBJECT_OFFERED_SUMMARY.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~1.1 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"enrollment variance\" -> SUBJECT_OFFERED_SUMMARY.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~1.1 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"coefficient of variation\" -> SUBJECT_OFFERED_SUMMARY.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~1.1 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"total subjects offered\" -> SUBJECT_OFFERED_SUMMARY.SUBJECT_ID: already repeats ~2.4 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct total subjects offered, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.SUBJECT_ID).",
    "- \"departments that grant degrees\" -> SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING: already repeats ~347.5 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x90.9, so a plain COUNT over the joined result counts duplicates. To count distinct departments that grant degrees, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING).",
    "- \"Political Science\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x90.9, so a plain COUNT over the joined result counts duplicates. To count distinct Political Science, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME).",
    "- \"Chemistry\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct Chemistry, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME)."
  ],
  "dw_1970": [
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each SIS_DEPARTMENT row appears ~161.3 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE repeats ~166.7 rows per value (10000 rows, 60 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~166.7 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.MEET_PLACE = FCLT_ROOMS.FCLT_ROOM_KEY: COURSE_CATALOG_SUBJECT_OFFERED.MEET_PLACE repeats ~18.1 rows per value (10000 rows, 553 values); FCLT_ROOMS.FCLT_ROOM_KEY is unique per row (10000 rows).",
    "  -> each FCLT_ROOMS row appears ~18.1 times after this join.",
    "- \"Physics department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct Physics department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"Physics department\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct Physics department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"departments that have more than 10 subjects offered\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE: already repeats ~166.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"2022FA term\" -> COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: already repeats ~105.3 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"standard deviation of the number of students enrolled\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"average area\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins multiply FCLT_ROOMS rows ~x18.1, so SUM/AVG over the joined result would count each FCLT_ROOMS row ~18.1 times. Aggregate it at FCLT_ROOMS's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"variance of area\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins multiply FCLT_ROOMS rows ~x18.1, so SUM/AVG over the joined result would count each FCLT_ROOMS row ~18.1 times. Aggregate it at FCLT_ROOMS's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"standard deviation of area\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins multiply FCLT_ROOMS rows ~x18.1, so SUM/AVG over the joined result would count each FCLT_ROOMS row ~18.1 times. Aggregate it at FCLT_ROOMS's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"rooms with area greater than 100\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins multiply FCLT_ROOMS rows ~x18.1, so SUM/AVG over the joined result would count each FCLT_ROOMS row ~18.1 times. Aggregate it at FCLT_ROOMS's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_2030": [
    "- Join SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE is unique per row (179 rows); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE repeats ~166.7 rows per value (10000 rows, 60 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~166.7 times after this join.",
    "- Join ACADEMIC_TERMS.(TERM_CODE,TERM_CODE) = COURSE_CATALOG_SUBJECT_OFFERED.(TERM_CODE,EFFECTIVE_TERM_CODE): ACADEMIC_TERMS.(TERM_CODE,TERM_CODE) is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.(TERM_CODE,EFFECTIVE_TERM_CODE) repeats ~11.3 rows per value (10000 rows, 886 values).",
    "  -> each ACADEMIC_TERMS row appears ~11.3 times after this join.",
    "- \"administrative department\" -> SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply SIS_ADMIN_DEPARTMENT rows.",
    "- \"administrative department name\" -> SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply SIS_ADMIN_DEPARTMENT rows.",
    "- \"'Mechanical Engineering' (Course 2)\" -> SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply SIS_ADMIN_DEPARTMENT rows.",
    "- \"academic year 2024\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"department\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"department name\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"'Mathematics' (Course 18)\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"regular academic terms\" -> ACADEMIC_TERMS.IS_REGULAR_TERM: already repeats ~72.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x11.3, so a plain COUNT over the joined result counts duplicates. To count distinct regular academic terms, use COUNT(DISTINCT ACADEMIC_TERMS.IS_REGULAR_TERM).",
    "- \"total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"number of subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows."
  ],
  "dw_2070": [
    "- Join SIS_DEPARTMENT.(SCHOOL_CODE,DEPARTMENT_CODE) = SIS_SUBJECT_CODE.(SCHOOL_CODE,DEPARTMENT_CODE): SIS_DEPARTMENT.(SCHOOL_CODE,DEPARTMENT_CODE) is unique per row (128 rows); SIS_SUBJECT_CODE.(SCHOOL_CODE,DEPARTMENT_CODE) repeats ~3.6 rows per value (221 rows, 62 values).",
    "  -> each SIS_DEPARTMENT row appears ~3.6 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE repeats ~196.1 rows per value (10000 rows, 51 values).",
    "  -> each SIS_DEPARTMENT row appears ~196.1 times after this join.",
    "- \"subject code\" -> SIS_SUBJECT_CODE.SUBJECT_CODE: unique per row in its own table; the hinted joins do not multiply SIS_SUBJECT_CODE rows.",
    "- \"subject code description\" -> SIS_SUBJECT_CODE.SUBJECT_CODE_DESC: already repeats ~1.2 rows per value in its own table; the hinted joins do not multiply SIS_SUBJECT_CODE rows.",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"school name\" -> SIS_SUBJECT_CODE.SCHOOL_NAME: already repeats ~24.6 rows per value in its own table; the hinted joins do not multiply SIS_SUBJECT_CODE rows.",
    "- \"average department budget code\" -> SIS_DEPARTMENT.DEPT_BUDGET_CODE: already repeats ~2.2 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct average department budget code, use COUNT(DISTINCT SIS_DEPARTMENT.DEPT_BUDGET_CODE).",
    "- \"subject title\" -> SUBJECT_OFFERED_SUMMARY.SUBJECT_TITLE: already repeats ~2.3 rows per value in its own table; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"total units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"dense rank over the department code\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct dense rank over the department code, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"number of subjects offered by the department\" -> SUBJECT_OFFERED_SUMMARY.SUBJECT_OFFERED_SUMMARY_KEY: unique per row in its own table; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"number of subjects offered by the department\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct number of subjects offered by the department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"degree granting\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct degree granting, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"Mathematics\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME)."
  ],
  "dw_2097": [
    "- Join ACADEMIC_TERMS.TERM_CODE = LIBRARY_RESERVE_MATRL_DETAIL.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); LIBRARY_RESERVE_MATRL_DETAIL.TERM_CODE repeats ~1250.0 rows per value (10000 rows, 8 values).",
    "  -> each ACADEMIC_TERMS row appears ~1250.0 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = SUBJECT_SUMMARY.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); SUBJECT_SUMMARY.TERM_CODE repeats ~83.3 rows per value (10000 rows, 120 values).",
    "  -> each ACADEMIC_TERMS row appears ~83.3 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each SIS_DEPARTMENT row appears ~161.3 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE repeats ~166.7 rows per value (10000 rows, 60 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~166.7 times after this join.",
    "- \"departments\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct departments, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"average number of enrolled students per subject\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"2022FA term\" -> TIP_SUBJECT_OFFERED.TERM_CODE: already repeats ~79.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"departments that grant degrees\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct departments that grant degrees, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"Electrical Engineering and Computer Science department\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct Electrical Engineering and Computer Science department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE)."
  ],
  "dw_2132": [
    "- Join STUDENT_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: STUDENT_DEPARTMENT.DEPARTMENT_CODE is unique per row (79 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each STUDENT_DEPARTMENT row appears ~161.3 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE repeats ~166.7 rows per value (10000 rows, 60 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~166.7 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE repeats ~196.1 rows per value (10000 rows, 51 values).",
    "  -> each SIS_DEPARTMENT row appears ~196.1 times after this join.",
    "- \"subjects offered in the term '2017JA'\" -> TIP_SUBJECT_OFFERED.TERM_CODE: already repeats ~79.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"Chemistry department\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"Chemistry department\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: already repeats ~161.3 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"Course 5\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: already repeats ~161.3 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"subject ID\" -> TIP_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"subject title\" -> TIP_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"offering department code\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: already repeats ~161.3 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"offering department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"department full name\" -> STUDENT_DEPARTMENT.DEPARTMENT_FULL_NAME: unique per row in its own table; the hinted joins multiply STUDENT_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct department full name, use COUNT(DISTINCT STUDENT_DEPARTMENT.DEPARTMENT_FULL_NAME).",
    "- \"school name\" -> STUDENT_DEPARTMENT.SCHOOL_NAME: already repeats ~8.8 rows per value in its own table; the hinted joins multiply STUDENT_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT STUDENT_DEPARTMENT.SCHOOL_NAME).",
    "- \"minimum number of enrolled students over each department code group\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"minimum number of enrolled students over each department code group\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: already repeats ~161.3 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"variance of the total units for subjects offered by that department in the year 2022\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"variance of the total units for subjects offered by that department in the year 2022\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct variance of the total units for subjects offered by that department in the year 2022, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"variance of the total units for subjects offered by that department in the year 2022\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct variance of the total units for subjects offered by that department in the year 2022, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"variance of the total units for subjects offered by that department in the year 2022\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"variance of the total units for subjects offered by that department in the year 2022\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct variance of the total units for subjects offered by that department in the year 2022, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"excluding the Biology department\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct excluding the Biology department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"degree-granting departments\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct degree-granting departments, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"average number of enrolled students for subjects offered by that department in the year 2022\" -> SUBJECT_OFFERED_SUMMARY.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"average number of enrolled students for subjects offered by that department in the year 2022\" -> SUBJECT_OFFERED_SUMMARY.TERM_CODE: already repeats ~81.3 rows per value in its own table; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"average number of enrolled students for subjects offered by that department in the year 2022\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct average number of enrolled students for subjects offered by that department in the year 2022, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"average number of enrolled students for subjects offered by that department in the year 2022\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct average number of enrolled students for subjects offered by that department in the year 2022, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING)."
  ],
  "dw_2227": [
    "- Join ACADEMIC_TERMS.(TERM_CODE,TERM_CODE) = COURSE_CATALOG_SUBJECT_OFFERED.(TERM_CODE,EFFECTIVE_TERM_CODE): ACADEMIC_TERMS.(TERM_CODE,TERM_CODE) is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.(TERM_CODE,EFFECTIVE_TERM_CODE) repeats ~11.3 rows per value (10000 rows, 886 values).",
    "  -> each ACADEMIC_TERMS row appears ~11.3 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = ACADEMIC_TERM_PARAMETER.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); ACADEMIC_TERM_PARAMETER.TERM_CODE is unique per row (3 rows).",
    "- \"academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x11.3, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR).",
    "- \"academic year description\" -> ACADEMIC_TERMS.ACADEMIC_YEAR_DESC: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x11.3, so a plain COUNT over the joined result counts duplicates. To count distinct academic year description, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR_DESC).",
    "- \"term code\" -> ACADEMIC_TERMS.TERM_CODE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x11.3, so a plain COUNT over the joined result counts duplicates. To count distinct term code, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_CODE).",
    "- \"term code\" -> ACADEMIC_TERM_PARAMETER.TERM_CODE: unique per row in its own table; the hinted joins do not multiply ACADEMIC_TERM_PARAMETER rows.",
    "- \"range of total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"variance of total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"standard deviation of total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"Mathematics subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"regular term\" -> ACADEMIC_TERMS.IS_REGULAR_TERM: already repeats ~72.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x11.3, so a plain COUNT over the joined result counts duplicates. To count distinct regular term, use COUNT(DISTINCT ACADEMIC_TERMS.IS_REGULAR_TERM).",
    "- \"non-future term\" -> ACADEMIC_TERMS.TERM_STATUS: already repeats ~36.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x11.3, so a plain COUNT over the joined result counts duplicates. To count distinct non-future term, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_STATUS).",
    "- \"academic term parameter code\" -> ACADEMIC_TERM_PARAMETER.TERM_CODE: unique per row in its own table; the hinted joins do not multiply ACADEMIC_TERM_PARAMETER rows.",
    "- \"term description\" -> ACADEMIC_TERM_PARAMETER.TERM_DESCRIPTION: unique per row in its own table; the hinted joins do not multiply ACADEMIC_TERM_PARAMETER rows.",
    "- \"average total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"variance of total units in effective term\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subjects offered this year\" -> COURSE_CATALOG_SUBJECT_OFFERED.IS_OFFERED_THIS_YEAR: already repeats ~5000.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"number of Mathematics subjects offered\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"maximum total units in effective term\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"effective term\" -> COURSE_CATALOG_SUBJECT_OFFERED.EFFECTIVE_TERM_CODE: already repeats ~200.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows."
  ],
  "dw_2293": [
    "- Join LIBRARY_RESERVE_MATRL_DETAIL.SUBJECT_ID = TIP_DETAIL.SUBJECT_ID: LIBRARY_RESERVE_MATRL_DETAIL.SUBJECT_ID repeats ~9.9 rows per value (10000 rows, 1011 values); TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values).",
    "  -> each LIBRARY_RESERVE_MATRL_DETAIL row appears ~2.8 times after this join.",
    "  -> each TIP_DETAIL row appears ~9.9 times after this join.",
    "- Join TIP_DETAIL.SUBJECT_ID = TIP_SUBJECT_OFFERED.SUBJECT_ID: TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values); TIP_SUBJECT_OFFERED.SUBJECT_ID repeats ~2.0 rows per value (10000 rows, 4974 values).",
    "  -> each TIP_DETAIL row appears ~2.0 times after this join.",
    "  -> each TIP_SUBJECT_OFFERED row appears ~2.8 times after this join.",
    "- Join STUDENT_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: STUDENT_DEPARTMENT.DEPARTMENT_CODE is unique per row (79 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each STUDENT_DEPARTMENT row appears ~161.3 times after this join.",
    "- \"department\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"Engineering\" -> TIP_SUBJECT_OFFERED.OFFER_SCHOOL_NAME: already repeats ~1111.1 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct Engineering, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_SCHOOL_NAME).",
    "- \"Science\" -> TIP_SUBJECT_OFFERED.OFFER_SCHOOL_NAME: already repeats ~1111.1 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct Science, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_SCHOOL_NAME).",
    "- \"reserved library materials\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: already repeats ~1.2 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct reserved library materials, use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY).",
    "- \"courses that have materials\" -> TIP_SUBJECT_OFFERED.IS_NO_COURSE_MATERIAL: already repeats ~5000.0 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct courses that have materials, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.IS_NO_COURSE_MATERIAL).",
    "- \"department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"total number of reserved library materials\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: already repeats ~1.2 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct total number of reserved library materials, use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY).",
    "- \"subject title\" -> TIP_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.4 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct subject title, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.SUBJECT_TITLE).",
    "- \"term code\" -> TIP_SUBJECT_OFFERED.TERM_CODE: already repeats ~79.4 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct term code, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.TERM_CODE).",
    "- \"offering department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct offering department name, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"responsible faculty name\" -> TIP_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_NAME: already repeats ~4.1 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct responsible faculty name, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_NAME).",
    "- \"department full name\" -> STUDENT_DEPARTMENT.DEPARTMENT_FULL_NAME: unique per row in its own table; the hinted joins multiply STUDENT_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct department full name, use COUNT(DISTINCT STUDENT_DEPARTMENT.DEPARTMENT_FULL_NAME).",
    "- \"school name\" -> STUDENT_DEPARTMENT.SCHOOL_NAME: already repeats ~8.8 rows per value in its own table; the hinted joins multiply STUDENT_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT STUDENT_DEPARTMENT.SCHOOL_NAME).",
    "- \"maximum number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~2.8 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"subject offered key\" -> TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY: unique per row in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct subject offered key, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY)."
  ],
  "dw_232": [
    "- Join TIP_DETAIL.SUBJECT_ID = TIP_SUBJECT_OFFERED.SUBJECT_ID: TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values); TIP_SUBJECT_OFFERED.SUBJECT_ID repeats ~2.0 rows per value (10000 rows, 4974 values).",
    "  -> each TIP_DETAIL row appears ~2.0 times after this join.",
    "  -> each TIP_SUBJECT_OFFERED row appears ~2.8 times after this join.",
    "- \"department\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~2.8 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"courses\" -> TIP_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct courses, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.SUBJECT_ID).",
    "- \"course material\" -> TIP_DETAIL.TIP_MATERIAL_STATUS_KEY: already repeats ~833.3 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x2.0, so a plain COUNT over the joined result counts duplicates. To count distinct course material, use COUNT(DISTINCT TIP_DETAIL.TIP_MATERIAL_STATUS_KEY)."
  ],
  "dw_2394": [
    "- Join TIP_DETAIL.SUBJECT_ID = TIP_SUBJECT_OFFERED.SUBJECT_ID: TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values); TIP_SUBJECT_OFFERED.SUBJECT_ID repeats ~2.0 rows per value (10000 rows, 4974 values).",
    "  -> each TIP_DETAIL row appears ~2.0 times after this join.",
    "  -> each TIP_SUBJECT_OFFERED row appears ~2.8 times after this join.",
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- Join SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE is unique per row (179 rows); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "- \"Chemistry department\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct Chemistry department, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"Chemistry department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct Chemistry department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"term\" -> TIP_SUBJECT_OFFERED.TERM_CODE: already repeats ~79.4 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct term, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.TERM_CODE).",
    "- \"department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"average number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~2.8 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"standard deviation of the number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~2.8 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"courses that have at least one associated course material\" -> TIP_DETAIL.TIP_MATERIAL_STATUS_KEY: already repeats ~833.3 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x2.0, so a plain COUNT over the joined result counts duplicates. To count distinct courses that have at least one associated course material, use COUNT(DISTINCT TIP_DETAIL.TIP_MATERIAL_STATUS_KEY).",
    "- \"maximum CIP program code\" -> SIS_COURSE_DESCRIPTION.CIP_PROGRAM_CODE: already repeats ~10.9 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"departments that grant degrees\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct departments that grant degrees, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"not the Mathematics department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct not the Mathematics department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"full department name\" -> SIS_DEPARTMENT.DEPARTMENT_FULL_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct full department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_FULL_NAME)."
  ],
  "dw_2443": [
    "- Join ACADEMIC_TERMS.(TERM_CODE,TERM_CODE) = COURSE_CATALOG_SUBJECT_OFFERED.(EFFECTIVE_TERM_CODE,TERM_CODE): ACADEMIC_TERMS.(TERM_CODE,TERM_CODE) is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.(EFFECTIVE_TERM_CODE,TERM_CODE) repeats ~11.3 rows per value (10000 rows, 886 values).",
    "  -> each ACADEMIC_TERMS row appears ~11.3 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = ACADEMIC_TERMS_ALL.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows).",
    "- \"academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x11.3, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR).",
    "- \"academic year description\" -> ACADEMIC_TERMS.ACADEMIC_YEAR_DESC: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x11.3, so a plain COUNT over the joined result counts duplicates. To count distinct academic year description, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR_DESC).",
    "- \"average total units of Mathematics subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"average total units of Mathematics subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"regular terms\" -> ACADEMIC_TERMS.IS_REGULAR_TERM: already repeats ~72.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x11.3, so a plain COUNT over the joined result counts duplicates. To count distinct regular terms, use COUNT(DISTINCT ACADEMIC_TERMS.IS_REGULAR_TERM).",
    "- \"average total units of all subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"Mathematics\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows."
  ],
  "dw_2633": [
    "- Join ACADEMIC_TERMS.TERM_CODE = ACADEMIC_TERM_PARAMETER.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); ACADEMIC_TERM_PARAMETER.TERM_CODE is unique per row (3 rows).",
    "- Join ACADEMIC_TERMS.TERM_CODE = SUBJECT_SUMMARY.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); SUBJECT_SUMMARY.TERM_CODE repeats ~83.3 rows per value (10000 rows, 120 values).",
    "  -> each ACADEMIC_TERMS row appears ~83.3 times after this join.",
    "- \"department\" -> SUBJECT_SUMMARY.DEPARTMENT_NAME: already repeats ~181.8 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"school\" -> SUBJECT_SUMMARY.SCHOOL_NAME: already repeats ~1000.0 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"current academic term\" -> ACADEMIC_TERM_PARAMETER.IS_CURRENT_TERM: already repeats ~1.5 rows per value in its own table; the hinted joins do not multiply ACADEMIC_TERM_PARAMETER rows.",
    "- \"maximum subject enrollment number\" -> SUBJECT_SUMMARY.SUBJECT_ENROLLMENT_NUMBER: already repeats ~57.8 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"term start date\" -> ACADEMIC_TERMS.TERM_START_DATE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x83.3, so a plain COUNT over the joined result counts duplicates. To count distinct term start date, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_START_DATE)."
  ],
  "dw_2669": [
    "- Join SPACE_SUPERVISOR_USAGE.DEPT_NAMES = SPACE_UNIT.DLC_KEY: SPACE_SUPERVISOR_USAGE.DEPT_NAMES repeats ~15.8 rows per value (2135 rows, 135 values); SPACE_UNIT.DLC_KEY repeats ~1.1 rows per value (150 rows, 132 values).",
    "  -> each SPACE_SUPERVISOR_USAGE row appears ~1.1 times after this join.",
    "  -> each SPACE_UNIT row appears ~15.8 times after this join.",
    "- \"department\" -> SPACE_UNIT.SPACE_UNIT: already repeats ~1.1 rows per value in its own table; the hinted joins multiply SPACE_UNIT rows ~x15.8, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SPACE_UNIT.SPACE_UNIT).",
    "- \"department name\" -> SPACE_UNIT.SPACE_UNIT: already repeats ~1.1 rows per value in its own table; the hinted joins multiply SPACE_UNIT rows ~x15.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SPACE_UNIT.SPACE_UNIT).",
    "- \"average of the number of supervisees\" -> SPACE_SUPERVISOR_USAGE.NUM_OF_SUPERVISEES is a numeric measure of SPACE_SUPERVISOR_USAGE; the hinted joins multiply SPACE_SUPERVISOR_USAGE rows ~x1.1, so SUM/AVG over the joined result would count each SPACE_SUPERVISOR_USAGE row ~1.1 times. Aggregate it at SPACE_SUPERVISOR_USAGE's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"range of the number of supervisees\" -> SPACE_SUPERVISOR_USAGE.NUM_OF_SUPERVISEES is a numeric measure of SPACE_SUPERVISOR_USAGE; the hinted joins multiply SPACE_SUPERVISOR_USAGE rows ~x1.1, so SUM/AVG over the joined result would count each SPACE_SUPERVISOR_USAGE row ~1.1 times. Aggregate it at SPACE_SUPERVISOR_USAGE's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"variance of the number of supervisees\" -> SPACE_SUPERVISOR_USAGE.NUM_OF_SUPERVISEES is a numeric measure of SPACE_SUPERVISOR_USAGE; the hinted joins multiply SPACE_SUPERVISOR_USAGE rows ~x1.1, so SUM/AVG over the joined result would count each SPACE_SUPERVISOR_USAGE row ~1.1 times. Aggregate it at SPACE_SUPERVISOR_USAGE's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"total square footage\" -> SPACE_SUPERVISOR_USAGE.SQFT is a numeric measure of SPACE_SUPERVISOR_USAGE; the hinted joins multiply SPACE_SUPERVISOR_USAGE rows ~x1.1, so SUM/AVG over the joined result would count each SPACE_SUPERVISOR_USAGE row ~1.1 times. Aggregate it at SPACE_SUPERVISOR_USAGE's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"total research volume\" -> SPACE_SUPERVISOR_USAGE.RESEARCH_VOLUME is a numeric measure of SPACE_SUPERVISOR_USAGE; the hinted joins multiply SPACE_SUPERVISOR_USAGE rows ~x1.1, so SUM/AVG over the joined result would count each SPACE_SUPERVISOR_USAGE row ~1.1 times. Aggregate it at SPACE_SUPERVISOR_USAGE's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"space units associated with that department\" -> SPACE_UNIT.DLC_KEY: already repeats ~1.1 rows per value in its own table; the hinted joins multiply SPACE_UNIT rows ~x15.8, so a plain COUNT over the joined result counts duplicates. To count distinct space units associated with that department, use COUNT(DISTINCT SPACE_UNIT.DLC_KEY).",
    "- \"space units associated with that department\" -> SPACE_SUPERVISOR_USAGE.DEPT_NAMES: already repeats ~15.8 rows per value in its own table; the hinted joins multiply SPACE_SUPERVISOR_USAGE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct space units associated with that department, use COUNT(DISTINCT SPACE_SUPERVISOR_USAGE.DEPT_NAMES)."
  ],
  "dw_2694": [
    "- Join SUBJECT_OFFERED.SUBJECT_ID = SUBJECT_OFFERED_SUMMARY.SUBJECT_ID: SUBJECT_OFFERED.SUBJECT_ID repeats ~4.8 rows per value (10000 rows, 2081 values); SUBJECT_OFFERED_SUMMARY.SUBJECT_ID repeats ~2.4 rows per value (10000 rows, 4122 values).",
    "  -> each SUBJECT_OFFERED row appears ~2.4 times after this join.",
    "  -> each SUBJECT_OFFERED_SUMMARY row appears ~4.8 times after this join.",
    "- Join SUBJECT_OFFERED.SUBJECT_ID = SUBJECT_SUMMARY.SUBJECT_ID: SUBJECT_OFFERED.SUBJECT_ID repeats ~4.8 rows per value (10000 rows, 2081 values); SUBJECT_SUMMARY.SUBJECT_ID repeats ~1.9 rows per value (10000 rows, 5251 values).",
    "  -> each SUBJECT_OFFERED row appears ~1.9 times after this join.",
    "  -> each SUBJECT_SUMMARY row appears ~4.8 times after this join.",
    "- \"subject key\" -> SUBJECT_OFFERED.SUBJECT_KEY: unique per row in its own table; the hinted joins multiply SUBJECT_OFFERED rows ~x2.4, so a plain COUNT over the joined result counts duplicates. To count distinct subject key, use COUNT(DISTINCT SUBJECT_OFFERED.SUBJECT_KEY).",
    "- \"subject title\" -> SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~4.5 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED rows ~x2.4, so a plain COUNT over the joined result counts duplicates. To count distinct subject title, use COUNT(DISTINCT SUBJECT_OFFERED.SUBJECT_TITLE).",
    "- \"department name\" -> SUBJECT_SUMMARY.DEPARTMENT_NAME: already repeats ~181.8 rows per value in its own table; the hinted joins multiply SUBJECT_SUMMARY rows ~x4.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SUBJECT_SUMMARY.DEPARTMENT_NAME).",
    "- \"number of subject offerings\" -> SUBJECT_OFFERED.SUBJECT_KEY: unique per row in its own table; the hinted joins multiply SUBJECT_OFFERED rows ~x2.4, so a plain COUNT over the joined result counts duplicates. To count distinct number of subject offerings, use COUNT(DISTINCT SUBJECT_OFFERED.SUBJECT_KEY).",
    "- \"total number of units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x4.8, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~4.8 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"total number of enrolled students\" -> SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED; the hinted joins multiply SUBJECT_OFFERED rows ~x2.4, so SUM/AVG over the joined result would count each SUBJECT_OFFERED row ~2.4 times. Aggregate it at SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"2012 summer term\" -> SUBJECT_OFFERED.TERM_CODE: already repeats ~370.4 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED rows ~x2.4, so a plain COUNT over the joined result counts duplicates. To count distinct 2012 summer term, use COUNT(DISTINCT SUBJECT_OFFERED.TERM_CODE).",
    "- \"Harvard Cross-Enrollment Program\" -> SUBJECT_SUMMARY.DEPARTMENT_NAME: already repeats ~181.8 rows per value in its own table; the hinted joins multiply SUBJECT_SUMMARY rows ~x4.8, so a plain COUNT over the joined result counts duplicates. To count distinct Harvard Cross-Enrollment Program, use COUNT(DISTINCT SUBJECT_SUMMARY.DEPARTMENT_NAME)."
  ],
  "dw_2715": [
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = TIP_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); TIP_SUBJECT_OFFERED.TERM_CODE repeats ~79.4 rows per value (10000 rows, 126 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~79.4 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = ACADEMIC_TERM_PARAMETER.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); ACADEMIC_TERM_PARAMETER.TERM_CODE is unique per row (3 rows).",
    "- \"subjects\" -> TIP_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"subjects\" -> TIP_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"subject ID\" -> TIP_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"subject title\" -> TIP_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"offering department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"term code '2025FA'\" -> TIP_SUBJECT_OFFERED.TERM_CODE: already repeats ~79.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"term description\" -> ACADEMIC_TERMS_ALL.TERM_DESCRIPTION: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct term description, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_DESCRIPTION).",
    "- \"academic year\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR).",
    "- \"term start date\" -> ACADEMIC_TERMS_ALL.TERM_START_DATE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct term start date, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_START_DATE).",
    "- \"term end date\" -> ACADEMIC_TERMS_ALL.TERM_END_DATE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct term end date, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_END_DATE).",
    "- \"term parameter\" -> ACADEMIC_TERM_PARAMETER.TERM_PARAMETER: unique per row in its own table; the hinted joins do not multiply ACADEMIC_TERM_PARAMETER rows.",
    "- \"minimum number of enrolled students in its offering department\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"minimum number of enrolled students in its offering department\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows."
  ],
  "dw_287": [
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each SIS_DEPARTMENT row appears ~161.3 times after this join.",
    "- Join STUDENT_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: STUDENT_DEPARTMENT.DEPARTMENT_CODE is unique per row (79 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each STUDENT_DEPARTMENT row appears ~161.3 times after this join.",
    "- \"subject offered\" -> TIP_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"Mathematics department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"Mathematics department\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"Course 18\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct Course 18, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"degree-granting department\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct degree-granting department, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"subject title\" -> TIP_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"school name\" -> STUDENT_DEPARTMENT.SCHOOL_NAME: already repeats ~8.8 rows per value in its own table; the hinted joins multiply STUDENT_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT STUDENT_DEPARTMENT.SCHOOL_NAME).",
    "- \"department code\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct department code, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"average number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows."
  ],
  "dw_2878": [
    "- Join LIBRARY_SUBJECT_OFFERED.SUBJECT_ID = TIP_DETAIL.SUBJECT_ID: LIBRARY_SUBJECT_OFFERED.SUBJECT_ID repeats ~4.4 rows per value (10000 rows, 2249 values); TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values).",
    "  -> each LIBRARY_SUBJECT_OFFERED row appears ~2.8 times after this join.",
    "  -> each TIP_DETAIL row appears ~4.4 times after this join.",
    "- Join SUBJECT_OFFERED_SUMMARY.SUBJECT_ID = TIP_DETAIL.SUBJECT_ID: SUBJECT_OFFERED_SUMMARY.SUBJECT_ID repeats ~2.4 rows per value (10000 rows, 4122 values); TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values).",
    "  -> each SUBJECT_OFFERED_SUMMARY row appears ~2.8 times after this join.",
    "  -> each TIP_DETAIL row appears ~2.4 times after this join.",
    "- \"department\" -> LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~625.0 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"course\" -> LIBRARY_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~4.4 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct course, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.SUBJECT_ID).",
    "- \"course\" -> SUBJECT_OFFERED_SUMMARY.SUBJECT_ID: already repeats ~2.4 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct course, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.SUBJECT_ID).",
    "- \"average enrollment\" -> SUBJECT_OFFERED_SUMMARY.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~2.8 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"terms\" -> SUBJECT_OFFERED_SUMMARY.TERM_CODE: already repeats ~81.3 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct terms, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.TERM_CODE).",
    "- \"TIP material\" -> TIP_DETAIL.SUBJECT_ID: already repeats ~2.8 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct TIP material, use COUNT(DISTINCT TIP_DETAIL.SUBJECT_ID).",
    "- \"TIP material\" -> TIP_DETAIL.RECORD_COUNT is a numeric measure of TIP_DETAIL; the hinted joins multiply TIP_DETAIL rows ~x4.4, so SUM/AVG over the joined result would count each TIP_DETAIL row ~4.4 times. Aggregate it at TIP_DETAIL's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"total units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~2.8 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_2933": [
    "- Join FCLT_ORGANIZATION.DLC_KEY = FCLT_ORG_DLC_KEY.DLC_KEY: FCLT_ORGANIZATION.DLC_KEY repeats ~1.3 rows per value (180 rows, 136 values); FCLT_ORG_DLC_KEY.DLC_KEY repeats ~1.2 rows per value (168 rows, 136 values).",
    "  -> each FCLT_ORGANIZATION row appears ~1.2 times after this join.",
    "  -> each FCLT_ORG_DLC_KEY row appears ~1.3 times after this join.",
    "- Join FCLT_ORGANIZATION.DLC_KEY = SPACE_UNIT.DLC_KEY: FCLT_ORGANIZATION.DLC_KEY repeats ~1.3 rows per value (180 rows, 136 values); SPACE_UNIT.DLC_KEY repeats ~1.1 rows per value (150 rows, 132 values).",
    "  -> each FCLT_ORGANIZATION row appears ~1.1 times after this join.",
    "  -> each SPACE_UNIT row appears ~1.3 times after this join.",
    "- \"organization name\" -> FCLT_ORGANIZATION.ORGANIZATION_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct organization name, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_NAME).",
    "- \"number of associated space units\" -> SPACE_UNIT.SPACE_UNIT_KEY: unique per row in its own table; the hinted joins multiply SPACE_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct number of associated space units, use COUNT(DISTINCT SPACE_UNIT.SPACE_UNIT_KEY).",
    "- \"average space unit key value\" -> SPACE_UNIT.SPACE_UNIT_KEY: unique per row in its own table; the hinted joins multiply SPACE_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct average space unit key value, use COUNT(DISTINCT SPACE_UNIT.SPACE_UNIT_KEY)."
  ],
  "dw_3148": [
    "- Join ACADEMIC_TERMS.TERM_CODE = ACADEMIC_TERMS_ALL.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows).",
    "- Join ACADEMIC_TERMS.TERM_CODE = TIP_DETAIL.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); TIP_DETAIL.TERM_CODE repeats ~158.7 rows per value (10000 rows, 63 values).",
    "  -> each ACADEMIC_TERMS row appears ~158.7 times after this join.",
    "- \"subject\" -> TIP_DETAIL.SUBJECT_ID: already repeats ~2.8 rows per value in its own table; the hinted joins do not multiply TIP_DETAIL rows.",
    "- \"term \"2010FA\"\" -> TIP_DETAIL.TERM_CODE: already repeats ~158.7 rows per value in its own table; the hinted joins do not multiply TIP_DETAIL rows.",
    "- \"subject ID\" -> TIP_DETAIL.SUBJECT_ID: already repeats ~2.8 rows per value in its own table; the hinted joins do not multiply TIP_DETAIL rows.",
    "- \"term description\" -> ACADEMIC_TERMS_ALL.TERM_DESCRIPTION: unique per row in its own table; the hinted joins do not multiply ACADEMIC_TERMS_ALL rows.",
    "- \"material status key\" -> TIP_DETAIL.TIP_MATERIAL_STATUS_KEY: already repeats ~833.3 rows per value in its own table; the hinted joins do not multiply TIP_DETAIL rows.",
    "- \"academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x158.7, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR).",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins do not multiply SIS_DEPARTMENT rows."
  ],
  "dw_3183": [
    "- Join SPACE_UNIT.SPACE_UNIT_CODE = ZPM_ROOMS_LOAD.SPACE_UNIT_CODE: SPACE_UNIT.SPACE_UNIT_CODE is unique per row (150 rows); ZPM_ROOMS_LOAD.SPACE_UNIT_CODE repeats ~151.5 rows per value (10000 rows, 66 values).",
    "  -> each SPACE_UNIT row appears ~151.5 times after this join.",
    "- \"space unit name\" -> SPACE_UNIT.SPACE_UNIT: already repeats ~1.1 rows per value in its own table; the hinted joins multiply SPACE_UNIT rows ~x151.5, so a plain COUNT over the joined result counts duplicates. To count distinct space unit name, use COUNT(DISTINCT SPACE_UNIT.SPACE_UNIT).",
    "- \"number of rooms assigned\" -> ZPM_ROOMS_LOAD.BUILDING_ROOM: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply ZPM_ROOMS_LOAD rows.",
    "- \"access level\" -> ZPM_ROOMS_LOAD.ACCESS_LEVEL: already repeats ~2500.0 rows per value in its own table; the hinted joins do not multiply ZPM_ROOMS_LOAD rows."
  ],
  "dw_323": [
    "- Join SIS_COURSE_DESCRIPTION.COURSE = SIS_SUBJECT_CODE.COURSE_NUMBER: SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values); SIS_SUBJECT_CODE.COURSE_NUMBER is unique per row (221 rows).",
    "  -> each SIS_SUBJECT_CODE row appears ~1.1 times after this join.",
    "- Join LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER = SIS_SUBJECT_CODE.COURSE_NUMBER: LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER repeats ~625.0 rows per value (10000 rows, 16 values); SIS_SUBJECT_CODE.COURSE_NUMBER is unique per row (221 rows).",
    "  -> each SIS_SUBJECT_CODE row appears ~625.0 times after this join.",
    "- \"department\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"department name\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"degree-granting subjects\" -> SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING: already repeats ~347.5 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"offered in the term '2014FA'\" -> LIBRARY_SUBJECT_OFFERED.TERM_CODE: already repeats ~192.3 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"more than 5 students enrolled\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows."
  ],
  "dw_3298": [
    "- Join ACADEMIC_TERMS.TERM_CODE = LIBRARY_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); LIBRARY_SUBJECT_OFFERED.TERM_CODE repeats ~192.3 rows per value (10000 rows, 52 values).",
    "  -> each ACADEMIC_TERMS row appears ~192.3 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS row appears ~105.3 times after this join.",
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- \"subject offered in the library\" -> LIBRARY_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~5.6 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"subject title\" -> LIBRARY_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~5.6 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"number of enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"offering department name\" -> LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~625.0 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"term description\" -> ACADEMIC_TERMS.TERM_DESCRIPTION: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct term description, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_DESCRIPTION).",
    "- \"academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR).",
    "- \"subject short title\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_SHORT_TITLE: already repeats ~2.6 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"maximum total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"Mathematics department\" -> LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~625.0 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"regular terms\" -> ACADEMIC_TERMS.IS_REGULAR_TERM: already repeats ~72.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct regular terms, use COUNT(DISTINCT ACADEMIC_TERMS.IS_REGULAR_TERM).",
    "- \"average CIP program code\" -> SIS_COURSE_DESCRIPTION.CIP_PROGRAM_CODE: already repeats ~10.9 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"CIP program code range\" -> SIS_COURSE_DESCRIPTION.CIP_PROGRAM_CODE: already repeats ~10.9 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"CIP program code variance\" -> SIS_COURSE_DESCRIPTION.CIP_PROGRAM_CODE: already repeats ~10.9 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"degree granting\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct degree granting, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"undergraduate course level\" -> SIS_COURSE_DESCRIPTION.COURSE_LEVEL: already repeats ~347.5 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows."
  ],
  "dw_3310": [
    "- Join SIS_COURSE_DESCRIPTION.COURSE = SUBJECT_OFFERED_SUMMARY.COURSE_NUMBER: SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values); SUBJECT_OFFERED_SUMMARY.COURSE_NUMBER repeats ~90.9 rows per value (10000 rows, 110 values).",
    "  -> each SIS_COURSE_DESCRIPTION row appears ~90.9 times after this join.",
    "  -> each SUBJECT_OFFERED_SUMMARY row appears ~1.1 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE = SIS_COURSE_DESCRIPTION.COURSE: COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE repeats ~181.8 rows per value (10000 rows, 55 values); SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values).",
    "  -> each COURSE_CATALOG_SUBJECT_OFFERED row appears ~1.1 times after this join.",
    "  -> each SIS_COURSE_DESCRIPTION row appears ~181.8 times after this join.",
    "- Join LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER = SIS_SUBJECT_CODE.COURSE_NUMBER: LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER repeats ~625.0 rows per value (10000 rows, 16 values); SIS_SUBJECT_CODE.COURSE_NUMBER is unique per row (221 rows).",
    "  -> each SIS_SUBJECT_CODE row appears ~625.0 times after this join.",
    "- \"subjects offered in the term '2022FA'\" -> COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: already repeats ~105.3 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered in the term '2022FA', use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE).",
    "- \"total of 12 units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~1.1 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"subject title from the summary\" -> SUBJECT_OFFERED_SUMMARY.SUBJECT_TITLE: already repeats ~2.3 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct subject title from the summary, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.SUBJECT_TITLE).",
    "- \"department name from the course description\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name from the course description, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME).",
    "- \"dense rank ... ordered by the subject code from the course catalog\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE: already repeats ~181.8 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct dense rank ... ordered by the subject code from the course catalog, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE).",
    "- \"school name\" -> SIS_COURSE_DESCRIPTION.SCHOOL_NAME: already repeats ~77.2 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.SCHOOL_NAME).",
    "- \"school name\" -> SIS_SUBJECT_CODE.SCHOOL_NAME: already repeats ~24.6 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x625.0, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_SUBJECT_CODE.SCHOOL_NAME).",
    "- \"number of offered subjects\" -> LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY: unique per row in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"minimum number of enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"number of enrolled students in the offered subject is greater than 3\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows."
  ],
  "dw_3545": [
    "- Join ACADEMIC_TERMS.TERM_CODE = LIBRARY_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); LIBRARY_SUBJECT_OFFERED.TERM_CODE repeats ~192.3 rows per value (10000 rows, 52 values).",
    "  -> each ACADEMIC_TERMS row appears ~192.3 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); SUBJECT_OFFERED.TERM_CODE repeats ~370.4 rows per value (10000 rows, 27 values).",
    "  -> each ACADEMIC_TERMS row appears ~370.4 times after this join.",
    "- Join STUDENT_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: STUDENT_DEPARTMENT.DEPARTMENT_CODE is unique per row (79 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each STUDENT_DEPARTMENT row appears ~161.3 times after this join.",
    "- \"department\" -> LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~625.0 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"department\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"department\" -> STUDENT_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply STUDENT_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT STUDENT_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x370.4, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR).",
    "- \"number of enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"library subject offered\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"department name\" -> LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~625.0 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"number of unique subjects offered\" -> SUBJECT_OFFERED.SUBJECT_ID: already repeats ~4.8 rows per value in its own table; the hinted joins do not multiply SUBJECT_OFFERED rows.",
    "- \"minimum start date of the term\" -> ACADEMIC_TERMS.TERM_START_DATE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x370.4, so a plain COUNT over the joined result counts duplicates. To count distinct minimum start date of the term, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_START_DATE).",
    "- \"term '2017JA'\" -> TIP_SUBJECT_OFFERED.TERM_CODE: already repeats ~79.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"subject title\" -> TIP_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"responsible faculty name\" -> TIP_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_NAME: already repeats ~4.1 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"offering department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"department full name\" -> STUDENT_DEPARTMENT.DEPARTMENT_FULL_NAME: unique per row in its own table; the hinted joins multiply STUDENT_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct department full name, use COUNT(DISTINCT STUDENT_DEPARTMENT.DEPARTMENT_FULL_NAME).",
    "- \"school name\" -> STUDENT_DEPARTMENT.SCHOOL_NAME: already repeats ~8.8 rows per value in its own table; the hinted joins multiply STUDENT_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT STUDENT_DEPARTMENT.SCHOOL_NAME).",
    "- \"minimum number of enrolled students in a window\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"minimum number of enrolled students in a window\" -> STUDENT_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply STUDENT_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct minimum number of enrolled students in a window, use COUNT(DISTINCT STUDENT_DEPARTMENT.DEPARTMENT_CODE)."
  ],
  "dw_3585": [
    "- Join FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY = FCLT_ORG_DLC_KEY.FCLT_ORGANIZATION_KEY: FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY repeats ~1.1 rows per value (180 rows, 168 values); FCLT_ORG_DLC_KEY.FCLT_ORGANIZATION_KEY is unique per row (168 rows).",
    "  -> each FCLT_ORG_DLC_KEY row appears ~1.1 times after this join.",
    "- Join FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY = FCLT_ROOMS.FCLT_ORGANIZATION_KEY: FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY repeats ~1.1 rows per value (180 rows, 168 values); FCLT_ROOMS.FCLT_ORGANIZATION_KEY repeats ~79.4 rows per value (10000 rows, 126 values).",
    "  -> each FCLT_ORGANIZATION row appears ~79.4 times after this join.",
    "  -> each FCLT_ROOMS row appears ~1.1 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS row appears ~105.3 times after this join.",
    "- \"department\" -> FCLT_ORGANIZATION.HR_DEPARTMENT_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT FCLT_ORGANIZATION.HR_DEPARTMENT_NAME).",
    "- \"department\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"department name\" -> FCLT_ORGANIZATION.HR_DEPARTMENT_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT FCLT_ORGANIZATION.HR_DEPARTMENT_NAME).",
    "- \"department name\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"average area\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so SUM/AVG over the joined result would count each FCLT_ROOMS row ~1.1 times. Aggregate it at FCLT_ROOMS's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"area range\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so SUM/AVG over the joined result would count each FCLT_ROOMS row ~1.1 times. Aggregate it at FCLT_ROOMS's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"area variance\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so SUM/AVG over the joined result would count each FCLT_ROOMS row ~1.1 times. Aggregate it at FCLT_ROOMS's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"rooms assigned to department\" -> FCLT_ROOMS.FCLT_ORGANIZATION_KEY: already repeats ~79.4 rows per value in its own table; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct rooms assigned to department, use COUNT(DISTINCT FCLT_ROOMS.FCLT_ORGANIZATION_KEY).",
    "- \"rooms assigned to department\" -> FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct rooms assigned to department, use COUNT(DISTINCT FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY).",
    "- \"rooms with area greater than 40\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so SUM/AVG over the joined result would count each FCLT_ROOMS row ~1.1 times. Aggregate it at FCLT_ROOMS's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"average total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"total units range\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"total units variance\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subjects offered in regular academic terms\" -> COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: already repeats ~105.3 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subjects offered in regular academic terms\" -> ACADEMIC_TERMS.TERM_CODE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered in regular academic terms, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_CODE).",
    "- \"subjects offered in regular academic terms\" -> ACADEMIC_TERMS.IS_REGULAR_TERM: already repeats ~72.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered in regular academic terms, use COUNT(DISTINCT ACADEMIC_TERMS.IS_REGULAR_TERM)."
  ],
  "dw_3643": [
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each SIS_DEPARTMENT row appears ~161.3 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = SUBJECT_SUMMARY.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); SUBJECT_SUMMARY.TERM_CODE repeats ~83.3 rows per value (10000 rows, 120 values).",
    "  -> each ACADEMIC_TERMS row appears ~83.3 times after this join.",
    "- \"subjects offered in the term 2019FA\" -> TIP_SUBJECT_OFFERED.TERM_CODE: already repeats ~79.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"subjects offered in the term 2009SP\" -> ACADEMIC_TERMS.TERM_CODE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x83.3, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered in the term 2009SP, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_CODE).",
    "- \"subjects offered in the term 2009SP\" -> SUBJECT_SUMMARY.TERM_CODE: already repeats ~83.3 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"subject ID\" -> TIP_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"subject title\" -> TIP_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"subject title\" -> SUBJECT_SUMMARY.SUBJECT_TITLE: already repeats ~2.1 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"department name\" -> SUBJECT_SUMMARY.DEPARTMENT_NAME: already repeats ~181.8 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"department full name\" -> SIS_DEPARTMENT.DEPARTMENT_FULL_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct department full name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_FULL_NAME).",
    "- \"total units for 2009SP\" -> SUBJECT_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_SUMMARY; the hinted joins do not multiply SUBJECT_SUMMARY rows."
  ],
  "dw_3778": [
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = ACADEMIC_TERM_PARAMETER.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); ACADEMIC_TERM_PARAMETER.TERM_CODE is unique per row (3 rows).",
    "- Join ACADEMIC_TERMS.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.EFFECTIVE_TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.EFFECTIVE_TERM_CODE repeats ~200.0 rows per value (10000 rows, 50 values).",
    "  -> each ACADEMIC_TERMS row appears ~200.0 times after this join.",
    "- \"course description\" -> SIS_COURSE_DESCRIPTION.COURSE_DESCRIPTION: already repeats ~1.3 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"engineering department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct engineering department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"engineering department dense rank\" -> SIS_DEPARTMENT.DEPT_BUDGET_CODE: already repeats ~2.2 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct engineering department dense rank, use COUNT(DISTINCT SIS_DEPARTMENT.DEPT_BUDGET_CODE).",
    "- \"School of Engineering\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct School of Engineering, use COUNT(DISTINCT SIS_DEPARTMENT.SCHOOL_NAME).",
    "- \"degree granting\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct degree granting, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"term parameter\" -> ACADEMIC_TERM_PARAMETER.TERM_PARAMETER: unique per row in its own table; the hinted joins do not multiply ACADEMIC_TERM_PARAMETER rows.",
    "- \"term code\" -> ACADEMIC_TERM_PARAMETER.TERM_CODE: unique per row in its own table; the hinted joins do not multiply ACADEMIC_TERM_PARAMETER rows.",
    "- \"term code\" -> ACADEMIC_TERMS.TERM_CODE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x200.0, so a plain COUNT over the joined result counts duplicates. To count distinct term code, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_CODE).",
    "- \"academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x200.0, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR).",
    "- \"term indicator\" -> ACADEMIC_TERM_PARAMETER.TERM_INDICATOR: unique per row in its own table; the hinted joins do not multiply ACADEMIC_TERM_PARAMETER rows.",
    "- \"offered department name\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"department running sum of academic year\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"department running sum of academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x200.0, so a plain COUNT over the joined result counts duplicates. To count distinct department running sum of academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR)."
  ],
  "dw_3847": [
    "- Join ACADEMIC_TERMS.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.EFFECTIVE_TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.EFFECTIVE_TERM_CODE repeats ~200.0 rows per value (10000 rows, 50 values).",
    "  -> each ACADEMIC_TERMS row appears ~200.0 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = SUBJECT_SUMMARY.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); SUBJECT_SUMMARY.TERM_CODE repeats ~83.3 rows per value (10000 rows, 120 values).",
    "  -> each ACADEMIC_TERMS row appears ~83.3 times after this join.",
    "- Join TIP_DETAIL.SUBJECT_ID = TIP_SUBJECT_OFFERED.SUBJECT_ID: TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values); TIP_SUBJECT_OFFERED.SUBJECT_ID repeats ~2.0 rows per value (10000 rows, 4974 values).",
    "  -> each TIP_DETAIL row appears ~2.0 times after this join.",
    "  -> each TIP_SUBJECT_OFFERED row appears ~2.8 times after this join.",
    "- Join SUBJECT_OFFERED_SUMMARY.SUBJECT_ID = TIP_DETAIL.SUBJECT_ID: SUBJECT_OFFERED_SUMMARY.SUBJECT_ID repeats ~2.4 rows per value (10000 rows, 4122 values); TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values).",
    "  -> each SUBJECT_OFFERED_SUMMARY row appears ~2.8 times after this join.",
    "  -> each TIP_DETAIL row appears ~2.4 times after this join.",
    "- \"subjects offered in the academic year 2024\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"total of 12 units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subject code\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE: already repeats ~181.8 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subject number\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_NUMBER: already repeats ~6.9 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subject title\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"department name\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"term description\" -> ACADEMIC_TERMS.TERM_DESCRIPTION: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x200.0, so a plain COUNT over the joined result counts duplicates. To count distinct term description, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_DESCRIPTION).",
    "- \"rank of each subject within its department ordered by subject code\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"rank of each subject within its department ordered by subject code\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE: already repeats ~181.8 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"average of the total units for subjects that have at least one enrolled student and have required course materials\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~2.8 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"average of the total units for subjects that have at least one enrolled student and have required course materials\" -> TIP_DETAIL.TIP_MATERIAL_STATUS_KEY: already repeats ~833.3 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x2.4, so a plain COUNT over the joined result counts duplicates. To count distinct average of the total units for subjects that have at least one enrolled student and have required course materials, use COUNT(DISTINCT TIP_DETAIL.TIP_MATERIAL_STATUS_KEY).",
    "- \"average of the total units for subjects that have at least one enrolled student and have required course materials\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~2.8 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"range of the total units for subjects that have at least one enrolled student and have required course materials\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~2.8 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"range of the total units for subjects that have at least one enrolled student and have required course materials\" -> TIP_DETAIL.TIP_MATERIAL_STATUS_KEY: already repeats ~833.3 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x2.4, so a plain COUNT over the joined result counts duplicates. To count distinct range of the total units for subjects that have at least one enrolled student and have required course materials, use COUNT(DISTINCT TIP_DETAIL.TIP_MATERIAL_STATUS_KEY).",
    "- \"range of the total units for subjects that have at least one enrolled student and have required course materials\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~2.8 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"variance of the total units for subjects that have at least one enrolled student and have required course materials\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~2.8 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"variance of the total units for subjects that have at least one enrolled student and have required course materials\" -> TIP_DETAIL.TIP_MATERIAL_STATUS_KEY: already repeats ~833.3 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x2.4, so a plain COUNT over the joined result counts duplicates. To count distinct variance of the total units for subjects that have at least one enrolled student and have required course materials, use COUNT(DISTINCT TIP_DETAIL.TIP_MATERIAL_STATUS_KEY).",
    "- \"variance of the total units for subjects that have at least one enrolled student and have required course materials\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~2.8 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_3862": [
    "- Join MIT_STUDENT_DIRECTORY.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: MIT_STUDENT_DIRECTORY.DEPARTMENT repeats ~204.1 rows per value (10000 rows, 49 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~204.1 times after this join.",
    "- Join LIBRARY_SUBJECT_OFFERED.SUBJECT_ID = TIP_DETAIL.SUBJECT_ID: LIBRARY_SUBJECT_OFFERED.SUBJECT_ID repeats ~4.4 rows per value (10000 rows, 2249 values); TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values).",
    "  -> each LIBRARY_SUBJECT_OFFERED row appears ~2.8 times after this join.",
    "  -> each TIP_DETAIL row appears ~4.4 times after this join.",
    "- Join SUBJECT_OFFERED_SUMMARY.SUBJECT_ID = TIP_DETAIL.SUBJECT_ID: SUBJECT_OFFERED_SUMMARY.SUBJECT_ID repeats ~2.4 rows per value (10000 rows, 4122 values); TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values).",
    "  -> each SUBJECT_OFFERED_SUMMARY row appears ~2.8 times after this join.",
    "  -> each TIP_DETAIL row appears ~2.4 times after this join.",
    "- \"student full name\" -> MIT_STUDENT_DIRECTORY.FULL_NAME: already repeats ~1.2 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"email address\" -> MIT_STUDENT_DIRECTORY.EMAIL_ADDRESS: already repeats ~3.0 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"department full name\" -> SIS_DEPARTMENT.DEPARTMENT_FULL_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct department full name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_FULL_NAME).",
    "- \"department is degree-granting\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct department is degree-granting, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"student year\" -> MIT_STUDENT_DIRECTORY.STUDENT_YEAR: already repeats ~2000.0 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"difference between length of full name and average length\" -> MIT_STUDENT_DIRECTORY.FULL_NAME: already repeats ~1.2 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"school name\" -> LIBRARY_SUBJECT_OFFERED.OFFER_SCHOOL_NAME: already repeats ~2000.0 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.OFFER_SCHOOL_NAME).",
    "- \"minimum number of enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x2.8, so SUM/AVG over the joined result would count each LIBRARY_SUBJECT_OFFERED row ~2.8 times. Aggregate it at LIBRARY_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"total number of units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~2.8 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"department code\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct department code, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"department code\" -> LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_CODE: already repeats ~625.0 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct department code, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_CODE).",
    "- \"required material\" -> TIP_DETAIL.TIP_MATERIAL_STATUS_KEY: already repeats ~833.3 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct required material, use COUNT(DISTINCT TIP_DETAIL.TIP_MATERIAL_STATUS_KEY)."
  ],
  "dw_3868": [
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each SIS_DEPARTMENT row appears ~161.3 times after this join.",
    "- Join STUDENT_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: STUDENT_DEPARTMENT.DEPARTMENT_CODE is unique per row (79 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each STUDENT_DEPARTMENT row appears ~161.3 times after this join.",
    "- Join LIBRARY_RESERVE_MATRL_DETAIL.SUBJECT_ID = TIP_DETAIL.SUBJECT_ID: LIBRARY_RESERVE_MATRL_DETAIL.SUBJECT_ID repeats ~9.9 rows per value (10000 rows, 1011 values); TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values).",
    "  -> each LIBRARY_RESERVE_MATRL_DETAIL row appears ~2.8 times after this join.",
    "  -> each TIP_DETAIL row appears ~9.9 times after this join.",
    "- Join TIP_DETAIL.SUBJECT_ID = TIP_SUBJECT_OFFERED.SUBJECT_ID: TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values); TIP_SUBJECT_OFFERED.SUBJECT_ID repeats ~2.0 rows per value (10000 rows, 4974 values).",
    "  -> each TIP_DETAIL row appears ~2.0 times after this join.",
    "  -> each TIP_SUBJECT_OFFERED row appears ~2.8 times after this join.",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"total enrollment\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~2.8 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"School of Engineering\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct School of Engineering, use COUNT(DISTINCT SIS_DEPARTMENT.SCHOOL_NAME).",
    "- \"students enrolled\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~2.8 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"library course instructor key\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY: already repeats ~6.2 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct library course instructor key, use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY).",
    "- \"library reserve catalog key\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: already repeats ~1.2 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct library reserve catalog key, use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY).",
    "- \"term code\" -> LIBRARY_RESERVE_MATRL_DETAIL.TERM_CODE: already repeats ~1250.0 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct term code, use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.TERM_CODE).",
    "- \"subject ID\" -> LIBRARY_RESERVE_MATRL_DETAIL.SUBJECT_ID: already repeats ~9.9 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct subject ID, use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.SUBJECT_ID).",
    "- \"subject ID\" -> TIP_DETAIL.SUBJECT_ID: already repeats ~2.8 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x9.9, so a plain COUNT over the joined result counts duplicates. To count distinct subject ID, use COUNT(DISTINCT TIP_DETAIL.SUBJECT_ID).",
    "- \"subject ID\" -> TIP_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct subject ID, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.SUBJECT_ID).",
    "- \"tip material key\" -> TIP_DETAIL.TIP_MATERIAL_KEY: already repeats ~1.4 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x9.9, so a plain COUNT over the joined result counts duplicates. To count distinct tip material key, use COUNT(DISTINCT TIP_DETAIL.TIP_MATERIAL_KEY).",
    "- \"tip material status key\" -> TIP_DETAIL.TIP_MATERIAL_STATUS_KEY: already repeats ~833.3 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x9.9, so a plain COUNT over the joined result counts duplicates. To count distinct tip material status key, use COUNT(DISTINCT TIP_DETAIL.TIP_MATERIAL_STATUS_KEY).",
    "- \"tip ISBN\" -> TIP_DETAIL.ISBN: already repeats ~2.0 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x9.9, so a plain COUNT over the joined result counts duplicates. To count distinct tip ISBN, use COUNT(DISTINCT TIP_DETAIL.ISBN).",
    "- \"subject title\" -> TIP_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.4 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct subject title, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.SUBJECT_TITLE).",
    "- \"windowed minimum tip material status key\" -> TIP_DETAIL.TIP_MATERIAL_STATUS_KEY: already repeats ~833.3 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x9.9, so a plain COUNT over the joined result counts duplicates. To count distinct windowed minimum tip material status key, use COUNT(DISTINCT TIP_DETAIL.TIP_MATERIAL_STATUS_KEY).",
    "- \"windowed minimum tip material status key\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~2.8 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"responsible faculty name\" -> TIP_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_NAME: already repeats ~4.1 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct responsible faculty name, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_NAME).",
    "- \"offer department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct offer department name, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"material status key equal to 'U'\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY: already repeats ~2000.0 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct material status key equal to 'U', use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY)."
  ],
  "dw_3901": [
    "- Join SUBJECT_OFFERED.TERM_CODE = SUBJECT_OFFERED_SUMMARY.TERM_CODE: SUBJECT_OFFERED.TERM_CODE repeats ~370.4 rows per value (10000 rows, 27 values); SUBJECT_OFFERED_SUMMARY.TERM_CODE repeats ~81.3 rows per value (10000 rows, 123 values).",
    "  -> each SUBJECT_OFFERED row appears ~81.3 times after this join.",
    "  -> each SUBJECT_OFFERED_SUMMARY row appears ~370.4 times after this join.",
    "- Join SUBJECT_OFFERED.TERM_CODE = SUBJECT_SUMMARY.TERM_CODE: SUBJECT_OFFERED.TERM_CODE repeats ~370.4 rows per value (10000 rows, 27 values); SUBJECT_SUMMARY.TERM_CODE repeats ~83.3 rows per value (10000 rows, 120 values).",
    "  -> each SUBJECT_OFFERED row appears ~83.3 times after this join.",
    "  -> each SUBJECT_SUMMARY row appears ~370.4 times after this join.",
    "- Join CIP.PROGRAM_CODE = SIS_COURSE_DESCRIPTION.CIP_PROGRAM_CODE: CIP.PROGRAM_CODE is unique per row (3059 rows); SIS_COURSE_DESCRIPTION.CIP_PROGRAM_CODE repeats ~10.9 rows per value (695 rows, 64 values).",
    "  -> each CIP row appears ~10.9 times after this join.",
    "- \"offer department code\" -> SUBJECT_OFFERED.OFFER_DEPT_CODE: already repeats ~10000.0 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED rows ~x83.3, so a plain COUNT over the joined result counts duplicates. To count distinct offer department code, use COUNT(DISTINCT SUBJECT_OFFERED.OFFER_DEPT_CODE).",
    "- \"2012SU term\" -> SUBJECT_OFFERED_SUMMARY.TERM_CODE: already repeats ~81.3 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x370.4, so a plain COUNT over the joined result counts duplicates. To count distinct 2012SU term, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.TERM_CODE).",
    "- \"2012SU term\" -> SUBJECT_SUMMARY.TERM_CODE: already repeats ~83.3 rows per value in its own table; the hinted joins multiply SUBJECT_SUMMARY rows ~x370.4, so a plain COUNT over the joined result counts duplicates. To count distinct 2012SU term, use COUNT(DISTINCT SUBJECT_SUMMARY.TERM_CODE).",
    "- \"total units\" -> SUBJECT_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_SUMMARY; the hinted joins multiply SUBJECT_SUMMARY rows ~x370.4, so SUM/AVG over the joined result would count each SUBJECT_SUMMARY row ~370.4 times. Aggregate it at SUBJECT_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"subject title\" -> SUBJECT_SUMMARY.SUBJECT_TITLE: already repeats ~2.1 rows per value in its own table; the hinted joins multiply SUBJECT_SUMMARY rows ~x370.4, so a plain COUNT over the joined result counts duplicates. To count distinct subject title, use COUNT(DISTINCT SUBJECT_SUMMARY.SUBJECT_TITLE).",
    "- \"course number\" -> SUBJECT_OFFERED_SUMMARY.COURSE_NUMBER: already repeats ~90.9 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x370.4, so a plain COUNT over the joined result counts duplicates. To count distinct course number, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.COURSE_NUMBER).",
    "- \"department name\" -> SUBJECT_SUMMARY.DEPARTMENT_NAME: already repeats ~181.8 rows per value in its own table; the hinted joins multiply SUBJECT_SUMMARY rows ~x370.4, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SUBJECT_SUMMARY.DEPARTMENT_NAME).",
    "- \"department name\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"dense rank\" -> SUBJECT_OFFERED.OFFER_DEPT_CODE: already repeats ~10000.0 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED rows ~x83.3, so a plain COUNT over the joined result counts duplicates. To count distinct dense rank, use COUNT(DISTINCT SUBJECT_OFFERED.OFFER_DEPT_CODE).",
    "- \"course\" -> SIS_COURSE_DESCRIPTION.COURSE: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"course description\" -> SIS_COURSE_DESCRIPTION.COURSE_DESCRIPTION: already repeats ~1.3 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"school name\" -> SIS_COURSE_DESCRIPTION.SCHOOL_NAME: already repeats ~77.2 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"program code\" -> CIP.PROGRAM_CODE: unique per row in its own table; the hinted joins multiply CIP rows ~x10.9, so a plain COUNT over the joined result counts duplicates. To count distinct program code, use COUNT(DISTINCT CIP.PROGRAM_CODE).",
    "- \"program title\" -> CIP.PROGRAM_TITLE: already repeats ~1.1 rows per value in its own table; the hinted joins multiply CIP rows ~x10.9, so a plain COUNT over the joined result counts duplicates. To count distinct program title, use COUNT(DISTINCT CIP.PROGRAM_TITLE).",
    "- \"category code\" -> CIP.CATEGORY_CODE: already repeats ~58.8 rows per value in its own table; the hinted joins multiply CIP rows ~x10.9, so a plain COUNT over the joined result counts duplicates. To count distinct category code, use COUNT(DISTINCT CIP.CATEGORY_CODE).",
    "- \"category title\" -> CIP.CATEGORY_TITLE: already repeats ~35.6 rows per value in its own table; the hinted joins multiply CIP rows ~x10.9, so a plain COUNT over the joined result counts duplicates. To count distinct category title, use COUNT(DISTINCT CIP.CATEGORY_TITLE).",
    "- \"four digit code\" -> CIP.FOUR_DIGIT_CODE: already repeats ~5.3 rows per value in its own table; the hinted joins multiply CIP rows ~x10.9, so a plain COUNT over the joined result counts duplicates. To count distinct four digit code, use COUNT(DISTINCT CIP.FOUR_DIGIT_CODE).",
    "- \"four digit title\" -> CIP.FOUR_DIGIT_TITLE: already repeats ~4.2 rows per value in its own table; the hinted joins multiply CIP rows ~x10.9, so a plain COUNT over the joined result counts duplicates. To count distinct four digit title, use COUNT(DISTINCT CIP.FOUR_DIGIT_TITLE).",
    "- \"minimum warehouse load date\" -> CIP.WAREHOUSE_LOAD_DATE: already repeats ~1529.5 rows per value in its own table; the hinted joins multiply CIP rows ~x10.9, so a plain COUNT over the joined result counts duplicates. To count distinct minimum warehouse load date, use COUNT(DISTINCT CIP.WAREHOUSE_LOAD_DATE).",
    "- \"department 6\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT: already repeats ~9.7 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"total subjects\" -> SUBJECT_SUMMARY.DEPARTMENT_NAME: already repeats ~181.8 rows per value in its own table; the hinted joins multiply SUBJECT_SUMMARY rows ~x370.4, so a plain COUNT over the joined result counts duplicates. To count distinct total subjects, use COUNT(DISTINCT SUBJECT_SUMMARY.DEPARTMENT_NAME).",
    "- \"total courses\" -> SIS_COURSE_DESCRIPTION.SCHOOL_NAME: already repeats ~77.2 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows."
  ],
  "dw_4004": [
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- Join CIS_COURSE_CATALOG.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: CIS_COURSE_CATALOG.DEPARTMENT_CODE repeats ~212.8 rows per value (10000 rows, 47 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~212.8 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.EFFECTIVE_TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.EFFECTIVE_TERM_CODE repeats ~200.0 rows per value (10000 rows, 50 values).",
    "  -> each ACADEMIC_TERMS row appears ~200.0 times after this join.",
    "- \"department name\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"department name\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"course level\" -> SIS_COURSE_DESCRIPTION.COURSE_LEVEL: already repeats ~347.5 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"degree granting\" -> SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING: already repeats ~347.5 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"total units\" -> CIS_COURSE_CATALOG.TOTAL_UNITS is a numeric measure of CIS_COURSE_CATALOG; the hinted joins do not multiply CIS_COURSE_CATALOG rows.",
    "- \"total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"unique courses\" -> SIS_COURSE_DESCRIPTION.COURSE: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"minimum total units\" -> CIS_COURSE_CATALOG.TOTAL_UNITS is a numeric measure of CIS_COURSE_CATALOG; the hinted joins do not multiply CIS_COURSE_CATALOG rows.",
    "- \"design units\" -> CIS_COURSE_CATALOG.DESIGN_UNITS is a numeric measure of CIS_COURSE_CATALOG; the hinted joins do not multiply CIS_COURSE_CATALOG rows.",
    "- \"academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x200.0, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR).",
    "- \"academic year description\" -> ACADEMIC_TERMS.ACADEMIC_YEAR_DESC: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x200.0, so a plain COUNT over the joined result counts duplicates. To count distinct academic year description, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR_DESC).",
    "- \"subject id\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"regular terms\" -> ACADEMIC_TERMS.IS_REGULAR_TERM: already repeats ~72.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x200.0, so a plain COUNT over the joined result counts duplicates. To count distinct regular terms, use COUNT(DISTINCT ACADEMIC_TERMS.IS_REGULAR_TERM)."
  ],
  "dw_4061": [
    "- Join ACADEMIC_TERMS.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.EFFECTIVE_TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.EFFECTIVE_TERM_CODE repeats ~200.0 rows per value (10000 rows, 50 values).",
    "  -> each ACADEMIC_TERMS row appears ~200.0 times after this join.",
    "- Join FCLT_BUILDING.FCLT_BUILDING_KEY = FCLT_ROOMS.FCLT_BUILDING_KEY: FCLT_BUILDING.FCLT_BUILDING_KEY is unique per row (242 rows); FCLT_ROOMS.FCLT_BUILDING_KEY repeats ~49.5 rows per value (10000 rows, 202 values).",
    "  -> each FCLT_BUILDING row appears ~49.5 times after this join.",
    "- \"department\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"department name\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x200.0, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR).",
    "- \"academic year description\" -> ACADEMIC_TERMS.ACADEMIC_YEAR_DESC: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x200.0, so a plain COUNT over the joined result counts duplicates. To count distinct academic year description, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR_DESC).",
    "- \"geometric mean of the total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subjects offered\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subjects offered\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"regular terms\" -> ACADEMIC_TERMS.IS_REGULAR_TERM: already repeats ~72.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x200.0, so a plain COUNT over the joined result counts duplicates. To count distinct regular terms, use COUNT(DISTINCT ACADEMIC_TERMS.IS_REGULAR_TERM).",
    "- \"total units greater than zero\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"academic building\" -> FCLT_BUILDING.BUILDING_TYPE: already repeats ~80.7 rows per value in its own table; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct academic building, use COUNT(DISTINCT FCLT_BUILDING.BUILDING_TYPE).",
    "- \"academic building\" -> FCLT_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct academic building, use COUNT(DISTINCT FCLT_BUILDING.BUILDING_NAME).",
    "- \"building name\" -> FCLT_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct building name, use COUNT(DISTINCT FCLT_BUILDING.BUILDING_NAME).",
    "- \"rooms larger than 40 square meters\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins do not multiply FCLT_ROOMS rows.",
    "- \"room area\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins do not multiply FCLT_ROOMS rows.",
    "- \"average of the room areas\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins do not multiply FCLT_ROOMS rows.",
    "- \"range of the room areas\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins do not multiply FCLT_ROOMS rows.",
    "- \"variance of the room areas\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins do not multiply FCLT_ROOMS rows.",
    "- \"total assignable area of the building\" -> FCLT_BUILDING.ASSIGNABLE_AREA is a numeric measure of FCLT_BUILDING; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so SUM/AVG over the joined result would count each FCLT_BUILDING row ~49.5 times. Aggregate it at FCLT_BUILDING's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"year the building was built\" -> FCLT_BUILDING.DATE_BUILT: already repeats ~2.2 rows per value in its own table; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct year the building was built, use COUNT(DISTINCT FCLT_BUILDING.DATE_BUILT)."
  ],
  "dw_4072": [
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- Join CIS_COURSE_CATALOG.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: CIS_COURSE_CATALOG.DEPARTMENT_CODE repeats ~212.8 rows per value (10000 rows, 47 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~212.8 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SIS_SUBJECT_CODE.DEPARTMENT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SIS_SUBJECT_CODE.DEPARTMENT_CODE repeats ~3.6 rows per value (221 rows, 61 values).",
    "  -> each SIS_DEPARTMENT row appears ~3.6 times after this join.",
    "- \"department name\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"course level\" -> SIS_COURSE_DESCRIPTION.COURSE_LEVEL: already repeats ~347.5 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"degree granting\" -> SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING: already repeats ~347.5 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"degree granting\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct degree granting, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"total units\" -> CIS_COURSE_CATALOG.TOTAL_UNITS is a numeric measure of CIS_COURSE_CATALOG; the hinted joins do not multiply CIS_COURSE_CATALOG rows.",
    "- \"unique courses\" -> SIS_COURSE_DESCRIPTION.COURSE: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"minimum total units\" -> CIS_COURSE_CATALOG.TOTAL_UNITS is a numeric measure of CIS_COURSE_CATALOG; the hinted joins do not multiply CIS_COURSE_CATALOG rows.",
    "- \"average total units\" -> CIS_COURSE_CATALOG.TOTAL_UNITS is a numeric measure of CIS_COURSE_CATALOG; the hinted joins do not multiply CIS_COURSE_CATALOG rows.",
    "- \"subject code\" -> SIS_SUBJECT_CODE.SUBJECT_CODE: unique per row in its own table; the hinted joins do not multiply SIS_SUBJECT_CODE rows.",
    "- \"number of subject codes\" -> SIS_SUBJECT_CODE.SUBJECT_CODE: unique per row in its own table; the hinted joins do not multiply SIS_SUBJECT_CODE rows.",
    "- \"minimum department budget code\" -> SIS_DEPARTMENT.DEPT_BUDGET_CODE: already repeats ~2.2 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct minimum department budget code, use COUNT(DISTINCT SIS_DEPARTMENT.DEPT_BUDGET_CODE).",
    "- \"full department name\" -> SIS_DEPARTMENT.DEPARTMENT_FULL_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct full department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_FULL_NAME)."
  ],
  "dw_4101": [
    "- Join FCLT_ORGANIZATION.DLC_KEY = FCLT_ORG_DLC_KEY.DLC_KEY: FCLT_ORGANIZATION.DLC_KEY repeats ~1.3 rows per value (180 rows, 136 values); FCLT_ORG_DLC_KEY.DLC_KEY repeats ~1.2 rows per value (168 rows, 136 values).",
    "  -> each FCLT_ORGANIZATION row appears ~1.2 times after this join.",
    "  -> each FCLT_ORG_DLC_KEY row appears ~1.3 times after this join.",
    "- Join FCLT_ORGANIZATION.DLC_KEY = SPACE_UNIT.DLC_KEY: FCLT_ORGANIZATION.DLC_KEY repeats ~1.3 rows per value (180 rows, 136 values); SPACE_UNIT.DLC_KEY repeats ~1.1 rows per value (150 rows, 132 values).",
    "  -> each FCLT_ORGANIZATION row appears ~1.1 times after this join.",
    "  -> each SPACE_UNIT row appears ~1.3 times after this join.",
    "- Join FCLT_ORG_DLC_KEY.DLC_KEY = MASTER_DEPT_HIERARCHY.DLC_KEY: FCLT_ORG_DLC_KEY.DLC_KEY repeats ~1.2 rows per value (168 rows, 136 values); MASTER_DEPT_HIERARCHY.DLC_KEY is unique per row (310 rows).",
    "  -> each MASTER_DEPT_HIERARCHY row appears ~1.2 times after this join.",
    "- \"department name\" -> MASTER_DEPT_HIERARCHY.DLC_NAME: unique per row in its own table; the hinted joins multiply MASTER_DEPT_HIERARCHY rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT MASTER_DEPT_HIERARCHY.DLC_NAME).",
    "- \"organization key\" -> FCLT_ORG_DLC_KEY.FCLT_ORGANIZATION_KEY: unique per row in its own table; the hinted joins multiply FCLT_ORG_DLC_KEY rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct organization key, use COUNT(DISTINCT FCLT_ORG_DLC_KEY.FCLT_ORGANIZATION_KEY).",
    "- \"Provost Area\" -> MASTER_DEPT_HIERARCHY.MASTER_DEPT_HIER_LEVEL_2_NAME: already repeats ~38.8 rows per value in its own table; the hinted joins multiply MASTER_DEPT_HIERARCHY rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct Provost Area, use COUNT(DISTINCT MASTER_DEPT_HIERARCHY.MASTER_DEPT_HIER_LEVEL_2_NAME).",
    "- \"number of organizations\" -> FCLT_ORG_DLC_KEY.FCLT_ORGANIZATION_KEY: unique per row in its own table; the hinted joins multiply FCLT_ORG_DLC_KEY rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct number of organizations, use COUNT(DISTINCT FCLT_ORG_DLC_KEY.FCLT_ORGANIZATION_KEY).",
    "- \"minimum department hierarchy level 3 codes\" -> MASTER_DEPT_HIERARCHY.MASTER_DEPT_HIER_LEVEL_3_CODE: already repeats ~15.5 rows per value in its own table; the hinted joins multiply MASTER_DEPT_HIERARCHY rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct minimum department hierarchy level 3 codes, use COUNT(DISTINCT MASTER_DEPT_HIERARCHY.MASTER_DEPT_HIER_LEVEL_3_CODE).",
    "- \"maximum department hierarchy level 3 codes\" -> MASTER_DEPT_HIERARCHY.MASTER_DEPT_HIER_LEVEL_3_CODE: already repeats ~15.5 rows per value in its own table; the hinted joins multiply MASTER_DEPT_HIERARCHY rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct maximum department hierarchy level 3 codes, use COUNT(DISTINCT MASTER_DEPT_HIERARCHY.MASTER_DEPT_HIER_LEVEL_3_CODE).",
    "- \"organization name\" -> FCLT_ORGANIZATION.ORGANIZATION_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct organization name, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_NAME).",
    "- \"number of associated space units\" -> SPACE_UNIT.SPACE_UNIT_KEY: unique per row in its own table; the hinted joins multiply SPACE_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct number of associated space units, use COUNT(DISTINCT SPACE_UNIT.SPACE_UNIT_KEY).",
    "- \"average number of space units\" -> SPACE_UNIT.SPACE_UNIT_KEY: unique per row in its own table; the hinted joins multiply SPACE_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct average number of space units, use COUNT(DISTINCT SPACE_UNIT.SPACE_UNIT_KEY).",
    "- \"holder category\" -> SPACE_UNIT.SPACE_UNIT_KEY: unique per row in its own table; the hinted joins multiply SPACE_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct holder category, use COUNT(DISTINCT SPACE_UNIT.SPACE_UNIT_KEY)."
  ],
  "dw_4128": [
    "- Join ACADEMIC_TERMS.TERM_CODE = ACADEMIC_TERM_PARAMETER.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); ACADEMIC_TERM_PARAMETER.TERM_CODE is unique per row (3 rows).",
    "- Join ACADEMIC_TERMS.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.EFFECTIVE_TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.EFFECTIVE_TERM_CODE repeats ~200.0 rows per value (10000 rows, 50 values).",
    "  -> each ACADEMIC_TERMS row appears ~200.0 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE repeats ~196.1 rows per value (10000 rows, 51 values).",
    "  -> each SIS_DEPARTMENT row appears ~196.1 times after this join.",
    "- \"term parameter\" -> ACADEMIC_TERM_PARAMETER.TERM_PARAMETER: unique per row in its own table; the hinted joins do not multiply ACADEMIC_TERM_PARAMETER rows.",
    "- \"term code\" -> ACADEMIC_TERM_PARAMETER.TERM_CODE: unique per row in its own table; the hinted joins do not multiply ACADEMIC_TERM_PARAMETER rows.",
    "- \"term code\" -> ACADEMIC_TERMS.TERM_CODE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x200.0, so a plain COUNT over the joined result counts duplicates. To count distinct term code, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_CODE).",
    "- \"academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x200.0, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR).",
    "- \"department name\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"term indicator\" -> ACADEMIC_TERM_PARAMETER.TERM_INDICATOR: unique per row in its own table; the hinted joins do not multiply ACADEMIC_TERM_PARAMETER rows.",
    "- \"total units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"enrolled student\" -> SUBJECT_OFFERED_SUMMARY.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"subjects offered\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subjects offered\" -> SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subjects\" -> SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"average of total units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"standard deviation of total units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"department\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME)."
  ],
  "dw_4155": [
    "- Join SIS_DEPARTMENT.SCHOOL_CODE = SIS_SUBJECT_CODE.SCHOOL_CODE: SIS_DEPARTMENT.SCHOOL_CODE repeats ~12.8 rows per value (128 rows, 10 values); SIS_SUBJECT_CODE.SCHOOL_CODE repeats ~24.6 rows per value (221 rows, 9 values).",
    "  -> each SIS_DEPARTMENT row appears ~24.6 times after this join.",
    "  -> each SIS_SUBJECT_CODE row appears ~12.8 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~105.3 times after this join.",
    "- \"subject code\" -> SIS_SUBJECT_CODE.SUBJECT_CODE: unique per row in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x12.8, so a plain COUNT over the joined result counts duplicates. To count distinct subject code, use COUNT(DISTINCT SIS_SUBJECT_CODE.SUBJECT_CODE).",
    "- \"subject code\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE: already repeats ~181.8 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subject code description\" -> SIS_SUBJECT_CODE.SUBJECT_CODE_DESC: already repeats ~1.2 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x12.8, so a plain COUNT over the joined result counts duplicates. To count distinct subject code description, use COUNT(DISTINCT SIS_SUBJECT_CODE.SUBJECT_CODE_DESC).",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x24.6, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"department name\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"school name\" -> SIS_SUBJECT_CODE.SCHOOL_NAME: already repeats ~24.6 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x12.8, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_SUBJECT_CODE.SCHOOL_NAME).",
    "- \"number of subject codes per school name\" -> SIS_SUBJECT_CODE.SCHOOL_NAME: already repeats ~24.6 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x12.8, so a plain COUNT over the joined result counts duplicates. To count distinct number of subject codes per school name, use COUNT(DISTINCT SIS_SUBJECT_CODE.SCHOOL_NAME).",
    "- \"subject code description is present\" -> SIS_SUBJECT_CODE.SUBJECT_CODE_DESC: already repeats ~1.2 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x12.8, so a plain COUNT over the joined result counts duplicates. To count distinct subject code description is present, use COUNT(DISTINCT SIS_SUBJECT_CODE.SUBJECT_CODE_DESC).",
    "- \"subject code description is missing\" -> SIS_SUBJECT_CODE.SUBJECT_CODE_DESC: already repeats ~1.2 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x12.8, so a plain COUNT over the joined result counts duplicates. To count distinct subject code description is missing, use COUNT(DISTINCT SIS_SUBJECT_CODE.SUBJECT_CODE_DESC).",
    "- \"subjects offered in the term with code '2022SU'\" -> ACADEMIC_TERMS_ALL.TERM_CODE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered in the term with code '2022SU', use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_CODE).",
    "- \"subjects offered in the term with code '2022SU'\" -> COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: already repeats ~105.3 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subject's ID\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"dense rank\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows."
  ],
  "dw_427": [
    "- Join SUBJECT_OFFERED.SUBJECT_ID_SORT = SUBJECT_OFFERED_SUMMARY.SUBJECT_ID_SORT: SUBJECT_OFFERED.SUBJECT_ID_SORT repeats ~4.8 rows per value (10000 rows, 2081 values); SUBJECT_OFFERED_SUMMARY.SUBJECT_ID_SORT repeats ~2.4 rows per value (10000 rows, 4122 values).",
    "  -> each SUBJECT_OFFERED row appears ~2.4 times after this join.",
    "  -> each SUBJECT_OFFERED_SUMMARY row appears ~4.8 times after this join.",
    "- \"department\" -> SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~10000.0 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED rows ~x2.4, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"Harvard Cross-Enrollment Program\" -> SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~10000.0 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED rows ~x2.4, so a plain COUNT over the joined result counts duplicates. To count distinct Harvard Cross-Enrollment Program, use COUNT(DISTINCT SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"2012\" -> SUBJECT_OFFERED.TERM_CODE: already repeats ~370.4 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED rows ~x2.4, so a plain COUNT over the joined result counts duplicates. To count distinct 2012, use COUNT(DISTINCT SUBJECT_OFFERED.TERM_CODE).",
    "- \"average total number of units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x4.8, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~4.8 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"subjects\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x4.8, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~4.8 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"subjects\" -> SUBJECT_OFFERED_SUMMARY.CLUSTER_TYPE_DESC: already repeats ~3333.3 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x4.8, so a plain COUNT over the joined result counts duplicates. To count distinct subjects, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.CLUSTER_TYPE_DESC).",
    "- \"cluster type\" -> SUBJECT_OFFERED_SUMMARY.CLUSTER_TYPE_DESC: already repeats ~3333.3 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x4.8, so a plain COUNT over the joined result counts duplicates. To count distinct cluster type, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.CLUSTER_TYPE_DESC).",
    "- \"SWE: School-Wide Electives\" -> SUBJECT_OFFERED_SUMMARY.CLUSTER_TYPE_DESC: already repeats ~3333.3 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x4.8, so a plain COUNT over the joined result counts duplicates. To count distinct SWE: School-Wide Electives, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.CLUSTER_TYPE_DESC)."
  ],
  "dw_4339": [
    "- Join SIS_COURSE_DESCRIPTION.COURSE = SIS_SUBJECT_CODE.COURSE_NUMBER: SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values); SIS_SUBJECT_CODE.COURSE_NUMBER is unique per row (221 rows).",
    "  -> each SIS_SUBJECT_CODE row appears ~1.1 times after this join.",
    "- Join SIS_COURSE_DESCRIPTION.COURSE = SUBJECT_OFFERED_SUMMARY.COURSE_NUMBER: SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values); SUBJECT_OFFERED_SUMMARY.COURSE_NUMBER repeats ~90.9 rows per value (10000 rows, 110 values).",
    "  -> each SIS_COURSE_DESCRIPTION row appears ~90.9 times after this join.",
    "  -> each SUBJECT_OFFERED_SUMMARY row appears ~1.1 times after this join.",
    "- Join LIBRARY_RESERVE_MATRL_DETAIL.SUBJECT_ID = TIP_DETAIL.SUBJECT_ID: LIBRARY_RESERVE_MATRL_DETAIL.SUBJECT_ID repeats ~9.9 rows per value (10000 rows, 1011 values); TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values).",
    "  -> each LIBRARY_RESERVE_MATRL_DETAIL row appears ~2.8 times after this join.",
    "  -> each TIP_DETAIL row appears ~9.9 times after this join.",
    "- Join TIP_DETAIL.SUBJECT_ID = TIP_SUBJECT_OFFERED.SUBJECT_ID: TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values); TIP_SUBJECT_OFFERED.SUBJECT_ID repeats ~2.0 rows per value (10000 rows, 4974 values).",
    "  -> each TIP_DETAIL row appears ~2.0 times after this join.",
    "  -> each TIP_SUBJECT_OFFERED row appears ~2.8 times after this join.",
    "- \"department\" -> SIS_SUBJECT_CODE.DEPARTMENT_CODE: already repeats ~3.6 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SIS_SUBJECT_CODE.DEPARTMENT_CODE).",
    "- \"department\" -> SIS_SUBJECT_CODE.DEPARTMENT_NAME: already repeats ~3.7 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SIS_SUBJECT_CODE.DEPARTMENT_NAME).",
    "- \"department code\" -> SIS_SUBJECT_CODE.DEPARTMENT_CODE: already repeats ~3.6 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct department code, use COUNT(DISTINCT SIS_SUBJECT_CODE.DEPARTMENT_CODE).",
    "- \"department name\" -> SIS_SUBJECT_CODE.DEPARTMENT_NAME: already repeats ~3.7 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_SUBJECT_CODE.DEPARTMENT_NAME).",
    "- \"variance of the total number of units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~1.1 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"average number of enrolled students\" -> SUBJECT_OFFERED_SUMMARY.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~1.1 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"enrollment category\" -> SUBJECT_OFFERED_SUMMARY.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~1.1 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"library reserve material detail\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY: already repeats ~6.2 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct library reserve material detail, use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY).",
    "- \"library reserve material detail\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: already repeats ~1.2 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct library reserve material detail, use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY).",
    "- \"library reserve material detail\" -> LIBRARY_RESERVE_MATRL_DETAIL.TERM_CODE: already repeats ~1250.0 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct library reserve material detail, use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.TERM_CODE).",
    "- \"library reserve material detail\" -> LIBRARY_RESERVE_MATRL_DETAIL.SUBJECT_ID: already repeats ~9.9 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct library reserve material detail, use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.SUBJECT_ID).",
    "- \"library reserve material detail\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY: already repeats ~2000.0 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct library reserve material detail, use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY).",
    "- \"material status key\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY: already repeats ~2000.0 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct material status key, use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY).",
    "- \"library course instructor key\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY: already repeats ~6.2 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct library course instructor key, use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY).",
    "- \"library reserve catalog key\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: already repeats ~1.2 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct library reserve catalog key, use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY).",
    "- \"term code\" -> LIBRARY_RESERVE_MATRL_DETAIL.TERM_CODE: already repeats ~1250.0 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct term code, use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.TERM_CODE).",
    "- \"subject ID\" -> LIBRARY_RESERVE_MATRL_DETAIL.SUBJECT_ID: already repeats ~9.9 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct subject ID, use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.SUBJECT_ID).",
    "- \"subject ID\" -> TIP_DETAIL.SUBJECT_ID: already repeats ~2.8 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x9.9, so a plain COUNT over the joined result counts duplicates. To count distinct subject ID, use COUNT(DISTINCT TIP_DETAIL.SUBJECT_ID).",
    "- \"subject ID\" -> TIP_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct subject ID, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.SUBJECT_ID).",
    "- \"tip material key\" -> TIP_DETAIL.TIP_MATERIAL_KEY: already repeats ~1.4 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x9.9, so a plain COUNT over the joined result counts duplicates. To count distinct tip material key, use COUNT(DISTINCT TIP_DETAIL.TIP_MATERIAL_KEY).",
    "- \"tip material status key\" -> TIP_DETAIL.TIP_MATERIAL_STATUS_KEY: already repeats ~833.3 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x9.9, so a plain COUNT over the joined result counts duplicates. To count distinct tip material status key, use COUNT(DISTINCT TIP_DETAIL.TIP_MATERIAL_STATUS_KEY).",
    "- \"tip ISBN\" -> TIP_DETAIL.ISBN: already repeats ~2.0 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x9.9, so a plain COUNT over the joined result counts duplicates. To count distinct tip ISBN, use COUNT(DISTINCT TIP_DETAIL.ISBN).",
    "- \"subject title\" -> TIP_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.4 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct subject title, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.SUBJECT_TITLE).",
    "- \"minimum tip material status key in a window\" -> TIP_DETAIL.TIP_MATERIAL_STATUS_KEY: already repeats ~833.3 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x9.9, so a plain COUNT over the joined result counts duplicates. To count distinct minimum tip material status key in a window, use COUNT(DISTINCT TIP_DETAIL.TIP_MATERIAL_STATUS_KEY).",
    "- \"minimum tip material status key in a window\" -> SUBJECT_OFFERED_SUMMARY.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~1.1 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"responsible faculty name\" -> TIP_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_NAME: already repeats ~4.1 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct responsible faculty name, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_NAME).",
    "- \"offer department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct offer department name, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"degree-granting subjects\" -> SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING: already repeats ~347.5 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x90.9, so a plain COUNT over the joined result counts duplicates. To count distinct degree-granting subjects, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING).",
    "- \"enrolled students\" -> SUBJECT_OFFERED_SUMMARY.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~1.1 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"subjects offered by that department\" -> SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE: already repeats ~196.1 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered by that department, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE)."
  ],
  "dw_4349": [
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = LIBRARY_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); LIBRARY_SUBJECT_OFFERED.TERM_CODE repeats ~192.3 rows per value (10000 rows, 52 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~192.3 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = ACADEMIC_TERMS_ALL.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows).",
    "- Join ACADEMIC_TERMS.TERM_CODE = TIP_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); TIP_SUBJECT_OFFERED.TERM_CODE repeats ~79.4 rows per value (10000 rows, 126 values).",
    "  -> each ACADEMIC_TERMS row appears ~79.4 times after this join.",
    "- \"academic year\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR).",
    "- \"academic year description\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct academic year description, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC).",
    "- \"geometric mean of the number of enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"library subjects\" -> LIBRARY_SUBJECT_OFFERED.TERM_CODE: already repeats ~192.3 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"subjects with more than zero enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"departments that offered such subjects\" -> LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_CODE: already repeats ~625.0 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"subjects offered per academic term\" -> TIP_SUBJECT_OFFERED.TERM_CODE: already repeats ~79.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"number of subjects offered\" -> TIP_SUBJECT_OFFERED.TERM_CODE: already repeats ~79.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"range of the number of subjects offered\" -> TIP_SUBJECT_OFFERED.TERM_CODE: already repeats ~79.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"variance of the number of subjects offered\" -> TIP_SUBJECT_OFFERED.TERM_CODE: already repeats ~79.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"standard deviation of the number of subjects offered\" -> TIP_SUBJECT_OFFERED.TERM_CODE: already repeats ~79.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"variability category\" -> TIP_SUBJECT_OFFERED.TERM_CODE: already repeats ~79.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows."
  ],
  "dw_4351": [
    "- Join ACADEMIC_TERMS.TERM_CODE = LIBRARY_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); LIBRARY_SUBJECT_OFFERED.TERM_CODE repeats ~192.3 rows per value (10000 rows, 52 values).",
    "  -> each ACADEMIC_TERMS row appears ~192.3 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SIS_SUBJECT_CODE.DEPARTMENT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SIS_SUBJECT_CODE.DEPARTMENT_CODE repeats ~3.6 rows per value (221 rows, 61 values).",
    "  -> each SIS_DEPARTMENT row appears ~3.6 times after this join.",
    "- \"courses\" -> LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER: already repeats ~625.0 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"Mathematics department\" -> LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~625.0 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"average for Mathematics department courses in the same academic year\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"average for Mathematics department courses in the same academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct average for Mathematics department courses in the same academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR).",
    "- \"course number\" -> LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER: already repeats ~625.0 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"course number\" -> SIS_SUBJECT_CODE.COURSE_NUMBER: unique per row in its own table; the hinted joins do not multiply SIS_SUBJECT_CODE rows.",
    "- \"subject title\" -> LIBRARY_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~5.6 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"number of enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"term description\" -> ACADEMIC_TERMS.TERM_DESCRIPTION: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct term description, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_DESCRIPTION).",
    "- \"academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR).",
    "- \"row number within each academic year group ordered by course number\" -> LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER: already repeats ~625.0 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"row number within each academic year group ordered by course number\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct row number within each academic year group ordered by course number, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR).",
    "- \"degree-granting department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x3.6, so a plain COUNT over the joined result counts duplicates. To count distinct degree-granting department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"degree-granting department\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x3.6, so a plain COUNT over the joined result counts duplicates. To count distinct degree-granting department, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x3.6, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"subject code\" -> SIS_SUBJECT_CODE.SUBJECT_CODE: unique per row in its own table; the hinted joins do not multiply SIS_SUBJECT_CODE rows.",
    "- \"subject code description\" -> SIS_SUBJECT_CODE.SUBJECT_CODE_DESC: already repeats ~1.2 rows per value in its own table; the hinted joins do not multiply SIS_SUBJECT_CODE rows.",
    "- \"average ASCII value of course number\" -> SIS_SUBJECT_CODE.COURSE_NUMBER: unique per row in its own table; the hinted joins do not multiply SIS_SUBJECT_CODE rows.",
    "- \"variance of ASCII value of course number\" -> SIS_SUBJECT_CODE.COURSE_NUMBER: unique per row in its own table; the hinted joins do not multiply SIS_SUBJECT_CODE rows."
  ],
  "dw_4374": [
    "- Join SIS_COURSE_DESCRIPTION.COURSE = SIS_SUBJECT_CODE.COURSE_NUMBER: SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values); SIS_SUBJECT_CODE.COURSE_NUMBER is unique per row (221 rows).",
    "  -> each SIS_SUBJECT_CODE row appears ~1.1 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE = SIS_COURSE_DESCRIPTION.COURSE: COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE repeats ~181.8 rows per value (10000 rows, 55 values); SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values).",
    "  -> each COURSE_CATALOG_SUBJECT_OFFERED row appears ~1.1 times after this join.",
    "  -> each SIS_COURSE_DESCRIPTION row appears ~181.8 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = LIBRARY_RESERVE_MATRL_DETAIL.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); LIBRARY_RESERVE_MATRL_DETAIL.TERM_CODE repeats ~1250.0 rows per value (10000 rows, 8 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~1250.0 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~105.3 times after this join.",
    "- \"department name\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME).",
    "- \"department name\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME).",
    "- \"academic year\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x1250.0, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR).",
    "- \"academic year description\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x1250.0, so a plain COUNT over the joined result counts duplicates. To count distinct academic year description, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC).",
    "- \"number of degree-granting subjects offered in 2022\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct number of degree-granting subjects offered in 2022, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR).",
    "- \"number of degree-granting subjects offered in 2022\" -> SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING: already repeats ~347.5 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct number of degree-granting subjects offered in 2022, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING).",
    "- \"total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~1.1 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"library reserve materials\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY: already repeats ~2000.0 rows per value in its own table; the hinted joins do not multiply LIBRARY_RESERVE_MATRL_DETAIL rows.",
    "- \"material status 'U'\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY: already repeats ~2000.0 rows per value in its own table; the hinted joins do not multiply LIBRARY_RESERVE_MATRL_DETAIL rows."
  ],
  "dw_4407": [
    "- Join SIS_DEPARTMENT.SCHOOL_CODE = SIS_SUBJECT_CODE.SCHOOL_CODE: SIS_DEPARTMENT.SCHOOL_CODE repeats ~12.8 rows per value (128 rows, 10 values); SIS_SUBJECT_CODE.SCHOOL_CODE repeats ~24.6 rows per value (221 rows, 9 values).",
    "  -> each SIS_DEPARTMENT row appears ~24.6 times after this join.",
    "  -> each SIS_SUBJECT_CODE row appears ~12.8 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~105.3 times after this join.",
    "- \"subject code\" -> SIS_SUBJECT_CODE.SUBJECT_CODE: unique per row in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x12.8, so a plain COUNT over the joined result counts duplicates. To count distinct subject code, use COUNT(DISTINCT SIS_SUBJECT_CODE.SUBJECT_CODE).",
    "- \"subject code description\" -> SIS_SUBJECT_CODE.SUBJECT_CODE_DESC: already repeats ~1.2 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x12.8, so a plain COUNT over the joined result counts duplicates. To count distinct subject code description, use COUNT(DISTINCT SIS_SUBJECT_CODE.SUBJECT_CODE_DESC).",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x24.6, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"department name\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"school name\" -> SIS_SUBJECT_CODE.SCHOOL_NAME: already repeats ~24.6 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x12.8, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_SUBJECT_CODE.SCHOOL_NAME).",
    "- \"degree granting\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x24.6, so a plain COUNT over the joined result counts duplicates. To count distinct degree granting, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"description status\" -> SIS_SUBJECT_CODE.SUBJECT_CODE_DESC: already repeats ~1.2 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x12.8, so a plain COUNT over the joined result counts duplicates. To count distinct description status, use COUNT(DISTINCT SIS_SUBJECT_CODE.SUBJECT_CODE_DESC).",
    "- \"total subject codes\" -> SIS_SUBJECT_CODE.SUBJECT_CODE: unique per row in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x12.8, so a plain COUNT over the joined result counts duplicates. To count distinct total subject codes, use COUNT(DISTINCT SIS_SUBJECT_CODE.SUBJECT_CODE).",
    "- \"subject ID\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"term code '2022SU'\" -> ACADEMIC_TERMS_ALL.TERM_CODE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct term code '2022SU', use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_CODE).",
    "- \"term code '2022SU'\" -> COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: already repeats ~105.3 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"dense rank\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows."
  ],
  "dw_4413": [
    "- Join ACADEMIC_TERMS.TERM_CODE = IAP_SUBJECT_DETAIL.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); IAP_SUBJECT_DETAIL.TERM_CODE repeats ~465.0 rows per value (465 rows, 1 values).",
    "  -> each ACADEMIC_TERMS row appears ~465.0 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = LIBRARY_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); LIBRARY_SUBJECT_OFFERED.TERM_CODE repeats ~192.3 rows per value (10000 rows, 52 values).",
    "  -> each ACADEMIC_TERMS row appears ~192.3 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = TIP_DETAIL.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); TIP_DETAIL.TERM_CODE repeats ~158.7 rows per value (10000 rows, 63 values).",
    "  -> each ACADEMIC_TERMS row appears ~158.7 times after this join.",
    "- \"enrollment type\" -> IAP_SUBJECT_DETAIL.ENROLLMENT_TYPE: already repeats ~116.2 rows per value in its own table; the hinted joins do not multiply IAP_SUBJECT_DETAIL rows.",
    "- \"Advance sign-up required\" -> IAP_SUBJECT_DETAIL.ENROLLMENT_TYPE: already repeats ~116.2 rows per value in its own table; the hinted joins do not multiply IAP_SUBJECT_DETAIL rows.",
    "- \"maximum enrollment\" -> IAP_SUBJECT_DETAIL.MAX_ENROLLMENT is a numeric measure of IAP_SUBJECT_DETAIL; the hinted joins do not multiply IAP_SUBJECT_DETAIL rows.",
    "- \"12\" -> IAP_SUBJECT_DETAIL.MAX_ENROLLMENT is a numeric measure of IAP_SUBJECT_DETAIL; the hinted joins do not multiply IAP_SUBJECT_DETAIL rows.",
    "- \"activity title\" -> IAP_SUBJECT_DETAIL.ACTIVITY_TITLE: already repeats ~4.4 rows per value in its own table; the hinted joins do not multiply IAP_SUBJECT_DETAIL rows.",
    "- \"term code\" -> IAP_SUBJECT_DETAIL.TERM_CODE: already repeats ~465.0 rows per value in its own table; the hinted joins do not multiply IAP_SUBJECT_DETAIL rows.",
    "- \"term description\" -> ACADEMIC_TERMS.TERM_DESCRIPTION: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x465.0, so a plain COUNT over the joined result counts duplicates. To count distinct term description, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_DESCRIPTION).",
    "- \"term start date\" -> ACADEMIC_TERMS.TERM_START_DATE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x465.0, so a plain COUNT over the joined result counts duplicates. To count distinct term start date, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_START_DATE).",
    "- \"subject title\" -> LIBRARY_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~5.6 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"number of enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"offer department name\" -> LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~625.0 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"responsible faculty name\" -> LIBRARY_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_NAME: already repeats ~6.9 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"window column\" -> IAP_SUBJECT_DETAIL.ACTIVITY_TITLE: already repeats ~4.4 rows per value in its own table; the hinted joins do not multiply IAP_SUBJECT_DETAIL rows.",
    "- \"window column\" -> LIBRARY_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~5.6 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"highest number of associated course materials\" -> TIP_DETAIL.TIP_MATERIAL_KEY: already repeats ~1.4 rows per value in its own table; the hinted joins do not multiply TIP_DETAIL rows.",
    "- \"number of enrolled students is at least 10\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"order the results by activity title within each subject title group\" -> LIBRARY_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~5.6 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"order the results by activity title within each subject title group\" -> IAP_SUBJECT_DETAIL.ACTIVITY_TITLE: already repeats ~4.4 rows per value in its own table; the hinted joins do not multiply IAP_SUBJECT_DETAIL rows."
  ],
  "dw_4513": [
    "- Join ACADEMIC_TERMS.TERM_CODE = TIP_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); TIP_SUBJECT_OFFERED.TERM_CODE repeats ~79.4 rows per value (10000 rows, 126 values).",
    "  -> each ACADEMIC_TERMS row appears ~79.4 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = ACADEMIC_TERM_PARAMETER.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); ACADEMIC_TERM_PARAMETER.TERM_CODE is unique per row (3 rows).",
    "- Join LIBRARY_RESERVE_MATRL_DETAIL.SUBJECT_ID = LIBRARY_SUBJECT_OFFERED.SUBJECT_ID: LIBRARY_RESERVE_MATRL_DETAIL.SUBJECT_ID repeats ~9.9 rows per value (10000 rows, 1011 values); LIBRARY_SUBJECT_OFFERED.SUBJECT_ID repeats ~4.4 rows per value (10000 rows, 2249 values).",
    "  -> each LIBRARY_RESERVE_MATRL_DETAIL row appears ~4.4 times after this join.",
    "  -> each LIBRARY_SUBJECT_OFFERED row appears ~9.9 times after this join.",
    "- \"subject offered key\" -> TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY: unique per row in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"subject title\" -> TIP_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"offering department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"term description\" -> ACADEMIC_TERMS.TERM_DESCRIPTION: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct term description, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_DESCRIPTION).",
    "- \"rank of each subject within its offering department\" -> TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY: unique per row in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"rank of each subject within its offering department\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"course number\" -> LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER: already repeats ~625.0 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x9.9, so a plain COUNT over the joined result counts duplicates. To count distinct course number, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER).",
    "- \"course number description\" -> LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER_DESC: already repeats ~625.0 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x9.9, so a plain COUNT over the joined result counts duplicates. To count distinct course number description, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER_DESC).",
    "- \"number of enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x9.9, so SUM/AVG over the joined result would count each LIBRARY_SUBJECT_OFFERED row ~9.9 times. Aggregate it at LIBRARY_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"reserve catalog key\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: already repeats ~1.2 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct reserve catalog key, use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY).",
    "- \"dense rank over the material status key\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY: already repeats ~2000.0 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct dense rank over the material status key, use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY).",
    "- \"start date of the term\" -> ACADEMIC_TERMS.TERM_START_DATE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct start date of the term, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_START_DATE).",
    "- \"end date of the term\" -> ACADEMIC_TERMS.TERM_END_DATE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct end date of the term, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_END_DATE).",
    "- \"description \"Fall Term 2024-2025\"\" -> ACADEMIC_TERMS.TERM_DESCRIPTION: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct description \"Fall Term 2024-2025\", use COUNT(DISTINCT ACADEMIC_TERMS.TERM_DESCRIPTION).",
    "- \"more than 0 enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x9.9, so SUM/AVG over the joined result would count each LIBRARY_SUBJECT_OFFERED row ~9.9 times. Aggregate it at LIBRARY_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"material status key of 'U'\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY: already repeats ~2000.0 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct material status key of 'U', use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY)."
  ],
  "dw_4522": [
    "- Join ACADEMIC_TERMS.TERM_CODE = TIP_DETAIL.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); TIP_DETAIL.TERM_CODE repeats ~158.7 rows per value (10000 rows, 63 values).",
    "  -> each ACADEMIC_TERMS row appears ~158.7 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = SUBJECT_SUMMARY.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); SUBJECT_SUMMARY.TERM_CODE repeats ~83.3 rows per value (10000 rows, 120 values).",
    "  -> each ACADEMIC_TERMS row appears ~83.3 times after this join.",
    "- Join LIBRARY_SUBJECT_OFFERED.SUBJECT_ID = TIP_DETAIL.SUBJECT_ID: LIBRARY_SUBJECT_OFFERED.SUBJECT_ID repeats ~4.4 rows per value (10000 rows, 2249 values); TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values).",
    "  -> each LIBRARY_SUBJECT_OFFERED row appears ~2.8 times after this join.",
    "  -> each TIP_DETAIL row appears ~4.4 times after this join.",
    "- Join SUBJECT_OFFERED_SUMMARY.SUBJECT_ID = TIP_DETAIL.SUBJECT_ID: SUBJECT_OFFERED_SUMMARY.SUBJECT_ID repeats ~2.4 rows per value (10000 rows, 4122 values); TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values).",
    "  -> each SUBJECT_OFFERED_SUMMARY row appears ~2.8 times after this join.",
    "  -> each TIP_DETAIL row appears ~2.4 times after this join.",
    "- \"subject title\" -> SUBJECT_SUMMARY.SUBJECT_TITLE: already repeats ~2.1 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"subject title\" -> LIBRARY_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~5.6 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct subject title, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.SUBJECT_TITLE).",
    "- \"department name\" -> SUBJECT_SUMMARY.DEPARTMENT_NAME: already repeats ~181.8 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"material count\" -> TIP_DETAIL.TIP_MATERIAL_KEY: already repeats ~1.4 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct material count, use COUNT(DISTINCT TIP_DETAIL.TIP_MATERIAL_KEY).",
    "- \"offering department name\" -> LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~625.0 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct offering department name, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"responsible faculty name\" -> LIBRARY_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_NAME: already repeats ~6.9 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct responsible faculty name, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_NAME).",
    "- \"TIP material status key\" -> TIP_DETAIL.TIP_MATERIAL_STATUS_KEY: already repeats ~833.3 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct TIP material status key, use COUNT(DISTINCT TIP_DETAIL.TIP_MATERIAL_STATUS_KEY).",
    "- \"total units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~2.8 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"window column\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~2.8 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"window column\" -> SUBJECT_OFFERED_SUMMARY.COURSE_NUMBER: already repeats ~90.9 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct window column, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.COURSE_NUMBER).",
    "- \"school name\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins do not multiply SIS_DEPARTMENT rows.",
    "- \"department phone\" -> SIS_ADMIN_DEPARTMENT.DEPARTMENT_PHONE_NUMBER: already repeats ~2.8 rows per value in its own table; the hinted joins do not multiply SIS_ADMIN_DEPARTMENT rows.",
    "- \"top 10 by number of materials\" -> TIP_DETAIL.TIP_MATERIAL_KEY: already repeats ~1.4 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct top 10 by number of materials, use COUNT(DISTINCT TIP_DETAIL.TIP_MATERIAL_KEY).",
    "- \"2014FA term\" -> LIBRARY_SUBJECT_OFFERED.TERM_CODE: already repeats ~192.3 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct 2014FA term, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.TERM_CODE).",
    "- \"enrolled student\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x2.8, so SUM/AVG over the joined result would count each LIBRARY_SUBJECT_OFFERED row ~2.8 times. Aggregate it at LIBRARY_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_4553": [
    "- Join ACADEMIC_TERMS.TERM_CODE = ACADEMIC_TERM_PARAMETER.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); ACADEMIC_TERM_PARAMETER.TERM_CODE is unique per row (3 rows).",
    "- Join ACADEMIC_TERMS.TERM_CODE = SUBJECT_SUMMARY.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); SUBJECT_SUMMARY.TERM_CODE repeats ~83.3 rows per value (10000 rows, 120 values).",
    "  -> each ACADEMIC_TERMS row appears ~83.3 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = ACADEMIC_TERMS_ALL.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows).",
    "- Join ACADEMIC_TERMS.TERM_CODE = TIP_DETAIL.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); TIP_DETAIL.TERM_CODE repeats ~158.7 rows per value (10000 rows, 63 values).",
    "  -> each ACADEMIC_TERMS row appears ~158.7 times after this join.",
    "- \"current academic term\" -> ACADEMIC_TERM_PARAMETER.IS_CURRENT_TERM: already repeats ~1.5 rows per value in its own table; the hinted joins do not multiply ACADEMIC_TERM_PARAMETER rows.",
    "- \"term code\" -> ACADEMIC_TERM_PARAMETER.TERM_CODE: unique per row in its own table; the hinted joins do not multiply ACADEMIC_TERM_PARAMETER rows.",
    "- \"term code\" -> ACADEMIC_TERMS.TERM_CODE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x158.7, so a plain COUNT over the joined result counts duplicates. To count distinct term code, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_CODE).",
    "- \"term code\" -> SUBJECT_SUMMARY.TERM_CODE: already repeats ~83.3 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"term description\" -> ACADEMIC_TERM_PARAMETER.TERM_DESCRIPTION: unique per row in its own table; the hinted joins do not multiply ACADEMIC_TERM_PARAMETER rows.",
    "- \"academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x158.7, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR).",
    "- \"number of subjects offered\" -> SUBJECT_SUMMARY.SUBJECT_ID: already repeats ~1.9 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"total subject enrollment number\" -> SUBJECT_SUMMARY.SUBJECT_ENROLLMENT_NUMBER: already repeats ~57.8 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"academic year description\" -> ACADEMIC_TERMS.ACADEMIC_YEAR_DESC: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x158.7, so a plain COUNT over the joined result counts duplicates. To count distinct academic year description, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR_DESC).",
    "- \"department with the highest number of subjects offered\" -> SUBJECT_SUMMARY.DEPARTMENT_NAME: already repeats ~181.8 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"count of subjects from that department\" -> SUBJECT_SUMMARY.SUBJECT_ID: already repeats ~1.9 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows."
  ],
  "dw_4570": [
    "- Join CIS_COURSE_CATALOG.DEPARTMENT_CODE = SIS_SUBJECT_CODE.DEPARTMENT_CODE: CIS_COURSE_CATALOG.DEPARTMENT_CODE repeats ~212.8 rows per value (10000 rows, 47 values); SIS_SUBJECT_CODE.DEPARTMENT_CODE repeats ~3.6 rows per value (221 rows, 61 values).",
    "  -> each CIS_COURSE_CATALOG row appears ~3.6 times after this join.",
    "  -> each SIS_SUBJECT_CODE row appears ~212.8 times after this join.",
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- Join MIT_STUDENT_DIRECTORY.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: MIT_STUDENT_DIRECTORY.DEPARTMENT repeats ~204.1 rows per value (10000 rows, 49 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~204.1 times after this join.",
    "- \"department name\" -> SIS_SUBJECT_CODE.DEPARTMENT_NAME: already repeats ~3.7 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_SUBJECT_CODE.DEPARTMENT_NAME).",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"maximum total units\" -> CIS_COURSE_CATALOG.TOTAL_UNITS is a numeric measure of CIS_COURSE_CATALOG; the hinted joins multiply CIS_COURSE_CATALOG rows ~x3.6, so SUM/AVG over the joined result would count each CIS_COURSE_CATALOG row ~3.6 times. Aggregate it at CIS_COURSE_CATALOG's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"school name\" -> SIS_SUBJECT_CODE.SCHOOL_NAME: already repeats ~24.6 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x212.8, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_SUBJECT_CODE.SCHOOL_NAME).",
    "- \"student's full name\" -> MIT_STUDENT_DIRECTORY.FULL_NAME: already repeats ~1.2 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"email address\" -> MIT_STUDENT_DIRECTORY.EMAIL_ADDRESS: already repeats ~3.0 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"course description\" -> SIS_COURSE_DESCRIPTION.COURSE_DESCRIPTION: already repeats ~1.3 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"course level\" -> SIS_COURSE_DESCRIPTION.COURSE_LEVEL: already repeats ~347.5 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"most common student year\" -> MIT_STUDENT_DIRECTORY.STUDENT_YEAR: already repeats ~2000.0 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"Electrical Eng & Computer Sci\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct Electrical Eng & Computer Sci, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME)."
  ],
  "dw_458": [
    "- Join FCLT_BUILDING.FCLT_BUILDING_KEY = FCLT_ROOMS.FCLT_BUILDING_KEY: FCLT_BUILDING.FCLT_BUILDING_KEY is unique per row (242 rows); FCLT_ROOMS.FCLT_BUILDING_KEY repeats ~49.5 rows per value (10000 rows, 202 values).",
    "  -> each FCLT_BUILDING row appears ~49.5 times after this join.",
    "- Join FCLT_BUILDING.FCLT_BUILDING_KEY = FCLT_BUILDING_ADDRESS.FCLT_BUILDING_KEY: FCLT_BUILDING.FCLT_BUILDING_KEY is unique per row (242 rows); FCLT_BUILDING_ADDRESS.FCLT_BUILDING_KEY repeats ~3.2 rows per value (785 rows, 242 values).",
    "  -> each FCLT_BUILDING row appears ~3.2 times after this join.",
    "- \"academic building\" -> FCLT_BUILDING.BUILDING_TYPE: already repeats ~80.7 rows per value in its own table; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct academic building, use COUNT(DISTINCT FCLT_BUILDING.BUILDING_TYPE).",
    "- \"Ashdown House\" -> FCLT_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct Ashdown House, use COUNT(DISTINCT FCLT_BUILDING.BUILDING_NAME).",
    "- \"area of its rooms\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins do not multiply FCLT_ROOMS rows.",
    "- \"building's full name\" -> FCLT_BUILDING.BUILDING_NAME_LONG: unique per row in its own table; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct building's full name, use COUNT(DISTINCT FCLT_BUILDING.BUILDING_NAME_LONG).",
    "- \"postal code\" -> FCLT_BUILDING_ADDRESS.POSTAL_CODE: already repeats ~56.1 rows per value in its own table; the hinted joins do not multiply FCLT_BUILDING_ADDRESS rows.",
    "- \"coefficient of variation\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins do not multiply FCLT_ROOMS rows."
  ],
  "dw_4588": [
    "- Join FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY = FCLT_ROOMS.FCLT_ORGANIZATION_KEY: FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY repeats ~1.1 rows per value (180 rows, 168 values); FCLT_ROOMS.FCLT_ORGANIZATION_KEY repeats ~79.4 rows per value (10000 rows, 126 values).",
    "  -> each FCLT_ORGANIZATION row appears ~79.4 times after this join.",
    "  -> each FCLT_ROOMS row appears ~1.1 times after this join.",
    "- Join FCLT_ORGANIZATION.DLC_KEY = SPACE_UNIT.DLC_KEY: FCLT_ORGANIZATION.DLC_KEY repeats ~1.3 rows per value (180 rows, 136 values); SPACE_UNIT.DLC_KEY repeats ~1.1 rows per value (150 rows, 132 values).",
    "  -> each FCLT_ORGANIZATION row appears ~1.1 times after this join.",
    "  -> each SPACE_UNIT row appears ~1.3 times after this join.",
    "- Join SPACE_SUPERVISOR_USAGE.DEPT_NAMES = SPACE_UNIT.DLC_KEY: SPACE_SUPERVISOR_USAGE.DEPT_NAMES repeats ~15.8 rows per value (2135 rows, 135 values); SPACE_UNIT.DLC_KEY repeats ~1.1 rows per value (150 rows, 132 values).",
    "  -> each SPACE_SUPERVISOR_USAGE row appears ~1.1 times after this join.",
    "  -> each SPACE_UNIT row appears ~15.8 times after this join.",
    "- \"major use description\" -> FCLT_ROOMS.MAJOR_USE_DESC: already repeats ~769.2 rows per value in its own table; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct major use description, use COUNT(DISTINCT FCLT_ROOMS.MAJOR_USE_DESC).",
    "- \"organization name\" -> FCLT_ORGANIZATION.ORGANIZATION_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct organization name, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_NAME).",
    "- \"room area\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so SUM/AVG over the joined result would count each FCLT_ROOMS row ~1.1 times. Aggregate it at FCLT_ROOMS's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"access level\" -> FCLT_ROOMS.ACCESS_LEVEL: already repeats ~2500.0 rows per value in its own table; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct access level, use COUNT(DISTINCT FCLT_ROOMS.ACCESS_LEVEL).",
    "- \"number of rooms\" -> FCLT_ROOMS.FCLT_ROOM_KEY: unique per row in its own table; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct number of rooms, use COUNT(DISTINCT FCLT_ROOMS.FCLT_ROOM_KEY).",
    "- \"minimum floor number\" -> FCLT_ROOMS.FLOOR: already repeats ~208.3 rows per value in its own table; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct minimum floor number, use COUNT(DISTINCT FCLT_ROOMS.FLOOR).",
    "- \"number of supervisees\" -> SPACE_SUPERVISOR_USAGE.NUM_OF_SUPERVISEES is a numeric measure of SPACE_SUPERVISOR_USAGE; the hinted joins multiply SPACE_SUPERVISOR_USAGE rows ~x1.1, so SUM/AVG over the joined result would count each SPACE_SUPERVISOR_USAGE row ~1.1 times. Aggregate it at SPACE_SUPERVISOR_USAGE's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"department\" -> SPACE_SUPERVISOR_USAGE.DEPT_NAMES: already repeats ~15.8 rows per value in its own table; the hinted joins multiply SPACE_SUPERVISOR_USAGE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SPACE_SUPERVISOR_USAGE.DEPT_NAMES).",
    "- \"research volume\" -> SPACE_SUPERVISOR_USAGE.RESEARCH_VOLUME is a numeric measure of SPACE_SUPERVISOR_USAGE; the hinted joins multiply SPACE_SUPERVISOR_USAGE rows ~x1.1, so SUM/AVG over the joined result would count each SPACE_SUPERVISOR_USAGE row ~1.1 times. Aggregate it at SPACE_SUPERVISOR_USAGE's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_461": [
    "- Join SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE is unique per row (179 rows); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "- \"department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins do not multiply SIS_DEPARTMENT rows.",
    "- \"degree granting\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins do not multiply SIS_DEPARTMENT rows.",
    "- \"department budget code\" -> SIS_DEPARTMENT.DEPT_BUDGET_CODE: already repeats ~2.2 rows per value in its own table; the hinted joins do not multiply SIS_DEPARTMENT rows.",
    "- \"Mathematics department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins do not multiply SIS_DEPARTMENT rows.",
    "- \"Physics department\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins do not multiply SIS_DEPARTMENT rows.",
    "- \"total clearing cost collector\" -> SIS_ADMIN_DEPARTMENT.CLEARING_COST_COLLECTOR: already repeats ~3.7 rows per value in its own table; the hinted joins do not multiply SIS_ADMIN_DEPARTMENT rows.",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins do not multiply SIS_DEPARTMENT rows."
  ],
  "dw_4610": [
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each SIS_DEPARTMENT row appears ~161.3 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SIS_SUBJECT_CODE.DEPARTMENT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SIS_SUBJECT_CODE.DEPARTMENT_CODE repeats ~3.6 rows per value (221 rows, 61 values).",
    "  -> each SIS_DEPARTMENT row appears ~3.6 times after this join.",
    "- Join LIBRARY_RESERVE_MATRL_DETAIL.SUBJECT_ID = TIP_DETAIL.SUBJECT_ID: LIBRARY_RESERVE_MATRL_DETAIL.SUBJECT_ID repeats ~9.9 rows per value (10000 rows, 1011 values); TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values).",
    "  -> each LIBRARY_RESERVE_MATRL_DETAIL row appears ~2.8 times after this join.",
    "  -> each TIP_DETAIL row appears ~9.9 times after this join.",
    "- Join SUBJECT_OFFERED_SUMMARY.SUBJECT_ID = TIP_DETAIL.SUBJECT_ID: SUBJECT_OFFERED_SUMMARY.SUBJECT_ID repeats ~2.4 rows per value (10000 rows, 4122 values); TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values).",
    "  -> each SUBJECT_OFFERED_SUMMARY row appears ~2.8 times after this join.",
    "  -> each TIP_DETAIL row appears ~2.4 times after this join.",
    "- \"subject title\" -> SUBJECT_OFFERED_SUMMARY.SUBJECT_TITLE: already repeats ~2.3 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct subject title, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.SUBJECT_TITLE).",
    "- \"offering department name\" -> SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_NAME: already repeats ~200.0 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct offering department name, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_NAME).",
    "- \"offering department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct offering department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"variance of the number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"standard deviation of the number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"departments that grant degrees\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct departments that grant degrees, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"subject code description\" -> SIS_SUBJECT_CODE.SUBJECT_CODE_DESC: already repeats ~1.2 rows per value in its own table; the hinted joins do not multiply SIS_SUBJECT_CODE rows.",
    "- \"count of reserved materials\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: already repeats ~1.2 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct count of reserved materials, use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY).",
    "- \"total number of units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~2.8 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"term '2009SP'\" -> LIBRARY_RESERVE_MATRL_DETAIL.TERM_CODE: already repeats ~1250.0 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_MATRL_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct term '2009SP', use COUNT(DISTINCT LIBRARY_RESERVE_MATRL_DETAIL.TERM_CODE)."
  ],
  "dw_4691": [
    "- Join TIP_DETAIL.SUBJECT_ID = TIP_SUBJECT_OFFERED.SUBJECT_ID: TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values); TIP_SUBJECT_OFFERED.SUBJECT_ID repeats ~2.0 rows per value (10000 rows, 4974 values).",
    "  -> each TIP_DETAIL row appears ~2.0 times after this join.",
    "  -> each TIP_SUBJECT_OFFERED row appears ~2.8 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = SUBJECT_SUMMARY.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); SUBJECT_SUMMARY.TERM_CODE repeats ~83.3 rows per value (10000 rows, 120 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~83.3 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = SUBJECT_SUMMARY.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); SUBJECT_SUMMARY.TERM_CODE repeats ~83.3 rows per value (10000 rows, 120 values).",
    "  -> each ACADEMIC_TERMS row appears ~83.3 times after this join.",
    "- \"department\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"department\" -> SUBJECT_SUMMARY.DEPARTMENT_NAME: already repeats ~181.8 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"department name\" -> SUBJECT_SUMMARY.DEPARTMENT_NAME: already repeats ~181.8 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"average number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~2.8 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"standard deviation of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~2.8 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"courses with at least one associated course material\" -> TIP_DETAIL.TIP_MATERIAL_STATUS_KEY: already repeats ~833.3 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x2.0, so a plain COUNT over the joined result counts duplicates. To count distinct courses with at least one associated course material, use COUNT(DISTINCT TIP_DETAIL.TIP_MATERIAL_STATUS_KEY).",
    "- \"courses with at least one associated course material\" -> TIP_DETAIL.SUBJECT_ID: already repeats ~2.8 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x2.0, so a plain COUNT over the joined result counts duplicates. To count distinct courses with at least one associated course material, use COUNT(DISTINCT TIP_DETAIL.SUBJECT_ID).",
    "- \"courses with at least one associated course material\" -> TIP_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct courses with at least one associated course material, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.SUBJECT_ID).",
    "- \"subject ID\" -> SUBJECT_SUMMARY.SUBJECT_ID: already repeats ~1.9 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"subject title\" -> SUBJECT_SUMMARY.SUBJECT_TITLE: already repeats ~2.1 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x83.3, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR).",
    "- \"subjects in the 'Engineering' school\" -> SUBJECT_SUMMARY.SCHOOL_NAME: already repeats ~1000.0 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"total units equal to 1\" -> SUBJECT_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_SUMMARY; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"term description\" -> ACADEMIC_TERMS.TERM_DESCRIPTION: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x83.3, so a plain COUNT over the joined result counts duplicates. To count distinct term description, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_DESCRIPTION).",
    "- \"subject rank within academic year\" -> SUBJECT_SUMMARY.SUBJECT_ID: already repeats ~1.9 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"subject rank within academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x83.3, so a plain COUNT over the joined result counts duplicates. To count distinct subject rank within academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR)."
  ],
  "dw_4725": [
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~105.3 times after this join.",
    "- Join TIP_DETAIL.SUBJECT_ID = TIP_SUBJECT_OFFERED.SUBJECT_ID: TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values); TIP_SUBJECT_OFFERED.SUBJECT_ID repeats ~2.0 rows per value (10000 rows, 4974 values).",
    "  -> each TIP_DETAIL row appears ~2.0 times after this join.",
    "  -> each TIP_SUBJECT_OFFERED row appears ~2.8 times after this join.",
    "- Join SUBJECT_OFFERED_SUMMARY.SUBJECT_ID = TIP_DETAIL.SUBJECT_ID: SUBJECT_OFFERED_SUMMARY.SUBJECT_ID repeats ~2.4 rows per value (10000 rows, 4122 values); TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values).",
    "  -> each SUBJECT_OFFERED_SUMMARY row appears ~2.8 times after this join.",
    "  -> each TIP_DETAIL row appears ~2.4 times after this join.",
    "- \"subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subjects\" -> TIP_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct subjects, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.SUBJECT_ID).",
    "- \"subject ID\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"department name\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"term description\" -> ACADEMIC_TERMS_ALL.TERM_DESCRIPTION: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct term description, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_DESCRIPTION).",
    "- \"term code '2022FA'\" -> COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: already repeats ~105.3 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"total units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~2.8 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"academic year\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"offering department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct offering department name, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"offering school name\" -> TIP_SUBJECT_OFFERED.OFFER_SCHOOL_NAME: already repeats ~1111.1 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct offering school name, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_SCHOOL_NAME).",
    "- \"number of unique subjects\" -> TIP_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct number of unique subjects, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.SUBJECT_ID).",
    "- \"minimum total units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~2.8 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"average number of enrolled students\" -> SUBJECT_OFFERED_SUMMARY.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~2.8 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"course material status 'RQ'\" -> TIP_DETAIL.TIP_MATERIAL_STATUS_KEY: already repeats ~833.3 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x2.4, so a plain COUNT over the joined result counts duplicates. To count distinct course material status 'RQ', use COUNT(DISTINCT TIP_DETAIL.TIP_MATERIAL_STATUS_KEY)."
  ],
  "dw_4771": [
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each SIS_DEPARTMENT row appears ~161.3 times after this join.",
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- Join TIP_DETAIL.SUBJECT_ID = TIP_SUBJECT_OFFERED.SUBJECT_ID: TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values); TIP_SUBJECT_OFFERED.SUBJECT_ID repeats ~2.0 rows per value (10000 rows, 4974 values).",
    "  -> each TIP_DETAIL row appears ~2.0 times after this join.",
    "  -> each TIP_SUBJECT_OFFERED row appears ~2.8 times after this join.",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"range of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~2.8 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"variance of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~2.8 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"subjects offered by that department\" -> TIP_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered by that department, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.SUBJECT_ID).",
    "- \"departments that grant degrees\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct departments that grant degrees, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"courses at the graduate level\" -> SIS_COURSE_DESCRIPTION.COURSE_LEVEL: already repeats ~347.5 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"distinct subject IDs\" -> TIP_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct distinct subject IDs, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.SUBJECT_ID).",
    "- \"minimum ISBN value\" -> TIP_DETAIL.ISBN: already repeats ~2.0 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x2.0, so a plain COUNT over the joined result counts duplicates. To count distinct minimum ISBN value, use COUNT(DISTINCT TIP_DETAIL.ISBN).",
    "- \"course materials\" -> TIP_DETAIL.ISBN: already repeats ~2.0 rows per value in its own table; the hinted joins multiply TIP_DETAIL rows ~x2.0, so a plain COUNT over the joined result counts duplicates. To count distinct course materials, use COUNT(DISTINCT TIP_DETAIL.ISBN).",
    "- \"offering departments\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct offering departments, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"offering schools\" -> TIP_SUBJECT_OFFERED.OFFER_SCHOOL_NAME: already repeats ~1111.1 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct offering schools, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_SCHOOL_NAME).",
    "- \"number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~2.8 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"average number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~2.8 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_4876": [
    "- Join HR_ORG_UNIT.DLC_KEY = MASTER_DEPT_HIERARCHY.DLC_KEY: HR_ORG_UNIT.DLC_KEY repeats ~2.8 rows per value (641 rows, 231 values); MASTER_DEPT_HIERARCHY.DLC_KEY is unique per row (310 rows).",
    "  -> each MASTER_DEPT_HIERARCHY row appears ~2.8 times after this join.",
    "- Join FCLT_ORGANIZATION.DLC_KEY = SPACE_UNIT.DLC_KEY: FCLT_ORGANIZATION.DLC_KEY repeats ~1.3 rows per value (180 rows, 136 values); SPACE_UNIT.DLC_KEY repeats ~1.1 rows per value (150 rows, 132 values).",
    "  -> each FCLT_ORGANIZATION row appears ~1.1 times after this join.",
    "  -> each SPACE_UNIT row appears ~1.3 times after this join.",
    "- Join FCLT_ORGANIZATION.DLC_KEY = HR_ORG_UNIT.DLC_KEY: FCLT_ORGANIZATION.DLC_KEY repeats ~1.3 rows per value (180 rows, 136 values); HR_ORG_UNIT.DLC_KEY repeats ~2.8 rows per value (641 rows, 231 values).",
    "  -> each FCLT_ORGANIZATION row appears ~2.8 times after this join.",
    "  -> each HR_ORG_UNIT row appears ~1.3 times after this join.",
    "- \"departments\" -> HR_ORG_UNIT.HR_DEPARTMENT_CODE: already repeats ~1.3 rows per value in its own table; the hinted joins multiply HR_ORG_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct departments, use COUNT(DISTINCT HR_ORG_UNIT.HR_DEPARTMENT_CODE).",
    "- \"departments\" -> HR_ORG_UNIT.HR_DEPARTMENT_NAME: already repeats ~1.3 rows per value in its own table; the hinted joins multiply HR_ORG_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct departments, use COUNT(DISTINCT HR_ORG_UNIT.HR_DEPARTMENT_NAME).",
    "- \"department code\" -> HR_ORG_UNIT.HR_DEPARTMENT_CODE: already repeats ~1.3 rows per value in its own table; the hinted joins multiply HR_ORG_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct department code, use COUNT(DISTINCT HR_ORG_UNIT.HR_DEPARTMENT_CODE).",
    "- \"department name\" -> HR_ORG_UNIT.HR_DEPARTMENT_NAME: already repeats ~1.3 rows per value in its own table; the hinted joins multiply HR_ORG_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT HR_ORG_UNIT.HR_DEPARTMENT_NAME).",
    "- \"school area name\" -> HR_ORG_UNIT.ORG_HIER_SCHOOL_AREA_NAME: already repeats ~22.9 rows per value in its own table; the hinted joins multiply HR_ORG_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct school area name, use COUNT(DISTINCT HR_ORG_UNIT.ORG_HIER_SCHOOL_AREA_NAME).",
    "- \"Provost Area\" -> MASTER_DEPT_HIERARCHY.MASTER_DEPT_HIER_LEVEL_2_NAME: already repeats ~38.8 rows per value in its own table; the hinted joins multiply MASTER_DEPT_HIERARCHY rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct Provost Area, use COUNT(DISTINCT MASTER_DEPT_HIERARCHY.MASTER_DEPT_HIER_LEVEL_2_NAME).",
    "- \"department rank within school area\" -> HR_ORG_UNIT.HR_DEPARTMENT_CODE: already repeats ~1.3 rows per value in its own table; the hinted joins multiply HR_ORG_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct department rank within school area, use COUNT(DISTINCT HR_ORG_UNIT.HR_DEPARTMENT_CODE).",
    "- \"department rank within school area\" -> HR_ORG_UNIT.ORG_HIER_SCHOOL_AREA_NAME: already repeats ~22.9 rows per value in its own table; the hinted joins multiply HR_ORG_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct department rank within school area, use COUNT(DISTINCT HR_ORG_UNIT.ORG_HIER_SCHOOL_AREA_NAME).",
    "- \"space unit count (for department)\" -> SPACE_UNIT.SPACE_UNIT_KEY: unique per row in its own table; the hinted joins multiply SPACE_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct space unit count (for department), use COUNT(DISTINCT SPACE_UNIT.SPACE_UNIT_KEY).",
    "- \"sum of space unit keys (for department)\" -> SPACE_UNIT.SPACE_UNIT_KEY: unique per row in its own table; the hinted joins multiply SPACE_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct sum of space unit keys (for department), use COUNT(DISTINCT SPACE_UNIT.SPACE_UNIT_KEY).",
    "- \"space unit key\" -> SPACE_UNIT.SPACE_UNIT_KEY: unique per row in its own table; the hinted joins multiply SPACE_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct space unit key, use COUNT(DISTINCT SPACE_UNIT.SPACE_UNIT_KEY).",
    "- \"space unit code\" -> SPACE_UNIT.SPACE_UNIT_CODE: unique per row in its own table; the hinted joins multiply SPACE_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct space unit code, use COUNT(DISTINCT SPACE_UNIT.SPACE_UNIT_CODE).",
    "- \"organization name\" -> FCLT_ORGANIZATION.ORGANIZATION_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct organization name, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_NAME).",
    "- \"HR department name (for space unit)\" -> HR_ORG_UNIT.HR_DEPARTMENT_NAME: already repeats ~1.3 rows per value in its own table; the hinted joins multiply HR_ORG_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct HR department name (for space unit), use COUNT(DISTINCT HR_ORG_UNIT.HR_DEPARTMENT_NAME).",
    "- \"sum of space unit keys within warehouse load date\" -> SPACE_UNIT.SPACE_UNIT_KEY: unique per row in its own table; the hinted joins multiply SPACE_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct sum of space unit keys within warehouse load date, use COUNT(DISTINCT SPACE_UNIT.SPACE_UNIT_KEY).",
    "- \"sum of space unit keys within warehouse load date\" -> SPACE_UNIT.WAREHOUSE_LOAD_DATE: already repeats ~150.0 rows per value in its own table; the hinted joins multiply SPACE_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct sum of space unit keys within warehouse load date, use COUNT(DISTINCT SPACE_UNIT.WAREHOUSE_LOAD_DATE).",
    "- \"warehouse load date\" -> SPACE_UNIT.WAREHOUSE_LOAD_DATE: already repeats ~150.0 rows per value in its own table; the hinted joins multiply SPACE_UNIT rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct warehouse load date, use COUNT(DISTINCT SPACE_UNIT.WAREHOUSE_LOAD_DATE)."
  ],
  "dw_4961": [
    "- Join ACADEMIC_TERMS.TERM_CODE = ACADEMIC_TERM_PARAMETER.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); ACADEMIC_TERM_PARAMETER.TERM_CODE is unique per row (3 rows).",
    "- Join ACADEMIC_TERMS.TERM_CODE = SUBJECT_SUMMARY.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); SUBJECT_SUMMARY.TERM_CODE repeats ~83.3 rows per value (10000 rows, 120 values).",
    "  -> each ACADEMIC_TERMS row appears ~83.3 times after this join.",
    "- Join LIBRARY_SUBJECT_OFFERED.SUBJECT_ID = TIP_DETAIL.SUBJECT_ID: LIBRARY_SUBJECT_OFFERED.SUBJECT_ID repeats ~4.4 rows per value (10000 rows, 2249 values); TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values).",
    "  -> each LIBRARY_SUBJECT_OFFERED row appears ~2.8 times after this join.",
    "  -> each TIP_DETAIL row appears ~4.4 times after this join.",
    "- Join SUBJECT_OFFERED_SUMMARY.SUBJECT_ID = TIP_DETAIL.SUBJECT_ID: SUBJECT_OFFERED_SUMMARY.SUBJECT_ID repeats ~2.4 rows per value (10000 rows, 4122 values); TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values).",
    "  -> each SUBJECT_OFFERED_SUMMARY row appears ~2.8 times after this join.",
    "  -> each TIP_DETAIL row appears ~2.4 times after this join.",
    "- \"department name\" -> SUBJECT_SUMMARY.DEPARTMENT_NAME: already repeats ~181.8 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"department name\" -> LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~625.0 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"total number of subject enrollments\" -> SUBJECT_SUMMARY.SUBJECT_ENROLLMENT_NUMBER: already repeats ~57.8 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"course number\" -> LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER: already repeats ~625.0 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct course number, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER).",
    "- \"number of unique library subjects offered\" -> LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY: unique per row in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct number of unique library subjects offered, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY).",
    "- \"minimum total units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~2.8 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"average number of enrolled students\" -> SUBJECT_OFFERED_SUMMARY.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~2.8 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"current term\" -> ACADEMIC_TERM_PARAMETER.IS_CURRENT_TERM: already repeats ~1.5 rows per value in its own table; the hinted joins do not multiply ACADEMIC_TERM_PARAMETER rows.",
    "- \"current term\" -> ACADEMIC_TERMS.TERM_CODE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x83.3, so a plain COUNT over the joined result counts duplicates. To count distinct current term, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_CODE).",
    "- \"current term\" -> SUBJECT_SUMMARY.TERM_CODE: already repeats ~83.3 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"enrolled students in the subject summary\" -> SUBJECT_OFFERED_SUMMARY.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x2.8, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~2.8 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_5008": [
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = TIP_DETAIL.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); TIP_DETAIL.TERM_CODE repeats ~158.7 rows per value (10000 rows, 63 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~158.7 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = ACADEMIC_TERM_PARAMETER.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); ACADEMIC_TERM_PARAMETER.TERM_CODE is unique per row (3 rows).",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~105.3 times after this join.",
    "- \"academic year\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x158.7, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR).",
    "- \"academic year description\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x158.7, so a plain COUNT over the joined result counts duplicates. To count distinct academic year description, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC).",
    "- \"variance of the record count of tip details\" -> TIP_DETAIL.RECORD_COUNT is a numeric measure of TIP_DETAIL; the hinted joins do not multiply TIP_DETAIL rows.",
    "- \"current or previous terms\" -> ACADEMIC_TERM_PARAMETER.TERM_PARAMETER: unique per row in its own table; the hinted joins do not multiply ACADEMIC_TERM_PARAMETER rows.",
    "- \"geometric mean of the record counts of required course materials\" -> TIP_DETAIL.RECORD_COUNT is a numeric measure of TIP_DETAIL; the hinted joins do not multiply TIP_DETAIL rows.",
    "- \"geometric mean of the record counts of required course materials\" -> TIP_DETAIL.TIP_MATERIAL_STATUS_KEY: already repeats ~833.3 rows per value in its own table; the hinted joins do not multiply TIP_DETAIL rows.",
    "- \"subjects offered\" -> COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: already repeats ~105.3 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"departments offering subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE: already repeats ~166.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows."
  ],
  "dw_5136": [
    "- Join FCLT_ORGANIZATION.DLC_KEY = MASTER_DEPT_HIERARCHY.DLC_KEY: FCLT_ORGANIZATION.DLC_KEY repeats ~1.3 rows per value (180 rows, 136 values); MASTER_DEPT_HIERARCHY.DLC_KEY is unique per row (310 rows).",
    "  -> each MASTER_DEPT_HIERARCHY row appears ~1.3 times after this join.",
    "- Join FCLT_ORGANIZATION.DLC_KEY = SPACE_UNIT.DLC_KEY: FCLT_ORGANIZATION.DLC_KEY repeats ~1.3 rows per value (180 rows, 136 values); SPACE_UNIT.DLC_KEY repeats ~1.1 rows per value (150 rows, 132 values).",
    "  -> each FCLT_ORGANIZATION row appears ~1.1 times after this join.",
    "  -> each SPACE_UNIT row appears ~1.3 times after this join.",
    "- Join SPACE_SUPERVISOR_USAGE.DEPT_NAMES = SPACE_UNIT.DLC_KEY: SPACE_SUPERVISOR_USAGE.DEPT_NAMES repeats ~15.8 rows per value (2135 rows, 135 values); SPACE_UNIT.DLC_KEY repeats ~1.1 rows per value (150 rows, 132 values).",
    "  -> each SPACE_SUPERVISOR_USAGE row appears ~1.1 times after this join.",
    "  -> each SPACE_UNIT row appears ~15.8 times after this join.",
    "- \"department hierarchy level 2 name\" -> MASTER_DEPT_HIERARCHY.MASTER_DEPT_HIER_LEVEL_2_NAME: already repeats ~38.8 rows per value in its own table; the hinted joins multiply MASTER_DEPT_HIERARCHY rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct department hierarchy level 2 name, use COUNT(DISTINCT MASTER_DEPT_HIERARCHY.MASTER_DEPT_HIER_LEVEL_2_NAME).",
    "- \"organization name\" -> FCLT_ORGANIZATION.ORGANIZATION_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct organization name, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_NAME).",
    "- \"organization is assignable\" -> FCLT_ORGANIZATION.ASSIGNABLE: already repeats ~90.0 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct organization is assignable, use COUNT(DISTINCT FCLT_ORGANIZATION.ASSIGNABLE).",
    "- \"number of organizations\" -> FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct number of organizations, use COUNT(DISTINCT FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY).",
    "- \"minimum organization number\" -> FCLT_ORGANIZATION.ORGANIZATION_NUMBER: already repeats ~1.2 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct minimum organization number, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_NUMBER).",
    "- \"number of supervisees\" -> SPACE_SUPERVISOR_USAGE.NUM_OF_SUPERVISEES is a numeric measure of SPACE_SUPERVISOR_USAGE; the hinted joins multiply SPACE_SUPERVISOR_USAGE rows ~x1.1, so SUM/AVG over the joined result would count each SPACE_SUPERVISOR_USAGE row ~1.1 times. Aggregate it at SPACE_SUPERVISOR_USAGE's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"geometric mean of the number of supervisees\" -> SPACE_SUPERVISOR_USAGE.NUM_OF_SUPERVISEES is a numeric measure of SPACE_SUPERVISOR_USAGE; the hinted joins multiply SPACE_SUPERVISOR_USAGE rows ~x1.1, so SUM/AVG over the joined result would count each SPACE_SUPERVISOR_USAGE row ~1.1 times. Aggregate it at SPACE_SUPERVISOR_USAGE's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"total research volume\" -> SPACE_SUPERVISOR_USAGE.RESEARCH_VOLUME is a numeric measure of SPACE_SUPERVISOR_USAGE; the hinted joins multiply SPACE_SUPERVISOR_USAGE rows ~x1.1, so SUM/AVG over the joined result would count each SPACE_SUPERVISOR_USAGE row ~1.1 times. Aggregate it at SPACE_SUPERVISOR_USAGE's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_5166": [
    "- Join FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY = FCLT_ROOMS.FCLT_ORGANIZATION_KEY: FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY repeats ~1.1 rows per value (180 rows, 168 values); FCLT_ROOMS.FCLT_ORGANIZATION_KEY repeats ~79.4 rows per value (10000 rows, 126 values).",
    "  -> each FCLT_ORGANIZATION row appears ~79.4 times after this join.",
    "  -> each FCLT_ROOMS row appears ~1.1 times after this join.",
    "- \"organization\" -> FCLT_ORGANIZATION.ORGANIZATION_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct organization, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_NAME).",
    "- \"organization name\" -> FCLT_ORGANIZATION.ORGANIZATION_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct organization name, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_NAME).",
    "- \"area of rooms\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so SUM/AVG over the joined result would count each FCLT_ROOMS row ~1.1 times. Aggregate it at FCLT_ROOMS's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"rooms assigned\" -> FCLT_ROOMS.FCLT_ORGANIZATION_KEY: already repeats ~79.4 rows per value in its own table; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct rooms assigned, use COUNT(DISTINCT FCLT_ROOMS.FCLT_ORGANIZATION_KEY).",
    "- \"more than 5 rooms\" -> FCLT_ROOMS.FCLT_ROOM_KEY: unique per row in its own table; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct more than 5 rooms, use COUNT(DISTINCT FCLT_ROOMS.FCLT_ROOM_KEY).",
    "- \"excluding organizations named 'ADMISSIONS'\" -> FCLT_ORGANIZATION.ORGANIZATION_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct excluding organizations named 'ADMISSIONS', use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_NAME)."
  ],
  "dw_5181": [
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE repeats ~166.7 rows per value (10000 rows, 60 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~166.7 times after this join.",
    "- \"department's full name\" -> SIS_DEPARTMENT.DEPARTMENT_FULL_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct department's full name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_FULL_NAME).",
    "- \"range of the total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"variance of the total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subjects offered\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"academic year 2024\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"degree-granting\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct degree-granting, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"Biology department (Course 7)\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct Biology department (Course 7), use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE)."
  ],
  "dw_5330": [
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each SIS_DEPARTMENT row appears ~161.3 times after this join.",
    "- Join MIT_STUDENT_DIRECTORY.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: MIT_STUDENT_DIRECTORY.DEPARTMENT repeats ~204.1 rows per value (10000 rows, 49 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~204.1 times after this join.",
    "- \"department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"average of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"variance of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"standard deviation of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"subjects offered by department\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: already repeats ~161.3 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"subjects offered by department\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered by department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"departments that grant degrees\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct departments that grant degrees, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"students in their fourth year\" -> MIT_STUDENT_DIRECTORY.STUDENT_YEAR: already repeats ~2000.0 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"Mathematics department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME)."
  ],
  "dw_5361": [
    "- Join FCLT_ORGANIZATION.DLC_KEY = MASTER_DEPT_HIERARCHY.DLC_KEY: FCLT_ORGANIZATION.DLC_KEY repeats ~1.3 rows per value (180 rows, 136 values); MASTER_DEPT_HIERARCHY.DLC_KEY is unique per row (310 rows).",
    "  -> each MASTER_DEPT_HIERARCHY row appears ~1.3 times after this join.",
    "- \"department hierarchy level 2 name\" -> MASTER_DEPT_HIERARCHY.MASTER_DEPT_HIER_LEVEL_2_NAME: already repeats ~38.8 rows per value in its own table; the hinted joins multiply MASTER_DEPT_HIERARCHY rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct department hierarchy level 2 name, use COUNT(DISTINCT MASTER_DEPT_HIERARCHY.MASTER_DEPT_HIER_LEVEL_2_NAME).",
    "- \"organization name\" -> FCLT_ORGANIZATION.ORGANIZATION_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply FCLT_ORGANIZATION rows.",
    "- \"organization is assignable\" -> FCLT_ORGANIZATION.ASSIGNABLE: already repeats ~90.0 rows per value in its own table; the hinted joins do not multiply FCLT_ORGANIZATION rows.",
    "- \"organization is not 'ADMISSIONS'\" -> FCLT_ORGANIZATION.ORGANIZATION_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply FCLT_ORGANIZATION rows.",
    "- \"ORGANIZATION_ID '138' and '250'\" -> FCLT_ORGANIZATION.ORGANIZATION_ID: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply FCLT_ORGANIZATION rows.",
    "- \"number of organizations\" -> FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply FCLT_ORGANIZATION rows.",
    "- \"minimum organization number\" -> FCLT_ORGANIZATION.ORGANIZATION_NUMBER: already repeats ~1.2 rows per value in its own table; the hinted joins do not multiply FCLT_ORGANIZATION rows."
  ],
  "dw_5415": [
    "- Join STUDENT_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: STUDENT_DEPARTMENT.DEPARTMENT_CODE is unique per row (79 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each STUDENT_DEPARTMENT row appears ~161.3 times after this join.",
    "- \"subject title\" -> TIP_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"term code\" -> TIP_SUBJECT_OFFERED.TERM_CODE: already repeats ~79.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"offering department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"responsible faculty name\" -> TIP_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_NAME: already repeats ~4.1 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"department full name\" -> STUDENT_DEPARTMENT.DEPARTMENT_FULL_NAME: unique per row in its own table; the hinted joins multiply STUDENT_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct department full name, use COUNT(DISTINCT STUDENT_DEPARTMENT.DEPARTMENT_FULL_NAME).",
    "- \"school name\" -> STUDENT_DEPARTMENT.SCHOOL_NAME: already repeats ~8.8 rows per value in its own table; the hinted joins multiply STUDENT_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT STUDENT_DEPARTMENT.SCHOOL_NAME).",
    "- \"maximum number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"Chemistry department (Course 5)\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"subjects that have course materials\" -> TIP_SUBJECT_OFFERED.IS_NO_COURSE_MATERIAL: already repeats ~5000.0 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows."
  ],
  "dw_5638": [
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~105.3 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = TIP_DETAIL.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); TIP_DETAIL.TERM_CODE repeats ~158.7 rows per value (10000 rows, 63 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~158.7 times after this join.",
    "- \"academic year\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x158.7, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR).",
    "- \"academic year description\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x158.7, so a plain COUNT over the joined result counts duplicates. To count distinct academic year description, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC).",
    "- \"average of the total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"standard deviation of the total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"Mathematics department\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"required or optional material\" -> TIP_DETAIL.TIP_MATERIAL_STATUS_KEY: already repeats ~833.3 rows per value in its own table; the hinted joins do not multiply TIP_DETAIL rows."
  ],
  "dw_5668": [
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- Join SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE is unique per row (179 rows); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "- \"CIP program code\" -> SIS_COURSE_DESCRIPTION.CIP_PROGRAM_CODE: already repeats ~10.9 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"departments\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct departments, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"School of Engineering\" -> SIS_COURSE_DESCRIPTION.SCHOOL_NAME: already repeats ~77.2 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"degree-granting departments\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct degree-granting departments, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"administrative department names\" -> SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply SIS_ADMIN_DEPARTMENT rows.",
    "- \"Mechanical Engineering department\" -> SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply SIS_ADMIN_DEPARTMENT rows.",
    "- \"Mechanical Engineering department\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT: already repeats ~9.7 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows."
  ],
  "dw_5711": [
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"school name\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_DEPARTMENT.SCHOOL_NAME).",
    "- \"degree granting\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct degree granting, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"Mathematics (Course 18)\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics (Course 18), use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"unique courses\" -> SIS_COURSE_DESCRIPTION.COURSE: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"CIP program code\" -> SIS_COURSE_DESCRIPTION.CIP_PROGRAM_CODE: already repeats ~10.9 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows."
  ],
  "dw_5769": [
    "- Join ACADEMIC_TERMS.TERM_CODE = LIBRARY_RESERVE_MATRL_DETAIL.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); LIBRARY_RESERVE_MATRL_DETAIL.TERM_CODE repeats ~1250.0 rows per value (10000 rows, 8 values).",
    "  -> each ACADEMIC_TERMS row appears ~1250.0 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS row appears ~105.3 times after this join.",
    "- \"minimum academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x1250.0, so a plain COUNT over the joined result counts duplicates. To count distinct minimum academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR).",
    "- \"sum of total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"fall term\" -> COURSE_CATALOG_SUBJECT_OFFERED.IS_OFFERED_FALL_TERM: already repeats ~5000.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"Mathematics department\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"library reserve material details\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY: already repeats ~2000.0 rows per value in its own table; the hinted joins do not multiply LIBRARY_RESERVE_MATRL_DETAIL rows.",
    "- \"material status 'U'\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY: already repeats ~2000.0 rows per value in its own table; the hinted joins do not multiply LIBRARY_RESERVE_MATRL_DETAIL rows."
  ],
  "dw_588": [
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = LIBRARY_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); LIBRARY_SUBJECT_OFFERED.TERM_CODE repeats ~192.3 rows per value (10000 rows, 52 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~192.3 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~105.3 times after this join.",
    "- \"academic years\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct academic years, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR).",
    "- \"academic year's description\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct academic year's description, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC).",
    "- \"standard deviation of enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"subjects\" -> LIBRARY_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~4.4 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"subject title\" -> LIBRARY_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~5.6 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"number of enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"fall term\" -> COURSE_CATALOG_SUBJECT_OFFERED.IS_OFFERED_FALL_TERM: already repeats ~5000.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"Mathematics department\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows."
  ],
  "dw_625": [
    "- Join SUBJECT_OFFERED.TERM_CODE = TIP_SUBJECT_OFFERED.TERM_CODE: SUBJECT_OFFERED.TERM_CODE repeats ~370.4 rows per value (10000 rows, 27 values); TIP_SUBJECT_OFFERED.TERM_CODE repeats ~79.4 rows per value (10000 rows, 126 values).",
    "  -> each SUBJECT_OFFERED row appears ~79.4 times after this join.",
    "  -> each TIP_SUBJECT_OFFERED row appears ~370.4 times after this join.",
    "- \"department\" -> SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~10000.0 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"Harvard Cross-Enrollment Program\" -> SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~10000.0 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct Harvard Cross-Enrollment Program, use COUNT(DISTINCT SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"Chemistry\" -> SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~10000.0 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct Chemistry, use COUNT(DISTINCT SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"subjects\" -> SUBJECT_OFFERED.SUBJECT_KEY: unique per row in its own table; the hinted joins multiply SUBJECT_OFFERED rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct subjects, use COUNT(DISTINCT SUBJECT_OFFERED.SUBJECT_KEY).",
    "- \"number of unique subjects\" -> SUBJECT_OFFERED.SUBJECT_KEY: unique per row in its own table; the hinted joins multiply SUBJECT_OFFERED rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct number of unique subjects, use COUNT(DISTINCT SUBJECT_OFFERED.SUBJECT_KEY).",
    "- \"students enrolled\" -> SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED; the hinted joins multiply SUBJECT_OFFERED rows ~x79.4, so SUM/AVG over the joined result would count each SUBJECT_OFFERED row ~79.4 times. Aggregate it at SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"TIP subject offering\" -> TIP_SUBJECT_OFFERED.TERM_CODE: already repeats ~79.4 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x370.4, so a plain COUNT over the joined result counts duplicates. To count distinct TIP subject offering, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.TERM_CODE).",
    "- \"TIP subject offering\" -> SUBJECT_OFFERED.TERM_CODE: already repeats ~370.4 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct TIP subject offering, use COUNT(DISTINCT SUBJECT_OFFERED.TERM_CODE).",
    "- \"term\" -> SUBJECT_OFFERED.TERM_CODE: already repeats ~370.4 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct term, use COUNT(DISTINCT SUBJECT_OFFERED.TERM_CODE).",
    "- \"term\" -> TIP_SUBJECT_OFFERED.TERM_CODE: already repeats ~79.4 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x370.4, so a plain COUNT over the joined result counts duplicates. To count distinct term, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.TERM_CODE)."
  ],
  "dw_649": [
    "- Join LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER = SIS_SUBJECT_CODE.COURSE_NUMBER: LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER repeats ~625.0 rows per value (10000 rows, 16 values); SIS_SUBJECT_CODE.COURSE_NUMBER is unique per row (221 rows).",
    "  -> each SIS_SUBJECT_CODE row appears ~625.0 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each SIS_DEPARTMENT row appears ~161.3 times after this join.",
    "- Join STUDENT_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: STUDENT_DEPARTMENT.DEPARTMENT_CODE is unique per row (79 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each STUDENT_DEPARTMENT row appears ~161.3 times after this join.",
    "- \"subjects offered\" -> LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER: already repeats ~625.0 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"subjects offered\" -> SIS_SUBJECT_CODE.COURSE_NUMBER: unique per row in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x625.0, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered, use COUNT(DISTINCT SIS_SUBJECT_CODE.COURSE_NUMBER).",
    "- \"term '2014FA'\" -> LIBRARY_SUBJECT_OFFERED.TERM_CODE: already repeats ~192.3 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"more than 5 enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"Mathematics department\" -> SIS_SUBJECT_CODE.DEPARTMENT_NAME: already repeats ~3.7 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x625.0, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics department, use COUNT(DISTINCT SIS_SUBJECT_CODE.DEPARTMENT_NAME).",
    "- \"Mathematics department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"Course 18\" -> SIS_SUBJECT_CODE.DEPARTMENT_CODE: already repeats ~3.6 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x625.0, so a plain COUNT over the joined result counts duplicates. To count distinct Course 18, use COUNT(DISTINCT SIS_SUBJECT_CODE.DEPARTMENT_CODE).",
    "- \"Course 18\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct Course 18, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"subject code\" -> SIS_SUBJECT_CODE.SUBJECT_CODE: unique per row in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x625.0, so a plain COUNT over the joined result counts duplicates. To count distinct subject code, use COUNT(DISTINCT SIS_SUBJECT_CODE.SUBJECT_CODE).",
    "- \"subject code description\" -> SIS_SUBJECT_CODE.SUBJECT_CODE_DESC: already repeats ~1.2 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x625.0, so a plain COUNT over the joined result counts duplicates. To count distinct subject code description, use COUNT(DISTINCT SIS_SUBJECT_CODE.SUBJECT_CODE_DESC).",
    "- \"department name\" -> SIS_SUBJECT_CODE.DEPARTMENT_NAME: already repeats ~3.7 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x625.0, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_SUBJECT_CODE.DEPARTMENT_NAME).",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"school name\" -> SIS_SUBJECT_CODE.SCHOOL_NAME: already repeats ~24.6 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x625.0, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_SUBJECT_CODE.SCHOOL_NAME).",
    "- \"subject title\" -> LIBRARY_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~5.6 rows per value in its own table; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"number of enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins do not multiply LIBRARY_SUBJECT_OFFERED rows.",
    "- \"number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"row numbers within each department name group ordered by subject code\" -> SIS_SUBJECT_CODE.DEPARTMENT_NAME: already repeats ~3.7 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x625.0, so a plain COUNT over the joined result counts duplicates. To count distinct row numbers within each department name group ordered by subject code, use COUNT(DISTINCT SIS_SUBJECT_CODE.DEPARTMENT_NAME).",
    "- \"row numbers within each department name group ordered by subject code\" -> SIS_SUBJECT_CODE.SUBJECT_CODE: unique per row in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x625.0, so a plain COUNT over the joined result counts duplicates. To count distinct row numbers within each department name group ordered by subject code, use COUNT(DISTINCT SIS_SUBJECT_CODE.SUBJECT_CODE).",
    "- \"coefficient of variation of the number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows."
  ],
  "dw_663": [
    "- Join MIT_STUDENT_DIRECTORY.FULL_NAME = SE_PERSON.FULL_NAME: MIT_STUDENT_DIRECTORY.FULL_NAME repeats ~1.2 rows per value (10000 rows, 8585 values); SE_PERSON.FULL_NAME is unique per row (10000 rows).",
    "  -> each SE_PERSON row appears ~1.2 times after this join.",
    "- Join ACADEMIC_TERMS.(TERM_CODE,TERM_CODE) = COURSE_CATALOG_SUBJECT_OFFERED.(TERM_CODE,EFFECTIVE_TERM_CODE): ACADEMIC_TERMS.(TERM_CODE,TERM_CODE) is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.(TERM_CODE,EFFECTIVE_TERM_CODE) repeats ~11.3 rows per value (10000 rows, 886 values).",
    "  -> each ACADEMIC_TERMS row appears ~11.3 times after this join.",
    "- \"employee type\" -> SE_PERSON.EMPLOYEE_TYPE: already repeats ~1250.0 rows per value in its own table; the hinted joins multiply SE_PERSON rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct employee type, use COUNT(DISTINCT SE_PERSON.EMPLOYEE_TYPE).",
    "- \"Student\" -> SE_PERSON.EMPLOYEE_TYPE: already repeats ~1250.0 rows per value in its own table; the hinted joins multiply SE_PERSON rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct Student, use COUNT(DISTINCT SE_PERSON.EMPLOYEE_TYPE).",
    "- \"department 6\" -> MIT_STUDENT_DIRECTORY.DEPARTMENT: already repeats ~204.1 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"department name\" -> MIT_STUDENT_DIRECTORY.DEPARTMENT_NAME: already repeats ~204.1 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"department name\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"Electrical Eng & Computer Sci\" -> MIT_STUDENT_DIRECTORY.DEPARTMENT_NAME: already repeats ~204.1 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"Electrical Eng & Computer Sci\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"MIT ID\" -> SE_PERSON.MIT_ID: unique per row in its own table; the hinted joins multiply SE_PERSON rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct MIT ID, use COUNT(DISTINCT SE_PERSON.MIT_ID).",
    "- \"full name\" -> SE_PERSON.FULL_NAME: unique per row in its own table; the hinted joins multiply SE_PERSON rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct full name, use COUNT(DISTINCT SE_PERSON.FULL_NAME).",
    "- \"full name\" -> MIT_STUDENT_DIRECTORY.FULL_NAME: already repeats ~1.2 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"student year\" -> MIT_STUDENT_DIRECTORY.STUDENT_YEAR: already repeats ~2000.0 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"regular academic terms\" -> ACADEMIC_TERMS.IS_REGULAR_TERM: already repeats ~72.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x11.3, so a plain COUNT over the joined result counts duplicates. To count distinct regular academic terms, use COUNT(DISTINCT ACADEMIC_TERMS.IS_REGULAR_TERM).",
    "- \"Mathematics\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows."
  ],
  "dw_714": [
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SIS_SUBJECT_CODE.DEPARTMENT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SIS_SUBJECT_CODE.DEPARTMENT_CODE repeats ~3.6 rows per value (221 rows, 61 values).",
    "  -> each SIS_DEPARTMENT row appears ~3.6 times after this join.",
    "- Join MIT_STUDENT_DIRECTORY.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: MIT_STUDENT_DIRECTORY.DEPARTMENT repeats ~204.1 rows per value (10000 rows, 49 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~204.1 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.EFFECTIVE_TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.EFFECTIVE_TERM_CODE repeats ~200.0 rows per value (10000 rows, 50 values).",
    "  -> each ACADEMIC_TERMS row appears ~200.0 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = SUBJECT_SUMMARY.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); SUBJECT_SUMMARY.TERM_CODE repeats ~83.3 rows per value (10000 rows, 120 values).",
    "  -> each ACADEMIC_TERMS row appears ~83.3 times after this join.",
    "- \"department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"department\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"degree granting\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct degree granting, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"Biology department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct Biology department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"variance of the number of students in each student year\" -> MIT_STUDENT_DIRECTORY.STUDENT_YEAR: already repeats ~2000.0 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"standard deviation of the number of students in each student year\" -> MIT_STUDENT_DIRECTORY.STUDENT_YEAR: already repeats ~2000.0 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"number of students in each student year\" -> MIT_STUDENT_DIRECTORY.STUDENT_YEAR: already repeats ~2000.0 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"Mathematics department\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x200.0, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR).",
    "- \"subject enrollment number\" -> SUBJECT_SUMMARY.SUBJECT_ENROLLMENT_NUMBER: already repeats ~57.8 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"number of unique subjects\" -> SUBJECT_SUMMARY.SUBJECT_ID: already repeats ~1.9 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"minimum total units offered\" -> SUBJECT_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_SUMMARY; the hinted joins do not multiply SUBJECT_SUMMARY rows."
  ],
  "dw_718": [
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~105.3 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE = SUBJECT_OFFERED.TERM_CODE: COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values); SUBJECT_OFFERED.TERM_CODE repeats ~370.4 rows per value (10000 rows, 27 values).",
    "  -> each COURSE_CATALOG_SUBJECT_OFFERED row appears ~370.4 times after this join.",
    "  -> each SUBJECT_OFFERED row appears ~105.3 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS row appears ~105.3 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = TIP_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); TIP_SUBJECT_OFFERED.TERM_CODE repeats ~79.4 rows per value (10000 rows, 126 values).",
    "  -> each ACADEMIC_TERMS row appears ~79.4 times after this join.",
    "- \"academic year\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR).",
    "- \"academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR).",
    "- \"academic year description\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct academic year description, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR_DESC).",
    "- \"coefficient of variation of the total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x370.4, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~370.4 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"Mathematics subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x370.4, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics subjects, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME).",
    "- \"fall term\" -> COURSE_CATALOG_SUBJECT_OFFERED.IS_OFFERED_FALL_TERM: already repeats ~5000.0 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x370.4, so a plain COUNT over the joined result counts duplicates. To count distinct fall term, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.IS_OFFERED_FALL_TERM).",
    "- \"total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x370.4, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~370.4 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"range of the number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"variance of the number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows."
  ],
  "dw_752": [
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each SIS_DEPARTMENT row appears ~161.3 times after this join.",
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SIS_SUBJECT_CODE.DEPARTMENT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SIS_SUBJECT_CODE.DEPARTMENT_CODE repeats ~3.6 rows per value (221 rows, 61 values).",
    "  -> each SIS_DEPARTMENT row appears ~3.6 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE repeats ~196.1 rows per value (10000 rows, 51 values).",
    "  -> each SIS_DEPARTMENT row appears ~196.1 times after this join.",
    "- \"degree-granting department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct degree-granting department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"degree-granting department\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct degree-granting department, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"graduate-level subjects\" -> SIS_COURSE_DESCRIPTION.COURSE_LEVEL: already repeats ~347.5 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"range of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"variance of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"total units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"range of total units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"variance of total units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"standard deviation of total units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"Biology\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct Biology, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"Chemistry\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct Chemistry, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME)."
  ],
  "dw_777": [
    "- Join TIP_DETAIL.TERM_CODE = TIP_SUBJECT_OFFERED.TERM_CODE: TIP_DETAIL.TERM_CODE repeats ~158.7 rows per value (10000 rows, 63 values); TIP_SUBJECT_OFFERED.TERM_CODE repeats ~79.4 rows per value (10000 rows, 126 values).",
    "  -> each TIP_DETAIL row appears ~79.4 times after this join.",
    "  -> each TIP_SUBJECT_OFFERED row appears ~158.7 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SIS_SUBJECT_CODE.DEPARTMENT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SIS_SUBJECT_CODE.DEPARTMENT_CODE repeats ~3.6 rows per value (221 rows, 61 values).",
    "  -> each SIS_DEPARTMENT row appears ~3.6 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE repeats ~166.7 rows per value (10000 rows, 60 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~166.7 times after this join.",
    "- \"department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x158.7, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"average enrollment\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x158.7, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~158.7 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"enrollment variance\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x158.7, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~158.7 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"average total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"variance of total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"Chemistry\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x158.7, so a plain COUNT over the joined result counts duplicates. To count distinct Chemistry, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"material record\" -> TIP_DETAIL.RECORD_COUNT is a numeric measure of TIP_DETAIL; the hinted joins multiply TIP_DETAIL rows ~x79.4, so SUM/AVG over the joined result would count each TIP_DETAIL row ~79.4 times. Aggregate it at TIP_DETAIL's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"degree-granting departments\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct degree-granting departments, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"Biology\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct Biology, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"2022 academic year\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows."
  ],
  "dw_779": [
    "- Join ACADEMIC_TERMS.(TERM_CODE,TERM_CODE) = COURSE_CATALOG_SUBJECT_OFFERED.(EFFECTIVE_TERM_CODE,TERM_CODE): ACADEMIC_TERMS.(TERM_CODE,TERM_CODE) is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.(EFFECTIVE_TERM_CODE,TERM_CODE) repeats ~11.3 rows per value (10000 rows, 886 values).",
    "  -> each ACADEMIC_TERMS row appears ~11.3 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = ACADEMIC_TERMS_ALL.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows).",
    "- \"academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x11.3, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR).",
    "- \"academic year description\" -> ACADEMIC_TERMS.ACADEMIC_YEAR_DESC: already repeats ~4.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x11.3, so a plain COUNT over the joined result counts duplicates. To count distinct academic year description, use COUNT(DISTINCT ACADEMIC_TERMS.ACADEMIC_YEAR_DESC).",
    "- \"average of the total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"standard deviation of the total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"variance of the total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subjects offered in regular terms\" -> ACADEMIC_TERMS.IS_REGULAR_TERM: already repeats ~72.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x11.3, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered in regular terms, use COUNT(DISTINCT ACADEMIC_TERMS.IS_REGULAR_TERM).",
    "- \"Mathematics department\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"term status of 'Previous'\" -> ACADEMIC_TERMS.TERM_STATUS: already repeats ~36.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x11.3, so a plain COUNT over the joined result counts duplicates. To count distinct term status of 'Previous', use COUNT(DISTINCT ACADEMIC_TERMS.TERM_STATUS)."
  ],
  "dw_817": [
    "- Join SPACE_DETAIL.FLOOR_KEY = SPACE_FLOOR.FLOOR_KEY: SPACE_DETAIL.FLOOR_KEY repeats ~370.4 rows per value (10000 rows, 27 values); SPACE_FLOOR.FLOOR_KEY is unique per row (49 rows).",
    "  -> each SPACE_FLOOR row appears ~370.4 times after this join.",
    "- \"floor\" -> SPACE_FLOOR.FLOOR_NAME: unique per row in its own table; the hinted joins multiply SPACE_FLOOR rows ~x370.4, so a plain COUNT over the joined result counts duplicates. To count distinct floor, use COUNT(DISTINCT SPACE_FLOOR.FLOOR_NAME).",
    "- \"Stata building\" -> SPACE_DETAIL.BUILDING_KEY: already repeats ~344.8 rows per value in its own table; the hinted joins do not multiply SPACE_DETAIL rows.",
    "- \"building 32\" -> SPACE_DETAIL.BUILDING_KEY: already repeats ~344.8 rows per value in its own table; the hinted joins do not multiply SPACE_DETAIL rows.",
    "- \"largest room square footage\" -> SPACE_DETAIL.ROOM_SQUARE_FOOTAGE is a numeric measure of SPACE_DETAIL; the hinted joins do not multiply SPACE_DETAIL rows.",
    "- \"largest room name\" -> SPACE_DETAIL.BUILDING_ROOM_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply SPACE_DETAIL rows.",
    "- \"rooms loaded into the warehouse on 19-DEC-24\" -> SPACE_DETAIL.WAREHOUSE_LOAD_DATE: already repeats ~10000.0 rows per value in its own table; the hinted joins do not multiply SPACE_DETAIL rows.",
    "- \"room square footage greater than 100\" -> SPACE_DETAIL.ROOM_SQUARE_FOOTAGE is a numeric measure of SPACE_DETAIL; the hinted joins do not multiply SPACE_DETAIL rows.",
    "- \"space unit\" -> SPACE_DETAIL.BUILDING_ROOM_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply SPACE_DETAIL rows.",
    "- \"space unit\" -> SPACE_DETAIL.ROOM_NUMBER: already repeats ~10.5 rows per value in its own table; the hinted joins do not multiply SPACE_DETAIL rows.",
    "- \"room square footage greater than 10\" -> SPACE_DETAIL.ROOM_SQUARE_FOOTAGE is a numeric measure of SPACE_DETAIL; the hinted joins do not multiply SPACE_DETAIL rows.",
    "- \"building room name\" -> SPACE_DETAIL.BUILDING_ROOM_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply SPACE_DETAIL rows.",
    "- \"room number\" -> SPACE_DETAIL.ROOM_NUMBER: already repeats ~10.5 rows per value in its own table; the hinted joins do not multiply SPACE_DETAIL rows.",
    "- \"room square footage\" -> SPACE_DETAIL.ROOM_SQUARE_FOOTAGE is a numeric measure of SPACE_DETAIL; the hinted joins do not multiply SPACE_DETAIL rows.",
    "- \"average room square footage within the floor\" -> SPACE_DETAIL.ROOM_SQUARE_FOOTAGE is a numeric measure of SPACE_DETAIL; the hinted joins do not multiply SPACE_DETAIL rows.",
    "- \"average room square footage per floor\" -> SPACE_DETAIL.ROOM_SQUARE_FOOTAGE is a numeric measure of SPACE_DETAIL; the hinted joins do not multiply SPACE_DETAIL rows."
  ],
  "dw_872": [
    "- Join CIS_COURSE_CATALOG.SUBJECT_ID = COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: CIS_COURSE_CATALOG.SUBJECT_ID repeats ~4.4 rows per value (10000 rows, 2269 values); COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID repeats ~2.0 rows per value (10000 rows, 4969 values).",
    "  -> each CIS_COURSE_CATALOG row appears ~2.0 times after this join.",
    "  -> each COURSE_CATALOG_SUBJECT_OFFERED row appears ~4.4 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE = SIS_COURSE_DESCRIPTION.COURSE: COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE repeats ~181.8 rows per value (10000 rows, 55 values); SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values).",
    "  -> each COURSE_CATALOG_SUBJECT_OFFERED row appears ~1.1 times after this join.",
    "  -> each SIS_COURSE_DESCRIPTION row appears ~181.8 times after this join.",
    "- \"subjects offered in the academic year 2024\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered in the academic year 2024, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR).",
    "- \"CIS course catalog\" -> CIS_COURSE_CATALOG.SUBJECT_ID: already repeats ~4.4 rows per value in its own table; the hinted joins multiply CIS_COURSE_CATALOG rows ~x2.0, so a plain COUNT over the joined result counts duplicates. To count distinct CIS course catalog, use COUNT(DISTINCT CIS_COURSE_CATALOG.SUBJECT_ID).",
    "- \"Mathematics department\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics department, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME).",
    "- \"Mathematics department\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct Mathematics department, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME).",
    "- \"Course 18\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct Course 18, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME).",
    "- \"Course 18\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct Course 18, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME).",
    "- \"subject ID\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct subject ID, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID).",
    "- \"subject code\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE: already repeats ~181.8 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct subject code, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE).",
    "- \"subject code\" -> SIS_COURSE_DESCRIPTION.COURSE: already repeats ~1.1 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct subject code, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.COURSE).",
    "- \"subject number\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_NUMBER: already repeats ~6.9 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct subject number, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_NUMBER).",
    "- \"department name\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME).",
    "- \"department name\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME).",
    "- \"subject short title\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_SHORT_TITLE: already repeats ~2.6 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct subject short title, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_SHORT_TITLE).",
    "- \"subject title\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.7 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct subject title, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_TITLE).",
    "- \"lecture units\" -> COURSE_CATALOG_SUBJECT_OFFERED.LECTURE_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~4.4 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"lab units\" -> COURSE_CATALOG_SUBJECT_OFFERED.LAB_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~4.4 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~4.4 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"grade type\" -> COURSE_CATALOG_SUBJECT_OFFERED.GRADE_TYPE: already repeats ~5000.0 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct grade type, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.GRADE_TYPE).",
    "- \"grade rule\" -> COURSE_CATALOG_SUBJECT_OFFERED.GRADE_RULE: already repeats ~2500.0 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct grade rule, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.GRADE_RULE).",
    "- \"minimum total units value over each department code group\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~4.4 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"minimum total units value over each department code group\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE: already repeats ~166.7 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct minimum total units value over each department code group, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE).",
    "- \"geometric mean of the total units for subjects offered by that department\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~4.4 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"geometric mean of the total units for subjects offered by that department\" -> SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME: already repeats ~9.8 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct geometric mean of the total units for subjects offered by that department, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.DEPARTMENT_NAME).",
    "- \"subjects offered this year\" -> COURSE_CATALOG_SUBJECT_OFFERED.IS_OFFERED_THIS_YEAR: already repeats ~5000.0 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered this year, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.IS_OFFERED_THIS_YEAR).",
    "- \"total units greater than zero\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.4, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~4.4 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_942": [
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~105.3 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = SUBJECT_SUMMARY.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); SUBJECT_SUMMARY.TERM_CODE repeats ~83.3 rows per value (10000 rows, 120 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~83.3 times after this join.",
    "- Join SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE is unique per row (179 rows); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "- \"academic year\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR).",
    "- \"department\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins do not multiply SIS_DEPARTMENT rows.",
    "- \"variance of total units\" -> SUBJECT_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_SUMMARY; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"standard deviation of total units\" -> SUBJECT_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_SUMMARY; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"subjects offered in that year and department\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subjects offered in that year and department\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered in that year and department, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR).",
    "- \"current term\" -> ACADEMIC_TERMS_ALL.IS_CURRENT_TERM: already repeats ~150.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct current term, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.IS_CURRENT_TERM).",
    "- \"total units\" -> SUBJECT_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_SUMMARY; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"Mathematics department\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"average clearing cost collector\" -> SIS_ADMIN_DEPARTMENT.CLEARING_COST_COLLECTOR: already repeats ~3.7 rows per value in its own table; the hinted joins do not multiply SIS_ADMIN_DEPARTMENT rows.",
    "- \"standard deviation of clearing cost collector\" -> SIS_ADMIN_DEPARTMENT.CLEARING_COST_COLLECTOR: already repeats ~3.7 rows per value in its own table; the hinted joins do not multiply SIS_ADMIN_DEPARTMENT rows.",
    "- \"departments that grant degrees\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins do not multiply SIS_DEPARTMENT rows.",
    "- \"Political Science department\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins do not multiply SIS_DEPARTMENT rows."
  ],
  "dw_960": [
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE repeats ~166.7 rows per value (10000 rows, 60 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~166.7 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE repeats ~196.1 rows per value (10000 rows, 51 values).",
    "  -> each SIS_DEPARTMENT row appears ~196.1 times after this join.",
    "- \"departments\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct departments, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"number of subjects offered\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"2022 academic year\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"Political Science department\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct Political Science department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"Biology department\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct Biology department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"departments that grant degrees\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct departments that grant degrees, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"variance of the total units\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows."
  ],
  "dw_965": [
    "- Join STUDENT_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: STUDENT_DEPARTMENT.DEPARTMENT_CODE is unique per row (79 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each STUDENT_DEPARTMENT row appears ~161.3 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE repeats ~166.7 rows per value (10000 rows, 60 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~166.7 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE repeats ~196.1 rows per value (10000 rows, 51 values).",
    "  -> each SIS_DEPARTMENT row appears ~196.1 times after this join.",
    "- \"subjects offered in the term '2017JA'\" -> TIP_SUBJECT_OFFERED.TERM_CODE: already repeats ~79.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"Chemistry department (Course 5)\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"Chemistry department (Course 5)\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: already repeats ~161.3 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"subject ID\" -> TIP_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"subject title\" -> TIP_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"offering department code\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: already repeats ~161.3 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"offering department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"number of enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"department full name\" -> STUDENT_DEPARTMENT.DEPARTMENT_FULL_NAME: unique per row in its own table; the hinted joins multiply STUDENT_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct department full name, use COUNT(DISTINCT STUDENT_DEPARTMENT.DEPARTMENT_FULL_NAME).",
    "- \"school name\" -> STUDENT_DEPARTMENT.SCHOOL_NAME: already repeats ~8.8 rows per value in its own table; the hinted joins multiply STUDENT_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT STUDENT_DEPARTMENT.SCHOOL_NAME).",
    "- \"minimum number of enrolled students over each department code group\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"minimum number of enrolled students over each department code group\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: already repeats ~161.3 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"variance of the total units for subjects offered by that department in the year 2022\" -> SUBJECT_OFFERED_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"variance of the total units for subjects offered by that department in the year 2022\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"variance of the total units for subjects offered by that department in the year 2022\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct variance of the total units for subjects offered by that department in the year 2022, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"departments that grant degrees\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct departments that grant degrees, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"exclude the Biology department (Course 7)\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct exclude the Biology department (Course 7), use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE)."
  ],
  "dw_995": [
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each SIS_DEPARTMENT row appears ~161.3 times after this join.",
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = TIP_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); TIP_SUBJECT_OFFERED.TERM_CODE repeats ~79.4 rows per value (10000 rows, 126 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~79.4 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = ACADEMIC_TERM_PARAMETER.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); ACADEMIC_TERM_PARAMETER.TERM_CODE is unique per row (3 rows).",
    "- \"department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"department\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"average enrollment\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"enrollment variance\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"subjects offered\" -> TIP_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"degree-granting courses\" -> SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING: already repeats ~347.5 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"departments\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct departments, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"departments\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct departments, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"Political Science\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct Political Science, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"Chemistry\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"Biology\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"academic year\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR).",
    "- \"subject count\" -> TIP_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"minimum term start date\" -> ACADEMIC_TERMS_ALL.TERM_START_DATE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct minimum term start date, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_START_DATE)."
  ],
  "dw_real_0": [
    "- Join FCLT_BUILDING_ADDRESS.FCLT_BUILDING_KEY = FCLT_ROOMS.FCLT_BUILDING_KEY: FCLT_BUILDING_ADDRESS.FCLT_BUILDING_KEY repeats ~3.2 rows per value (785 rows, 242 values); FCLT_ROOMS.FCLT_BUILDING_KEY repeats ~49.5 rows per value (10000 rows, 202 values).",
    "  -> each FCLT_BUILDING_ADDRESS row appears ~49.5 times after this join.",
    "  -> each FCLT_ROOMS row appears ~3.2 times after this join.",
    "- Join FCLT_ORG_DLC_KEY.FCLT_ORGANIZATION_KEY = FCLT_ROOMS.FCLT_ORGANIZATION_KEY: FCLT_ORG_DLC_KEY.FCLT_ORGANIZATION_KEY is unique per row (168 rows); FCLT_ROOMS.FCLT_ORGANIZATION_KEY repeats ~79.4 rows per value (10000 rows, 126 values).",
    "  -> each FCLT_ORG_DLC_KEY row appears ~79.4 times after this join.",
    "- Join FCLT_ORG_DLC_KEY.DLC_KEY = MASTER_DEPT_HIERARCHY.DLC_KEY: FCLT_ORG_DLC_KEY.DLC_KEY repeats ~1.2 rows per value (168 rows, 136 values); MASTER_DEPT_HIERARCHY.DLC_KEY is unique per row (310 rows).",
    "  -> each MASTER_DEPT_HIERARCHY row appears ~1.2 times after this join.",
    "- Join BUILDINGS.BUILDING_KEY = FCLT_BUILDING_ADDRESS.FCLT_BUILDING_KEY: BUILDINGS.BUILDING_KEY is unique per row (211 rows); FCLT_BUILDING_ADDRESS.FCLT_BUILDING_KEY repeats ~3.2 rows per value (785 rows, 242 values).",
    "  -> each BUILDINGS row appears ~3.2 times after this join.",
    "- \"building key\" -> FCLT_BUILDING_ADDRESS.FCLT_BUILDING_KEY: already repeats ~3.2 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_ADDRESS rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct building key, use COUNT(DISTINCT FCLT_BUILDING_ADDRESS.FCLT_BUILDING_KEY).",
    "- \"building street address\" -> BUILDINGS.BUILDING_STREET_ADDRESS: already repeats ~1.4 rows per value in its own table; the hinted joins multiply BUILDINGS rows ~x3.2, so a plain COUNT over the joined result counts duplicates. To count distinct building street address, use COUNT(DISTINCT BUILDINGS.BUILDING_STREET_ADDRESS).",
    "- \"building street address\" -> FCLT_BUILDING_ADDRESS.ADDRESS_PURPOSE: already repeats ~60.4 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_ADDRESS rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct building street address, use COUNT(DISTINCT FCLT_BUILDING_ADDRESS.ADDRESS_PURPOSE).",
    "- \"city\" -> FCLT_BUILDING_ADDRESS.CITY: already repeats ~71.4 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_ADDRESS rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct city, use COUNT(DISTINCT FCLT_BUILDING_ADDRESS.CITY).",
    "- \"state\" -> FCLT_BUILDING_ADDRESS.STATE: already repeats ~392.5 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_ADDRESS rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct state, use COUNT(DISTINCT FCLT_BUILDING_ADDRESS.STATE).",
    "- \"postal code\" -> FCLT_BUILDING_ADDRESS.POSTAL_CODE: already repeats ~56.1 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_ADDRESS rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct postal code, use COUNT(DISTINCT FCLT_BUILDING_ADDRESS.POSTAL_CODE).",
    "- \"history department\" -> MASTER_DEPT_HIERARCHY.DLC_NAME: unique per row in its own table; the hinted joins multiply MASTER_DEPT_HIERARCHY rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct history department, use COUNT(DISTINCT MASTER_DEPT_HIERARCHY.DLC_NAME)."
  ],
  "dw_real_1": [
    "- Join IAP_SUBJECT_DETAIL.IAP_SUBJECT_PERSON_KEY = IAP_SUBJECT_PERSON.IAP_SUBJECT_PERSON_KEY: IAP_SUBJECT_DETAIL.IAP_SUBJECT_PERSON_KEY repeats ~3.3 rows per value (465 rows, 142 values); IAP_SUBJECT_PERSON.IAP_SUBJECT_PERSON_KEY repeats ~2.9 rows per value (1113 rows, 390 values).",
    "  -> each IAP_SUBJECT_DETAIL row appears ~2.9 times after this join.",
    "  -> each IAP_SUBJECT_PERSON row appears ~3.3 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = IAP_SUBJECT_DETAIL.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); IAP_SUBJECT_DETAIL.TERM_CODE repeats ~465.0 rows per value (465 rows, 1 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~465.0 times after this join.",
    "- Join IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY = IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY: IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY repeats ~3.3 rows per value (465 rows, 142 values); IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY repeats ~2.8 rows per value (1199 rows, 421 values).",
    "  -> each IAP_SUBJECT_DETAIL row appears ~2.8 times after this join.",
    "  -> each IAP_SUBJECT_SESSION row appears ~3.3 times after this join.",
    "- \"activity titles\" -> IAP_SUBJECT_DETAIL.ACTIVITY_TITLE: already repeats ~4.4 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x2.9, so a plain COUNT over the joined result counts duplicates. To count distinct activity titles, use COUNT(DISTINCT IAP_SUBJECT_DETAIL.ACTIVITY_TITLE).",
    "- \"locations\" -> IAP_SUBJECT_SESSION.SESSION_LOCATION: already repeats ~29.2 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_SESSION rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct locations, use COUNT(DISTINCT IAP_SUBJECT_SESSION.SESSION_LOCATION).",
    "- \"term start date\" -> ACADEMIC_TERMS_ALL.TERM_START_DATE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x465.0, so a plain COUNT over the joined result counts duplicates. To count distinct term start date, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_START_DATE).",
    "- \"supervisor name\" -> IAP_SUBJECT_PERSON.PERSON_NAME: unique per row in its own table; the hinted joins multiply IAP_SUBJECT_PERSON rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct supervisor name, use COUNT(DISTINCT IAP_SUBJECT_PERSON.PERSON_NAME).",
    "- \"supervisor name\" -> IAP_SUBJECT_PERSON.PERSON_ROLE: already repeats ~371.0 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_PERSON rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct supervisor name, use COUNT(DISTINCT IAP_SUBJECT_PERSON.PERSON_ROLE)."
  ],
  "dw_real_10": [
    "- Join FAC_BUILDING.FAC_BUILDING_KEY = FAC_FLOOR.BUILDING_KEY: FAC_BUILDING.FAC_BUILDING_KEY is unique per row (242 rows); FAC_FLOOR.BUILDING_KEY repeats ~4.5 rows per value (1079 rows, 239 values).",
    "  -> each FAC_BUILDING row appears ~4.5 times after this join.",
    "- \"name\" -> FAC_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins multiply FAC_BUILDING rows ~x4.5, so a plain COUNT over the joined result counts duplicates. To count distinct name, use COUNT(DISTINCT FAC_BUILDING.BUILDING_NAME).",
    "- \"floor\" -> FAC_FLOOR.FLOOR: already repeats ~21.2 rows per value in its own table; the hinted joins do not multiply FAC_FLOOR rows."
  ],
  "dw_real_100": [
    "- Join SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE is unique per row (179 rows); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "- \"term\" -> COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: already repeats ~105.3 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"department\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"courses\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"equivalent subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.EQUIVALENT_SUBJECTS: already repeats ~26.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"school name\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins do not multiply SIS_DEPARTMENT rows.",
    "- \"department phone number\" -> SIS_ADMIN_DEPARTMENT.DEPARTMENT_PHONE_NUMBER: already repeats ~2.8 rows per value in its own table; the hinted joins do not multiply SIS_ADMIN_DEPARTMENT rows."
  ],
  "dw_real_101": [
    "- Join ACADEMIC_TERMS.TERM_CODE = IAP_SUBJECT_DETAIL.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); IAP_SUBJECT_DETAIL.TERM_CODE repeats ~465.0 rows per value (465 rows, 1 values).",
    "  -> each ACADEMIC_TERMS row appears ~465.0 times after this join.",
    "- Join IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY = IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY: IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY repeats ~3.3 rows per value (465 rows, 142 values); IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY repeats ~2.8 rows per value (1199 rows, 421 values).",
    "  -> each IAP_SUBJECT_DETAIL row appears ~2.8 times after this join.",
    "  -> each IAP_SUBJECT_SESSION row appears ~3.3 times after this join.",
    "- Join IAP_SUBJECT_CATEGORY.IAP_SUBJECT_CATEGORY_KEY = IAP_SUBJECT_DETAIL.IAP_SUBJECT_CATEGORY_KEY: IAP_SUBJECT_CATEGORY.IAP_SUBJECT_CATEGORY_KEY is unique per row (49 rows); IAP_SUBJECT_DETAIL.IAP_SUBJECT_CATEGORY_KEY repeats ~10.1 rows per value (465 rows, 46 values).",
    "  -> each IAP_SUBJECT_CATEGORY row appears ~10.1 times after this join.",
    "- \"current term status\" -> ACADEMIC_TERMS.IS_CURRENT_TERM: already repeats ~72.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x465.0, so a plain COUNT over the joined result counts duplicates. To count distinct current term status, use COUNT(DISTINCT ACADEMIC_TERMS.IS_CURRENT_TERM).",
    "- \"name of the session\" -> IAP_SUBJECT_CATEGORY.IAP_CATEGORY_NAME: unique per row in its own table; the hinted joins multiply IAP_SUBJECT_CATEGORY rows ~x10.1, so a plain COUNT over the joined result counts duplicates. To count distinct name of the session, use COUNT(DISTINCT IAP_SUBJECT_CATEGORY.IAP_CATEGORY_NAME).",
    "- \"IAP sessions\" -> IAP_SUBJECT_SESSION.SESSION_DATE: already repeats ~34.3 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_SESSION rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct IAP sessions, use COUNT(DISTINCT IAP_SUBJECT_SESSION.SESSION_DATE).",
    "- \"session time\" -> IAP_SUBJECT_SESSION.SESSION_START_TIME: already repeats ~38.7 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_SESSION rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct session time, use COUNT(DISTINCT IAP_SUBJECT_SESSION.SESSION_START_TIME).",
    "- \"session time\" -> IAP_SUBJECT_SESSION.SESSION_END_TIME: already repeats ~28.5 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_SESSION rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct session time, use COUNT(DISTINCT IAP_SUBJECT_SESSION.SESSION_END_TIME)."
  ],
  "dw_real_102": [
    "- \"academic year\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR: already repeats ~3.8 rows per value in its own table; the hinted joins do not multiply ACADEMIC_TERMS_ALL rows.",
    "- \"term code\" -> ACADEMIC_TERMS_ALL.TERM_CODE: unique per row in its own table; the hinted joins do not multiply ACADEMIC_TERMS_ALL rows.",
    "- \"subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows."
  ],
  "dw_real_103": [
    "- Join MIT_STUDENT_DIRECTORY.FULL_NAME = SE_PERSON.FULL_NAME: MIT_STUDENT_DIRECTORY.FULL_NAME repeats ~1.2 rows per value (10000 rows, 8585 values); SE_PERSON.FULL_NAME is unique per row (10000 rows).",
    "  -> each SE_PERSON row appears ~1.2 times after this join.",
    "- Join MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER = SE_PERSON.KRB_NAME: MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER repeats ~1.5 rows per value (10000 rows, 6469 values); SE_PERSON.KRB_NAME repeats ~1.9 rows per value (10000 rows, 5341 values).",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.9 times after this join.",
    "  -> each SE_PERSON row appears ~1.5 times after this join.",
    "- Join MOIRA_LIST.MOIRA_LIST_KEY = MOIRA_LIST_DETAIL.MOIRA_LIST_KEY: MOIRA_LIST.MOIRA_LIST_KEY repeats ~1.1 rows per value (10000 rows, 8815 values); MOIRA_LIST_DETAIL.MOIRA_LIST_KEY repeats ~1.6 rows per value (10000 rows, 6395 values).",
    "  -> each MOIRA_LIST row appears ~1.6 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "- Join MOIRA_LIST_DETAIL.MOIRA_LIST_OWNER_KEY = MOIRA_LIST_OWNER.MOIRA_LIST_OWNER_KEY: MOIRA_LIST_DETAIL.MOIRA_LIST_OWNER_KEY repeats ~2.5 rows per value (10000 rows, 3966 values); MOIRA_LIST_OWNER.MOIRA_LIST_OWNER_KEY repeats ~1.1 rows per value (10000 rows, 9042 values).",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "  -> each MOIRA_LIST_OWNER row appears ~2.5 times after this join.",
    "- \"names\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct names, use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_NAME).",
    "- \"members\" -> MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER: already repeats ~1.5 rows per value in its own table; the hinted joins multiply MOIRA_LIST_DETAIL rows ~x1.9, so a plain COUNT over the joined result counts duplicates. To count distinct members, use COUNT(DISTINCT MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER).",
    "- \"computer science\" -> MIT_STUDENT_DIRECTORY.DEPARTMENT: already repeats ~204.1 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"owner\" -> MOIRA_LIST_OWNER.OWNER: already repeats ~1.2 rows per value in its own table; the hinted joins multiply MOIRA_LIST_OWNER rows ~x2.5, so a plain COUNT over the joined result counts duplicates. To count distinct owner, use COUNT(DISTINCT MOIRA_LIST_OWNER.OWNER)."
  ],
  "dw_real_104": [
    "- Join MOIRA_LIST.(MOIRA_LIST_KEY,MOIRA_LIST_KEY) = MOIRA_LIST_DETAIL.(MOIRA_LIST_KEY,MOIRA_LIST_KEY): MOIRA_LIST.(MOIRA_LIST_KEY,MOIRA_LIST_KEY) repeats ~1.1 rows per value (10000 rows, 8815 values); MOIRA_LIST_DETAIL.(MOIRA_LIST_KEY,MOIRA_LIST_KEY) repeats ~1.6 rows per value (10000 rows, 6395 values).",
    "  -> each MOIRA_LIST row appears ~1.6 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "- Join MIT_STUDENT_DIRECTORY.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: MIT_STUDENT_DIRECTORY.DEPARTMENT repeats ~204.1 rows per value (10000 rows, 49 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~204.1 times after this join.",
    "- Join SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE is unique per row (179 rows); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "- Join MIT_STUDENT_DIRECTORY.FULL_NAME = SE_PERSON.FULL_NAME: MIT_STUDENT_DIRECTORY.FULL_NAME repeats ~1.2 rows per value (10000 rows, 8585 values); SE_PERSON.FULL_NAME is unique per row (10000 rows).",
    "  -> each SE_PERSON row appears ~1.2 times after this join.",
    "- \"last names\" -> MIT_STUDENT_DIRECTORY.LAST_NAME: already repeats ~29.5 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"names\" -> MIT_STUDENT_DIRECTORY.FULL_NAME: already repeats ~1.2 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"phone numbers\" -> SIS_ADMIN_DEPARTMENT.DEPARTMENT_PHONE_NUMBER: already repeats ~2.8 rows per value in its own table; the hinted joins do not multiply SIS_ADMIN_DEPARTMENT rows.",
    "- \"mailing lists\" -> MOIRA_LIST.MOIRA_LIST_KEY: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct mailing lists, use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_KEY)."
  ],
  "dw_real_105": [
    "- Join MOIRA_LIST.(MOIRA_LIST_KEY,MOIRA_LIST_KEY) = MOIRA_LIST_DETAIL.(MOIRA_LIST_KEY,MOIRA_LIST_KEY): MOIRA_LIST.(MOIRA_LIST_KEY,MOIRA_LIST_KEY) repeats ~1.1 rows per value (10000 rows, 8815 values); MOIRA_LIST_DETAIL.(MOIRA_LIST_KEY,MOIRA_LIST_KEY) repeats ~1.6 rows per value (10000 rows, 6395 values).",
    "  -> each MOIRA_LIST row appears ~1.6 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "- Join MIT_STUDENT_DIRECTORY.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: MIT_STUDENT_DIRECTORY.DEPARTMENT repeats ~204.1 rows per value (10000 rows, 49 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~204.1 times after this join.",
    "- \"names\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct names, use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_NAME).",
    "- \"subscribers\" -> MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER: already repeats ~1.5 rows per value in its own table; the hinted joins multiply MOIRA_LIST_DETAIL rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct subscribers, use COUNT(DISTINCT MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER).",
    "- \"public status\" -> MOIRA_LIST.IS_PUBLIC: already repeats ~5000.0 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct public status, use COUNT(DISTINCT MOIRA_LIST.IS_PUBLIC).",
    "- \"department\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"students\" -> MIT_STUDENT_DIRECTORY.FULL_NAME: already repeats ~1.2 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows."
  ],
  "dw_real_106": [
    "- Join FAC_BUILDING.FAC_BUILDING_KEY = FAC_FLOOR.BUILDING_KEY: FAC_BUILDING.FAC_BUILDING_KEY is unique per row (242 rows); FAC_FLOOR.BUILDING_KEY repeats ~4.5 rows per value (1079 rows, 239 values).",
    "  -> each FAC_BUILDING row appears ~4.5 times after this join.",
    "- Join FAC_FLOOR.(BUILDING_KEY,FLOOR) = FAC_ROOMS.(BUILDING_KEY,FLOOR): FAC_FLOOR.(BUILDING_KEY,FLOOR) is unique per row (1079 rows); FAC_ROOMS.(BUILDING_KEY,FLOOR) repeats ~12.1 rows per value (10000 rows, 827 values).",
    "  -> each FAC_FLOOR row appears ~12.1 times after this join.",
    "- \"building name\" -> FAC_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins multiply FAC_BUILDING rows ~x4.5, so a plain COUNT over the joined result counts duplicates. To count distinct building name, use COUNT(DISTINCT FAC_BUILDING.BUILDING_NAME).",
    "- \"building number\" -> FAC_BUILDING.BUILDING_NUMBER: unique per row in its own table; the hinted joins multiply FAC_BUILDING rows ~x4.5, so a plain COUNT over the joined result counts duplicates. To count distinct building number, use COUNT(DISTINCT FAC_BUILDING.BUILDING_NUMBER).",
    "- \"assignable area\" -> FAC_FLOOR.ASSIGNABLE_AREA is a numeric measure of FAC_FLOOR; the hinted joins multiply FAC_FLOOR rows ~x12.1, so SUM/AVG over the joined result would count each FAC_FLOOR row ~12.1 times. Aggregate it at FAC_FLOOR's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"non-assignable area\" -> FAC_FLOOR.NON_ASSIGNABLE_AREA is a numeric measure of FAC_FLOOR; the hinted joins multiply FAC_FLOOR rows ~x12.1, so SUM/AVG over the joined result would count each FAC_FLOOR row ~12.1 times. Aggregate it at FAC_FLOOR's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"room\" -> FAC_ROOMS.FAC_ROOM_KEY: unique per row in its own table; the hinted joins do not multiply FAC_ROOMS rows."
  ],
  "dw_real_107": [
    "- Join FAC_MAJOR_USE.MAJOR_USE_KEY = FAC_ROOMS.MAJOR_USE_KEY: FAC_MAJOR_USE.MAJOR_USE_KEY is unique per row (14 rows); FAC_ROOMS.MAJOR_USE_KEY repeats ~1428.6 rows per value (10000 rows, 7 values).",
    "  -> each FAC_MAJOR_USE row appears ~1428.6 times after this join.",
    "- Join FAC_ORGANIZATION.ORGANIZATION_KEY = FAC_ROOMS.ORGANIZATION_KEY: FAC_ORGANIZATION.ORGANIZATION_KEY is unique per row (169 rows); FAC_ROOMS.ORGANIZATION_KEY repeats ~1111.1 rows per value (10000 rows, 9 values).",
    "  -> each FAC_ORGANIZATION row appears ~1111.1 times after this join.",
    "- \"building 45\" -> FAC_ROOMS.BUILDING_KEY: already repeats ~53.2 rows per value in its own table; the hinted joins do not multiply FAC_ROOMS rows.",
    "- \"full name\" -> FAC_ROOMS.ROOM_FULL_NAME: already repeats ~10000.0 rows per value in its own table; the hinted joins do not multiply FAC_ROOMS rows.",
    "- \"area\" -> FAC_ROOMS.AREA is a numeric measure of FAC_ROOMS; the hinted joins do not multiply FAC_ROOMS rows.",
    "- \"major use\" -> FAC_MAJOR_USE.MAJOR_USE: unique per row in its own table; the hinted joins multiply FAC_MAJOR_USE rows ~x1428.6, so a plain COUNT over the joined result counts duplicates. To count distinct major use, use COUNT(DISTINCT FAC_MAJOR_USE.MAJOR_USE).",
    "- \"organization\" -> FAC_ORGANIZATION.ORGANIZATION_NAME: unique per row in its own table; the hinted joins multiply FAC_ORGANIZATION rows ~x1111.1, so a plain COUNT over the joined result counts duplicates. To count distinct organization, use COUNT(DISTINCT FAC_ORGANIZATION.ORGANIZATION_NAME).",
    "- \"rooms\" -> FAC_ROOMS.FAC_ROOM_KEY: unique per row in its own table; the hinted joins do not multiply FAC_ROOMS rows."
  ],
  "dw_real_108": [
    "- Join FAC_BUILDING.FAC_BUILDING_KEY = FAC_BUILDING_ADDRESS.BUILDING_KEY: FAC_BUILDING.FAC_BUILDING_KEY is unique per row (242 rows); FAC_BUILDING_ADDRESS.BUILDING_KEY repeats ~3.2 rows per value (785 rows, 242 values).",
    "  -> each FAC_BUILDING row appears ~3.2 times after this join.",
    "- Join FAC_BUILDING.FAC_BUILDING_KEY = FAC_ROOMS.BUILDING_KEY: FAC_BUILDING.FAC_BUILDING_KEY is unique per row (242 rows); FAC_ROOMS.BUILDING_KEY repeats ~53.2 rows per value (10000 rows, 188 values).",
    "  -> each FAC_BUILDING row appears ~53.2 times after this join.",
    "- \"name\" -> FAC_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins multiply FAC_BUILDING rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct name, use COUNT(DISTINCT FAC_BUILDING.BUILDING_NAME).",
    "- \"number\" -> FAC_BUILDING.BUILDING_NUMBER: unique per row in its own table; the hinted joins multiply FAC_BUILDING rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct number, use COUNT(DISTINCT FAC_BUILDING.BUILDING_NUMBER).",
    "- \"construction date\" -> FAC_BUILDING.DATE_BUILT: already repeats ~2.2 rows per value in its own table; the hinted joins multiply FAC_BUILDING rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct construction date, use COUNT(DISTINCT FAC_BUILDING.DATE_BUILT).",
    "- \"type\" -> FAC_BUILDING.BUILDING_TYPE: already repeats ~80.7 rows per value in its own table; the hinted joins multiply FAC_BUILDING rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct type, use COUNT(DISTINCT FAC_BUILDING.BUILDING_TYPE).",
    "- \"addresses\" -> FAC_BUILDING_ADDRESS.BUILDING_ADDRESS_KEY: unique per row in its own table; the hinted joins do not multiply FAC_BUILDING_ADDRESS rows.",
    "- \"gross area\" -> FAC_BUILDING.EXT_GROSS_AREA is a numeric measure of FAC_BUILDING; the hinted joins multiply FAC_BUILDING rows ~x53.2, so SUM/AVG over the joined result would count each FAC_BUILDING row ~53.2 times. Aggregate it at FAC_BUILDING's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"rooms\" -> FAC_ROOMS.FAC_ROOM_KEY: unique per row in its own table; the hinted joins do not multiply FAC_ROOMS rows."
  ],
  "dw_real_109": [
    "- Join LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY = LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY: LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY repeats ~6.5 rows per value (10000 rows, 1550 values); LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY is unique per row (10000 rows).",
    "  -> each LIBRARY_SUBJECT_OFFERED row appears ~6.5 times after this join.",
    "- Join LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY repeats ~1.2 rows per value (10000 rows, 8268 values).",
    "  -> each LIBRARY_RESERVE_CATALOG row appears ~1.2 times after this join.",
    "- Join LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY: LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY repeats ~6.2 rows per value (10000 rows, 1614 values).",
    "  -> each LIBRARY_COURSE_INSTRUCTOR row appears ~6.2 times after this join.",
    "- \"name\" -> LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~625.0 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so a plain COUNT over the joined result counts duplicates. To count distinct name, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"course\" -> LIBRARY_SUBJECT_OFFERED.MASTER_COURSE_NUMBER: already repeats ~270.3 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so a plain COUNT over the joined result counts duplicates. To count distinct course, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.MASTER_COURSE_NUMBER).",
    "- \"reserved materials\" -> LIBRARY_RESERVE_CATALOG.CATALOG_TITLE: already repeats ~1.9 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_CATALOG rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct reserved materials, use COUNT(DISTINCT LIBRARY_RESERVE_CATALOG.CATALOG_TITLE).",
    "- \"instructors\" -> LIBRARY_COURSE_INSTRUCTOR.INSTRUCTOR_NAME: unique per row in its own table; the hinted joins multiply LIBRARY_COURSE_INSTRUCTOR rows ~x6.2, so a plain COUNT over the joined result counts duplicates. To count distinct instructors, use COUNT(DISTINCT LIBRARY_COURSE_INSTRUCTOR.INSTRUCTOR_NAME)."
  ],
  "dw_real_11": [
    "- Join IAP_SUBJECT_CATEGORY.IAP_SUBJECT_CATEGORY_KEY = IAP_SUBJECT_DETAIL.IAP_SUBJECT_CATEGORY_KEY: IAP_SUBJECT_CATEGORY.IAP_SUBJECT_CATEGORY_KEY is unique per row (49 rows); IAP_SUBJECT_DETAIL.IAP_SUBJECT_CATEGORY_KEY repeats ~10.1 rows per value (465 rows, 46 values).",
    "  -> each IAP_SUBJECT_CATEGORY row appears ~10.1 times after this join.",
    "- Join IAP_SUBJECT_DETAIL.TERM_CODE = TIME_DAY.ACADEMIC_TERM_CODE: IAP_SUBJECT_DETAIL.TERM_CODE repeats ~465.0 rows per value (465 rows, 1 values); TIME_DAY.ACADEMIC_TERM_CODE repeats ~35.8 rows per value (10000 rows, 279 values).",
    "  -> each IAP_SUBJECT_DETAIL row appears ~35.8 times after this join.",
    "  -> each TIME_DAY row appears ~465.0 times after this join.",
    "- \"category name\" -> IAP_SUBJECT_CATEGORY.IAP_CATEGORY_NAME: unique per row in its own table; the hinted joins multiply IAP_SUBJECT_CATEGORY rows ~x10.1, so a plain COUNT over the joined result counts duplicates. To count distinct category name, use COUNT(DISTINCT IAP_SUBJECT_CATEGORY.IAP_CATEGORY_NAME).",
    "- \"academic year\" -> TIME_DAY.ACADEMIC_YEAR: already repeats ~125.0 rows per value in its own table; the hinted joins multiply TIME_DAY rows ~x465.0, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT TIME_DAY.ACADEMIC_YEAR).",
    "- \"fee collected\" -> IAP_SUBJECT_DETAIL.FEE is a numeric measure of IAP_SUBJECT_DETAIL; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x35.8, so SUM/AVG over the joined result would count each IAP_SUBJECT_DETAIL row ~35.8 times. Aggregate it at IAP_SUBJECT_DETAIL's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"IAP subjects\" -> IAP_SUBJECT_DETAIL.ACTIVITY_TITLE: already repeats ~4.4 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x35.8, so a plain COUNT over the joined result counts duplicates. To count distinct IAP subjects, use COUNT(DISTINCT IAP_SUBJECT_DETAIL.ACTIVITY_TITLE).",
    "- \"enrollment\" -> IAP_SUBJECT_DETAIL.MAX_ENROLLMENT is a numeric measure of IAP_SUBJECT_DETAIL; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x35.8, so SUM/AVG over the joined result would count each IAP_SUBJECT_DETAIL row ~35.8 times. Aggregate it at IAP_SUBJECT_DETAIL's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_real_110": [
    "- Join LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY = LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY: LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY repeats ~6.5 rows per value (10000 rows, 1550 values); LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY is unique per row (10000 rows).",
    "  -> each LIBRARY_SUBJECT_OFFERED row appears ~6.5 times after this join.",
    "- Join LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY: LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS_KEY is unique per row (6 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY repeats ~2000.0 rows per value (10000 rows, 5 values).",
    "  -> each LIBRARY_MATERIAL_STATUS row appears ~2000.0 times after this join.",
    "- \"title\" -> LIBRARY_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~5.6 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so a plain COUNT over the joined result counts duplicates. To count distinct title, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.SUBJECT_TITLE).",
    "- \"reserved materials\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: already repeats ~1.2 rows per value in its own table; the hinted joins do not multiply LIBRARY_RESERVE_MATRL_DETAIL rows.",
    "- \"material status\" -> LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS: already repeats ~1.2 rows per value in its own table; the hinted joins multiply LIBRARY_MATERIAL_STATUS rows ~x2000.0, so a plain COUNT over the joined result counts duplicates. To count distinct material status, use COUNT(DISTINCT LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS)."
  ],
  "dw_real_111": [
    "- Join LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY: LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY repeats ~6.2 rows per value (10000 rows, 1614 values).",
    "  -> each LIBRARY_COURSE_INSTRUCTOR row appears ~6.2 times after this join.",
    "- Join LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY repeats ~1.2 rows per value (10000 rows, 8268 values).",
    "  -> each LIBRARY_RESERVE_CATALOG row appears ~1.2 times after this join.",
    "- Join LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY: LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS_KEY is unique per row (6 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY repeats ~2000.0 rows per value (10000 rows, 5 values).",
    "  -> each LIBRARY_MATERIAL_STATUS row appears ~2000.0 times after this join.",
    "- \"name\" -> LIBRARY_COURSE_INSTRUCTOR.INSTRUCTOR_NAME: unique per row in its own table; the hinted joins multiply LIBRARY_COURSE_INSTRUCTOR rows ~x6.2, so a plain COUNT over the joined result counts duplicates. To count distinct name, use COUNT(DISTINCT LIBRARY_COURSE_INSTRUCTOR.INSTRUCTOR_NAME).",
    "- \"course\" -> LIBRARY_COURSE_INSTRUCTOR.COURSE_NAME: already repeats ~3.3 rows per value in its own table; the hinted joins multiply LIBRARY_COURSE_INSTRUCTOR rows ~x6.2, so a plain COUNT over the joined result counts duplicates. To count distinct course, use COUNT(DISTINCT LIBRARY_COURSE_INSTRUCTOR.COURSE_NAME).",
    "- \"material assignments\" -> LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY: unique per row in its own table; the hinted joins multiply LIBRARY_COURSE_INSTRUCTOR rows ~x6.2, so a plain COUNT over the joined result counts duplicates. To count distinct material assignments, use COUNT(DISTINCT LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY).",
    "- \"publication year\" -> LIBRARY_RESERVE_CATALOG.CATALOG_YEAR: already repeats ~111.1 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_CATALOG rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct publication year, use COUNT(DISTINCT LIBRARY_RESERVE_CATALOG.CATALOG_YEAR).",
    "- \"status\" -> LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS: already repeats ~1.2 rows per value in its own table; the hinted joins multiply LIBRARY_MATERIAL_STATUS rows ~x2000.0, so a plain COUNT over the joined result counts duplicates. To count distinct status, use COUNT(DISTINCT LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS)."
  ],
  "dw_real_112": [
    "- Join LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY repeats ~1.2 rows per value (10000 rows, 8268 values).",
    "  -> each LIBRARY_RESERVE_CATALOG row appears ~1.2 times after this join.",
    "- Join LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY: LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS_KEY is unique per row (6 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY repeats ~2000.0 rows per value (10000 rows, 5 values).",
    "  -> each LIBRARY_MATERIAL_STATUS row appears ~2000.0 times after this join.",
    "- Join LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY: LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY repeats ~6.2 rows per value (10000 rows, 1614 values).",
    "  -> each LIBRARY_COURSE_INSTRUCTOR row appears ~6.2 times after this join.",
    "- \"publication year\" -> LIBRARY_RESERVE_CATALOG.CATALOG_YEAR: already repeats ~111.1 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_CATALOG rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct publication year, use COUNT(DISTINCT LIBRARY_RESERVE_CATALOG.CATALOG_YEAR).",
    "- \"reserved materials\" -> LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY: unique per row in its own table; the hinted joins multiply LIBRARY_RESERVE_CATALOG rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct reserved materials, use COUNT(DISTINCT LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY).",
    "- \"title\" -> LIBRARY_RESERVE_CATALOG.CATALOG_TITLE: already repeats ~1.9 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_CATALOG rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct title, use COUNT(DISTINCT LIBRARY_RESERVE_CATALOG.CATALOG_TITLE).",
    "- \"status\" -> LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS: already repeats ~1.2 rows per value in its own table; the hinted joins multiply LIBRARY_MATERIAL_STATUS rows ~x2000.0, so a plain COUNT over the joined result counts duplicates. To count distinct status, use COUNT(DISTINCT LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS).",
    "- \"courses\" -> LIBRARY_COURSE_INSTRUCTOR.COURSE_NAME: already repeats ~3.3 rows per value in its own table; the hinted joins multiply LIBRARY_COURSE_INSTRUCTOR rows ~x6.2, so a plain COUNT over the joined result counts duplicates. To count distinct courses, use COUNT(DISTINCT LIBRARY_COURSE_INSTRUCTOR.COURSE_NAME)."
  ],
  "dw_real_113": [
    "- Join IAP_SUBJECT_CATEGORY.IAP_SUBJECT_CATEGORY_KEY = IAP_SUBJECT_DETAIL.IAP_SUBJECT_CATEGORY_KEY: IAP_SUBJECT_CATEGORY.IAP_SUBJECT_CATEGORY_KEY is unique per row (49 rows); IAP_SUBJECT_DETAIL.IAP_SUBJECT_CATEGORY_KEY repeats ~10.1 rows per value (465 rows, 46 values).",
    "  -> each IAP_SUBJECT_CATEGORY row appears ~10.1 times after this join.",
    "- Join IAP_SUBJECT_DETAIL.IAP_SUBJECT_SPONSOR_KEY = IAP_SUBJECT_SPONSOR.IAP_SUBJECT_SPONSOR_KEY: IAP_SUBJECT_DETAIL.IAP_SUBJECT_SPONSOR_KEY repeats ~6.8 rows per value (465 rows, 68 values); IAP_SUBJECT_SPONSOR.IAP_SUBJECT_SPONSOR_KEY is unique per row (68 rows).",
    "  -> each IAP_SUBJECT_SPONSOR row appears ~6.8 times after this join.",
    "- \"category name\" -> IAP_SUBJECT_CATEGORY.IAP_CATEGORY_NAME: unique per row in its own table; the hinted joins multiply IAP_SUBJECT_CATEGORY rows ~x10.1, so a plain COUNT over the joined result counts duplicates. To count distinct category name, use COUNT(DISTINCT IAP_SUBJECT_CATEGORY.IAP_CATEGORY_NAME).",
    "- \"sponsor name\" -> IAP_SUBJECT_SPONSOR.SPONSOR_NAME: unique per row in its own table; the hinted joins multiply IAP_SUBJECT_SPONSOR rows ~x6.8, so a plain COUNT over the joined result counts duplicates. To count distinct sponsor name, use COUNT(DISTINCT IAP_SUBJECT_SPONSOR.SPONSOR_NAME).",
    "- \"activities\" -> IAP_SUBJECT_DETAIL.IAP_SUBJECT_CATEGORY_KEY: already repeats ~10.1 rows per value in its own table; the hinted joins do not multiply IAP_SUBJECT_DETAIL rows.",
    "- \"fee\" -> IAP_SUBJECT_DETAIL.FEE is a numeric measure of IAP_SUBJECT_DETAIL; the hinted joins do not multiply IAP_SUBJECT_DETAIL rows."
  ],
  "dw_real_114": [
    "- Join IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY = IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY: IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY repeats ~3.3 rows per value (465 rows, 142 values); IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY repeats ~2.8 rows per value (1199 rows, 421 values).",
    "  -> each IAP_SUBJECT_DETAIL row appears ~2.8 times after this join.",
    "  -> each IAP_SUBJECT_SESSION row appears ~3.3 times after this join.",
    "- Join IAP_SUBJECT_DETAIL.IAP_SUBJECT_SPONSOR_KEY = IAP_SUBJECT_SPONSOR.IAP_SUBJECT_SPONSOR_KEY: IAP_SUBJECT_DETAIL.IAP_SUBJECT_SPONSOR_KEY repeats ~6.8 rows per value (465 rows, 68 values); IAP_SUBJECT_SPONSOR.IAP_SUBJECT_SPONSOR_KEY is unique per row (68 rows).",
    "  -> each IAP_SUBJECT_SPONSOR row appears ~6.8 times after this join.",
    "- \"sponsor name\" -> IAP_SUBJECT_SPONSOR.SPONSOR_NAME: unique per row in its own table; the hinted joins multiply IAP_SUBJECT_SPONSOR rows ~x6.8, so a plain COUNT over the joined result counts duplicates. To count distinct sponsor name, use COUNT(DISTINCT IAP_SUBJECT_SPONSOR.SPONSOR_NAME).",
    "- \"IAP sessions\" -> IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY: already repeats ~2.8 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_SESSION rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct IAP sessions, use COUNT(DISTINCT IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY).",
    "- \"subjects\" -> IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY: already repeats ~3.3 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct subjects, use COUNT(DISTINCT IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY)."
  ],
  "dw_real_115": [
    "- Join IAP_SUBJECT_DETAIL.IAP_SUBJECT_PERSON_KEY = IAP_SUBJECT_PERSON.IAP_SUBJECT_PERSON_KEY: IAP_SUBJECT_DETAIL.IAP_SUBJECT_PERSON_KEY repeats ~3.3 rows per value (465 rows, 142 values); IAP_SUBJECT_PERSON.IAP_SUBJECT_PERSON_KEY repeats ~2.9 rows per value (1113 rows, 390 values).",
    "  -> each IAP_SUBJECT_DETAIL row appears ~2.9 times after this join.",
    "  -> each IAP_SUBJECT_PERSON row appears ~3.3 times after this join.",
    "- Join IAP_SUBJECT_CATEGORY.IAP_SUBJECT_CATEGORY_KEY = IAP_SUBJECT_DETAIL.IAP_SUBJECT_CATEGORY_KEY: IAP_SUBJECT_CATEGORY.IAP_SUBJECT_CATEGORY_KEY is unique per row (49 rows); IAP_SUBJECT_DETAIL.IAP_SUBJECT_CATEGORY_KEY repeats ~10.1 rows per value (465 rows, 46 values).",
    "  -> each IAP_SUBJECT_CATEGORY row appears ~10.1 times after this join.",
    "- \"role\" -> IAP_SUBJECT_PERSON.PERSON_ROLE: already repeats ~371.0 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_PERSON rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct role, use COUNT(DISTINCT IAP_SUBJECT_PERSON.PERSON_ROLE).",
    "- \"category name\" -> IAP_SUBJECT_CATEGORY.IAP_CATEGORY_NAME: unique per row in its own table; the hinted joins multiply IAP_SUBJECT_CATEGORY rows ~x10.1, so a plain COUNT over the joined result counts duplicates. To count distinct category name, use COUNT(DISTINCT IAP_SUBJECT_CATEGORY.IAP_CATEGORY_NAME).",
    "- \"people\" -> IAP_SUBJECT_PERSON.IAP_SUBJECT_PERSON_KEY: already repeats ~2.9 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_PERSON rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct people, use COUNT(DISTINCT IAP_SUBJECT_PERSON.IAP_SUBJECT_PERSON_KEY).",
    "- \"fee\" -> IAP_SUBJECT_DETAIL.FEE is a numeric measure of IAP_SUBJECT_DETAIL; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x2.9, so SUM/AVG over the joined result would count each IAP_SUBJECT_DETAIL row ~2.9 times. Aggregate it at IAP_SUBJECT_DETAIL's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_real_116": [
    "- Join FAC_BUILDING.FAC_BUILDING_KEY = FAC_ROOMS.BUILDING_KEY: FAC_BUILDING.FAC_BUILDING_KEY is unique per row (242 rows); FAC_ROOMS.BUILDING_KEY repeats ~53.2 rows per value (10000 rows, 188 values).",
    "  -> each FAC_BUILDING row appears ~53.2 times after this join.",
    "- Join FAC_FLOOR.FLOOR_KEY = FAC_ROOMS.FLOOR_KEY: FAC_FLOOR.FLOOR_KEY is unique per row (1079 rows); FAC_ROOMS.FLOOR_KEY repeats ~12.1 rows per value (10000 rows, 827 values).",
    "  -> each FAC_FLOOR row appears ~12.1 times after this join.",
    "- \"name of the buildings\" -> FAC_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins multiply FAC_BUILDING rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct name of the buildings, use COUNT(DISTINCT FAC_BUILDING.BUILDING_NAME).",
    "- \"floors\" -> FAC_FLOOR.LEVEL_ID: already repeats ~36.0 rows per value in its own table; the hinted joins multiply FAC_FLOOR rows ~x12.1, so a plain COUNT over the joined result counts duplicates. To count distinct floors, use COUNT(DISTINCT FAC_FLOOR.LEVEL_ID)."
  ],
  "dw_real_117": [
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_MIT_ID = EMPLOYEE_DIRECTORY.MIT_ID: COURSE_CATALOG_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_MIT_ID repeats ~4.1 rows per value (10000 rows, 2418 values); EMPLOYEE_DIRECTORY.MIT_ID is unique per row (10000 rows).",
    "  -> each EMPLOYEE_DIRECTORY row appears ~4.1 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~105.3 times after this join.",
    "- \"summer term\" -> ACADEMIC_TERMS_ALL.TERM_CODE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct summer term, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_CODE).",
    "- \"subject titles\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"instructors\" -> EMPLOYEE_DIRECTORY.FULL_NAME: unique per row in its own table; the hinted joins multiply EMPLOYEE_DIRECTORY rows ~x4.1, so a plain COUNT over the joined result counts duplicates. To count distinct instructors, use COUNT(DISTINCT EMPLOYEE_DIRECTORY.FULL_NAME).",
    "- \"instructor name\" -> EMPLOYEE_DIRECTORY.FULL_NAME: unique per row in its own table; the hinted joins multiply EMPLOYEE_DIRECTORY rows ~x4.1, so a plain COUNT over the joined result counts duplicates. To count distinct instructor name, use COUNT(DISTINCT EMPLOYEE_DIRECTORY.FULL_NAME)."
  ],
  "dw_real_118": [
    "- Join MOIRA_LIST.MOIRA_LIST_KEY = MOIRA_LIST_DETAIL.MOIRA_LIST_KEY: MOIRA_LIST.MOIRA_LIST_KEY repeats ~1.1 rows per value (10000 rows, 8815 values); MOIRA_LIST_DETAIL.MOIRA_LIST_KEY repeats ~1.6 rows per value (10000 rows, 6395 values).",
    "  -> each MOIRA_LIST row appears ~1.6 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "- Join MOIRA_LIST_DETAIL.MOIRA_LIST_OWNER_KEY = MOIRA_LIST_OWNER.MOIRA_LIST_OWNER_KEY: MOIRA_LIST_DETAIL.MOIRA_LIST_OWNER_KEY repeats ~2.5 rows per value (10000 rows, 3966 values); MOIRA_LIST_OWNER.MOIRA_LIST_OWNER_KEY repeats ~1.1 rows per value (10000 rows, 9042 values).",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "  -> each MOIRA_LIST_OWNER row appears ~2.5 times after this join.",
    "- \"owner key\" -> MOIRA_LIST_OWNER.MOIRA_LIST_OWNER_KEY: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST_OWNER rows ~x2.5, so a plain COUNT over the joined result counts duplicates. To count distinct owner key, use COUNT(DISTINCT MOIRA_LIST_OWNER.MOIRA_LIST_OWNER_KEY).",
    "- \"owner\" -> MOIRA_LIST_OWNER.OWNER: already repeats ~1.2 rows per value in its own table; the hinted joins multiply MOIRA_LIST_OWNER rows ~x2.5, so a plain COUNT over the joined result counts duplicates. To count distinct owner, use COUNT(DISTINCT MOIRA_LIST_OWNER.OWNER).",
    "- \"mailing lists\" -> MOIRA_LIST.MOIRA_LIST_KEY: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct mailing lists, use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_KEY).",
    "- \"members\" -> MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER: already repeats ~1.5 rows per value in its own table; the hinted joins multiply MOIRA_LIST_DETAIL rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct members, use COUNT(DISTINCT MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER)."
  ],
  "dw_real_119": [
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- Join SIS_COURSE_DESCRIPTION.COURSE = SIS_SUBJECT_CODE.COURSE_NUMBER: SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values); SIS_SUBJECT_CODE.COURSE_NUMBER is unique per row (221 rows).",
    "  -> each SIS_SUBJECT_CODE row appears ~1.1 times after this join.",
    "- \"school name\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_DEPARTMENT.SCHOOL_NAME).",
    "- \"courses\" -> SIS_COURSE_DESCRIPTION.COURSE: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"degree-granting courses\" -> SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING: already repeats ~347.5 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows."
  ],
  "dw_real_12": [
    "- Join IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY = IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY: IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY repeats ~3.3 rows per value (465 rows, 142 values); IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY repeats ~2.8 rows per value (1199 rows, 421 values).",
    "  -> each IAP_SUBJECT_DETAIL row appears ~2.8 times after this join.",
    "  -> each IAP_SUBJECT_SESSION row appears ~3.3 times after this join.",
    "- Join IAP_SUBJECT_DETAIL.IAP_SUBJECT_SPONSOR_KEY = IAP_SUBJECT_SPONSOR.IAP_SUBJECT_SPONSOR_KEY: IAP_SUBJECT_DETAIL.IAP_SUBJECT_SPONSOR_KEY repeats ~6.8 rows per value (465 rows, 68 values); IAP_SUBJECT_SPONSOR.IAP_SUBJECT_SPONSOR_KEY is unique per row (68 rows).",
    "  -> each IAP_SUBJECT_SPONSOR row appears ~6.8 times after this join.",
    "- \"sponsor name\" -> IAP_SUBJECT_SPONSOR.SPONSOR_NAME: unique per row in its own table; the hinted joins multiply IAP_SUBJECT_SPONSOR rows ~x6.8, so a plain COUNT over the joined result counts duplicates. To count distinct sponsor name, use COUNT(DISTINCT IAP_SUBJECT_SPONSOR.SPONSOR_NAME).",
    "- \"sessions\" -> IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY: already repeats ~2.8 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_SESSION rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct sessions, use COUNT(DISTINCT IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY).",
    "- \"enrollment\" -> IAP_SUBJECT_DETAIL.MAX_ENROLLMENT is a numeric measure of IAP_SUBJECT_DETAIL; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x2.8, so SUM/AVG over the joined result would count each IAP_SUBJECT_DETAIL row ~2.8 times. Aggregate it at IAP_SUBJECT_DETAIL's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"fee\" -> IAP_SUBJECT_DETAIL.FEE is a numeric measure of IAP_SUBJECT_DETAIL; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x2.8, so SUM/AVG over the joined result would count each IAP_SUBJECT_DETAIL row ~2.8 times. Aggregate it at IAP_SUBJECT_DETAIL's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"info\" -> IAP_SUBJECT_SESSION.HAS_SESSION_INFO: already repeats ~599.5 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_SESSION rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct info, use COUNT(DISTINCT IAP_SUBJECT_SESSION.HAS_SESSION_INFO)."
  ],
  "dw_real_120": [
    "- Join MIT_STUDENT_DIRECTORY.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: MIT_STUDENT_DIRECTORY.DEPARTMENT repeats ~204.1 rows per value (10000 rows, 49 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~204.1 times after this join.",
    "- Join SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE is unique per row (179 rows); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"department phone number\" -> SIS_ADMIN_DEPARTMENT.DEPARTMENT_PHONE_NUMBER: already repeats ~2.8 rows per value in its own table; the hinted joins do not multiply SIS_ADMIN_DEPARTMENT rows.",
    "- \"students\" -> MIT_STUDENT_DIRECTORY.EMAIL_ADDRESS: already repeats ~3.0 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"student full name\" -> MIT_STUDENT_DIRECTORY.FULL_NAME: already repeats ~1.2 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows."
  ],
  "dw_real_13": [
    "- Join IAP_SUBJECT_DETAIL.IAP_SUBJECT_PERSON_KEY = IAP_SUBJECT_PERSON.IAP_SUBJECT_PERSON_KEY: IAP_SUBJECT_DETAIL.IAP_SUBJECT_PERSON_KEY repeats ~3.3 rows per value (465 rows, 142 values); IAP_SUBJECT_PERSON.IAP_SUBJECT_PERSON_KEY repeats ~2.9 rows per value (1113 rows, 390 values).",
    "  -> each IAP_SUBJECT_DETAIL row appears ~2.9 times after this join.",
    "  -> each IAP_SUBJECT_PERSON row appears ~3.3 times after this join.",
    "- Join IAP_SUBJECT_DETAIL.TERM_CODE = TIME_DAY.ACADEMIC_TERM_CODE: IAP_SUBJECT_DETAIL.TERM_CODE repeats ~465.0 rows per value (465 rows, 1 values); TIME_DAY.ACADEMIC_TERM_CODE repeats ~35.8 rows per value (10000 rows, 279 values).",
    "  -> each IAP_SUBJECT_DETAIL row appears ~35.8 times after this join.",
    "  -> each TIME_DAY row appears ~465.0 times after this join.",
    "- \"email\" -> IAP_SUBJECT_PERSON.PERSON_EMAIL: already repeats ~3.0 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_PERSON rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct email, use COUNT(DISTINCT IAP_SUBJECT_PERSON.PERSON_EMAIL).",
    "- \"name\" -> IAP_SUBJECT_PERSON.PERSON_NAME: unique per row in its own table; the hinted joins multiply IAP_SUBJECT_PERSON rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct name, use COUNT(DISTINCT IAP_SUBJECT_PERSON.PERSON_NAME).",
    "- \"academic year\" -> TIME_DAY.ACADEMIC_YEAR: already repeats ~125.0 rows per value in its own table; the hinted joins multiply TIME_DAY rows ~x465.0, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT TIME_DAY.ACADEMIC_YEAR).",
    "- \"IAP subjects\" -> IAP_SUBJECT_DETAIL.ACTIVITY_TITLE: already repeats ~4.4 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x35.8, so a plain COUNT over the joined result counts duplicates. To count distinct IAP subjects, use COUNT(DISTINCT IAP_SUBJECT_DETAIL.ACTIVITY_TITLE).",
    "- \"fees\" -> IAP_SUBJECT_DETAIL.FEE is a numeric measure of IAP_SUBJECT_DETAIL; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x35.8, so SUM/AVG over the joined result would count each IAP_SUBJECT_DETAIL row ~35.8 times. Aggregate it at IAP_SUBJECT_DETAIL's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"course enrollment\" -> IAP_SUBJECT_DETAIL.MAX_ENROLLMENT is a numeric measure of IAP_SUBJECT_DETAIL; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x35.8, so SUM/AVG over the joined result would count each IAP_SUBJECT_DETAIL row ~35.8 times. Aggregate it at IAP_SUBJECT_DETAIL's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_real_14": [
    "- Join IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY = IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY: IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY repeats ~3.3 rows per value (465 rows, 142 values); IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY repeats ~2.8 rows per value (1199 rows, 421 values).",
    "  -> each IAP_SUBJECT_DETAIL row appears ~2.8 times after this join.",
    "  -> each IAP_SUBJECT_SESSION row appears ~3.3 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = IAP_SUBJECT_DETAIL.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); IAP_SUBJECT_DETAIL.TERM_CODE repeats ~465.0 rows per value (465 rows, 1 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~465.0 times after this join.",
    "- \"term code\" -> IAP_SUBJECT_DETAIL.TERM_CODE: already repeats ~465.0 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct term code, use COUNT(DISTINCT IAP_SUBJECT_DETAIL.TERM_CODE).",
    "- \"term description\" -> ACADEMIC_TERMS_ALL.TERM_DESCRIPTION: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x465.0, so a plain COUNT over the joined result counts duplicates. To count distinct term description, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_DESCRIPTION).",
    "- \"IAP sessions\" -> IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY: already repeats ~2.8 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_SESSION rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct IAP sessions, use COUNT(DISTINCT IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY).",
    "- \"fee\" -> IAP_SUBJECT_DETAIL.FEE is a numeric measure of IAP_SUBJECT_DETAIL; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x2.8, so SUM/AVG over the joined result would count each IAP_SUBJECT_DETAIL row ~2.8 times. Aggregate it at IAP_SUBJECT_DETAIL's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"enrollment\" -> IAP_SUBJECT_DETAIL.MAX_ENROLLMENT is a numeric measure of IAP_SUBJECT_DETAIL; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x2.8, so SUM/AVG over the joined result would count each IAP_SUBJECT_DETAIL row ~2.8 times. Aggregate it at IAP_SUBJECT_DETAIL's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_real_15": [
    "- Join IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY = IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY: IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY repeats ~3.3 rows per value (465 rows, 142 values); IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY repeats ~2.8 rows per value (1199 rows, 421 values).",
    "  -> each IAP_SUBJECT_DETAIL row appears ~2.8 times after this join.",
    "  -> each IAP_SUBJECT_SESSION row appears ~3.3 times after this join.",
    "- Join BUILDINGS.BUILDING_NUMBER = IAP_SUBJECT_SESSION.SESSION_LOCATION: BUILDINGS.BUILDING_NUMBER is unique per row (211 rows); IAP_SUBJECT_SESSION.SESSION_LOCATION repeats ~29.2 rows per value (1199 rows, 41 values).",
    "  -> each BUILDINGS row appears ~29.2 times after this join.",
    "- \"name of the building\" -> BUILDINGS.BUILDING_NAME: unique per row in its own table; the hinted joins multiply BUILDINGS rows ~x29.2, so a plain COUNT over the joined result counts duplicates. To count distinct name of the building, use COUNT(DISTINCT BUILDINGS.BUILDING_NAME).",
    "- \"subjects\" -> IAP_SUBJECT_DETAIL.ACTIVITY_TITLE: already repeats ~4.4 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct subjects, use COUNT(DISTINCT IAP_SUBJECT_DETAIL.ACTIVITY_TITLE).",
    "- \"fee\" -> IAP_SUBJECT_DETAIL.FEE is a numeric measure of IAP_SUBJECT_DETAIL; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x2.8, so SUM/AVG over the joined result would count each IAP_SUBJECT_DETAIL row ~2.8 times. Aggregate it at IAP_SUBJECT_DETAIL's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"sessions\" -> IAP_SUBJECT_SESSION.SESSION_END_TIME: already repeats ~28.5 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_SESSION rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct sessions, use COUNT(DISTINCT IAP_SUBJECT_SESSION.SESSION_END_TIME).",
    "- \"sessions\" -> IAP_SUBJECT_SESSION.SESSION_START_TIME: already repeats ~38.7 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_SESSION rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct sessions, use COUNT(DISTINCT IAP_SUBJECT_SESSION.SESSION_START_TIME)."
  ],
  "dw_real_16": [
    "- Join IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY = IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY: IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY repeats ~3.3 rows per value (465 rows, 142 values); IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY repeats ~2.8 rows per value (1199 rows, 421 values).",
    "  -> each IAP_SUBJECT_DETAIL row appears ~2.8 times after this join.",
    "  -> each IAP_SUBJECT_SESSION row appears ~3.3 times after this join.",
    "- Join BUILDINGS.BUILDING_NUMBER = IAP_SUBJECT_SESSION.SESSION_LOCATION: BUILDINGS.BUILDING_NUMBER is unique per row (211 rows); IAP_SUBJECT_SESSION.SESSION_LOCATION repeats ~29.2 rows per value (1199 rows, 41 values).",
    "  -> each BUILDINGS row appears ~29.2 times after this join.",
    "- \"subjects\" -> IAP_SUBJECT_DETAIL.ACTIVITY_TITLE: already repeats ~4.4 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct subjects, use COUNT(DISTINCT IAP_SUBJECT_DETAIL.ACTIVITY_TITLE).",
    "- \"fee\" -> IAP_SUBJECT_DETAIL.FEE is a numeric measure of IAP_SUBJECT_DETAIL; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x2.8, so SUM/AVG over the joined result would count each IAP_SUBJECT_DETAIL row ~2.8 times. Aggregate it at IAP_SUBJECT_DETAIL's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"sessions\" -> IAP_SUBJECT_SESSION.SESSION_END_TIME: already repeats ~28.5 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_SESSION rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct sessions, use COUNT(DISTINCT IAP_SUBJECT_SESSION.SESSION_END_TIME).",
    "- \"sessions\" -> IAP_SUBJECT_SESSION.SESSION_START_TIME: already repeats ~38.7 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_SESSION rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct sessions, use COUNT(DISTINCT IAP_SUBJECT_SESSION.SESSION_START_TIME)."
  ],
  "dw_real_17": [
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- Join SIS_COURSE_DESCRIPTION.COURSE = SIS_SUBJECT_CODE.SUBJECT_CODE: SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values); SIS_SUBJECT_CODE.SUBJECT_CODE is unique per row (221 rows).",
    "  -> each SIS_SUBJECT_CODE row appears ~1.1 times after this join.",
    "- \"school code\" -> SIS_SUBJECT_CODE.SCHOOL_CODE: already repeats ~24.6 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct school code, use COUNT(DISTINCT SIS_SUBJECT_CODE.SCHOOL_CODE).",
    "- \"school name\" -> SIS_SUBJECT_CODE.SCHOOL_NAME: already repeats ~24.6 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_SUBJECT_CODE.SCHOOL_NAME).",
    "- \"full name of the department\" -> SIS_DEPARTMENT.DEPARTMENT_FULL_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct full name of the department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_FULL_NAME).",
    "- \"course level\" -> SIS_COURSE_DESCRIPTION.COURSE_LEVEL: already repeats ~347.5 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"courses\" -> SIS_COURSE_DESCRIPTION.COURSE: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"degree-granting courses\" -> SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING: already repeats ~347.5 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows."
  ],
  "dw_real_18": [
    "- Join SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE is unique per row (179 rows); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- Join SIS_COURSE_DESCRIPTION.COURSE = SIS_SUBJECT_CODE.SUBJECT_CODE: SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values); SIS_SUBJECT_CODE.SUBJECT_CODE is unique per row (221 rows).",
    "  -> each SIS_SUBJECT_CODE row appears ~1.1 times after this join.",
    "- \"school code\" -> SIS_SUBJECT_CODE.SCHOOL_CODE: already repeats ~24.6 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct school code, use COUNT(DISTINCT SIS_SUBJECT_CODE.SCHOOL_CODE).",
    "- \"school name\" -> SIS_SUBJECT_CODE.SCHOOL_NAME: already repeats ~24.6 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_SUBJECT_CODE.SCHOOL_NAME).",
    "- \"department code\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct department code, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"department name\" -> SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply SIS_ADMIN_DEPARTMENT rows.",
    "- \"phone numbers\" -> SIS_ADMIN_DEPARTMENT.DEPARTMENT_PHONE_NUMBER: already repeats ~2.8 rows per value in its own table; the hinted joins do not multiply SIS_ADMIN_DEPARTMENT rows.",
    "- \"course level\" -> SIS_COURSE_DESCRIPTION.COURSE_LEVEL: already repeats ~347.5 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows."
  ],
  "dw_real_19": [
    "- Join SIS_COURSE_DESCRIPTION.COURSE = SIS_SUBJECT_CODE.SUBJECT_CODE: SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values); SIS_SUBJECT_CODE.SUBJECT_CODE is unique per row (221 rows).",
    "  -> each SIS_SUBJECT_CODE row appears ~1.1 times after this join.",
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- \"school code\" -> SIS_SUBJECT_CODE.SCHOOL_CODE: already repeats ~24.6 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct school code, use COUNT(DISTINCT SIS_SUBJECT_CODE.SCHOOL_CODE).",
    "- \"school name\" -> SIS_SUBJECT_CODE.SCHOOL_NAME: already repeats ~24.6 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_SUBJECT_CODE.SCHOOL_NAME).",
    "- \"DLC key\" -> SIS_DEPARTMENT.DLC_KEY: already repeats ~1.8 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct DLC key, use COUNT(DISTINCT SIS_DEPARTMENT.DLC_KEY).",
    "- \"graduate level\" -> SIS_COURSE_DESCRIPTION.GRADUATE_LEVEL: already repeats ~347.5 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"SIS subjects\" -> SIS_SUBJECT_CODE.SUBJECT_CODE: unique per row in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct SIS subjects, use COUNT(DISTINCT SIS_SUBJECT_CODE.SUBJECT_CODE).",
    "- \"course numbers\" -> SIS_SUBJECT_CODE.COURSE_NUMBER: unique per row in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct course numbers, use COUNT(DISTINCT SIS_SUBJECT_CODE.COURSE_NUMBER).",
    "- \"departments\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct departments, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE)."
  ],
  "dw_real_2": [
    "- Join LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY: LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY repeats ~6.2 rows per value (10000 rows, 1614 values).",
    "  -> each LIBRARY_COURSE_INSTRUCTOR row appears ~6.2 times after this join.",
    "- Join LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY = LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY: LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY repeats ~6.5 rows per value (10000 rows, 1550 values); LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY is unique per row (10000 rows).",
    "  -> each LIBRARY_SUBJECT_OFFERED row appears ~6.5 times after this join.",
    "- Join LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY repeats ~1.2 rows per value (10000 rows, 8268 values).",
    "  -> each LIBRARY_RESERVE_CATALOG row appears ~1.2 times after this join.",
    "- \"course instructor names\" -> LIBRARY_COURSE_INSTRUCTOR.INSTRUCTOR_NAME: unique per row in its own table; the hinted joins multiply LIBRARY_COURSE_INSTRUCTOR rows ~x6.2, so a plain COUNT over the joined result counts duplicates. To count distinct course instructor names, use COUNT(DISTINCT LIBRARY_COURSE_INSTRUCTOR.INSTRUCTOR_NAME).",
    "- \"course titles\" -> LIBRARY_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~5.6 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so a plain COUNT over the joined result counts duplicates. To count distinct course titles, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.SUBJECT_TITLE).",
    "- \"amount of material\" -> LIBRARY_RESERVE_CATALOG.CATALOG_ISBN: already repeats ~2.6 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_CATALOG rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct amount of material, use COUNT(DISTINCT LIBRARY_RESERVE_CATALOG.CATALOG_ISBN).",
    "- \"course instructor key\" -> LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY: unique per row in its own table; the hinted joins multiply LIBRARY_COURSE_INSTRUCTOR rows ~x6.2, so a plain COUNT over the joined result counts duplicates. To count distinct course instructor key, use COUNT(DISTINCT LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY).",
    "- \"key of subject offered\" -> LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY: unique per row in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so a plain COUNT over the joined result counts duplicates. To count distinct key of subject offered, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY)."
  ],
  "dw_real_20": [
    "- Join SPACE_SUPERVISOR_USAGE.DEPT_NAMES = SPACE_UNIT.DLC_KEY: SPACE_SUPERVISOR_USAGE.DEPT_NAMES repeats ~15.8 rows per value (2135 rows, 135 values); SPACE_UNIT.DLC_KEY repeats ~1.1 rows per value (150 rows, 132 values).",
    "  -> each SPACE_SUPERVISOR_USAGE row appears ~1.1 times after this join.",
    "  -> each SPACE_UNIT row appears ~15.8 times after this join.",
    "- \"building component\" -> SPACE_DETAIL.BUILDING_COMPONENT: already repeats ~294.1 rows per value in its own table; the hinted joins do not multiply SPACE_DETAIL rows.",
    "- \"name of the building\" -> BUILDINGS.BUILDING_NAME: unique per row in its own table; the hinted joins do not multiply BUILDINGS rows.",
    "- \"square footage\" -> SPACE_DETAIL.ROOM_SQUARE_FOOTAGE is a numeric measure of SPACE_DETAIL; the hinted joins do not multiply SPACE_DETAIL rows.",
    "- \"rooms\" -> SPACE_DETAIL.BUILDING_ROOM: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply SPACE_DETAIL rows.",
    "- \"floors\" -> SPACE_FLOOR.FLOOR: unique per row in its own table; the hinted joins do not multiply SPACE_FLOOR rows.",
    "- \"facility organizations\" -> SPACE_UNIT.FCLT_ORGANIZATION_KEY: unique per row in its own table; the hinted joins multiply SPACE_UNIT rows ~x15.8, so a plain COUNT over the joined result counts duplicates. To count distinct facility organizations, use COUNT(DISTINCT SPACE_UNIT.FCLT_ORGANIZATION_KEY).",
    "- \"supervisors\" -> SPACE_SUPERVISOR_USAGE.MIT_ID: unique per row in its own table; the hinted joins multiply SPACE_SUPERVISOR_USAGE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct supervisors, use COUNT(DISTINCT SPACE_SUPERVISOR_USAGE.MIT_ID).",
    "- \"supervisees\" -> SPACE_SUPERVISOR_USAGE.NUM_OF_SUPERVISEES is a numeric measure of SPACE_SUPERVISOR_USAGE; the hinted joins multiply SPACE_SUPERVISOR_USAGE rows ~x1.1, so SUM/AVG over the joined result would count each SPACE_SUPERVISOR_USAGE row ~1.1 times. Aggregate it at SPACE_SUPERVISOR_USAGE's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_real_21": [
    "- Join SPACE_SUPERVISOR_USAGE.DEPT_NAMES = SPACE_UNIT.DLC_KEY: SPACE_SUPERVISOR_USAGE.DEPT_NAMES repeats ~15.8 rows per value (2135 rows, 135 values); SPACE_UNIT.DLC_KEY repeats ~1.1 rows per value (150 rows, 132 values).",
    "  -> each SPACE_SUPERVISOR_USAGE row appears ~1.1 times after this join.",
    "  -> each SPACE_UNIT row appears ~15.8 times after this join.",
    "- \"DLC key\" -> SPACE_UNIT.DLC_KEY: already repeats ~1.1 rows per value in its own table; the hinted joins multiply SPACE_UNIT rows ~x15.8, so a plain COUNT over the joined result counts duplicates. To count distinct DLC key, use COUNT(DISTINCT SPACE_UNIT.DLC_KEY).",
    "- \"name of the DLC\" -> FCLT_ORGANIZATION.DLC_NAME: already repeats ~1.3 rows per value in its own table; the hinted joins do not multiply FCLT_ORGANIZATION rows.",
    "- \"floors\" -> SPACE_FLOOR.FLOOR: unique per row in its own table; the hinted joins do not multiply SPACE_FLOOR rows.",
    "- \"square footage\" -> SPACE_DETAIL.ROOM_SQUARE_FOOTAGE is a numeric measure of SPACE_DETAIL; the hinted joins do not multiply SPACE_DETAIL rows.",
    "- \"facility organizations\" -> SPACE_UNIT.FCLT_ORGANIZATION_KEY: unique per row in its own table; the hinted joins multiply SPACE_UNIT rows ~x15.8, so a plain COUNT over the joined result counts duplicates. To count distinct facility organizations, use COUNT(DISTINCT SPACE_UNIT.FCLT_ORGANIZATION_KEY).",
    "- \"supervisors\" -> SPACE_SUPERVISOR_USAGE.MIT_ID: unique per row in its own table; the hinted joins multiply SPACE_SUPERVISOR_USAGE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct supervisors, use COUNT(DISTINCT SPACE_SUPERVISOR_USAGE.MIT_ID).",
    "- \"supervisees\" -> SPACE_SUPERVISOR_USAGE.NUM_OF_SUPERVISEES is a numeric measure of SPACE_SUPERVISOR_USAGE; the hinted joins multiply SPACE_SUPERVISOR_USAGE rows ~x1.1, so SUM/AVG over the joined result would count each SPACE_SUPERVISOR_USAGE row ~1.1 times. Aggregate it at SPACE_SUPERVISOR_USAGE's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"building heights\" -> FCLT_BUILDING.BUILDING_HEIGHT: already repeats ~2.2 rows per value in its own table; the hinted joins do not multiply FCLT_BUILDING rows."
  ],
  "dw_real_22": [
    "- Join TIP_DETAIL.TIP_SUBJECT_OFFERED_KEY = TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY: TIP_DETAIL.TIP_SUBJECT_OFFERED_KEY repeats ~1.4 rows per value (10000 rows, 7340 values); TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY is unique per row (10000 rows).",
    "  -> each TIP_SUBJECT_OFFERED row appears ~1.4 times after this join.",
    "- Join TIP_DETAIL.TIP_MATERIAL_KEY = TIP_MATERIAL.TIP_MATERIAL_KEY: TIP_DETAIL.TIP_MATERIAL_KEY repeats ~1.4 rows per value (10000 rows, 7360 values); TIP_MATERIAL.TIP_MATERIAL_KEY is unique per row (10000 rows).",
    "  -> each TIP_MATERIAL row appears ~1.4 times after this join.",
    "- \"department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"TIP subjects\" -> TIP_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct TIP subjects, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.SUBJECT_ID).",
    "- \"enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x1.4, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~1.4 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"rental new price\" -> TIP_MATERIAL.RENTAL_NEW_PRICE is a numeric measure of TIP_MATERIAL; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so SUM/AVG over the joined result would count each TIP_MATERIAL row ~1.4 times. Aggregate it at TIP_MATERIAL's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_real_23": [
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID = SUBJECT_OFFERED.SUBJECT_ID: COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID repeats ~2.0 rows per value (10000 rows, 4969 values); SUBJECT_OFFERED.SUBJECT_ID repeats ~4.8 rows per value (10000 rows, 2081 values).",
    "  -> each COURSE_CATALOG_SUBJECT_OFFERED row appears ~4.8 times after this join.",
    "  -> each SUBJECT_OFFERED row appears ~2.0 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS row appears ~105.3 times after this join.",
    "- Join EMPLOYEE_DIRECTORY.MIT_ID = SUBJECT_OFFERED.RESPONSIBLE_FACULTY_MIT_ID: EMPLOYEE_DIRECTORY.MIT_ID is unique per row (10000 rows); SUBJECT_OFFERED.RESPONSIBLE_FACULTY_MIT_ID repeats ~10000.0 rows per value (10000 rows, 0 values).",
    "  -> each EMPLOYEE_DIRECTORY row appears ~10000.0 times after this join.",
    "- \"current academic term\" -> ACADEMIC_TERMS.IS_CURRENT_TERM: already repeats ~72.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct current academic term, use COUNT(DISTINCT ACADEMIC_TERMS.IS_CURRENT_TERM).",
    "- \"academic year\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.8, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR).",
    "- \"term code\" -> COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: already repeats ~105.3 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.8, so a plain COUNT over the joined result counts duplicates. To count distinct term code, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE).",
    "- \"hgn code\" -> SUBJECT_OFFERED.HGN_CODE: already repeats ~3333.3 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED rows ~x2.0, so a plain COUNT over the joined result counts duplicates. To count distinct hgn code, use COUNT(DISTINCT SUBJECT_OFFERED.HGN_CODE).",
    "- \"courses\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.8, so a plain COUNT over the joined result counts duplicates. To count distinct courses, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID).",
    "- \"number of units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.8, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~4.8 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"department name\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x4.8, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME).",
    "- \"name\" -> EMPLOYEE_DIRECTORY.FULL_NAME: unique per row in its own table; the hinted joins multiply EMPLOYEE_DIRECTORY rows ~x10000.0, so a plain COUNT over the joined result counts duplicates. To count distinct name, use COUNT(DISTINCT EMPLOYEE_DIRECTORY.FULL_NAME).",
    "- \"email address\" -> EMPLOYEE_DIRECTORY.EMAIL_ADDRESS: already repeats ~1.5 rows per value in its own table; the hinted joins multiply EMPLOYEE_DIRECTORY rows ~x10000.0, so a plain COUNT over the joined result counts duplicates. To count distinct email address, use COUNT(DISTINCT EMPLOYEE_DIRECTORY.EMAIL_ADDRESS)."
  ],
  "dw_real_24": [
    "- Join IAP_SUBJECT_CATEGORY.IAP_SUBJECT_CATEGORY_KEY = IAP_SUBJECT_DETAIL.IAP_SUBJECT_CATEGORY_KEY: IAP_SUBJECT_CATEGORY.IAP_SUBJECT_CATEGORY_KEY is unique per row (49 rows); IAP_SUBJECT_DETAIL.IAP_SUBJECT_CATEGORY_KEY repeats ~10.1 rows per value (465 rows, 46 values).",
    "  -> each IAP_SUBJECT_CATEGORY row appears ~10.1 times after this join.",
    "- Join IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY = IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY: IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY repeats ~3.3 rows per value (465 rows, 142 values); IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY repeats ~2.8 rows per value (1199 rows, 421 values).",
    "  -> each IAP_SUBJECT_DETAIL row appears ~2.8 times after this join.",
    "  -> each IAP_SUBJECT_SESSION row appears ~3.3 times after this join.",
    "- Join IAP_SUBJECT_DETAIL.IAP_SUBJECT_SPONSOR_KEY = IAP_SUBJECT_SPONSOR.IAP_SUBJECT_SPONSOR_KEY: IAP_SUBJECT_DETAIL.IAP_SUBJECT_SPONSOR_KEY repeats ~6.8 rows per value (465 rows, 68 values); IAP_SUBJECT_SPONSOR.IAP_SUBJECT_SPONSOR_KEY is unique per row (68 rows).",
    "  -> each IAP_SUBJECT_SPONSOR row appears ~6.8 times after this join.",
    "- \"titles\" -> IAP_SUBJECT_DETAIL.ACTIVITY_TITLE: already repeats ~4.4 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct titles, use COUNT(DISTINCT IAP_SUBJECT_DETAIL.ACTIVITY_TITLE).",
    "- \"categories\" -> IAP_SUBJECT_CATEGORY.IAP_CATEGORY_NAME: unique per row in its own table; the hinted joins multiply IAP_SUBJECT_CATEGORY rows ~x10.1, so a plain COUNT over the joined result counts duplicates. To count distinct categories, use COUNT(DISTINCT IAP_SUBJECT_CATEGORY.IAP_CATEGORY_NAME).",
    "- \"session titles\" -> IAP_SUBJECT_SESSION.SESSION_TITLE: already repeats ~5.3 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_SESSION rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct session titles, use COUNT(DISTINCT IAP_SUBJECT_SESSION.SESSION_TITLE).",
    "- \"session start time\" -> IAP_SUBJECT_SESSION.SESSION_START_TIME: already repeats ~38.7 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_SESSION rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct session start time, use COUNT(DISTINCT IAP_SUBJECT_SESSION.SESSION_START_TIME).",
    "- \"session end time\" -> IAP_SUBJECT_SESSION.SESSION_END_TIME: already repeats ~28.5 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_SESSION rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct session end time, use COUNT(DISTINCT IAP_SUBJECT_SESSION.SESSION_END_TIME).",
    "- \"sponsor names\" -> IAP_SUBJECT_SPONSOR.SPONSOR_NAME: unique per row in its own table; the hinted joins multiply IAP_SUBJECT_SPONSOR rows ~x6.8, so a plain COUNT over the joined result counts duplicates. To count distinct sponsor names, use COUNT(DISTINCT IAP_SUBJECT_SPONSOR.SPONSOR_NAME).",
    "- \"sessions\" -> IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY: already repeats ~2.8 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_SESSION rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct sessions, use COUNT(DISTINCT IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY)."
  ],
  "dw_real_25": [
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- Join SIS_COURSE_DESCRIPTION.COURSE = SIS_SUBJECT_CODE.COURSE_NUMBER: SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values); SIS_SUBJECT_CODE.COURSE_NUMBER is unique per row (221 rows).",
    "  -> each SIS_SUBJECT_CODE row appears ~1.1 times after this join.",
    "- \"department names\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct department names, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"SIS subject code\" -> SIS_SUBJECT_CODE.SUBJECT_CODE: unique per row in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct SIS subject code, use COUNT(DISTINCT SIS_SUBJECT_CODE.SUBJECT_CODE).",
    "- \"subject code descriptions\" -> SIS_SUBJECT_CODE.SUBJECT_CODE_DESC: already repeats ~1.2 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct subject code descriptions, use COUNT(DISTINCT SIS_SUBJECT_CODE.SUBJECT_CODE_DESC).",
    "- \"graduate level\" -> SIS_COURSE_DESCRIPTION.GRADUATE_LEVEL: already repeats ~347.5 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"courses\" -> SIS_SUBJECT_CODE.COURSE_NUMBER: unique per row in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct courses, use COUNT(DISTINCT SIS_SUBJECT_CODE.COURSE_NUMBER)."
  ],
  "dw_real_26": [
    "- \"term code\" -> ACADEMIC_TERMS_ALL.TERM_CODE: unique per row in its own table; the hinted joins do not multiply ACADEMIC_TERMS_ALL rows.",
    "- \"term description\" -> ACADEMIC_TERMS_ALL.TERM_DESCRIPTION: unique per row in its own table; the hinted joins do not multiply ACADEMIC_TERMS_ALL rows.",
    "- \"term is current or not\" -> ACADEMIC_TERM_PARAMETER.IS_CURRENT_TERM: already repeats ~1.5 rows per value in its own table; the hinted joins do not multiply ACADEMIC_TERM_PARAMETER rows.",
    "- \"CIS courses\" -> CIS_COURSE_CATALOG.SUBJECT_ID: already repeats ~4.4 rows per value in its own table; the hinted joins do not multiply CIS_COURSE_CATALOG rows."
  ],
  "dw_real_27": [
    "- Join TIP_DETAIL.TIP_MATERIAL_STATUS_KEY = TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS_KEY: TIP_DETAIL.TIP_MATERIAL_STATUS_KEY repeats ~833.3 rows per value (10000 rows, 12 values); TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS_KEY is unique per row (12 rows).",
    "  -> each TIP_MATERIAL_STATUS row appears ~833.3 times after this join.",
    "- Join TIP_DETAIL.TIP_SUBJECT_OFFERED_KEY = TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY: TIP_DETAIL.TIP_SUBJECT_OFFERED_KEY repeats ~1.4 rows per value (10000 rows, 7340 values); TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY is unique per row (10000 rows).",
    "  -> each TIP_SUBJECT_OFFERED row appears ~1.4 times after this join.",
    "- Join STUDENT_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: STUDENT_DEPARTMENT.DEPARTMENT_CODE is unique per row (79 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each STUDENT_DEPARTMENT row appears ~161.3 times after this join.",
    "- \"author\" -> TIP_MATERIAL.AUTHOR: already repeats ~1.8 rows per value in its own table; the hinted joins do not multiply TIP_MATERIAL rows.",
    "- \"school name\" -> STUDENT_DEPARTMENT.SCHOOL_NAME: already repeats ~8.8 rows per value in its own table; the hinted joins multiply STUDENT_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT STUDENT_DEPARTMENT.SCHOOL_NAME).",
    "- \"material status\" -> TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS: already repeats ~1.5 rows per value in its own table; the hinted joins multiply TIP_MATERIAL_STATUS rows ~x833.3, so a plain COUNT over the joined result counts duplicates. To count distinct material status, use COUNT(DISTINCT TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS).",
    "- \"record counts\" -> TIP_DETAIL.RECORD_COUNT is a numeric measure of TIP_DETAIL; the hinted joins do not multiply TIP_DETAIL rows.",
    "- \"courses\" -> TIP_DETAIL.SUBJECT_ID: already repeats ~2.8 rows per value in its own table; the hinted joins do not multiply TIP_DETAIL rows."
  ],
  "dw_real_28": [
    "- Join TIP_DETAIL.TIP_MATERIAL_KEY = TIP_MATERIAL.TIP_MATERIAL_KEY: TIP_DETAIL.TIP_MATERIAL_KEY repeats ~1.4 rows per value (10000 rows, 7360 values); TIP_MATERIAL.TIP_MATERIAL_KEY is unique per row (10000 rows).",
    "  -> each TIP_MATERIAL row appears ~1.4 times after this join.",
    "- Join TIP_DETAIL.TIP_MATERIAL_STATUS_KEY = TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS_KEY: TIP_DETAIL.TIP_MATERIAL_STATUS_KEY repeats ~833.3 rows per value (10000 rows, 12 values); TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS_KEY is unique per row (12 rows).",
    "  -> each TIP_MATERIAL_STATUS row appears ~833.3 times after this join.",
    "- Join TIP_DETAIL.TIP_SUBJECT_OFFERED_KEY = TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY: TIP_DETAIL.TIP_SUBJECT_OFFERED_KEY repeats ~1.4 rows per value (10000 rows, 7340 values); TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY is unique per row (10000 rows).",
    "  -> each TIP_SUBJECT_OFFERED row appears ~1.4 times after this join.",
    "- \"course number\" -> TIP_SUBJECT_OFFERED.COURSE_NUMBER: already repeats ~172.4 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct course number, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.COURSE_NUMBER).",
    "- \"subject title\" -> TIP_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.4 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct subject title, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.SUBJECT_TITLE).",
    "- \"material status\" -> TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS: already repeats ~1.5 rows per value in its own table; the hinted joins multiply TIP_MATERIAL_STATUS rows ~x833.3, so a plain COUNT over the joined result counts duplicates. To count distinct material status, use COUNT(DISTINCT TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS).",
    "- \"new shelf price\" -> TIP_MATERIAL.NEW_SHELF_PRICE is a numeric measure of TIP_MATERIAL; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so SUM/AVG over the joined result would count each TIP_MATERIAL row ~1.4 times. Aggregate it at TIP_MATERIAL's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"used shelf price\" -> TIP_MATERIAL.USED_SHELF_PRICE is a numeric measure of TIP_MATERIAL; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so SUM/AVG over the joined result would count each TIP_MATERIAL row ~1.4 times. Aggregate it at TIP_MATERIAL's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"schools\" -> TIP_SUBJECT_OFFERED.OFFER_SCHOOL_NAME: already repeats ~1111.1 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct schools, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_SCHOOL_NAME).",
    "- \"materials\" -> TIP_DETAIL.TIP_MATERIAL_KEY: already repeats ~1.4 rows per value in its own table; the hinted joins do not multiply TIP_DETAIL rows.",
    "- \"TIP subject\" -> TIP_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.4 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct TIP subject, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.SUBJECT_TITLE)."
  ],
  "dw_real_29": [
    "- Join TIP_DETAIL.TIP_MATERIAL_KEY = TIP_MATERIAL.TIP_MATERIAL_KEY: TIP_DETAIL.TIP_MATERIAL_KEY repeats ~1.4 rows per value (10000 rows, 7360 values); TIP_MATERIAL.TIP_MATERIAL_KEY is unique per row (10000 rows).",
    "  -> each TIP_MATERIAL row appears ~1.4 times after this join.",
    "- Join TIP_DETAIL.TIP_MATERIAL_STATUS_KEY = TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS_KEY: TIP_DETAIL.TIP_MATERIAL_STATUS_KEY repeats ~833.3 rows per value (10000 rows, 12 values); TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS_KEY is unique per row (12 rows).",
    "  -> each TIP_MATERIAL_STATUS row appears ~833.3 times after this join.",
    "- Join TIP_DETAIL.TIP_SUBJECT_OFFERED_KEY = TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY: TIP_DETAIL.TIP_SUBJECT_OFFERED_KEY repeats ~1.4 rows per value (10000 rows, 7340 values); TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY is unique per row (10000 rows).",
    "  -> each TIP_SUBJECT_OFFERED row appears ~1.4 times after this join.",
    "- Join STUDENT_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: STUDENT_DEPARTMENT.DEPARTMENT_CODE is unique per row (79 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each STUDENT_DEPARTMENT row appears ~161.3 times after this join.",
    "- \"material status\" -> TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS: already repeats ~1.5 rows per value in its own table; the hinted joins multiply TIP_MATERIAL_STATUS rows ~x833.3, so a plain COUNT over the joined result counts duplicates. To count distinct material status, use COUNT(DISTINCT TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS).",
    "- \"materials\" -> TIP_DETAIL.TIP_MATERIAL_KEY: already repeats ~1.4 rows per value in its own table; the hinted joins do not multiply TIP_DETAIL rows.",
    "- \"subjects\" -> TIP_DETAIL.SUBJECT_ID: already repeats ~2.8 rows per value in its own table; the hinted joins do not multiply TIP_DETAIL rows.",
    "- \"schools\" -> STUDENT_DEPARTMENT.SCHOOL_NAME: already repeats ~8.8 rows per value in its own table; the hinted joins multiply STUDENT_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct schools, use COUNT(DISTINCT STUDENT_DEPARTMENT.SCHOOL_NAME).",
    "- \"publication year\" -> TIP_MATERIAL.YEAR: already repeats ~120.5 rows per value in its own table; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct publication year, use COUNT(DISTINCT TIP_MATERIAL.YEAR)."
  ],
  "dw_real_3": [
    "- Join EMPLOYEE_DIRECTORY.OFFICE_LOCATION = FAC_ROOMS.FAC_ROOM_KEY: EMPLOYEE_DIRECTORY.OFFICE_LOCATION repeats ~1.9 rows per value (10000 rows, 5396 values); FAC_ROOMS.FAC_ROOM_KEY is unique per row (10000 rows).",
    "  -> each FAC_ROOMS row appears ~1.9 times after this join.",
    "- Join FAC_BUILDING_ADDRESS.BUILDING_KEY = FAC_ROOMS.BUILDING_KEY: FAC_BUILDING_ADDRESS.BUILDING_KEY repeats ~3.2 rows per value (785 rows, 242 values); FAC_ROOMS.BUILDING_KEY repeats ~53.2 rows per value (10000 rows, 188 values).",
    "  -> each FAC_BUILDING_ADDRESS row appears ~53.2 times after this join.",
    "  -> each FAC_ROOMS row appears ~3.2 times after this join.",
    "- Join BUILDINGS.BUILDING_KEY = FAC_BUILDING_ADDRESS.BUILDING_KEY: BUILDINGS.BUILDING_KEY is unique per row (211 rows); FAC_BUILDING_ADDRESS.BUILDING_KEY repeats ~3.2 rows per value (785 rows, 242 values).",
    "  -> each BUILDINGS row appears ~3.2 times after this join.",
    "- \"room\" -> FAC_ROOMS.ROOM: already repeats ~2.9 rows per value in its own table; the hinted joins multiply FAC_ROOMS rows ~x3.2, so a plain COUNT over the joined result counts duplicates. To count distinct room, use COUNT(DISTINCT FAC_ROOMS.ROOM).",
    "- \"floor\" -> FAC_ROOMS.FLOOR: already repeats ~212.8 rows per value in its own table; the hinted joins multiply FAC_ROOMS rows ~x3.2, so a plain COUNT over the joined result counts duplicates. To count distinct floor, use COUNT(DISTINCT FAC_ROOMS.FLOOR).",
    "- \"building key\" -> FAC_BUILDING_ADDRESS.BUILDING_KEY: already repeats ~3.2 rows per value in its own table; the hinted joins multiply FAC_BUILDING_ADDRESS rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct building key, use COUNT(DISTINCT FAC_BUILDING_ADDRESS.BUILDING_KEY).",
    "- \"building street address\" -> BUILDINGS.BUILDING_STREET_ADDRESS: already repeats ~1.4 rows per value in its own table; the hinted joins multiply BUILDINGS rows ~x3.2, so a plain COUNT over the joined result counts duplicates. To count distinct building street address, use COUNT(DISTINCT BUILDINGS.BUILDING_STREET_ADDRESS).",
    "- \"building street address\" -> FAC_BUILDING_ADDRESS.ADDRESS_PURPOSE: already repeats ~60.4 rows per value in its own table; the hinted joins multiply FAC_BUILDING_ADDRESS rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct building street address, use COUNT(DISTINCT FAC_BUILDING_ADDRESS.ADDRESS_PURPOSE).",
    "- \"city\" -> FAC_BUILDING_ADDRESS.CITY: already repeats ~71.4 rows per value in its own table; the hinted joins multiply FAC_BUILDING_ADDRESS rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct city, use COUNT(DISTINCT FAC_BUILDING_ADDRESS.CITY).",
    "- \"state\" -> FAC_BUILDING_ADDRESS.STATE: already repeats ~392.5 rows per value in its own table; the hinted joins multiply FAC_BUILDING_ADDRESS rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct state, use COUNT(DISTINCT FAC_BUILDING_ADDRESS.STATE).",
    "- \"postal code\" -> FAC_BUILDING_ADDRESS.POSTAL_CODE: already repeats ~56.1 rows per value in its own table; the hinted joins multiply FAC_BUILDING_ADDRESS rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct postal code, use COUNT(DISTINCT FAC_BUILDING_ADDRESS.POSTAL_CODE).",
    "- \"Professor Summer Haynes\" -> EMPLOYEE_DIRECTORY.FULL_NAME: unique per row in its own table; the hinted joins do not multiply EMPLOYEE_DIRECTORY rows.",
    "- \"office\" -> EMPLOYEE_DIRECTORY.OFFICE_LOCATION: already repeats ~1.9 rows per value in its own table; the hinted joins do not multiply EMPLOYEE_DIRECTORY rows."
  ],
  "dw_real_30": [
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = TIP_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); TIP_SUBJECT_OFFERED.TERM_CODE repeats ~79.4 rows per value (10000 rows, 126 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~79.4 times after this join.",
    "- Join TIP_DETAIL.TIP_SUBJECT_OFFERED_KEY = TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY: TIP_DETAIL.TIP_SUBJECT_OFFERED_KEY repeats ~1.4 rows per value (10000 rows, 7340 values); TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY is unique per row (10000 rows).",
    "  -> each TIP_SUBJECT_OFFERED row appears ~1.4 times after this join.",
    "- Join TIP_DETAIL.TIP_MATERIAL_KEY = TIP_MATERIAL.TIP_MATERIAL_KEY: TIP_DETAIL.TIP_MATERIAL_KEY repeats ~1.4 rows per value (10000 rows, 7360 values); TIP_MATERIAL.TIP_MATERIAL_KEY is unique per row (10000 rows).",
    "  -> each TIP_MATERIAL row appears ~1.4 times after this join.",
    "- Join TIP_DETAIL.TIP_MATERIAL_STATUS_KEY = TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS_KEY: TIP_DETAIL.TIP_MATERIAL_STATUS_KEY repeats ~833.3 rows per value (10000 rows, 12 values); TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS_KEY is unique per row (12 rows).",
    "  -> each TIP_MATERIAL_STATUS row appears ~833.3 times after this join.",
    "- \"term description\" -> ACADEMIC_TERMS_ALL.TERM_DESCRIPTION: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct term description, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_DESCRIPTION).",
    "- \"term is current or not\" -> ACADEMIC_TERMS_ALL.IS_CURRENT_TERM: already repeats ~150.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct term is current or not, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.IS_CURRENT_TERM).",
    "- \"TIP subjects\" -> TIP_SUBJECT_OFFERED.COURSE_NUMBER: already repeats ~172.4 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct TIP subjects, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.COURSE_NUMBER).",
    "- \"materials\" -> TIP_MATERIAL.ISBN: already repeats ~1.5 rows per value in its own table; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct materials, use COUNT(DISTINCT TIP_MATERIAL.ISBN).",
    "- \"enrolled students\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x1.4, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~1.4 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"schools\" -> TIP_SUBJECT_OFFERED.OFFER_SCHOOL_NAME: already repeats ~1111.1 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct schools, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_SCHOOL_NAME).",
    "- \"number of records\" -> TIP_DETAIL.RECORD_COUNT is a numeric measure of TIP_DETAIL; the hinted joins do not multiply TIP_DETAIL rows.",
    "- \"term code\" -> ACADEMIC_TERMS_ALL.TERM_CODE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct term code, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_CODE)."
  ],
  "dw_real_31": [
    "- Join BUILDINGS.BUILDING_KEY = FAC_BUILDING.FAC_BUILDING_KEY: BUILDINGS.BUILDING_KEY is unique per row (211 rows); FAC_BUILDING.FAC_BUILDING_KEY is unique per row (242 rows).",
    "- Join FAC_BUILDING.FAC_BUILDING_KEY = FAC_ROOMS.BUILDING_KEY: FAC_BUILDING.FAC_BUILDING_KEY is unique per row (242 rows); FAC_ROOMS.BUILDING_KEY repeats ~53.2 rows per value (10000 rows, 188 values).",
    "  -> each FAC_BUILDING row appears ~53.2 times after this join.",
    "- Join FAC_ORGANIZATION.ORGANIZATION_KEY = FAC_ROOMS.ORGANIZATION_KEY: FAC_ORGANIZATION.ORGANIZATION_KEY is unique per row (169 rows); FAC_ROOMS.ORGANIZATION_KEY repeats ~1111.1 rows per value (10000 rows, 9 values).",
    "  -> each FAC_ORGANIZATION row appears ~1111.1 times after this join.",
    "- \"names\" -> BUILDINGS.BUILDING_NAME: unique per row in its own table; the hinted joins do not multiply BUILDINGS rows.",
    "- \"building numbers\" -> BUILDINGS.BUILDING_NUMBER: unique per row in its own table; the hinted joins do not multiply BUILDINGS rows.",
    "- \"building height\" -> FAC_BUILDING.BUILDING_HEIGHT: already repeats ~2.2 rows per value in its own table; the hinted joins multiply FAC_BUILDING rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct building height, use COUNT(DISTINCT FAC_BUILDING.BUILDING_HEIGHT).",
    "- \"street address\" -> BUILDINGS.BUILDING_STREET_ADDRESS: already repeats ~1.4 rows per value in its own table; the hinted joins do not multiply BUILDINGS rows.",
    "- \"city\" -> FAC_BUILDING_ADDRESS.CITY: already repeats ~71.4 rows per value in its own table; the hinted joins do not multiply FAC_BUILDING_ADDRESS rows.",
    "- \"state\" -> FAC_BUILDING_ADDRESS.STATE: already repeats ~392.5 rows per value in its own table; the hinted joins do not multiply FAC_BUILDING_ADDRESS rows.",
    "- \"HR department name\" -> FAC_ORGANIZATION.HR_DEPARTMENT_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FAC_ORGANIZATION rows ~x1111.1, so a plain COUNT over the joined result counts duplicates. To count distinct HR department name, use COUNT(DISTINCT FAC_ORGANIZATION.HR_DEPARTMENT_NAME).",
    "- \"assignable square footage\" -> BUILDINGS.BLDG_ASSIGNABLE_SQUARE_FOOTAGE is a numeric measure of BUILDINGS; the hinted joins do not multiply BUILDINGS rows.",
    "- \"square footage\" -> FAC_ROOMS.AREA is a numeric measure of FAC_ROOMS; the hinted joins do not multiply FAC_ROOMS rows."
  ],
  "dw_real_32": [
    "- Join ACADEMIC_TERMS.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS row appears ~105.3 times after this join.",
    "- \"2023 Fall term\" -> COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: already repeats ~105.3 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"term descriptions\" -> ACADEMIC_TERMS.TERM_DESCRIPTION: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct term descriptions, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_DESCRIPTION).",
    "- \"subject titles\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"prerequisites\" -> COURSE_CATALOG_SUBJECT_OFFERED.PREREQUISITES: already repeats ~5.2 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"term code\" -> COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: already repeats ~105.3 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"instructor\" -> COURSE_CATALOG_SUBJECT_OFFERED.FALL_INSTRUCTORS: already repeats ~2.9 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"courses\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows."
  ],
  "dw_real_33": [
    "- Join FAC_BUILDING.FAC_BUILDING_KEY = FAC_ROOMS.BUILDING_KEY: FAC_BUILDING.FAC_BUILDING_KEY is unique per row (242 rows); FAC_ROOMS.BUILDING_KEY repeats ~53.2 rows per value (10000 rows, 188 values).",
    "  -> each FAC_BUILDING row appears ~53.2 times after this join.",
    "- Join FAC_FLOOR.FLOOR_KEY = FAC_ROOMS.FLOOR_KEY: FAC_FLOOR.FLOOR_KEY is unique per row (1079 rows); FAC_ROOMS.FLOOR_KEY repeats ~12.1 rows per value (10000 rows, 827 values).",
    "  -> each FAC_FLOOR row appears ~12.1 times after this join.",
    "- Join FAC_ORGANIZATION.ORGANIZATION_KEY = FAC_ROOMS.ORGANIZATION_KEY: FAC_ORGANIZATION.ORGANIZATION_KEY is unique per row (169 rows); FAC_ROOMS.ORGANIZATION_KEY repeats ~1111.1 rows per value (10000 rows, 9 values).",
    "  -> each FAC_ORGANIZATION row appears ~1111.1 times after this join.",
    "- \"full name of the rooms\" -> FAC_ROOMS.ROOM_FULL_NAME: already repeats ~10000.0 rows per value in its own table; the hinted joins do not multiply FAC_ROOMS rows.",
    "- \"building names\" -> FAC_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins multiply FAC_BUILDING rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct building names, use COUNT(DISTINCT FAC_BUILDING.BUILDING_NAME).",
    "- \"floor numbers\" -> FAC_FLOOR.FLOOR: already repeats ~21.2 rows per value in its own table; the hinted joins multiply FAC_FLOOR rows ~x12.1, so a plain COUNT over the joined result counts duplicates. To count distinct floor numbers, use COUNT(DISTINCT FAC_FLOOR.FLOOR).",
    "- \"organizations\" -> FAC_ORGANIZATION.ORGANIZATION_NAME: unique per row in its own table; the hinted joins multiply FAC_ORGANIZATION rows ~x1111.1, so a plain COUNT over the joined result counts duplicates. To count distinct organizations, use COUNT(DISTINCT FAC_ORGANIZATION.ORGANIZATION_NAME).",
    "- \"name of the departments\" -> FAC_ORGANIZATION.HR_DEPARTMENT_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FAC_ORGANIZATION rows ~x1111.1, so a plain COUNT over the joined result counts duplicates. To count distinct name of the departments, use COUNT(DISTINCT FAC_ORGANIZATION.HR_DEPARTMENT_NAME).",
    "- \"room area\" -> FAC_ROOMS.AREA is a numeric measure of FAC_ROOMS; the hinted joins do not multiply FAC_ROOMS rows.",
    "- \"assignable floor area\" -> FAC_FLOOR.ASSIGNABLE_AREA is a numeric measure of FAC_FLOOR; the hinted joins multiply FAC_FLOOR rows ~x12.1, so SUM/AVG over the joined result would count each FAC_FLOOR row ~12.1 times. Aggregate it at FAC_FLOOR's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_real_34": [
    "- \"full name\" -> FAC_ROOMS.ROOM_FULL_NAME: already repeats ~10000.0 rows per value in its own table; the hinted joins do not multiply FAC_ROOMS rows.",
    "- \"building names\" -> FAC_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins do not multiply FAC_BUILDING rows.",
    "- \"floor numbers\" -> FAC_FLOOR.FLOOR: already repeats ~21.2 rows per value in its own table; the hinted joins do not multiply FAC_FLOOR rows.",
    "- \"organizations\" -> FAC_ORGANIZATION.ORGANIZATION_NAME: unique per row in its own table; the hinted joins do not multiply FAC_ORGANIZATION rows.",
    "- \"departments\" -> FAC_ORGANIZATION.HR_DEPARTMENT_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply FAC_ORGANIZATION rows.",
    "- \"room area\" -> FAC_ROOMS.AREA is a numeric measure of FAC_ROOMS; the hinted joins do not multiply FAC_ROOMS rows.",
    "- \"floor area\" -> FAC_ROOMS.AREA is a numeric measure of FAC_ROOMS; the hinted joins do not multiply FAC_ROOMS rows.",
    "- \"floor area\" -> FAC_ROOMS.FLOOR_KEY: already repeats ~12.1 rows per value in its own table; the hinted joins do not multiply FAC_ROOMS rows."
  ],
  "dw_real_35": [
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); SUBJECT_OFFERED.TERM_CODE repeats ~370.4 rows per value (10000 rows, 27 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~370.4 times after this join.",
    "- Join EMPLOYEE_DIRECTORY.MIT_ID = SUBJECT_OFFERED.RESPONSIBLE_FACULTY_MIT_ID: EMPLOYEE_DIRECTORY.MIT_ID is unique per row (10000 rows); SUBJECT_OFFERED.RESPONSIBLE_FACULTY_MIT_ID repeats ~10000.0 rows per value (10000 rows, 0 values).",
    "  -> each EMPLOYEE_DIRECTORY row appears ~10000.0 times after this join.",
    "- \"academic year\" -> ACADEMIC_TERMS_ALL.ACADEMIC_YEAR: already repeats ~3.8 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x370.4, so a plain COUNT over the joined result counts duplicates. To count distinct academic year, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.ACADEMIC_YEAR).",
    "- \"instructors names\" -> EMPLOYEE_DIRECTORY.FULL_NAME: unique per row in its own table; the hinted joins multiply EMPLOYEE_DIRECTORY rows ~x10000.0, so a plain COUNT over the joined result counts duplicates. To count distinct instructors names, use COUNT(DISTINCT EMPLOYEE_DIRECTORY.FULL_NAME).",
    "- \"courses\" -> SUBJECT_OFFERED.COURSE_NUMBER: already repeats ~10000.0 rows per value in its own table; the hinted joins do not multiply SUBJECT_OFFERED rows."
  ],
  "dw_real_36": [
    "- Join FAC_BUILDING.FAC_BUILDING_KEY = FAC_ROOMS.BUILDING_KEY: FAC_BUILDING.FAC_BUILDING_KEY is unique per row (242 rows); FAC_ROOMS.BUILDING_KEY repeats ~53.2 rows per value (10000 rows, 188 values).",
    "  -> each FAC_BUILDING row appears ~53.2 times after this join.",
    "- Join FAC_FLOOR.FLOOR_KEY = FAC_ROOMS.FLOOR_KEY: FAC_FLOOR.FLOOR_KEY is unique per row (1079 rows); FAC_ROOMS.FLOOR_KEY repeats ~12.1 rows per value (10000 rows, 827 values).",
    "  -> each FAC_FLOOR row appears ~12.1 times after this join.",
    "- Join FAC_ORGANIZATION.ORGANIZATION_KEY = FAC_ROOMS.ORGANIZATION_KEY: FAC_ORGANIZATION.ORGANIZATION_KEY is unique per row (169 rows); FAC_ROOMS.ORGANIZATION_KEY repeats ~1111.1 rows per value (10000 rows, 9 values).",
    "  -> each FAC_ORGANIZATION row appears ~1111.1 times after this join.",
    "- \"building names\" -> FAC_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins multiply FAC_BUILDING rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct building names, use COUNT(DISTINCT FAC_BUILDING.BUILDING_NAME).",
    "- \"department names\" -> FAC_ORGANIZATION.HR_DEPARTMENT_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FAC_ORGANIZATION rows ~x1111.1, so a plain COUNT over the joined result counts duplicates. To count distinct department names, use COUNT(DISTINCT FAC_ORGANIZATION.HR_DEPARTMENT_NAME).",
    "- \"organizations\" -> FAC_ORGANIZATION.ORGANIZATION: unique per row in its own table; the hinted joins multiply FAC_ORGANIZATION rows ~x1111.1, so a plain COUNT over the joined result counts duplicates. To count distinct organizations, use COUNT(DISTINCT FAC_ORGANIZATION.ORGANIZATION).",
    "- \"floor number\" -> FAC_FLOOR.LEVEL_ID: already repeats ~36.0 rows per value in its own table; the hinted joins multiply FAC_FLOOR rows ~x12.1, so a plain COUNT over the joined result counts duplicates. To count distinct floor number, use COUNT(DISTINCT FAC_FLOOR.LEVEL_ID).",
    "- \"room\" -> FAC_ROOMS.FAC_ROOM_KEY: unique per row in its own table; the hinted joins do not multiply FAC_ROOMS rows.",
    "- \"building key\" -> FAC_BUILDING.FAC_BUILDING_KEY: unique per row in its own table; the hinted joins multiply FAC_BUILDING rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct building key, use COUNT(DISTINCT FAC_BUILDING.FAC_BUILDING_KEY)."
  ],
  "dw_real_37": [
    "- Join MIT_STUDENT_DIRECTORY.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: MIT_STUDENT_DIRECTORY.DEPARTMENT repeats ~204.1 rows per value (10000 rows, 49 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~204.1 times after this join.",
    "- Join SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE is unique per row (179 rows); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "- \"first name\" -> MIT_STUDENT_DIRECTORY.FIRST_NAME: already repeats ~27.5 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"full names\" -> MIT_STUDENT_DIRECTORY.FULL_NAME: already repeats ~1.2 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"email addresses\" -> MIT_STUDENT_DIRECTORY.EMAIL_ADDRESS: already repeats ~3.0 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"department names\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct department names, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"department phone numbers\" -> SIS_ADMIN_DEPARTMENT.DEPARTMENT_PHONE_NUMBER: already repeats ~2.8 rows per value in its own table; the hinted joins do not multiply SIS_ADMIN_DEPARTMENT rows.",
    "- \"school names\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct school names, use COUNT(DISTINCT SIS_DEPARTMENT.SCHOOL_NAME).",
    "- \"student\" -> MIT_STUDENT_DIRECTORY.EMAIL_ADDRESS: already repeats ~3.0 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"department\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"school\" -> SIS_DEPARTMENT.SCHOOL_CODE: already repeats ~12.8 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct school, use COUNT(DISTINCT SIS_DEPARTMENT.SCHOOL_CODE)."
  ],
  "dw_real_38": [
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_MIT_ID = EMPLOYEE_DIRECTORY.MIT_ID: COURSE_CATALOG_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_MIT_ID repeats ~4.1 rows per value (10000 rows, 2418 values); EMPLOYEE_DIRECTORY.MIT_ID is unique per row (10000 rows).",
    "  -> each EMPLOYEE_DIRECTORY row appears ~4.1 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~105.3 times after this join.",
    "- \"titles\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"fall term\" -> ACADEMIC_TERMS_ALL.TERM_CODE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct fall term, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_CODE).",
    "- \"instructor names\" -> EMPLOYEE_DIRECTORY.FULL_NAME: unique per row in its own table; the hinted joins multiply EMPLOYEE_DIRECTORY rows ~x4.1, so a plain COUNT over the joined result counts duplicates. To count distinct instructor names, use COUNT(DISTINCT EMPLOYEE_DIRECTORY.FULL_NAME).",
    "- \"instructor emails\" -> EMPLOYEE_DIRECTORY.EMAIL_ADDRESS: already repeats ~1.5 rows per value in its own table; the hinted joins multiply EMPLOYEE_DIRECTORY rows ~x4.1, so a plain COUNT over the joined result counts duplicates. To count distinct instructor emails, use COUNT(DISTINCT EMPLOYEE_DIRECTORY.EMAIL_ADDRESS)."
  ],
  "dw_real_39": [
    "- Join BUILDINGS.BUILDING_KEY = FAC_BUILDING.FAC_BUILDING_KEY: BUILDINGS.BUILDING_KEY is unique per row (211 rows); FAC_BUILDING.FAC_BUILDING_KEY is unique per row (242 rows).",
    "- Join FAC_BUILDING.FAC_BUILDING_KEY = FAC_ROOMS.BUILDING_KEY: FAC_BUILDING.FAC_BUILDING_KEY is unique per row (242 rows); FAC_ROOMS.BUILDING_KEY repeats ~53.2 rows per value (10000 rows, 188 values).",
    "  -> each FAC_BUILDING row appears ~53.2 times after this join.",
    "- Join FAC_ORGANIZATION.ORGANIZATION_KEY = FAC_ROOMS.ORGANIZATION_KEY: FAC_ORGANIZATION.ORGANIZATION_KEY is unique per row (169 rows); FAC_ROOMS.ORGANIZATION_KEY repeats ~1111.1 rows per value (10000 rows, 9 values).",
    "  -> each FAC_ORGANIZATION row appears ~1111.1 times after this join.",
    "- \"building names\" -> BUILDINGS.BUILDING_NAME: unique per row in its own table; the hinted joins do not multiply BUILDINGS rows.",
    "- \"names of HR departments\" -> FAC_ORGANIZATION.HR_DEPARTMENT_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FAC_ORGANIZATION rows ~x1111.1, so a plain COUNT over the joined result counts duplicates. To count distinct names of HR departments, use COUNT(DISTINCT FAC_ORGANIZATION.HR_DEPARTMENT_NAME).",
    "- \"gross square footage\" -> BUILDINGS.BLDG_GROSS_SQUARE_FOOTAGE is a numeric measure of BUILDINGS; the hinted joins do not multiply BUILDINGS rows.",
    "- \"assignable square footage\" -> BUILDINGS.BLDG_ASSIGNABLE_SQUARE_FOOTAGE is a numeric measure of BUILDINGS; the hinted joins do not multiply BUILDINGS rows.",
    "- \"built year\" -> FAC_BUILDING.DATE_BUILT: already repeats ~2.2 rows per value in its own table; the hinted joins multiply FAC_BUILDING rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct built year, use COUNT(DISTINCT FAC_BUILDING.DATE_BUILT).",
    "- \"building key\" -> BUILDINGS.BUILDING_KEY: unique per row in its own table; the hinted joins do not multiply BUILDINGS rows."
  ],
  "dw_real_4": [
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.MEET_PLACE = FCLT_ROOMS.FCLT_ROOM_KEY: COURSE_CATALOG_SUBJECT_OFFERED.MEET_PLACE repeats ~18.1 rows per value (10000 rows, 553 values); FCLT_ROOMS.FCLT_ROOM_KEY is unique per row (10000 rows).",
    "  -> each FCLT_ROOMS row appears ~18.1 times after this join.",
    "- Join FCLT_BUILDING_ADDRESS.FCLT_BUILDING_KEY = FCLT_ROOMS.FCLT_BUILDING_KEY: FCLT_BUILDING_ADDRESS.FCLT_BUILDING_KEY repeats ~3.2 rows per value (785 rows, 242 values); FCLT_ROOMS.FCLT_BUILDING_KEY repeats ~49.5 rows per value (10000 rows, 202 values).",
    "  -> each FCLT_BUILDING_ADDRESS row appears ~49.5 times after this join.",
    "  -> each FCLT_ROOMS row appears ~3.2 times after this join.",
    "- Join FCLT_BUILDING.FCLT_BUILDING_KEY = FCLT_ROOMS.FCLT_BUILDING_KEY: FCLT_BUILDING.FCLT_BUILDING_KEY is unique per row (242 rows); FCLT_ROOMS.FCLT_BUILDING_KEY repeats ~49.5 rows per value (10000 rows, 202 values).",
    "  -> each FCLT_BUILDING row appears ~49.5 times after this join.",
    "- Join BUILDINGS.BUILDING_KEY = FCLT_ROOMS.FCLT_BUILDING_KEY: BUILDINGS.BUILDING_KEY is unique per row (211 rows); FCLT_ROOMS.FCLT_BUILDING_KEY repeats ~49.5 rows per value (10000 rows, 202 values).",
    "  -> each BUILDINGS row appears ~49.5 times after this join.",
    "- \"full room names\" -> FCLT_ROOMS.ROOM_FULL_NAME: already repeats ~625.0 rows per value in its own table; the hinted joins multiply FCLT_ROOMS rows ~x18.1, so a plain COUNT over the joined result counts duplicates. To count distinct full room names, use COUNT(DISTINCT FCLT_ROOMS.ROOM_FULL_NAME).",
    "- \"building names\" -> BUILDINGS.BUILDING_NAME: unique per row in its own table; the hinted joins multiply BUILDINGS rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct building names, use COUNT(DISTINCT BUILDINGS.BUILDING_NAME).",
    "- \"street addresses\" -> BUILDINGS.BUILDING_STREET_ADDRESS: already repeats ~1.4 rows per value in its own table; the hinted joins multiply BUILDINGS rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct street addresses, use COUNT(DISTINCT BUILDINGS.BUILDING_STREET_ADDRESS).",
    "- \"street addresses\" -> FCLT_BUILDING_ADDRESS.ADDRESS_PURPOSE: already repeats ~60.4 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_ADDRESS rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct street addresses, use COUNT(DISTINCT FCLT_BUILDING_ADDRESS.ADDRESS_PURPOSE).",
    "- \"cities\" -> FCLT_BUILDING_ADDRESS.CITY: already repeats ~71.4 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_ADDRESS rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct cities, use COUNT(DISTINCT FCLT_BUILDING_ADDRESS.CITY).",
    "- \"states\" -> FCLT_BUILDING_ADDRESS.STATE: already repeats ~392.5 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_ADDRESS rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct states, use COUNT(DISTINCT FCLT_BUILDING_ADDRESS.STATE).",
    "- \"postal codes\" -> FCLT_BUILDING_ADDRESS.POSTAL_CODE: already repeats ~56.1 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_ADDRESS rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct postal codes, use COUNT(DISTINCT FCLT_BUILDING_ADDRESS.POSTAL_CODE).",
    "- \"building heights\" -> FCLT_BUILDING.BUILDING_HEIGHT: already repeats ~2.2 rows per value in its own table; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct building heights, use COUNT(DISTINCT FCLT_BUILDING.BUILDING_HEIGHT).",
    "- \"Computer Science\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows."
  ],
  "dw_real_40": [
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_MIT_ID = EMPLOYEE_DIRECTORY.MIT_ID: COURSE_CATALOG_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_MIT_ID repeats ~4.1 rows per value (10000 rows, 2418 values); EMPLOYEE_DIRECTORY.MIT_ID is unique per row (10000 rows).",
    "  -> each EMPLOYEE_DIRECTORY row appears ~4.1 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS row appears ~105.3 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.MEET_PLACE = FAC_ROOMS.FAC_ROOM_KEY: COURSE_CATALOG_SUBJECT_OFFERED.MEET_PLACE repeats ~18.1 rows per value (10000 rows, 553 values); FAC_ROOMS.FAC_ROOM_KEY is unique per row (10000 rows).",
    "  -> each FAC_ROOMS row appears ~18.1 times after this join.",
    "- Join FAC_FLOOR.FLOOR_KEY = FAC_ROOMS.FLOOR_KEY: FAC_FLOOR.FLOOR_KEY is unique per row (1079 rows); FAC_ROOMS.FLOOR_KEY repeats ~12.1 rows per value (10000 rows, 827 values).",
    "  -> each FAC_FLOOR row appears ~12.1 times after this join.",
    "- Join BUILDINGS.BUILDING_KEY = FAC_ROOMS.BUILDING_KEY: BUILDINGS.BUILDING_KEY is unique per row (211 rows); FAC_ROOMS.BUILDING_KEY repeats ~53.2 rows per value (10000 rows, 188 values).",
    "  -> each BUILDINGS row appears ~53.2 times after this join.",
    "- Join FAC_BUILDING_ADDRESS.BUILDING_KEY = FAC_ROOMS.BUILDING_KEY: FAC_BUILDING_ADDRESS.BUILDING_KEY repeats ~3.2 rows per value (785 rows, 242 values); FAC_ROOMS.BUILDING_KEY repeats ~53.2 rows per value (10000 rows, 188 values).",
    "  -> each FAC_BUILDING_ADDRESS row appears ~53.2 times after this join.",
    "  -> each FAC_ROOMS row appears ~3.2 times after this join.",
    "- \"titles of subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"summer term\" -> ACADEMIC_TERMS.TERM_CODE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct summer term, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_CODE).",
    "- \"descriptions\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_DESCRIPTION: already repeats ~2.3 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"responsible faculty names\" -> COURSE_CATALOG_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_NAME: already repeats ~4.2 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"email address\" -> EMPLOYEE_DIRECTORY.EMAIL_ADDRESS: already repeats ~1.5 rows per value in its own table; the hinted joins multiply EMPLOYEE_DIRECTORY rows ~x4.1, so a plain COUNT over the joined result counts duplicates. To count distinct email address, use COUNT(DISTINCT EMPLOYEE_DIRECTORY.EMAIL_ADDRESS).",
    "- \"building name\" -> BUILDINGS.BUILDING_NAME: unique per row in its own table; the hinted joins multiply BUILDINGS rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct building name, use COUNT(DISTINCT BUILDINGS.BUILDING_NAME).",
    "- \"room name\" -> FAC_ROOMS.ROOM_FULL_NAME: already repeats ~10000.0 rows per value in its own table; the hinted joins multiply FAC_ROOMS rows ~x18.1, so a plain COUNT over the joined result counts duplicates. To count distinct room name, use COUNT(DISTINCT FAC_ROOMS.ROOM_FULL_NAME).",
    "- \"floor level\" -> FAC_FLOOR.LEVEL_ID: already repeats ~36.0 rows per value in its own table; the hinted joins multiply FAC_FLOOR rows ~x12.1, so a plain COUNT over the joined result counts duplicates. To count distinct floor level, use COUNT(DISTINCT FAC_FLOOR.LEVEL_ID).",
    "- \"building street address\" -> BUILDINGS.BUILDING_STREET_ADDRESS: already repeats ~1.4 rows per value in its own table; the hinted joins multiply BUILDINGS rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct building street address, use COUNT(DISTINCT BUILDINGS.BUILDING_STREET_ADDRESS).",
    "- \"building street address\" -> FAC_BUILDING_ADDRESS.ADDRESS_PURPOSE: already repeats ~60.4 rows per value in its own table; the hinted joins multiply FAC_BUILDING_ADDRESS rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct building street address, use COUNT(DISTINCT FAC_BUILDING_ADDRESS.ADDRESS_PURPOSE).",
    "- \"courses\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"department\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE: already repeats ~166.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows."
  ],
  "dw_real_41": [
    "- Join CIS_HASS_ATTRIBUTE.HASS_ATTRIBUTE = COURSE_CATALOG_SUBJECT_OFFERED.HASS_ATTRIBUTE: CIS_HASS_ATTRIBUTE.HASS_ATTRIBUTE repeats ~1.1 rows per value (17 rows, 16 values); COURSE_CATALOG_SUBJECT_OFFERED.HASS_ATTRIBUTE repeats ~2000.0 rows per value (10000 rows, 5 values).",
    "  -> each CIS_HASS_ATTRIBUTE row appears ~2000.0 times after this join.",
    "  -> each COURSE_CATALOG_SUBJECT_OFFERED row appears ~1.1 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE repeats ~166.7 rows per value (10000 rows, 60 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~166.7 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~105.3 times after this join.",
    "- \"term code\" -> ACADEMIC_TERMS_ALL.TERM_CODE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct term code, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_CODE).",
    "- \"term description\" -> ACADEMIC_TERMS_ALL.TERM_DESCRIPTION: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct term description, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_DESCRIPTION).",
    "- \"attribute desciption\" -> COURSE_CATALOG_SUBJECT_OFFERED.HASS_ATTRIBUTE_DESC: already repeats ~2000.0 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct attribute desciption, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.HASS_ATTRIBUTE_DESC).",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"school name\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_DEPARTMENT.SCHOOL_NAME).",
    "- \"subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct subjects, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID).",
    "- \"area of humanities, arts and social studies\" -> CIS_HASS_ATTRIBUTE.CIS_ATTRIBUTE_GROUP: already repeats ~8.5 rows per value in its own table; the hinted joins multiply CIS_HASS_ATTRIBUTE rows ~x2000.0, so a plain COUNT over the joined result counts duplicates. To count distinct area of humanities, arts and social studies, use COUNT(DISTINCT CIS_HASS_ATTRIBUTE.CIS_ATTRIBUTE_GROUP)."
  ],
  "dw_real_42": [
    "- Join FAC_BUILDING.FAC_BUILDING_KEY = FAC_BUILDING_ADDRESS.BUILDING_KEY: FAC_BUILDING.FAC_BUILDING_KEY is unique per row (242 rows); FAC_BUILDING_ADDRESS.BUILDING_KEY repeats ~3.2 rows per value (785 rows, 242 values).",
    "  -> each FAC_BUILDING row appears ~3.2 times after this join.",
    "- Join FAC_BUILDING.FAC_BUILDING_KEY = FAC_FLOOR.BUILDING_KEY: FAC_BUILDING.FAC_BUILDING_KEY is unique per row (242 rows); FAC_FLOOR.BUILDING_KEY repeats ~4.5 rows per value (1079 rows, 239 values).",
    "  -> each FAC_BUILDING row appears ~4.5 times after this join.",
    "- Join FAC_FLOOR.FLOOR_KEY = FAC_ROOMS.FLOOR_KEY: FAC_FLOOR.FLOOR_KEY is unique per row (1079 rows); FAC_ROOMS.FLOOR_KEY repeats ~12.1 rows per value (10000 rows, 827 values).",
    "  -> each FAC_FLOOR row appears ~12.1 times after this join.",
    "- Join BUILDINGS.BUILDING_KEY = FAC_BUILDING.FAC_BUILDING_KEY: BUILDINGS.BUILDING_KEY is unique per row (211 rows); FAC_BUILDING.FAC_BUILDING_KEY is unique per row (242 rows).",
    "- \"building names\" -> FAC_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins multiply FAC_BUILDING rows ~x4.5, so a plain COUNT over the joined result counts duplicates. To count distinct building names, use COUNT(DISTINCT FAC_BUILDING.BUILDING_NAME).",
    "- \"height\" -> FAC_BUILDING.BUILDING_HEIGHT: already repeats ~2.2 rows per value in its own table; the hinted joins multiply FAC_BUILDING rows ~x4.5, so a plain COUNT over the joined result counts duplicates. To count distinct height, use COUNT(DISTINCT FAC_BUILDING.BUILDING_HEIGHT).",
    "- \"street address\" -> BUILDINGS.BUILDING_STREET_ADDRESS: already repeats ~1.4 rows per value in its own table; the hinted joins do not multiply BUILDINGS rows.",
    "- \"street address\" -> FAC_BUILDING_ADDRESS.ADDRESS_PURPOSE: already repeats ~60.4 rows per value in its own table; the hinted joins do not multiply FAC_BUILDING_ADDRESS rows.",
    "- \"city\" -> FAC_BUILDING_ADDRESS.CITY: already repeats ~71.4 rows per value in its own table; the hinted joins do not multiply FAC_BUILDING_ADDRESS rows.",
    "- \"state\" -> FAC_BUILDING_ADDRESS.STATE: already repeats ~392.5 rows per value in its own table; the hinted joins do not multiply FAC_BUILDING_ADDRESS rows.",
    "- \"postal code\" -> FAC_BUILDING_ADDRESS.POSTAL_CODE: already repeats ~56.1 rows per value in its own table; the hinted joins do not multiply FAC_BUILDING_ADDRESS rows.",
    "- \"gross square footage\" -> FAC_BUILDING.EXT_GROSS_AREA is a numeric measure of FAC_BUILDING; the hinted joins multiply FAC_BUILDING rows ~x4.5, so SUM/AVG over the joined result would count each FAC_BUILDING row ~4.5 times. Aggregate it at FAC_BUILDING's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"assignable square footage\" -> FAC_BUILDING.ASSIGNABLE_AREA is a numeric measure of FAC_BUILDING; the hinted joins multiply FAC_BUILDING rows ~x4.5, so SUM/AVG over the joined result would count each FAC_BUILDING row ~4.5 times. Aggregate it at FAC_BUILDING's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"floor level\" -> FAC_FLOOR.LEVEL_ID: already repeats ~36.0 rows per value in its own table; the hinted joins multiply FAC_FLOOR rows ~x12.1, so a plain COUNT over the joined result counts duplicates. To count distinct floor level, use COUNT(DISTINCT FAC_FLOOR.LEVEL_ID).",
    "- \"area\" -> FAC_ROOMS.AREA is a numeric measure of FAC_ROOMS; the hinted joins do not multiply FAC_ROOMS rows.",
    "- \"building key\" -> FAC_BUILDING.FAC_BUILDING_KEY: unique per row in its own table; the hinted joins multiply FAC_BUILDING rows ~x4.5, so a plain COUNT over the joined result counts duplicates. To count distinct building key, use COUNT(DISTINCT FAC_BUILDING.FAC_BUILDING_KEY)."
  ],
  "dw_real_43": [
    "- Join CIP.PROGRAM_CODE = SIS_COURSE_DESCRIPTION.CIP_PROGRAM_CODE: CIP.PROGRAM_CODE is unique per row (3059 rows); SIS_COURSE_DESCRIPTION.CIP_PROGRAM_CODE repeats ~10.9 rows per value (695 rows, 64 values).",
    "  -> each CIP row appears ~10.9 times after this join.",
    "- Join SIS_COURSE_DESCRIPTION.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_COURSE_DESCRIPTION.DEPARTMENT repeats ~9.7 rows per value (695 rows, 72 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~9.7 times after this join.",
    "- \"category title\" -> CIP.CATEGORY_TITLE: already repeats ~35.6 rows per value in its own table; the hinted joins multiply CIP rows ~x10.9, so a plain COUNT over the joined result counts duplicates. To count distinct category title, use COUNT(DISTINCT CIP.CATEGORY_TITLE).",
    "- \"version\" -> CIP.VERSION: already repeats ~764.8 rows per value in its own table; the hinted joins multiply CIP rows ~x10.9, so a plain COUNT over the joined result counts duplicates. To count distinct version, use COUNT(DISTINCT CIP.VERSION).",
    "- \"department name\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"school name\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_DEPARTMENT.SCHOOL_NAME).",
    "- \"course level\" -> SIS_COURSE_DESCRIPTION.COURSE_LEVEL: already repeats ~347.5 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"degree-granting courses\" -> SIS_COURSE_DESCRIPTION.IS_DEGREE_GRANTING: already repeats ~347.5 rows per value in its own table; the hinted joins do not multiply SIS_COURSE_DESCRIPTION rows.",
    "- \"CIP category code\" -> CIP.CATEGORY_CODE: already repeats ~58.8 rows per value in its own table; the hinted joins multiply CIP rows ~x10.9, so a plain COUNT over the joined result counts duplicates. To count distinct CIP category code, use COUNT(DISTINCT CIP.CATEGORY_CODE)."
  ],
  "dw_real_44": [
    "- Join LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY: LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY repeats ~6.2 rows per value (10000 rows, 1614 values).",
    "  -> each LIBRARY_COURSE_INSTRUCTOR row appears ~6.2 times after this join.",
    "- Join LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY repeats ~1.2 rows per value (10000 rows, 8268 values).",
    "  -> each LIBRARY_RESERVE_CATALOG row appears ~1.2 times after this join.",
    "- Join LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY: LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS_KEY is unique per row (6 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY repeats ~2000.0 rows per value (10000 rows, 5 values).",
    "  -> each LIBRARY_MATERIAL_STATUS row appears ~2000.0 times after this join.",
    "- \"library materials\" -> LIBRARY_RESERVE_CATALOG.CATALOG_SYSTEM_NUMBER: unique per row in its own table; the hinted joins multiply LIBRARY_RESERVE_CATALOG rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct library materials, use COUNT(DISTINCT LIBRARY_RESERVE_CATALOG.CATALOG_SYSTEM_NUMBER).",
    "- \"publication years\" -> LIBRARY_RESERVE_CATALOG.CATALOG_YEAR: already repeats ~111.1 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_CATALOG rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct publication years, use COUNT(DISTINCT LIBRARY_RESERVE_CATALOG.CATALOG_YEAR).",
    "- \"materials status\" -> LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS: already repeats ~1.2 rows per value in its own table; the hinted joins multiply LIBRARY_MATERIAL_STATUS rows ~x2000.0, so a plain COUNT over the joined result counts duplicates. To count distinct materials status, use COUNT(DISTINCT LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS).",
    "- \"course name\" -> LIBRARY_COURSE_INSTRUCTOR.COURSE_NAME: already repeats ~3.3 rows per value in its own table; the hinted joins multiply LIBRARY_COURSE_INSTRUCTOR rows ~x6.2, so a plain COUNT over the joined result counts duplicates. To count distinct course name, use COUNT(DISTINCT LIBRARY_COURSE_INSTRUCTOR.COURSE_NAME)."
  ],
  "dw_real_45": [
    "- Join LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY: LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY repeats ~6.2 rows per value (10000 rows, 1614 values).",
    "  -> each LIBRARY_COURSE_INSTRUCTOR row appears ~6.2 times after this join.",
    "- Join LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY repeats ~1.2 rows per value (10000 rows, 8268 values).",
    "  -> each LIBRARY_RESERVE_CATALOG row appears ~1.2 times after this join.",
    "- Join LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY = LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY: LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY repeats ~6.5 rows per value (10000 rows, 1550 values); LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY is unique per row (10000 rows).",
    "  -> each LIBRARY_SUBJECT_OFFERED row appears ~6.5 times after this join.",
    "- \"library reserve materials\" -> LIBRARY_RESERVE_CATALOG.CATALOG_SYSTEM_NUMBER: unique per row in its own table; the hinted joins multiply LIBRARY_RESERVE_CATALOG rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct library reserve materials, use COUNT(DISTINCT LIBRARY_RESERVE_CATALOG.CATALOG_SYSTEM_NUMBER).",
    "- \"publication years\" -> LIBRARY_RESERVE_CATALOG.CATALOG_YEAR: already repeats ~111.1 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_CATALOG rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct publication years, use COUNT(DISTINCT LIBRARY_RESERVE_CATALOG.CATALOG_YEAR).",
    "- \"enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so SUM/AVG over the joined result would count each LIBRARY_SUBJECT_OFFERED row ~6.5 times. Aggregate it at LIBRARY_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"course instructor\" -> LIBRARY_COURSE_INSTRUCTOR.INSTRUCTOR_NAME: unique per row in its own table; the hinted joins multiply LIBRARY_COURSE_INSTRUCTOR rows ~x6.2, so a plain COUNT over the joined result counts duplicates. To count distinct course instructor, use COUNT(DISTINCT LIBRARY_COURSE_INSTRUCTOR.INSTRUCTOR_NAME)."
  ],
  "dw_real_46": [
    "- Join LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY: LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS_KEY is unique per row (6 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY repeats ~2000.0 rows per value (10000 rows, 5 values).",
    "  -> each LIBRARY_MATERIAL_STATUS row appears ~2000.0 times after this join.",
    "- Join LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY = LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY: LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY repeats ~6.5 rows per value (10000 rows, 1550 values); LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY is unique per row (10000 rows).",
    "  -> each LIBRARY_SUBJECT_OFFERED row appears ~6.5 times after this join.",
    "- Join LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY: LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY repeats ~6.2 rows per value (10000 rows, 1614 values).",
    "  -> each LIBRARY_COURSE_INSTRUCTOR row appears ~6.2 times after this join.",
    "- Join LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY repeats ~1.2 rows per value (10000 rows, 8268 values).",
    "  -> each LIBRARY_RESERVE_CATALOG row appears ~1.2 times after this join.",
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = LIBRARY_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); LIBRARY_SUBJECT_OFFERED.TERM_CODE repeats ~192.3 rows per value (10000 rows, 52 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~192.3 times after this join.",
    "- \"material status\" -> LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS: already repeats ~1.2 rows per value in its own table; the hinted joins multiply LIBRARY_MATERIAL_STATUS rows ~x2000.0, so a plain COUNT over the joined result counts duplicates. To count distinct material status, use COUNT(DISTINCT LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS).",
    "- \"term description\" -> ACADEMIC_TERMS_ALL.TERM_DESCRIPTION: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct term description, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_DESCRIPTION).",
    "- \"courses\" -> LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER: already repeats ~625.0 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so a plain COUNT over the joined result counts duplicates. To count distinct courses, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER).",
    "- \"materials\" -> LIBRARY_RESERVE_CATALOG.CATALOG_SYSTEM_NUMBER: unique per row in its own table; the hinted joins multiply LIBRARY_RESERVE_CATALOG rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct materials, use COUNT(DISTINCT LIBRARY_RESERVE_CATALOG.CATALOG_SYSTEM_NUMBER).",
    "- \"departments\" -> LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_CODE: already repeats ~625.0 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so a plain COUNT over the joined result counts duplicates. To count distinct departments, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_CODE).",
    "- \"school\" -> LIBRARY_SUBJECT_OFFERED.OFFER_SCHOOL_NAME: already repeats ~2000.0 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so a plain COUNT over the joined result counts duplicates. To count distinct school, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.OFFER_SCHOOL_NAME).",
    "- \"instructors\" -> LIBRARY_COURSE_INSTRUCTOR.INSTRUCTOR_NAME: unique per row in its own table; the hinted joins multiply LIBRARY_COURSE_INSTRUCTOR rows ~x6.2, so a plain COUNT over the joined result counts duplicates. To count distinct instructors, use COUNT(DISTINCT LIBRARY_COURSE_INSTRUCTOR.INSTRUCTOR_NAME).",
    "- \"library material status code\" -> LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS_CODE: unique per row in its own table; the hinted joins multiply LIBRARY_MATERIAL_STATUS rows ~x2000.0, so a plain COUNT over the joined result counts duplicates. To count distinct library material status code, use COUNT(DISTINCT LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS_CODE).",
    "- \"term code\" -> LIBRARY_SUBJECT_OFFERED.TERM_CODE: already repeats ~192.3 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so a plain COUNT over the joined result counts duplicates. To count distinct term code, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.TERM_CODE)."
  ],
  "dw_real_47": [
    "- Join SPACE_DETAIL.SPACE_UNIT_KEY = SPACE_UNIT.SPACE_UNIT_KEY: SPACE_DETAIL.SPACE_UNIT_KEY repeats ~151.5 rows per value (10000 rows, 66 values); SPACE_UNIT.SPACE_UNIT_KEY is unique per row (150 rows).",
    "  -> each SPACE_UNIT row appears ~151.5 times after this join.",
    "- Join SPACE_DETAIL.SPACE_USAGE_KEY = SPACE_USAGE.SPACE_USAGE_KEY: SPACE_DETAIL.SPACE_USAGE_KEY repeats ~163.9 rows per value (10000 rows, 61 values); SPACE_USAGE.SPACE_USAGE_KEY is unique per row (88 rows).",
    "  -> each SPACE_USAGE row appears ~163.9 times after this join.",
    "- Join SPACE_DETAIL.FLOOR_KEY = SPACE_FLOOR.FLOOR_KEY: SPACE_DETAIL.FLOOR_KEY repeats ~370.4 rows per value (10000 rows, 27 values); SPACE_FLOOR.FLOOR_KEY is unique per row (49 rows).",
    "  -> each SPACE_FLOOR row appears ~370.4 times after this join.",
    "- Join BUILDINGS.BUILDING_KEY = SPACE_DETAIL.BUILDING_KEY: BUILDINGS.BUILDING_KEY is unique per row (211 rows); SPACE_DETAIL.BUILDING_KEY repeats ~344.8 rows per value (10000 rows, 29 values).",
    "  -> each BUILDINGS row appears ~344.8 times after this join.",
    "- Join FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY = SPACE_UNIT.FCLT_ORGANIZATION_KEY: FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY repeats ~1.1 rows per value (180 rows, 168 values); SPACE_UNIT.FCLT_ORGANIZATION_KEY is unique per row (150 rows).",
    "  -> each SPACE_UNIT row appears ~1.1 times after this join.",
    "- Join BUILDINGS.BUILDING_KEY = FCLT_BUILDING_ADDRESS.FCLT_BUILDING_KEY: BUILDINGS.BUILDING_KEY is unique per row (211 rows); FCLT_BUILDING_ADDRESS.FCLT_BUILDING_KEY repeats ~3.2 rows per value (785 rows, 242 values).",
    "  -> each BUILDINGS row appears ~3.2 times after this join.",
    "- \"building 36\" -> BUILDINGS.BUILDING_NUMBER: unique per row in its own table; the hinted joins multiply BUILDINGS rows ~x344.8, so a plain COUNT over the joined result counts duplicates. To count distinct building 36, use COUNT(DISTINCT BUILDINGS.BUILDING_NUMBER).",
    "- \"space units\" -> SPACE_UNIT.SPACE_UNIT: already repeats ~1.1 rows per value in its own table; the hinted joins multiply SPACE_UNIT rows ~x151.5, so a plain COUNT over the joined result counts duplicates. To count distinct space units, use COUNT(DISTINCT SPACE_UNIT.SPACE_UNIT).",
    "- \"floor\" -> SPACE_FLOOR.FLOOR: unique per row in its own table; the hinted joins multiply SPACE_FLOOR rows ~x370.4, so a plain COUNT over the joined result counts duplicates. To count distinct floor, use COUNT(DISTINCT SPACE_FLOOR.FLOOR).",
    "- \"building name\" -> BUILDINGS.BUILDING_NAME: unique per row in its own table; the hinted joins multiply BUILDINGS rows ~x344.8, so a plain COUNT over the joined result counts duplicates. To count distinct building name, use COUNT(DISTINCT BUILDINGS.BUILDING_NAME).",
    "- \"building street address\" -> BUILDINGS.BUILDING_STREET_ADDRESS: already repeats ~1.4 rows per value in its own table; the hinted joins multiply BUILDINGS rows ~x344.8, so a plain COUNT over the joined result counts duplicates. To count distinct building street address, use COUNT(DISTINCT BUILDINGS.BUILDING_STREET_ADDRESS).",
    "- \"building street address\" -> FCLT_BUILDING_ADDRESS.ADDRESS_PURPOSE: already repeats ~60.4 rows per value in its own table; the hinted joins do not multiply FCLT_BUILDING_ADDRESS rows.",
    "- \"space usage\" -> SPACE_USAGE.SPACE_USAGE: already repeats ~1.8 rows per value in its own table; the hinted joins multiply SPACE_USAGE rows ~x163.9, so a plain COUNT over the joined result counts duplicates. To count distinct space usage, use COUNT(DISTINCT SPACE_USAGE.SPACE_USAGE).",
    "- \"organizations\" -> FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply FCLT_ORGANIZATION rows."
  ],
  "dw_real_48": [
    "- Join MOIRA_LIST.MOIRA_LIST_KEY = MOIRA_LIST_DETAIL.MOIRA_LIST_KEY: MOIRA_LIST.MOIRA_LIST_KEY repeats ~1.1 rows per value (10000 rows, 8815 values); MOIRA_LIST_DETAIL.MOIRA_LIST_KEY repeats ~1.6 rows per value (10000 rows, 6395 values).",
    "  -> each MOIRA_LIST row appears ~1.6 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "- Join MOIRA_LIST_DETAIL.MOIRA_LIST_OWNER_KEY = MOIRA_LIST_OWNER.MOIRA_LIST_OWNER_KEY: MOIRA_LIST_DETAIL.MOIRA_LIST_OWNER_KEY repeats ~2.5 rows per value (10000 rows, 3966 values); MOIRA_LIST_OWNER.MOIRA_LIST_OWNER_KEY repeats ~1.1 rows per value (10000 rows, 9042 values).",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "  -> each MOIRA_LIST_OWNER row appears ~2.5 times after this join.",
    "- \"name\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct name, use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_NAME).",
    "- \"mailing list\" -> MOIRA_LIST.IS_MOIRA_MAILING_LIST: already repeats ~5000.0 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct mailing list, use COUNT(DISTINCT MOIRA_LIST.IS_MOIRA_MAILING_LIST).",
    "- \"moira group\" -> MOIRA_LIST.IS_MOIRA_GROUP: already repeats ~5000.0 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct moira group, use COUNT(DISTINCT MOIRA_LIST.IS_MOIRA_GROUP).",
    "- \"NFS group\" -> MOIRA_LIST.IS_NFS_GROUP: already repeats ~5000.0 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct NFS group, use COUNT(DISTINCT MOIRA_LIST.IS_NFS_GROUP).",
    "- \"owner\" -> MOIRA_LIST_OWNER.OWNER: already repeats ~1.2 rows per value in its own table; the hinted joins multiply MOIRA_LIST_OWNER rows ~x2.5, so a plain COUNT over the joined result counts duplicates. To count distinct owner, use COUNT(DISTINCT MOIRA_LIST_OWNER.OWNER).",
    "- \"people\" -> MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER: already repeats ~1.5 rows per value in its own table; the hinted joins multiply MOIRA_LIST_DETAIL rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct people, use COUNT(DISTINCT MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER)."
  ],
  "dw_real_49": [
    "- Join MOIRA_LIST.MOIRA_LIST_KEY = MOIRA_LIST_DETAIL.MOIRA_LIST_KEY: MOIRA_LIST.MOIRA_LIST_KEY repeats ~1.1 rows per value (10000 rows, 8815 values); MOIRA_LIST_DETAIL.MOIRA_LIST_KEY repeats ~1.6 rows per value (10000 rows, 6395 values).",
    "  -> each MOIRA_LIST row appears ~1.6 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "- Join MOIRA_LIST_DETAIL.MOIRA_LIST_OWNER_KEY = MOIRA_LIST_OWNER.MOIRA_LIST_OWNER_KEY: MOIRA_LIST_DETAIL.MOIRA_LIST_OWNER_KEY repeats ~2.5 rows per value (10000 rows, 3966 values); MOIRA_LIST_OWNER.MOIRA_LIST_OWNER_KEY repeats ~1.1 rows per value (10000 rows, 9042 values).",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "  -> each MOIRA_LIST_OWNER row appears ~2.5 times after this join.",
    "- \"names\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct names, use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_NAME).",
    "- \"owners\" -> MOIRA_LIST_OWNER.OWNER: already repeats ~1.2 rows per value in its own table; the hinted joins multiply MOIRA_LIST_OWNER rows ~x2.5, so a plain COUNT over the joined result counts duplicates. To count distinct owners, use COUNT(DISTINCT MOIRA_LIST_OWNER.OWNER).",
    "- \"public status\" -> MOIRA_LIST.IS_PUBLIC: already repeats ~5000.0 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct public status, use COUNT(DISTINCT MOIRA_LIST.IS_PUBLIC).",
    "- \"hidden status\" -> MOIRA_LIST.IS_HIDDEN: already repeats ~10000.0 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct hidden status, use COUNT(DISTINCT MOIRA_LIST.IS_HIDDEN).",
    "- \"members\" -> MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER: already repeats ~1.5 rows per value in its own table; the hinted joins multiply MOIRA_LIST_DETAIL rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct members, use COUNT(DISTINCT MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER)."
  ],
  "dw_real_5": [
    "- Join FCLT_ROOMS.FCLT_ROOM_KEY = SUBJECT_OFFERED.MEET_PLACE: FCLT_ROOMS.FCLT_ROOM_KEY is unique per row (10000 rows); SUBJECT_OFFERED.MEET_PLACE repeats ~10000.0 rows per value (10000 rows, 0 values).",
    "  -> each FCLT_ROOMS row appears ~10000.0 times after this join.",
    "- Join FCLT_BUILDING_ADDRESS.FCLT_BUILDING_KEY = FCLT_ROOMS.FCLT_BUILDING_KEY: FCLT_BUILDING_ADDRESS.FCLT_BUILDING_KEY repeats ~3.2 rows per value (785 rows, 242 values); FCLT_ROOMS.FCLT_BUILDING_KEY repeats ~49.5 rows per value (10000 rows, 202 values).",
    "  -> each FCLT_BUILDING_ADDRESS row appears ~49.5 times after this join.",
    "  -> each FCLT_ROOMS row appears ~3.2 times after this join.",
    "- Join BUILDINGS.BUILDING_KEY = FCLT_ROOMS.FCLT_BUILDING_KEY: BUILDINGS.BUILDING_KEY is unique per row (211 rows); FCLT_ROOMS.FCLT_BUILDING_KEY repeats ~49.5 rows per value (10000 rows, 202 values).",
    "  -> each BUILDINGS row appears ~49.5 times after this join.",
    "- \"term code\" -> SUBJECT_OFFERED.TERM_CODE: already repeats ~370.4 rows per value in its own table; the hinted joins do not multiply SUBJECT_OFFERED rows.",
    "- \"subject title\" -> SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~4.5 rows per value in its own table; the hinted joins do not multiply SUBJECT_OFFERED rows.",
    "- \"room\" -> FCLT_ROOMS.ROOM: already repeats ~2.8 rows per value in its own table; the hinted joins multiply FCLT_ROOMS rows ~x10000.0, so a plain COUNT over the joined result counts duplicates. To count distinct room, use COUNT(DISTINCT FCLT_ROOMS.ROOM).",
    "- \"floor\" -> FCLT_ROOMS.FLOOR: already repeats ~208.3 rows per value in its own table; the hinted joins multiply FCLT_ROOMS rows ~x10000.0, so a plain COUNT over the joined result counts duplicates. To count distinct floor, use COUNT(DISTINCT FCLT_ROOMS.FLOOR).",
    "- \"building key\" -> BUILDINGS.BUILDING_KEY: unique per row in its own table; the hinted joins multiply BUILDINGS rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct building key, use COUNT(DISTINCT BUILDINGS.BUILDING_KEY).",
    "- \"building street address\" -> BUILDINGS.BUILDING_STREET_ADDRESS: already repeats ~1.4 rows per value in its own table; the hinted joins multiply BUILDINGS rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct building street address, use COUNT(DISTINCT BUILDINGS.BUILDING_STREET_ADDRESS).",
    "- \"building street address\" -> FCLT_BUILDING_ADDRESS.ADDRESS_PURPOSE: already repeats ~60.4 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_ADDRESS rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct building street address, use COUNT(DISTINCT FCLT_BUILDING_ADDRESS.ADDRESS_PURPOSE).",
    "- \"city\" -> FCLT_BUILDING_ADDRESS.CITY: already repeats ~71.4 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_ADDRESS rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct city, use COUNT(DISTINCT FCLT_BUILDING_ADDRESS.CITY).",
    "- \"state\" -> FCLT_BUILDING_ADDRESS.STATE: already repeats ~392.5 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_ADDRESS rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct state, use COUNT(DISTINCT FCLT_BUILDING_ADDRESS.STATE).",
    "- \"postal code\" -> FCLT_BUILDING_ADDRESS.POSTAL_CODE: already repeats ~56.1 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_ADDRESS rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct postal code, use COUNT(DISTINCT FCLT_BUILDING_ADDRESS.POSTAL_CODE).",
    "- \"formats\" -> SUBJECT_OFFERED.FORM_TYPE: already repeats ~10000.0 rows per value in its own table; the hinted joins do not multiply SUBJECT_OFFERED rows.",
    "- \"number of enrolled students\" -> SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED; the hinted joins do not multiply SUBJECT_OFFERED rows.",
    "- \"attendees\" -> SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED; the hinted joins do not multiply SUBJECT_OFFERED rows."
  ],
  "dw_real_50": [
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_MIT_ID = EMPLOYEE_DIRECTORY.MIT_ID: COURSE_CATALOG_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_MIT_ID repeats ~4.1 rows per value (10000 rows, 2418 values); EMPLOYEE_DIRECTORY.MIT_ID is unique per row (10000 rows).",
    "  -> each EMPLOYEE_DIRECTORY row appears ~4.1 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS row appears ~105.3 times after this join.",
    "- \"names\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply MOIRA_LIST rows.",
    "- \"faculty\" -> EMPLOYEE_DIRECTORY.MIT_ID: unique per row in its own table; the hinted joins multiply EMPLOYEE_DIRECTORY rows ~x4.1, so a plain COUNT over the joined result counts duplicates. To count distinct faculty, use COUNT(DISTINCT EMPLOYEE_DIRECTORY.MIT_ID).",
    "- \"courses in the summer\" -> ACADEMIC_TERMS.TERM_CODE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct courses in the summer, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_CODE).",
    "- \"financial aid years\" -> ACADEMIC_TERMS.FINANCIAL_AID_YEAR: already repeats ~3.9 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct financial aid years, use COUNT(DISTINCT ACADEMIC_TERMS.FINANCIAL_AID_YEAR).",
    "- \"people\" -> MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER: already repeats ~1.5 rows per value in its own table; the hinted joins do not multiply MOIRA_LIST_DETAIL rows."
  ],
  "dw_real_51": [
    "- Join MOIRA_LIST.MOIRA_LIST_KEY = MOIRA_LIST_DETAIL.MOIRA_LIST_KEY: MOIRA_LIST.MOIRA_LIST_KEY repeats ~1.1 rows per value (10000 rows, 8815 values); MOIRA_LIST_DETAIL.MOIRA_LIST_KEY repeats ~1.6 rows per value (10000 rows, 6395 values).",
    "  -> each MOIRA_LIST row appears ~1.6 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "- Join MIT_STUDENT_DIRECTORY.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: MIT_STUDENT_DIRECTORY.DEPARTMENT repeats ~204.1 rows per value (10000 rows, 49 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~204.1 times after this join.",
    "- \"duo users\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct duo users, use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_NAME).",
    "- \"students\" -> SE_PERSON.MIT_ID: unique per row in its own table; the hinted joins do not multiply SE_PERSON rows.",
    "- \"departments\" -> SIS_DEPARTMENT.DEPARTMENT_CODE: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct departments, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_CODE).",
    "- \"schools\" -> SIS_DEPARTMENT.SCHOOL_CODE: already repeats ~12.8 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct schools, use COUNT(DISTINCT SIS_DEPARTMENT.SCHOOL_CODE)."
  ],
  "dw_real_52": [
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_MIT_ID = employee_directory.MIT_ID: COURSE_CATALOG_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_MIT_ID repeats ~4.1 rows per value (10000 rows, 2418 values); employee_directory.MIT_ID is unique per row (10000 rows).",
    "  -> each employee_directory row appears ~4.1 times after this join.",
    "- Join MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER = employee_directory.KRB_NAME: MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER repeats ~1.5 rows per value (10000 rows, 6469 values); employee_directory.KRB_NAME repeats ~1.9 rows per value (10000 rows, 5390 values).",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.9 times after this join.",
    "  -> each employee_directory row appears ~1.5 times after this join.",
    "- Join MOIRA_LIST.MOIRA_LIST_KEY = MOIRA_LIST_DETAIL.MOIRA_LIST_KEY: MOIRA_LIST.MOIRA_LIST_KEY repeats ~1.1 rows per value (10000 rows, 8815 values); MOIRA_LIST_DETAIL.MOIRA_LIST_KEY repeats ~1.6 rows per value (10000 rows, 6395 values).",
    "  -> each MOIRA_LIST row appears ~1.6 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "- \"faculty\" -> COURSE_CATALOG_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_MIT_ID: already repeats ~4.1 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"2023 fall term\" -> COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: already repeats ~105.3 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"name of mailing lists\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct name of mailing lists, use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_NAME).",
    "- \"members\" -> MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER: already repeats ~1.5 rows per value in its own table; the hinted joins multiply MOIRA_LIST_DETAIL rows ~x1.9, so a plain COUNT over the joined result counts duplicates. To count distinct members, use COUNT(DISTINCT MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER).",
    "- \"courses\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows."
  ],
  "dw_real_53": [
    "- Join LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY: LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY repeats ~6.2 rows per value (10000 rows, 1614 values).",
    "  -> each LIBRARY_COURSE_INSTRUCTOR row appears ~6.2 times after this join.",
    "- Join LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY repeats ~1.2 rows per value (10000 rows, 8268 values).",
    "  -> each LIBRARY_RESERVE_CATALOG row appears ~1.2 times after this join.",
    "- Join LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY = LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY: LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY repeats ~6.5 rows per value (10000 rows, 1550 values); LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY is unique per row (10000 rows).",
    "  -> each LIBRARY_SUBJECT_OFFERED row appears ~6.5 times after this join.",
    "- Join LIBRARY_COURSE_INSTRUCTOR.INSTRUCTOR_NAME = SE_PERSON.FULL_NAME: LIBRARY_COURSE_INSTRUCTOR.INSTRUCTOR_NAME is unique per row (10000 rows); SE_PERSON.FULL_NAME is unique per row (10000 rows).",
    "- \"'keeper-zephyr' mailing list\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply MOIRA_LIST rows.",
    "- \"name of mailing lists\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply MOIRA_LIST rows.",
    "- \"instructor name\" -> LIBRARY_COURSE_INSTRUCTOR.INSTRUCTOR_NAME: unique per row in its own table; the hinted joins multiply LIBRARY_COURSE_INSTRUCTOR rows ~x6.2, so a plain COUNT over the joined result counts duplicates. To count distinct instructor name, use COUNT(DISTINCT LIBRARY_COURSE_INSTRUCTOR.INSTRUCTOR_NAME).",
    "- \"publication years\" -> LIBRARY_RESERVE_CATALOG.CATALOG_YEAR: already repeats ~111.1 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_CATALOG rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct publication years, use COUNT(DISTINCT LIBRARY_RESERVE_CATALOG.CATALOG_YEAR).",
    "- \"enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so SUM/AVG over the joined result would count each LIBRARY_SUBJECT_OFFERED row ~6.5 times. Aggregate it at LIBRARY_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_real_54": [
    "- Join MOIRA_LIST.MOIRA_LIST_KEY = MOIRA_LIST_DETAIL.MOIRA_LIST_KEY: MOIRA_LIST.MOIRA_LIST_KEY repeats ~1.1 rows per value (10000 rows, 8815 values); MOIRA_LIST_DETAIL.MOIRA_LIST_KEY repeats ~1.6 rows per value (10000 rows, 6395 values).",
    "  -> each MOIRA_LIST row appears ~1.6 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "- Join MOIRA_LIST_DETAIL.MOIRA_LIST_OWNER_KEY = MOIRA_LIST_OWNER.MOIRA_LIST_OWNER_KEY: MOIRA_LIST_DETAIL.MOIRA_LIST_OWNER_KEY repeats ~2.5 rows per value (10000 rows, 3966 values); MOIRA_LIST_OWNER.MOIRA_LIST_OWNER_KEY repeats ~1.1 rows per value (10000 rows, 9042 values).",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "  -> each MOIRA_LIST_OWNER row appears ~2.5 times after this join.",
    "- \"name\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct name, use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_NAME).",
    "- \"Professor Ayden Hopkins\" -> SE_PERSON.FULL_NAME: unique per row in its own table; the hinted joins do not multiply SE_PERSON rows.",
    "- \"description\" -> MOIRA_LIST.MOIRA_LIST_DESCRIPTION: already repeats ~10000.0 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct description, use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_DESCRIPTION).",
    "- \"owner\" -> MOIRA_LIST_OWNER.OWNER: already repeats ~1.2 rows per value in its own table; the hinted joins multiply MOIRA_LIST_OWNER rows ~x2.5, so a plain COUNT over the joined result counts duplicates. To count distinct owner, use COUNT(DISTINCT MOIRA_LIST_OWNER.OWNER).",
    "- \"people\" -> MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER: already repeats ~1.5 rows per value in its own table; the hinted joins multiply MOIRA_LIST_DETAIL rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct people, use COUNT(DISTINCT MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER).",
    "- \"tenured faculty\" -> SE_PERSON.PAYROLL_RANK: already repeats ~384.6 rows per value in its own table; the hinted joins do not multiply SE_PERSON rows.",
    "- \"tenured faculty\" -> MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER: already repeats ~1.5 rows per value in its own table; the hinted joins multiply MOIRA_LIST_DETAIL rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct tenured faculty, use COUNT(DISTINCT MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER)."
  ],
  "dw_real_55": [
    "- Join MIT_STUDENT_DIRECTORY.FULL_NAME = SE_PERSON.FULL_NAME: MIT_STUDENT_DIRECTORY.FULL_NAME repeats ~1.2 rows per value (10000 rows, 8585 values); SE_PERSON.FULL_NAME is unique per row (10000 rows).",
    "  -> each SE_PERSON row appears ~1.2 times after this join.",
    "- Join MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER = SE_PERSON.KRB_NAME: MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER repeats ~1.5 rows per value (10000 rows, 6469 values); SE_PERSON.KRB_NAME repeats ~1.9 rows per value (10000 rows, 5341 values).",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.9 times after this join.",
    "  -> each SE_PERSON row appears ~1.5 times after this join.",
    "- Join MOIRA_LIST.MOIRA_LIST_KEY = MOIRA_LIST_DETAIL.MOIRA_LIST_KEY: MOIRA_LIST.MOIRA_LIST_KEY repeats ~1.1 rows per value (10000 rows, 8815 values); MOIRA_LIST_DETAIL.MOIRA_LIST_KEY repeats ~1.6 rows per value (10000 rows, 6395 values).",
    "  -> each MOIRA_LIST row appears ~1.6 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "- Join MIT_STUDENT_DIRECTORY.DEPARTMENT = SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE: MIT_STUDENT_DIRECTORY.DEPARTMENT repeats ~204.1 rows per value (10000 rows, 49 values); SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE is unique per row (179 rows).",
    "  -> each SIS_ADMIN_DEPARTMENT row appears ~204.1 times after this join.",
    "- \"name\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct name, use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_NAME).",
    "- \"department name\" -> SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply SIS_ADMIN_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_NAME).",
    "- \"students\" -> MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER: already repeats ~1.5 rows per value in its own table; the hinted joins multiply MOIRA_LIST_DETAIL rows ~x1.9, so a plain COUNT over the joined result counts duplicates. To count distinct students, use COUNT(DISTINCT MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER).",
    "- \"Management department\" -> SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply SIS_ADMIN_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct Management department, use COUNT(DISTINCT SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_NAME)."
  ],
  "dw_real_56": [
    "- Join FAC_BUILDING.FAC_BUILDING_KEY = FAC_ROOMS.BUILDING_KEY: FAC_BUILDING.FAC_BUILDING_KEY is unique per row (242 rows); FAC_ROOMS.BUILDING_KEY repeats ~53.2 rows per value (10000 rows, 188 values).",
    "  -> each FAC_BUILDING row appears ~53.2 times after this join.",
    "- Join FAC_FLOOR.FLOOR_KEY = FAC_ROOMS.FLOOR_KEY: FAC_FLOOR.FLOOR_KEY is unique per row (1079 rows); FAC_ROOMS.FLOOR_KEY repeats ~12.1 rows per value (10000 rows, 827 values).",
    "  -> each FAC_FLOOR row appears ~12.1 times after this join.",
    "- Join EMPLOYEE_DIRECTORY.OFFICE_LOCATION = FAC_ROOMS.FAC_ROOM_KEY: EMPLOYEE_DIRECTORY.OFFICE_LOCATION repeats ~1.9 rows per value (10000 rows, 5396 values); FAC_ROOMS.FAC_ROOM_KEY is unique per row (10000 rows).",
    "  -> each FAC_ROOMS row appears ~1.9 times after this join.",
    "- Join EMPLOYEE_DIRECTORY.KRB_NAME = MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER: EMPLOYEE_DIRECTORY.KRB_NAME repeats ~1.9 rows per value (10000 rows, 5390 values); MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER repeats ~1.5 rows per value (10000 rows, 6469 values).",
    "  -> each EMPLOYEE_DIRECTORY row appears ~1.5 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.9 times after this join.",
    "- Join MOIRA_LIST.MOIRA_LIST_KEY = MOIRA_LIST_DETAIL.MOIRA_LIST_KEY: MOIRA_LIST.MOIRA_LIST_KEY repeats ~1.1 rows per value (10000 rows, 8815 values); MOIRA_LIST_DETAIL.MOIRA_LIST_KEY repeats ~1.6 rows per value (10000 rows, 6395 values).",
    "  -> each MOIRA_LIST row appears ~1.6 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "- \"floors\" -> FAC_FLOOR.LEVEL_ID: already repeats ~36.0 rows per value in its own table; the hinted joins multiply FAC_FLOOR rows ~x12.1, so a plain COUNT over the joined result counts duplicates. To count distinct floors, use COUNT(DISTINCT FAC_FLOOR.LEVEL_ID).",
    "- \"building name\" -> FAC_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins multiply FAC_BUILDING rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct building name, use COUNT(DISTINCT FAC_BUILDING.BUILDING_NAME).",
    "- \"names of the mailing lists\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct names of the mailing lists, use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_NAME).",
    "- \"kerberos\" -> EMPLOYEE_DIRECTORY.KRB_NAME: already repeats ~1.9 rows per value in its own table; the hinted joins multiply EMPLOYEE_DIRECTORY rows ~x1.5, so a plain COUNT over the joined result counts duplicates. To count distinct kerberos, use COUNT(DISTINCT EMPLOYEE_DIRECTORY.KRB_NAME)."
  ],
  "dw_real_57": [
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_MIT_ID = EMPLOYEE_DIRECTORY.MIT_ID: COURSE_CATALOG_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_MIT_ID repeats ~4.1 rows per value (10000 rows, 2418 values); EMPLOYEE_DIRECTORY.MIT_ID is unique per row (10000 rows).",
    "  -> each EMPLOYEE_DIRECTORY row appears ~4.1 times after this join.",
    "- \"last names\" -> COURSE_CATALOG_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_NAME: already repeats ~4.2 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"names of the lists\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins do not multiply MOIRA_LIST rows.",
    "- \"subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"faculty\" -> COURSE_CATALOG_SUBJECT_OFFERED.RESPONSIBLE_FACULTY_NAME: already repeats ~4.2 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows."
  ],
  "dw_real_58": [
    "- Join MOIRA_LIST.(MOIRA_LIST_KEY,MOIRA_LIST_KEY) = MOIRA_LIST_DETAIL.(MOIRA_LIST_KEY,MOIRA_LIST_KEY): MOIRA_LIST.(MOIRA_LIST_KEY,MOIRA_LIST_KEY) repeats ~1.1 rows per value (10000 rows, 8815 values); MOIRA_LIST_DETAIL.(MOIRA_LIST_KEY,MOIRA_LIST_KEY) repeats ~1.6 rows per value (10000 rows, 6395 values).",
    "  -> each MOIRA_LIST row appears ~1.6 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "- Join MIT_STUDENT_DIRECTORY.DEPARTMENT = SIS_DEPARTMENT.DEPARTMENT_CODE: MIT_STUDENT_DIRECTORY.DEPARTMENT repeats ~204.1 rows per value (10000 rows, 49 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~204.1 times after this join.",
    "- Join SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE is unique per row (179 rows); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "- Join MIT_STUDENT_DIRECTORY.FULL_NAME = SE_PERSON.FULL_NAME: MIT_STUDENT_DIRECTORY.FULL_NAME repeats ~1.2 rows per value (10000 rows, 8585 values); SE_PERSON.FULL_NAME is unique per row (10000 rows).",
    "  -> each SE_PERSON row appears ~1.2 times after this join.",
    "- Join MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER = SE_PERSON.KRB_NAME: MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER repeats ~1.5 rows per value (10000 rows, 6469 values); SE_PERSON.KRB_NAME repeats ~1.9 rows per value (10000 rows, 5341 values).",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.9 times after this join.",
    "  -> each SE_PERSON row appears ~1.5 times after this join.",
    "- \"last names\" -> MIT_STUDENT_DIRECTORY.LAST_NAME: already repeats ~29.5 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"mailing list 'beacon-date-date'\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct mailing list 'beacon-date-date', use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_NAME).",
    "- \"names\" -> MIT_STUDENT_DIRECTORY.FULL_NAME: already repeats ~1.2 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows.",
    "- \"phone numbers\" -> SIS_ADMIN_DEPARTMENT.DEPARTMENT_PHONE_NUMBER: already repeats ~2.8 rows per value in its own table; the hinted joins do not multiply SIS_ADMIN_DEPARTMENT rows.",
    "- \"size of the mailing list\" -> MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER: already repeats ~1.5 rows per value in its own table; the hinted joins multiply MOIRA_LIST_DETAIL rows ~x1.9, so a plain COUNT over the joined result counts duplicates. To count distinct size of the mailing list, use COUNT(DISTINCT MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER)."
  ],
  "dw_real_59": [
    "- Join MOIRA_LIST.MOIRA_LIST_KEY = MOIRA_LIST_DETAIL.MOIRA_LIST_KEY: MOIRA_LIST.MOIRA_LIST_KEY repeats ~1.1 rows per value (10000 rows, 8815 values); MOIRA_LIST_DETAIL.MOIRA_LIST_KEY repeats ~1.6 rows per value (10000 rows, 6395 values).",
    "  -> each MOIRA_LIST row appears ~1.6 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "- Join MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER = SE_PERSON.KRB_NAME: MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER repeats ~1.5 rows per value (10000 rows, 6469 values); SE_PERSON.KRB_NAME repeats ~1.9 rows per value (10000 rows, 5341 values).",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.9 times after this join.",
    "  -> each SE_PERSON row appears ~1.5 times after this join.",
    "- \"mailing lists\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct mailing lists, use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_NAME).",
    "- \"names\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct names, use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_NAME).",
    "- \"members\" -> SE_PERSON.MIT_ID: unique per row in its own table; the hinted joins multiply SE_PERSON rows ~x1.5, so a plain COUNT over the joined result counts duplicates. To count distinct members, use COUNT(DISTINCT SE_PERSON.MIT_ID).",
    "- \"Electrical Engineering and Computer Science department\" -> SE_PERSON.ORGANIZATION: already repeats ~31.2 rows per value in its own table; the hinted joins multiply SE_PERSON rows ~x1.5, so a plain COUNT over the joined result counts duplicates. To count distinct Electrical Engineering and Computer Science department, use COUNT(DISTINCT SE_PERSON.ORGANIZATION)."
  ],
  "dw_real_6": [
    "- Join TIP_DETAIL.TIP_MATERIAL_KEY = TIP_MATERIAL.TIP_MATERIAL_KEY: TIP_DETAIL.TIP_MATERIAL_KEY repeats ~1.4 rows per value (10000 rows, 7360 values); TIP_MATERIAL.TIP_MATERIAL_KEY is unique per row (10000 rows).",
    "  -> each TIP_MATERIAL row appears ~1.4 times after this join.",
    "- Join TIP_DETAIL.SUBJECT_ID = TIP_SUBJECT_OFFERED.SUBJECT_ID: TIP_DETAIL.SUBJECT_ID repeats ~2.8 rows per value (10000 rows, 3623 values); TIP_SUBJECT_OFFERED.SUBJECT_ID repeats ~2.0 rows per value (10000 rows, 4974 values).",
    "  -> each TIP_DETAIL row appears ~2.0 times after this join.",
    "  -> each TIP_SUBJECT_OFFERED row appears ~2.8 times after this join.",
    "- \"subject titles\" -> TIP_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.4 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct subject titles, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.SUBJECT_TITLE).",
    "- \"material titles\" -> TIP_MATERIAL.TITLE: already repeats ~1.6 rows per value in its own table; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct material titles, use COUNT(DISTINCT TIP_MATERIAL.TITLE).",
    "- \"ISBN numbers\" -> TIP_MATERIAL.ISBN: already repeats ~1.5 rows per value in its own table; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct ISBN numbers, use COUNT(DISTINCT TIP_MATERIAL.ISBN).",
    "- \"cost of new materials\" -> TIP_MATERIAL.NEW_SHELF_PRICE is a numeric measure of TIP_MATERIAL; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so SUM/AVG over the joined result would count each TIP_MATERIAL row ~1.4 times. Aggregate it at TIP_MATERIAL's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_real_60": [
    "- Join MIT_STUDENT_DIRECTORY.FULL_NAME = SE_PERSON.FULL_NAME: MIT_STUDENT_DIRECTORY.FULL_NAME repeats ~1.2 rows per value (10000 rows, 8585 values); SE_PERSON.FULL_NAME is unique per row (10000 rows).",
    "  -> each SE_PERSON row appears ~1.2 times after this join.",
    "- Join MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER = SE_PERSON.KRB_NAME: MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER repeats ~1.5 rows per value (10000 rows, 6469 values); SE_PERSON.KRB_NAME repeats ~1.9 rows per value (10000 rows, 5341 values).",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.9 times after this join.",
    "  -> each SE_PERSON row appears ~1.5 times after this join.",
    "- Join MOIRA_LIST.MOIRA_LIST_KEY = MOIRA_LIST_DETAIL.MOIRA_LIST_KEY: MOIRA_LIST.MOIRA_LIST_KEY repeats ~1.1 rows per value (10000 rows, 8815 values); MOIRA_LIST_DETAIL.MOIRA_LIST_KEY repeats ~1.6 rows per value (10000 rows, 6395 values).",
    "  -> each MOIRA_LIST row appears ~1.6 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "- Join MIT_STUDENT_DIRECTORY.DEPARTMENT = SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE: MIT_STUDENT_DIRECTORY.DEPARTMENT repeats ~204.1 rows per value (10000 rows, 49 values); SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE is unique per row (179 rows).",
    "  -> each SIS_ADMIN_DEPARTMENT row appears ~204.1 times after this join.",
    "- \"'kangaroo-inspire-yearn' email list\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct 'kangaroo-inspire-yearn' email list, use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_NAME).",
    "- \"department name\" -> SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply SIS_ADMIN_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_NAME).",
    "- \"students\" -> MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER: already repeats ~1.5 rows per value in its own table; the hinted joins multiply MOIRA_LIST_DETAIL rows ~x1.9, so a plain COUNT over the joined result counts duplicates. To count distinct students, use COUNT(DISTINCT MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER)."
  ],
  "dw_real_61": [
    "- Join MOIRA_LIST.MOIRA_LIST_KEY = MOIRA_LIST_DETAIL.MOIRA_LIST_KEY: MOIRA_LIST.MOIRA_LIST_KEY repeats ~1.1 rows per value (10000 rows, 8815 values); MOIRA_LIST_DETAIL.MOIRA_LIST_KEY repeats ~1.6 rows per value (10000 rows, 6395 values).",
    "  -> each MOIRA_LIST row appears ~1.6 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "- Join EMPLOYEE_DIRECTORY.KRB_NAME = MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER: EMPLOYEE_DIRECTORY.KRB_NAME repeats ~1.9 rows per value (10000 rows, 5390 values); MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER repeats ~1.5 rows per value (10000 rows, 6469 values).",
    "  -> each EMPLOYEE_DIRECTORY row appears ~1.5 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.9 times after this join.",
    "- \"physical offices in building 24\" -> EMPLOYEE_DIRECTORY.OFFICE_LOCATION: already repeats ~1.9 rows per value in its own table; the hinted joins multiply EMPLOYEE_DIRECTORY rows ~x1.5, so a plain COUNT over the joined result counts duplicates. To count distinct physical offices in building 24, use COUNT(DISTINCT EMPLOYEE_DIRECTORY.OFFICE_LOCATION).",
    "- \"name\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct name, use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_NAME).",
    "- \"subscribers\" -> MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER: already repeats ~1.5 rows per value in its own table; the hinted joins multiply MOIRA_LIST_DETAIL rows ~x1.9, so a plain COUNT over the joined result counts duplicates. To count distinct subscribers, use COUNT(DISTINCT MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER)."
  ],
  "dw_real_62": [
    "- Join MOIRA_LIST.MOIRA_LIST_KEY = MOIRA_LIST_DETAIL.MOIRA_LIST_KEY: MOIRA_LIST.MOIRA_LIST_KEY repeats ~1.1 rows per value (10000 rows, 8815 values); MOIRA_LIST_DETAIL.MOIRA_LIST_KEY repeats ~1.6 rows per value (10000 rows, 6395 values).",
    "  -> each MOIRA_LIST row appears ~1.6 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "- \"support staff or faculty\" -> SE_PERSON.EMPLOYEE_TYPE: already repeats ~1250.0 rows per value in its own table; the hinted joins do not multiply SE_PERSON rows.",
    "- \"name\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct name, use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_NAME).",
    "- \"subscribers\" -> SE_PERSON.EMPLOYEE_TYPE: already repeats ~1250.0 rows per value in its own table; the hinted joins do not multiply SE_PERSON rows.",
    "- \"active status\" -> MOIRA_LIST.IS_ACTIVE: already repeats ~10000.0 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct active status, use COUNT(DISTINCT MOIRA_LIST.IS_ACTIVE)."
  ],
  "dw_real_63": [
    "- Join MOIRA_LIST.MOIRA_LIST_KEY = MOIRA_LIST_DETAIL.MOIRA_LIST_KEY: MOIRA_LIST.MOIRA_LIST_KEY repeats ~1.1 rows per value (10000 rows, 8815 values); MOIRA_LIST_DETAIL.MOIRA_LIST_KEY repeats ~1.6 rows per value (10000 rows, 6395 values).",
    "  -> each MOIRA_LIST row appears ~1.6 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "- Join MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER = SE_PERSON.KRB_NAME: MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER repeats ~1.5 rows per value (10000 rows, 6469 values); SE_PERSON.KRB_NAME repeats ~1.9 rows per value (10000 rows, 5341 values).",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.9 times after this join.",
    "  -> each SE_PERSON row appears ~1.5 times after this join.",
    "- Join MIT_STUDENT_DIRECTORY.FULL_NAME = SE_PERSON.FULL_NAME: MIT_STUDENT_DIRECTORY.FULL_NAME repeats ~1.2 rows per value (10000 rows, 8585 values); SE_PERSON.FULL_NAME is unique per row (10000 rows).",
    "  -> each SE_PERSON row appears ~1.2 times after this join.",
    "- Join MIT_STUDENT_DIRECTORY.DEPARTMENT = SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE: MIT_STUDENT_DIRECTORY.DEPARTMENT repeats ~204.1 rows per value (10000 rows, 49 values); SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_CODE is unique per row (179 rows).",
    "  -> each SIS_ADMIN_DEPARTMENT row appears ~204.1 times after this join.",
    "- \"'ocean-apple' mailing list\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct 'ocean-apple' mailing list, use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_NAME).",
    "- \"department name\" -> SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply SIS_ADMIN_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_ADMIN_DEPARTMENT.SIS_ADMIN_DEPARTMENT_NAME).",
    "- \"phone number\" -> SIS_ADMIN_DEPARTMENT.DEPARTMENT_PHONE_NUMBER: already repeats ~2.8 rows per value in its own table; the hinted joins multiply SIS_ADMIN_DEPARTMENT rows ~x204.1, so a plain COUNT over the joined result counts duplicates. To count distinct phone number, use COUNT(DISTINCT SIS_ADMIN_DEPARTMENT.DEPARTMENT_PHONE_NUMBER).",
    "- \"students\" -> MIT_STUDENT_DIRECTORY.FULL_NAME: already repeats ~1.2 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows."
  ],
  "dw_real_64": [
    "- Join MOIRA_LIST.MOIRA_LIST_KEY = MOIRA_LIST_DETAIL.MOIRA_LIST_KEY: MOIRA_LIST.MOIRA_LIST_KEY repeats ~1.1 rows per value (10000 rows, 8815 values); MOIRA_LIST_DETAIL.MOIRA_LIST_KEY repeats ~1.6 rows per value (10000 rows, 6395 values).",
    "  -> each MOIRA_LIST row appears ~1.6 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "- Join MOIRA_LIST_DETAIL.MOIRA_LIST_OWNER_KEY = MOIRA_LIST_OWNER.MOIRA_LIST_OWNER_KEY: MOIRA_LIST_DETAIL.MOIRA_LIST_OWNER_KEY repeats ~2.5 rows per value (10000 rows, 3966 values); MOIRA_LIST_OWNER.MOIRA_LIST_OWNER_KEY repeats ~1.1 rows per value (10000 rows, 9042 values).",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "  -> each MOIRA_LIST_OWNER row appears ~2.5 times after this join.",
    "- \"owner\" -> MOIRA_LIST_OWNER.OWNER: already repeats ~1.2 rows per value in its own table; the hinted joins multiply MOIRA_LIST_OWNER rows ~x2.5, so a plain COUNT over the joined result counts duplicates. To count distinct owner, use COUNT(DISTINCT MOIRA_LIST_OWNER.OWNER).",
    "- \"owner type\" -> MOIRA_LIST_OWNER.OWNER_TYPE: already repeats ~3333.3 rows per value in its own table; the hinted joins multiply MOIRA_LIST_OWNER rows ~x2.5, so a plain COUNT over the joined result counts duplicates. To count distinct owner type, use COUNT(DISTINCT MOIRA_LIST_OWNER.OWNER_TYPE).",
    "- \"member visibility\" -> MOIRA_LIST.IS_PUBLIC: already repeats ~5000.0 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct member visibility, use COUNT(DISTINCT MOIRA_LIST.IS_PUBLIC).",
    "- \"members\" -> MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER: already repeats ~1.5 rows per value in its own table; the hinted joins multiply MOIRA_LIST_DETAIL rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct members, use COUNT(DISTINCT MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER)."
  ],
  "dw_real_65": [
    "- Join SIS_COURSE_DESCRIPTION.COURSE = SUBJECT_OFFERED_SUMMARY.COURSE_NUMBER: SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values); SUBJECT_OFFERED_SUMMARY.COURSE_NUMBER repeats ~90.9 rows per value (10000 rows, 110 values).",
    "  -> each SIS_COURSE_DESCRIPTION row appears ~90.9 times after this join.",
    "  -> each SUBJECT_OFFERED_SUMMARY row appears ~1.1 times after this join.",
    "- Join TIP_DETAIL.TIP_MATERIAL_KEY = TIP_MATERIAL.TIP_MATERIAL_KEY: TIP_DETAIL.TIP_MATERIAL_KEY repeats ~1.4 rows per value (10000 rows, 7360 values); TIP_MATERIAL.TIP_MATERIAL_KEY is unique per row (10000 rows).",
    "  -> each TIP_MATERIAL row appears ~1.4 times after this join.",
    "- \"biology courses\" -> SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_NAME: already repeats ~200.0 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct biology courses, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_NAME).",
    "- \"cluster type\" -> SUBJECT_OFFERED_SUMMARY.CLUSTER_TYPE: already repeats ~3333.3 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct cluster type, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.CLUSTER_TYPE).",
    "- \"course level\" -> SIS_COURSE_DESCRIPTION.COURSE_LEVEL: already repeats ~347.5 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x90.9, so a plain COUNT over the joined result counts duplicates. To count distinct course level, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.COURSE_LEVEL).",
    "- \"name of the department\" -> SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_NAME: already repeats ~200.0 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct name of the department, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_NAME).",
    "- \"course title\" -> SUBJECT_OFFERED_SUMMARY.SUBJECT_TITLE: already repeats ~2.3 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct course title, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.SUBJECT_TITLE).",
    "- \"enrollment\" -> SUBJECT_OFFERED_SUMMARY.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~1.1 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"course materials\" -> TIP_MATERIAL.TITLE: already repeats ~1.6 rows per value in its own table; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct course materials, use COUNT(DISTINCT TIP_MATERIAL.TITLE).",
    "- \"new price\" -> TIP_MATERIAL.NEW_SHELF_PRICE is a numeric measure of TIP_MATERIAL; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so SUM/AVG over the joined result would count each TIP_MATERIAL row ~1.4 times. Aggregate it at TIP_MATERIAL's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"use price\" -> TIP_MATERIAL.USED_SHELF_PRICE is a numeric measure of TIP_MATERIAL; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so SUM/AVG over the joined result would count each TIP_MATERIAL row ~1.4 times. Aggregate it at TIP_MATERIAL's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"record count for TIP materials\" -> TIP_DETAIL.RECORD_COUNT is a numeric measure of TIP_DETAIL; the hinted joins do not multiply TIP_DETAIL rows.",
    "- \"library titles\" -> LIBRARY_RESERVE_CATALOG.CATALOG_TITLE: already repeats ~1.9 rows per value in its own table; the hinted joins do not multiply LIBRARY_RESERVE_CATALOG rows.",
    "- \"library ISBNs\" -> LIBRARY_RESERVE_CATALOG.CATALOG_ISBN: already repeats ~2.6 rows per value in its own table; the hinted joins do not multiply LIBRARY_RESERVE_CATALOG rows."
  ],
  "dw_real_66": [
    "- Join ACADEMIC_TERMS.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE repeats ~105.3 rows per value (10000 rows, 95 values).",
    "  -> each ACADEMIC_TERMS row appears ~105.3 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE = SIS_DEPARTMENT.DEPARTMENT_CODE: COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_CODE repeats ~166.7 rows per value (10000 rows, 60 values); SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows).",
    "  -> each SIS_DEPARTMENT row appears ~166.7 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE = SIS_COURSE_DESCRIPTION.COURSE: COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_CODE repeats ~181.8 rows per value (10000 rows, 55 values); SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values).",
    "  -> each COURSE_CATALOG_SUBJECT_OFFERED row appears ~1.1 times after this join.",
    "  -> each SIS_COURSE_DESCRIPTION row appears ~181.8 times after this join.",
    "- \"subjects offered this year\" -> COURSE_CATALOG_SUBJECT_OFFERED.IS_OFFERED_THIS_YEAR: already repeats ~5000.0 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct subjects offered this year, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.IS_OFFERED_THIS_YEAR).",
    "- \"department name\" -> COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME: already repeats ~196.1 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.DEPARTMENT_NAME).",
    "- \"school name\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x166.7, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_DEPARTMENT.SCHOOL_NAME).",
    "- \"subject ID\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct subject ID, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID).",
    "- \"subject title\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.7 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct subject title, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_TITLE).",
    "- \"course level\" -> SIS_COURSE_DESCRIPTION.COURSE_LEVEL: already repeats ~347.5 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x181.8, so a plain COUNT over the joined result counts duplicates. To count distinct course level, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.COURSE_LEVEL).",
    "- \"total units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~1.1 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"term\" -> COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: already repeats ~105.3 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct term, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE).",
    "- \"term description\" -> ACADEMIC_TERMS.TERM_DESCRIPTION: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x105.3, so a plain COUNT over the joined result counts duplicates. To count distinct term description, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_DESCRIPTION).",
    "- \"instructors\" -> COURSE_CATALOG_SUBJECT_OFFERED.FALL_INSTRUCTORS: already repeats ~2.9 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct instructors, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.FALL_INSTRUCTORS).",
    "- \"instructors\" -> COURSE_CATALOG_SUBJECT_OFFERED.SPRING_INSTRUCTORS: already repeats ~2.9 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct instructors, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SPRING_INSTRUCTORS)."
  ],
  "dw_real_67": [
    "- Join CIS_COURSE_CATALOG.SUBJECT_CODE = SIS_SUBJECT_CODE.SUBJECT_CODE: CIS_COURSE_CATALOG.SUBJECT_CODE repeats ~243.9 rows per value (10000 rows, 41 values); SIS_SUBJECT_CODE.SUBJECT_CODE is unique per row (221 rows).",
    "  -> each SIS_SUBJECT_CODE row appears ~243.9 times after this join.",
    "- Join CIS_COURSE_CATALOG.SUBJECT_ID = SUBJECT_OFFERED_SUMMARY.SUBJECT_ID: CIS_COURSE_CATALOG.SUBJECT_ID repeats ~4.4 rows per value (10000 rows, 2269 values); SUBJECT_OFFERED_SUMMARY.SUBJECT_ID repeats ~2.4 rows per value (10000 rows, 4122 values).",
    "  -> each CIS_COURSE_CATALOG row appears ~2.4 times after this join.",
    "  -> each SUBJECT_OFFERED_SUMMARY row appears ~4.4 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SIS_SUBJECT_CODE.DEPARTMENT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SIS_SUBJECT_CODE.DEPARTMENT_CODE repeats ~3.6 rows per value (221 rows, 61 values).",
    "  -> each SIS_DEPARTMENT row appears ~3.6 times after this join.",
    "- \"Political Science\" -> SIS_DEPARTMENT.DEPARTMENT_NAME: unique per row in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x3.6, so a plain COUNT over the joined result counts duplicates. To count distinct Political Science, use COUNT(DISTINCT SIS_DEPARTMENT.DEPARTMENT_NAME).",
    "- \"name\" -> CIS_COURSE_CATALOG.HASS_ATTRIBUTE: already repeats ~10000.0 rows per value in its own table; the hinted joins multiply CIS_COURSE_CATALOG rows ~x2.4, so a plain COUNT over the joined result counts duplicates. To count distinct name, use COUNT(DISTINCT CIS_COURSE_CATALOG.HASS_ATTRIBUTE).",
    "- \"description\" -> CIS_COURSE_CATALOG.HASS_ATTRIBUTE_DESC: already repeats ~10000.0 rows per value in its own table; the hinted joins multiply CIS_COURSE_CATALOG rows ~x2.4, so a plain COUNT over the joined result counts duplicates. To count distinct description, use COUNT(DISTINCT CIS_COURSE_CATALOG.HASS_ATTRIBUTE_DESC).",
    "- \"subjects\" -> CIS_COURSE_CATALOG.SUBJECT_ID: already repeats ~4.4 rows per value in its own table; the hinted joins multiply CIS_COURSE_CATALOG rows ~x2.4, so a plain COUNT over the joined result counts duplicates. To count distinct subjects, use COUNT(DISTINCT CIS_COURSE_CATALOG.SUBJECT_ID).",
    "- \"units\" -> CIS_COURSE_CATALOG.TOTAL_UNITS is a numeric measure of CIS_COURSE_CATALOG; the hinted joins multiply CIS_COURSE_CATALOG rows ~x2.4, so SUM/AVG over the joined result would count each CIS_COURSE_CATALOG row ~2.4 times. Aggregate it at CIS_COURSE_CATALOG's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"enrollment\" -> SUBJECT_OFFERED_SUMMARY.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x4.4, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~4.4 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"departments that grant degrees\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x3.6, so a plain COUNT over the joined result counts duplicates. To count distinct departments that grant degrees, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"subject code description\" -> SIS_SUBJECT_CODE.SUBJECT_CODE_DESC: already repeats ~1.2 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x243.9, so a plain COUNT over the joined result counts duplicates. To count distinct subject code description, use COUNT(DISTINCT SIS_SUBJECT_CODE.SUBJECT_CODE_DESC)."
  ],
  "dw_real_68": [
    "- Join SIS_COURSE_DESCRIPTION.COURSE = SUBJECT_OFFERED_SUMMARY.COURSE_NUMBER: SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values); SUBJECT_OFFERED_SUMMARY.COURSE_NUMBER repeats ~90.9 rows per value (10000 rows, 110 values).",
    "  -> each SIS_COURSE_DESCRIPTION row appears ~90.9 times after this join.",
    "  -> each SUBJECT_OFFERED_SUMMARY row appears ~1.1 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_CODE repeats ~196.1 rows per value (10000 rows, 51 values).",
    "  -> each SIS_DEPARTMENT row appears ~196.1 times after this join.",
    "- \"cluster type\" -> SUBJECT_OFFERED_SUMMARY.CLUSTER_TYPE: already repeats ~3333.3 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct cluster type, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.CLUSTER_TYPE).",
    "- \"name of the department\" -> SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_NAME: already repeats ~200.0 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct name of the department, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.OFFER_DEPT_NAME).",
    "- \"school name\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_DEPARTMENT.SCHOOL_NAME).",
    "- \"whether the department grants degrees\" -> SIS_DEPARTMENT.IS_DEGREE_GRANTING: already repeats ~64.0 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x196.1, so a plain COUNT over the joined result counts duplicates. To count distinct whether the department grants degrees, use COUNT(DISTINCT SIS_DEPARTMENT.IS_DEGREE_GRANTING).",
    "- \"subjects\" -> SUBJECT_OFFERED_SUMMARY.SUBJECT_ID: already repeats ~2.4 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct subjects, use COUNT(DISTINCT SUBJECT_OFFERED_SUMMARY.SUBJECT_ID).",
    "- \"enrollment\" -> SUBJECT_OFFERED_SUMMARY.NUM_ENROLLED_STUDENTS is a numeric measure of SUBJECT_OFFERED_SUMMARY; the hinted joins multiply SUBJECT_OFFERED_SUMMARY rows ~x1.1, so SUM/AVG over the joined result would count each SUBJECT_OFFERED_SUMMARY row ~1.1 times. Aggregate it at SUBJECT_OFFERED_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_real_69": [
    "- Join FAC_BUILDING.FAC_BUILDING_KEY = SUBJECT_OFFERED.MEET_PLACE: FAC_BUILDING.FAC_BUILDING_KEY is unique per row (242 rows); SUBJECT_OFFERED.MEET_PLACE repeats ~10000.0 rows per value (10000 rows, 0 values).",
    "  -> each FAC_BUILDING row appears ~10000.0 times after this join.",
    "- Join SIS_COURSE_DESCRIPTION.COURSE = SUBJECT_OFFERED.COURSE_NUMBER: SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values); SUBJECT_OFFERED.COURSE_NUMBER repeats ~10000.0 rows per value (10000 rows, 1 values).",
    "  -> each SIS_COURSE_DESCRIPTION row appears ~10000.0 times after this join.",
    "  -> each SUBJECT_OFFERED row appears ~1.1 times after this join.",
    "- Join FAC_BUILDING.FAC_BUILDING_KEY = FAC_BUILDING_ADDRESS.BUILDING_KEY: FAC_BUILDING.FAC_BUILDING_KEY is unique per row (242 rows); FAC_BUILDING_ADDRESS.BUILDING_KEY repeats ~3.2 rows per value (785 rows, 242 values).",
    "  -> each FAC_BUILDING row appears ~3.2 times after this join.",
    "- \"buildings at MIT\" -> FAC_BUILDING.SITE: already repeats ~24.2 rows per value in its own table; the hinted joins multiply FAC_BUILDING rows ~x10000.0, so a plain COUNT over the joined result counts duplicates. To count distinct buildings at MIT, use COUNT(DISTINCT FAC_BUILDING.SITE).",
    "- \"name of the building\" -> FAC_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins multiply FAC_BUILDING rows ~x10000.0, so a plain COUNT over the joined result counts duplicates. To count distinct name of the building, use COUNT(DISTINCT FAC_BUILDING.BUILDING_NAME).",
    "- \"course level\" -> SIS_COURSE_DESCRIPTION.COURSE_LEVEL: already repeats ~347.5 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x10000.0, so a plain COUNT over the joined result counts duplicates. To count distinct course level, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.COURSE_LEVEL).",
    "- \"courses\" -> SUBJECT_OFFERED_SUMMARY.SUBJECT_ID: already repeats ~2.4 rows per value in its own table; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows.",
    "- \"instructors\" -> SUBJECT_OFFERED_SUMMARY.RESPONSIBLE_FACULTY_NAME: already repeats ~26.5 rows per value in its own table; the hinted joins do not multiply SUBJECT_OFFERED_SUMMARY rows."
  ],
  "dw_real_7": [
    "- Join EMPLOYEE_DIRECTORY.OFFICE_LOCATION = FCLT_ROOMS.BUILDING_ROOM: EMPLOYEE_DIRECTORY.OFFICE_LOCATION repeats ~1.9 rows per value (10000 rows, 5396 values); FCLT_ROOMS.BUILDING_ROOM is unique per row (10000 rows).",
    "  -> each FCLT_ROOMS row appears ~1.9 times after this join.",
    "- \"long names of buildings\" -> FCLT_BUILDING_HIST.BUILDING_NAME_LONG: already repeats ~37.3 rows per value in its own table; the hinted joins do not multiply FCLT_BUILDING_HIST rows.",
    "- \"built year\" -> FCLT_BUILDING_HIST.DATE_BUILT: already repeats ~98.0 rows per value in its own table; the hinted joins do not multiply FCLT_BUILDING_HIST rows.",
    "- \"employees\" -> EMPLOYEE_DIRECTORY.MIT_ID: unique per row in its own table; the hinted joins do not multiply EMPLOYEE_DIRECTORY rows."
  ],
  "dw_real_70": [
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.MEET_PLACE = FAC_ROOMS.FAC_ROOM_KEY: COURSE_CATALOG_SUBJECT_OFFERED.MEET_PLACE repeats ~18.1 rows per value (10000 rows, 553 values); FAC_ROOMS.FAC_ROOM_KEY is unique per row (10000 rows).",
    "  -> each FAC_ROOMS row appears ~18.1 times after this join.",
    "- Join FAC_BUILDING.FAC_BUILDING_KEY = FAC_ROOMS.BUILDING_KEY: FAC_BUILDING.FAC_BUILDING_KEY is unique per row (242 rows); FAC_ROOMS.BUILDING_KEY repeats ~53.2 rows per value (10000 rows, 188 values).",
    "  -> each FAC_BUILDING row appears ~53.2 times after this join.",
    "- Join FAC_MAJOR_USE.MAJOR_USE_KEY = FAC_ROOMS.MAJOR_USE_KEY: FAC_MAJOR_USE.MAJOR_USE_KEY is unique per row (14 rows); FAC_ROOMS.MAJOR_USE_KEY repeats ~1428.6 rows per value (10000 rows, 7 values).",
    "  -> each FAC_MAJOR_USE row appears ~1428.6 times after this join.",
    "- Join FAC_BUILDING.FAC_BUILDING_KEY = FAC_BUILDING_ADDRESS.BUILDING_KEY: FAC_BUILDING.FAC_BUILDING_KEY is unique per row (242 rows); FAC_BUILDING_ADDRESS.BUILDING_KEY repeats ~3.2 rows per value (785 rows, 242 values).",
    "  -> each FAC_BUILDING row appears ~3.2 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID = SIS_COURSE_DESCRIPTION.COURSE: COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID repeats ~2.0 rows per value (10000 rows, 4969 values); SIS_COURSE_DESCRIPTION.COURSE repeats ~1.1 rows per value (695 rows, 630 values).",
    "  -> each COURSE_CATALOG_SUBJECT_OFFERED row appears ~1.1 times after this join.",
    "  -> each SIS_COURSE_DESCRIPTION row appears ~2.0 times after this join.",
    "- \"room number\" -> COURSE_CATALOG_SUBJECT_OFFERED.MEET_PLACE: already repeats ~18.1 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct room number, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.MEET_PLACE).",
    "- \"building name\" -> FAC_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins multiply FAC_BUILDING rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct building name, use COUNT(DISTINCT FAC_BUILDING.BUILDING_NAME).",
    "- \"building number\" -> FAC_BUILDING.BUILDING_NUMBER: unique per row in its own table; the hinted joins multiply FAC_BUILDING rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct building number, use COUNT(DISTINCT FAC_BUILDING.BUILDING_NUMBER).",
    "- \"building city\" -> FAC_BUILDING_ADDRESS.CITY: already repeats ~71.4 rows per value in its own table; the hinted joins do not multiply FAC_BUILDING_ADDRESS rows.",
    "- \"building state\" -> FAC_BUILDING_ADDRESS.STATE: already repeats ~392.5 rows per value in its own table; the hinted joins do not multiply FAC_BUILDING_ADDRESS rows.",
    "- \"area\" -> FAC_ROOMS.AREA is a numeric measure of FAC_ROOMS; the hinted joins multiply FAC_ROOMS rows ~x18.1, so SUM/AVG over the joined result would count each FAC_ROOMS row ~18.1 times. Aggregate it at FAC_ROOMS's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"organization name\" -> FAC_ROOMS.ORGANIZATION_NAME: already repeats ~1111.1 rows per value in its own table; the hinted joins multiply FAC_ROOMS rows ~x18.1, so a plain COUNT over the joined result counts duplicates. To count distinct organization name, use COUNT(DISTINCT FAC_ROOMS.ORGANIZATION_NAME).",
    "- \"room usage\" -> FAC_MAJOR_USE.MAJOR_USE: unique per row in its own table; the hinted joins multiply FAC_MAJOR_USE rows ~x1428.6, so a plain COUNT over the joined result counts duplicates. To count distinct room usage, use COUNT(DISTINCT FAC_MAJOR_USE.MAJOR_USE).",
    "- \"term code\" -> COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE: already repeats ~105.3 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct term code, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.TERM_CODE).",
    "- \"course level\" -> SIS_COURSE_DESCRIPTION.COURSE_LEVEL: already repeats ~347.5 rows per value in its own table; the hinted joins multiply SIS_COURSE_DESCRIPTION rows ~x2.0, so a plain COUNT over the joined result counts duplicates. To count distinct course level, use COUNT(DISTINCT SIS_COURSE_DESCRIPTION.COURSE_LEVEL).",
    "- \"subjects\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct subjects, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID).",
    "- \"meeting times\" -> COURSE_CATALOG_SUBJECT_OFFERED.MEET_TIME: already repeats ~12.2 rows per value in its own table; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct meeting times, use COUNT(DISTINCT COURSE_CATALOG_SUBJECT_OFFERED.MEET_TIME).",
    "- \"units\" -> COURSE_CATALOG_SUBJECT_OFFERED.TOTAL_UNITS is a numeric measure of COURSE_CATALOG_SUBJECT_OFFERED; the hinted joins multiply COURSE_CATALOG_SUBJECT_OFFERED rows ~x1.1, so SUM/AVG over the joined result would count each COURSE_CATALOG_SUBJECT_OFFERED row ~1.1 times. Aggregate it at COURSE_CATALOG_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_real_71": [
    "- Join LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY: LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS_KEY is unique per row (6 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY repeats ~2000.0 rows per value (10000 rows, 5 values).",
    "  -> each LIBRARY_MATERIAL_STATUS row appears ~2000.0 times after this join.",
    "- Join LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY repeats ~1.2 rows per value (10000 rows, 8268 values).",
    "  -> each LIBRARY_RESERVE_CATALOG row appears ~1.2 times after this join.",
    "- Join LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY = LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY: LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY repeats ~6.5 rows per value (10000 rows, 1550 values); LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY is unique per row (10000 rows).",
    "  -> each LIBRARY_SUBJECT_OFFERED row appears ~6.5 times after this join.",
    "- Join LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER = SIS_SUBJECT_CODE.COURSE_NUMBER: LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER repeats ~625.0 rows per value (10000 rows, 16 values); SIS_SUBJECT_CODE.COURSE_NUMBER is unique per row (221 rows).",
    "  -> each SIS_SUBJECT_CODE row appears ~625.0 times after this join.",
    "- \"cataloged on or after 2000\" -> LIBRARY_RESERVE_CATALOG.CATALOG_YEAR: already repeats ~111.1 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_CATALOG rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct cataloged on or after 2000, use COUNT(DISTINCT LIBRARY_RESERVE_CATALOG.CATALOG_YEAR).",
    "- \"material status\" -> LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS: already repeats ~1.2 rows per value in its own table; the hinted joins multiply LIBRARY_MATERIAL_STATUS rows ~x2000.0, so a plain COUNT over the joined result counts duplicates. To count distinct material status, use COUNT(DISTINCT LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS).",
    "- \"department name\" -> SIS_SUBJECT_CODE.DEPARTMENT_NAME: already repeats ~3.7 rows per value in its own table; the hinted joins multiply SIS_SUBJECT_CODE rows ~x625.0, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT SIS_SUBJECT_CODE.DEPARTMENT_NAME).",
    "- \"catalog items\" -> LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY: unique per row in its own table; the hinted joins multiply LIBRARY_RESERVE_CATALOG rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct catalog items, use COUNT(DISTINCT LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY).",
    "- \"enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so SUM/AVG over the joined result would count each LIBRARY_SUBJECT_OFFERED row ~6.5 times. Aggregate it at LIBRARY_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_real_72": [
    "- Join LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY: LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY repeats ~6.2 rows per value (10000 rows, 1614 values).",
    "  -> each LIBRARY_COURSE_INSTRUCTOR row appears ~6.2 times after this join.",
    "- Join LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY repeats ~1.2 rows per value (10000 rows, 8268 values).",
    "  -> each LIBRARY_RESERVE_CATALOG row appears ~1.2 times after this join.",
    "- Join LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY = LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY: LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY repeats ~6.5 rows per value (10000 rows, 1550 values); LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY is unique per row (10000 rows).",
    "  -> each LIBRARY_SUBJECT_OFFERED row appears ~6.5 times after this join.",
    "- \"name of the department\" -> LIBRARY_COURSE_INSTRUCTOR.DEPARTMENT: already repeats ~78.1 rows per value in its own table; the hinted joins multiply LIBRARY_COURSE_INSTRUCTOR rows ~x6.2, so a plain COUNT over the joined result counts duplicates. To count distinct name of the department, use COUNT(DISTINCT LIBRARY_COURSE_INSTRUCTOR.DEPARTMENT).",
    "- \"courses\" -> LIBRARY_COURSE_INSTRUCTOR.COURSE_NAME: already repeats ~3.3 rows per value in its own table; the hinted joins multiply LIBRARY_COURSE_INSTRUCTOR rows ~x6.2, so a plain COUNT over the joined result counts duplicates. To count distinct courses, use COUNT(DISTINCT LIBRARY_COURSE_INSTRUCTOR.COURSE_NAME).",
    "- \"enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so SUM/AVG over the joined result would count each LIBRARY_SUBJECT_OFFERED row ~6.5 times. Aggregate it at LIBRARY_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"catalog items\" -> LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY: unique per row in its own table; the hinted joins multiply LIBRARY_RESERVE_CATALOG rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct catalog items, use COUNT(DISTINCT LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY).",
    "- \"enrollment\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so SUM/AVG over the joined result would count each LIBRARY_SUBJECT_OFFERED row ~6.5 times. Aggregate it at LIBRARY_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_real_73": [
    "- Join TIP_DETAIL.TIP_MATERIAL_KEY = TIP_MATERIAL.TIP_MATERIAL_KEY: TIP_DETAIL.TIP_MATERIAL_KEY repeats ~1.4 rows per value (10000 rows, 7360 values); TIP_MATERIAL.TIP_MATERIAL_KEY is unique per row (10000 rows).",
    "  -> each TIP_MATERIAL row appears ~1.4 times after this join.",
    "- Join TIP_DETAIL.TIP_SUBJECT_OFFERED_KEY = TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY: TIP_DETAIL.TIP_SUBJECT_OFFERED_KEY repeats ~1.4 rows per value (10000 rows, 7340 values); TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY is unique per row (10000 rows).",
    "  -> each TIP_SUBJECT_OFFERED row appears ~1.4 times after this join.",
    "- Join TIP_DETAIL.TIP_MATERIAL_STATUS_KEY = TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS_KEY: TIP_DETAIL.TIP_MATERIAL_STATUS_KEY repeats ~833.3 rows per value (10000 rows, 12 values); TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS_KEY is unique per row (12 rows).",
    "  -> each TIP_MATERIAL_STATUS row appears ~833.3 times after this join.",
    "- Join SIS_DEPARTMENT.DEPARTMENT_CODE = TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE: SIS_DEPARTMENT.DEPARTMENT_CODE is unique per row (128 rows); TIP_SUBJECT_OFFERED.OFFER_DEPT_CODE repeats ~161.3 rows per value (10000 rows, 62 values).",
    "  -> each SIS_DEPARTMENT row appears ~161.3 times after this join.",
    "- \"courses with materials\" -> TIP_SUBJECT_OFFERED.IS_NO_COURSE_MATERIAL: already repeats ~5000.0 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct courses with materials, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.IS_NO_COURSE_MATERIAL).",
    "- \"department name\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"school name\" -> SIS_DEPARTMENT.SCHOOL_NAME: already repeats ~11.6 rows per value in its own table; the hinted joins multiply SIS_DEPARTMENT rows ~x161.3, so a plain COUNT over the joined result counts duplicates. To count distinct school name, use COUNT(DISTINCT SIS_DEPARTMENT.SCHOOL_NAME).",
    "- \"course materials\" -> TIP_MATERIAL.TITLE: already repeats ~1.6 rows per value in its own table; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct course materials, use COUNT(DISTINCT TIP_MATERIAL.TITLE).",
    "- \"courses\" -> TIP_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.4 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct courses, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.SUBJECT_TITLE).",
    "- \"new\" -> TIP_MATERIAL.NEW_SHELF_PRICE is a numeric measure of TIP_MATERIAL; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so SUM/AVG over the joined result would count each TIP_MATERIAL row ~1.4 times. Aggregate it at TIP_MATERIAL's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"used\" -> TIP_MATERIAL.USED_SHELF_PRICE is a numeric measure of TIP_MATERIAL; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so SUM/AVG over the joined result would count each TIP_MATERIAL row ~1.4 times. Aggregate it at TIP_MATERIAL's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"material records\" -> TIP_DETAIL.RECORD_COUNT is a numeric measure of TIP_DETAIL; the hinted joins do not multiply TIP_DETAIL rows.",
    "- \"material statuses\" -> TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS: already repeats ~1.5 rows per value in its own table; the hinted joins multiply TIP_MATERIAL_STATUS rows ~x833.3, so a plain COUNT over the joined result counts duplicates. To count distinct material statuses, use COUNT(DISTINCT TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS)."
  ],
  "dw_real_74": [
    "- Join TIP_DETAIL.TIP_MATERIAL_STATUS_KEY = TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS_KEY: TIP_DETAIL.TIP_MATERIAL_STATUS_KEY repeats ~833.3 rows per value (10000 rows, 12 values); TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS_KEY is unique per row (12 rows).",
    "  -> each TIP_MATERIAL_STATUS row appears ~833.3 times after this join.",
    "- Join TIP_DETAIL.TIP_MATERIAL_KEY = TIP_MATERIAL.TIP_MATERIAL_KEY: TIP_DETAIL.TIP_MATERIAL_KEY repeats ~1.4 rows per value (10000 rows, 7360 values); TIP_MATERIAL.TIP_MATERIAL_KEY is unique per row (10000 rows).",
    "  -> each TIP_MATERIAL row appears ~1.4 times after this join.",
    "- Join TIP_DETAIL.TIP_SUBJECT_OFFERED_KEY = TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY: TIP_DETAIL.TIP_SUBJECT_OFFERED_KEY repeats ~1.4 rows per value (10000 rows, 7340 values); TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY is unique per row (10000 rows).",
    "  -> each TIP_SUBJECT_OFFERED row appears ~1.4 times after this join.",
    "- \"TIP material status\" -> TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS: already repeats ~1.5 rows per value in its own table; the hinted joins multiply TIP_MATERIAL_STATUS rows ~x833.3, so a plain COUNT over the joined result counts duplicates. To count distinct TIP material status, use COUNT(DISTINCT TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS).",
    "- \"materials\" -> TIP_MATERIAL.TITLE: already repeats ~1.6 rows per value in its own table; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct materials, use COUNT(DISTINCT TIP_MATERIAL.TITLE).",
    "- \"records\" -> TIP_DETAIL.RECORD_COUNT is a numeric measure of TIP_DETAIL; the hinted joins do not multiply TIP_DETAIL rows.",
    "- \"student enrollment\" -> TIP_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of TIP_SUBJECT_OFFERED; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x1.4, so SUM/AVG over the joined result would count each TIP_SUBJECT_OFFERED row ~1.4 times. Aggregate it at TIP_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_real_75": [
    "- Join FCLT_FLOOR.FCLT_FLOOR_KEY = FCLT_ROOMS.FCLT_FLOOR_KEY: FCLT_FLOOR.FCLT_FLOOR_KEY is unique per row (1079 rows); FCLT_ROOMS.FCLT_FLOOR_KEY repeats ~10.8 rows per value (10000 rows, 925 values).",
    "  -> each FCLT_FLOOR row appears ~10.8 times after this join.",
    "- Join FCLT_BUILDING.FCLT_BUILDING_KEY = FCLT_FLOOR.FCLT_BUILDING_KEY: FCLT_BUILDING.FCLT_BUILDING_KEY is unique per row (242 rows); FCLT_FLOOR.FCLT_BUILDING_KEY repeats ~4.5 rows per value (1079 rows, 239 values).",
    "  -> each FCLT_BUILDING row appears ~4.5 times after this join.",
    "- Join FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY = FCLT_ROOMS.FCLT_ORGANIZATION_KEY: FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY repeats ~1.1 rows per value (180 rows, 168 values); FCLT_ROOMS.FCLT_ORGANIZATION_KEY repeats ~79.4 rows per value (10000 rows, 126 values).",
    "  -> each FCLT_ORGANIZATION row appears ~79.4 times after this join.",
    "  -> each FCLT_ROOMS row appears ~1.1 times after this join.",
    "- \"room\" -> FCLT_ROOMS.FCLT_ROOM_KEY: unique per row in its own table; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct room, use COUNT(DISTINCT FCLT_ROOMS.FCLT_ROOM_KEY).",
    "- \"building name\" -> FCLT_BUILDING.BUILDING_NAME_LONG: unique per row in its own table; the hinted joins multiply FCLT_BUILDING rows ~x4.5, so a plain COUNT over the joined result counts duplicates. To count distinct building name, use COUNT(DISTINCT FCLT_BUILDING.BUILDING_NAME_LONG).",
    "- \"floor number\" -> FCLT_FLOOR.FLOOR: already repeats ~21.2 rows per value in its own table; the hinted joins multiply FCLT_FLOOR rows ~x10.8, so a plain COUNT over the joined result counts duplicates. To count distinct floor number, use COUNT(DISTINCT FCLT_FLOOR.FLOOR).",
    "- \"room number\" -> FCLT_ROOMS.BUILDING_ROOM: unique per row in its own table; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct room number, use COUNT(DISTINCT FCLT_ROOMS.BUILDING_ROOM).",
    "- \"ownership type\" -> FCLT_BUILDING.OWNERSHIP_TYPE: already repeats ~121.0 rows per value in its own table; the hinted joins multiply FCLT_BUILDING rows ~x4.5, so a plain COUNT over the joined result counts duplicates. To count distinct ownership type, use COUNT(DISTINCT FCLT_BUILDING.OWNERSHIP_TYPE).",
    "- \"organization name\" -> FCLT_ORGANIZATION.ORGANIZATION_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct organization name, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_NAME).",
    "- \"number of rooms\" -> FCLT_ROOMS.FCLT_ROOM_KEY: unique per row in its own table; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct number of rooms, use COUNT(DISTINCT FCLT_ROOMS.FCLT_ROOM_KEY).",
    "- \"area\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so SUM/AVG over the joined result would count each FCLT_ROOMS row ~1.1 times. Aggregate it at FCLT_ROOMS's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_real_76": [
    "- Join ACADEMIC_TERMS.TERM_START_DATE = TIME_DAY.CALENDAR_DATE: ACADEMIC_TERMS.TERM_START_DATE is unique per row (144 rows); TIME_DAY.CALENDAR_DATE is unique per row (10000 rows).",
    "- Join TIME_DAY.FISCAL_PERIOD = TIME_MONTH.FISCAL_PERIOD: TIME_DAY.FISCAL_PERIOD repeats ~9.7 rows per value (10000 rows, 1026 values); TIME_MONTH.FISCAL_PERIOD is unique per row (640 rows).",
    "  -> each TIME_MONTH row appears ~9.7 times after this join.",
    "- Join TIME_MONTH.FISCAL_YEAR = TIME_QUARTER.FISCAL_YEAR: TIME_MONTH.FISCAL_YEAR repeats ~16.0 rows per value (640 rows, 40 values); TIME_QUARTER.FISCAL_YEAR repeats ~4.0 rows per value (144 rows, 36 values).",
    "  -> each TIME_MONTH row appears ~4.0 times after this join.",
    "  -> each TIME_QUARTER row appears ~16.0 times after this join.",
    "- Join ACADEMIC_TERMS.TERM_CODE = ACADEMIC_TERM_PARAMETER.TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); ACADEMIC_TERM_PARAMETER.TERM_CODE is unique per row (3 rows).",
    "- \"financial aid year\" -> ACADEMIC_TERMS.FINANCIAL_AID_YEAR: already repeats ~3.9 rows per value in its own table; the hinted joins do not multiply ACADEMIC_TERMS rows.",
    "- \"academic year\" -> ACADEMIC_TERMS.ACADEMIC_YEAR: already repeats ~4.0 rows per value in its own table; the hinted joins do not multiply ACADEMIC_TERMS rows.",
    "- \"fiscal periods\" -> TIME_MONTH.FISCAL_PERIOD: unique per row in its own table; the hinted joins multiply TIME_MONTH rows ~x9.7, so a plain COUNT over the joined result counts duplicates. To count distinct fiscal periods, use COUNT(DISTINCT TIME_MONTH.FISCAL_PERIOD).",
    "- \"quarters\" -> TIME_QUARTER.FY_QUARTER_CODE: unique per row in its own table; the hinted joins multiply TIME_QUARTER rows ~x16.0, so a plain COUNT over the joined result counts duplicates. To count distinct quarters, use COUNT(DISTINCT TIME_QUARTER.FY_QUARTER_CODE).",
    "- \"start term date\" -> TIME_DAY.START_DATE: already repeats ~9.7 rows per value in its own table; the hinted joins do not multiply TIME_DAY rows.",
    "- \"end term date\" -> TIME_DAY.END_DATE: already repeats ~9.7 rows per value in its own table; the hinted joins do not multiply TIME_DAY rows.",
    "- \"department-level term parameters\" -> ACADEMIC_TERM_PARAMETER.TERM_PARAMETER: unique per row in its own table; the hinted joins do not multiply ACADEMIC_TERM_PARAMETER rows."
  ],
  "dw_real_77": [
    "- Join FCLT_BUILDING.FCLT_BUILDING_KEY = FCLT_FLOOR.FCLT_BUILDING_KEY: FCLT_BUILDING.FCLT_BUILDING_KEY is unique per row (242 rows); FCLT_FLOOR.FCLT_BUILDING_KEY repeats ~4.5 rows per value (1079 rows, 239 values).",
    "  -> each FCLT_BUILDING row appears ~4.5 times after this join.",
    "- Join FCLT_BUILDING.FCLT_BUILDING_KEY = FCLT_BUILDING_ADDRESS.FCLT_BUILDING_KEY: FCLT_BUILDING.FCLT_BUILDING_KEY is unique per row (242 rows); FCLT_BUILDING_ADDRESS.FCLT_BUILDING_KEY repeats ~3.2 rows per value (785 rows, 242 values).",
    "  -> each FCLT_BUILDING row appears ~3.2 times after this join.",
    "- Join FCLT_BUILDING.(FCLT_BUILDING_KEY,FCLT_BUILDING_KEY) = FCLT_ROOMS.(FCLT_BUILDING_KEY,FCLT_BUILDING_KEY): FCLT_BUILDING.(FCLT_BUILDING_KEY,FCLT_BUILDING_KEY) is unique per row (242 rows); FCLT_ROOMS.(FCLT_BUILDING_KEY,FCLT_BUILDING_KEY) repeats ~49.5 rows per value (10000 rows, 202 values).",
    "  -> each FCLT_BUILDING row appears ~49.5 times after this join.",
    "- Join FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY = FCLT_ROOMS.FCLT_ORGANIZATION_KEY: FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY repeats ~1.1 rows per value (180 rows, 168 values); FCLT_ROOMS.FCLT_ORGANIZATION_KEY repeats ~79.4 rows per value (10000 rows, 126 values).",
    "  -> each FCLT_ORGANIZATION row appears ~79.4 times after this join.",
    "  -> each FCLT_ROOMS row appears ~1.1 times after this join.",
    "- \"campus sector\" -> FCLT_BUILDING.CAMPUS_SECTOR: already repeats ~30.2 rows per value in its own table; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct campus sector, use COUNT(DISTINCT FCLT_BUILDING.CAMPUS_SECTOR).",
    "- \"name of the building\" -> FCLT_BUILDING.BUILDING_NAME_LONG: unique per row in its own table; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct name of the building, use COUNT(DISTINCT FCLT_BUILDING.BUILDING_NAME_LONG).",
    "- \"city\" -> FCLT_BUILDING_ADDRESS.CITY: already repeats ~71.4 rows per value in its own table; the hinted joins do not multiply FCLT_BUILDING_ADDRESS rows.",
    "- \"state\" -> FCLT_BUILDING_ADDRESS.STATE: already repeats ~392.5 rows per value in its own table; the hinted joins do not multiply FCLT_BUILDING_ADDRESS rows.",
    "- \"floors\" -> FCLT_FLOOR.FLOOR: already repeats ~21.2 rows per value in its own table; the hinted joins do not multiply FCLT_FLOOR rows.",
    "- \"area\" -> FCLT_BUILDING.ASSIGNABLE_AREA is a numeric measure of FCLT_BUILDING; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so SUM/AVG over the joined result would count each FCLT_BUILDING row ~49.5 times. Aggregate it at FCLT_BUILDING's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"rooms\" -> FCLT_ROOMS.FCLT_ROOM_KEY: unique per row in its own table; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct rooms, use COUNT(DISTINCT FCLT_ROOMS.FCLT_ROOM_KEY).",
    "- \"organizations\" -> FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct organizations, use COUNT(DISTINCT FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY).",
    "- \"ownership type\" -> FCLT_BUILDING.OWNERSHIP_TYPE: already repeats ~121.0 rows per value in its own table; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct ownership type, use COUNT(DISTINCT FCLT_BUILDING.OWNERSHIP_TYPE)."
  ],
  "dw_real_78": [
    "- Join TIP_DETAIL.TIP_MATERIAL_KEY = TIP_MATERIAL.TIP_MATERIAL_KEY: TIP_DETAIL.TIP_MATERIAL_KEY repeats ~1.4 rows per value (10000 rows, 7360 values); TIP_MATERIAL.TIP_MATERIAL_KEY is unique per row (10000 rows).",
    "  -> each TIP_MATERIAL row appears ~1.4 times after this join.",
    "- Join TIP_DETAIL.TIP_SUBJECT_OFFERED_KEY = TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY: TIP_DETAIL.TIP_SUBJECT_OFFERED_KEY repeats ~1.4 rows per value (10000 rows, 7340 values); TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY is unique per row (10000 rows).",
    "  -> each TIP_SUBJECT_OFFERED row appears ~1.4 times after this join.",
    "- Join LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: LIBRARY_RESERVE_CATALOG.LIBRARY_RESERVE_CATALOG_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY repeats ~1.2 rows per value (10000 rows, 8268 values).",
    "  -> each LIBRARY_RESERVE_CATALOG row appears ~1.2 times after this join.",
    "- Join LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY = LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY: LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY repeats ~6.5 rows per value (10000 rows, 1550 values); LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY is unique per row (10000 rows).",
    "  -> each LIBRARY_SUBJECT_OFFERED row appears ~6.5 times after this join.",
    "- Join LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY: LIBRARY_COURSE_INSTRUCTOR.LIBRARY_COURSE_INSTRUCTOR_KEY is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_COURSE_INSTRUCTOR_KEY repeats ~6.2 rows per value (10000 rows, 1614 values).",
    "  -> each LIBRARY_COURSE_INSTRUCTOR row appears ~6.2 times after this join.",
    "- \"name of the department\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct name of the department, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"title of the TIP material\" -> TIP_MATERIAL.TITLE: already repeats ~1.6 rows per value in its own table; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct title of the TIP material, use COUNT(DISTINCT TIP_MATERIAL.TITLE).",
    "- \"author\" -> TIP_MATERIAL.AUTHOR: already repeats ~1.8 rows per value in its own table; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct author, use COUNT(DISTINCT TIP_MATERIAL.AUTHOR).",
    "- \"ISBN\" -> TIP_MATERIAL.ISBN: already repeats ~1.5 rows per value in its own table; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct ISBN, use COUNT(DISTINCT TIP_MATERIAL.ISBN).",
    "- \"library term code\" -> LIBRARY_RESERVE_MATRL_DETAIL.TERM_CODE: already repeats ~1250.0 rows per value in its own table; the hinted joins do not multiply LIBRARY_RESERVE_MATRL_DETAIL rows.",
    "- \"available in the library reserves\" -> LIBRARY_RESERVE_CATALOG.CATALOG_ISBN: already repeats ~2.6 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_CATALOG rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct available in the library reserves, use COUNT(DISTINCT LIBRARY_RESERVE_CATALOG.CATALOG_ISBN).",
    "- \"instructors\" -> LIBRARY_COURSE_INSTRUCTOR.INSTRUCTOR_NAME: unique per row in its own table; the hinted joins multiply LIBRARY_COURSE_INSTRUCTOR rows ~x6.2, so a plain COUNT over the joined result counts duplicates. To count distinct instructors, use COUNT(DISTINCT LIBRARY_COURSE_INSTRUCTOR.INSTRUCTOR_NAME)."
  ],
  "dw_real_79": [
    "- Join TIP_DETAIL.TIP_SUBJECT_OFFERED_KEY = TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY: TIP_DETAIL.TIP_SUBJECT_OFFERED_KEY repeats ~1.4 rows per value (10000 rows, 7340 values); TIP_SUBJECT_OFFERED.TIP_SUBJECT_OFFERED_KEY is unique per row (10000 rows).",
    "  -> each TIP_SUBJECT_OFFERED row appears ~1.4 times after this join.",
    "- Join TIP_DETAIL.TIP_MATERIAL_STATUS_KEY = TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS_KEY: TIP_DETAIL.TIP_MATERIAL_STATUS_KEY repeats ~833.3 rows per value (10000 rows, 12 values); TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS_KEY is unique per row (12 rows).",
    "  -> each TIP_MATERIAL_STATUS row appears ~833.3 times after this join.",
    "- Join LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY = LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY: LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_SUBJECT_OFFERED_KEY repeats ~6.5 rows per value (10000 rows, 1550 values); LIBRARY_SUBJECT_OFFERED.LIBRARY_SUBJECT_OFFERED_KEY is unique per row (10000 rows).",
    "  -> each LIBRARY_SUBJECT_OFFERED row appears ~6.5 times after this join.",
    "- Join LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS_KEY = LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY: LIBRARY_MATERIAL_STATUS.LIBRARY_MATERIAL_STATUS_KEY is unique per row (6 rows); LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_MATERIAL_STATUS_KEY repeats ~2000.0 rows per value (10000 rows, 5 values).",
    "  -> each LIBRARY_MATERIAL_STATUS row appears ~2000.0 times after this join.",
    "- \"name of the department\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins multiply TIP_SUBJECT_OFFERED rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct name of the department, use COUNT(DISTINCT TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"material status\" -> TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS: already repeats ~1.5 rows per value in its own table; the hinted joins multiply TIP_MATERIAL_STATUS rows ~x833.3, so a plain COUNT over the joined result counts duplicates. To count distinct material status, use COUNT(DISTINCT TIP_MATERIAL_STATUS.TIP_MATERIAL_STATUS).",
    "- \"TIP materials\" -> TIP_DETAIL.TIP_MATERIAL_KEY: already repeats ~1.4 rows per value in its own table; the hinted joins do not multiply TIP_DETAIL rows.",
    "- \"library materials\" -> LIBRARY_RESERVE_MATRL_DETAIL.LIBRARY_RESERVE_CATALOG_KEY: already repeats ~1.2 rows per value in its own table; the hinted joins do not multiply LIBRARY_RESERVE_MATRL_DETAIL rows."
  ],
  "dw_real_8": [
    "- Join CIS_COURSE_CATALOG.SUBJECT_ID = COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: CIS_COURSE_CATALOG.SUBJECT_ID repeats ~4.4 rows per value (10000 rows, 2269 values); COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID repeats ~2.0 rows per value (10000 rows, 4969 values).",
    "  -> each CIS_COURSE_CATALOG row appears ~2.0 times after this join.",
    "  -> each COURSE_CATALOG_SUBJECT_OFFERED row appears ~4.4 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.MEET_PLACE = FCLT_ROOMS.FCLT_ROOM_KEY: COURSE_CATALOG_SUBJECT_OFFERED.MEET_PLACE repeats ~18.1 rows per value (10000 rows, 553 values); FCLT_ROOMS.FCLT_ROOM_KEY is unique per row (10000 rows).",
    "  -> each FCLT_ROOMS row appears ~18.1 times after this join.",
    "- Join FCLT_BUILDING.FCLT_BUILDING_KEY = FCLT_ROOMS.FCLT_BUILDING_KEY: FCLT_BUILDING.FCLT_BUILDING_KEY is unique per row (242 rows); FCLT_ROOMS.FCLT_BUILDING_KEY repeats ~49.5 rows per value (10000 rows, 202 values).",
    "  -> each FCLT_BUILDING row appears ~49.5 times after this join.",
    "- \"building key\" -> FCLT_BUILDING.FCLT_BUILDING_KEY: unique per row in its own table; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct building key, use COUNT(DISTINCT FCLT_BUILDING.FCLT_BUILDING_KEY).",
    "- \"building name\" -> FCLT_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct building name, use COUNT(DISTINCT FCLT_BUILDING.BUILDING_NAME).",
    "- \"courses offered by the Center for International Studies\" -> CIS_COURSE_CATALOG.SUBJECT_ID: already repeats ~4.4 rows per value in its own table; the hinted joins multiply CIS_COURSE_CATALOG rows ~x2.0, so a plain COUNT over the joined result counts duplicates. To count distinct courses offered by the Center for International Studies, use COUNT(DISTINCT CIS_COURSE_CATALOG.SUBJECT_ID)."
  ],
  "dw_real_80": [
    "- Join FAC_BUILDING.(FAC_BUILDING_KEY,FAC_BUILDING_KEY,FAC_BUILDING_KEY,FAC_BUILDING_KEY) = FAC_BUILDING_ADDRESS.(BUILDING_KEY,BUILDING_KEY,BUILDING_KEY,BUILDING_KEY): FAC_BUILDING.(FAC_BUILDING_KEY,FAC_BUILDING_KEY,FAC_BUILDING_KEY,FAC_BUILDING_KEY) is unique per row (242 rows); FAC_BUILDING_ADDRESS.(BUILDING_KEY,BUILDING_KEY,BUILDING_KEY,BUILDING_KEY) repeats ~3.2 rows per value (785 rows, 242 values).",
    "  -> each FAC_BUILDING row appears ~3.2 times after this join.",
    "- Join BUILDINGS.BUILDING_KEY = FAC_BUILDING_ADDRESS.BUILDING_KEY: BUILDINGS.BUILDING_KEY is unique per row (211 rows); FAC_BUILDING_ADDRESS.BUILDING_KEY repeats ~3.2 rows per value (785 rows, 242 values).",
    "  -> each BUILDINGS row appears ~3.2 times after this join.",
    "- \"subdivision\" -> FAC_BUILDING.PARENT_BUILDING_NUMBER: already repeats ~26.9 rows per value in its own table; the hinted joins multiply FAC_BUILDING rows ~x3.2, so a plain COUNT over the joined result counts duplicates. To count distinct subdivision, use COUNT(DISTINCT FAC_BUILDING.PARENT_BUILDING_NUMBER).",
    "- \"building number\" -> FAC_BUILDING.BUILDING_NUMBER: unique per row in its own table; the hinted joins multiply FAC_BUILDING rows ~x3.2, so a plain COUNT over the joined result counts duplicates. To count distinct building number, use COUNT(DISTINCT FAC_BUILDING.BUILDING_NUMBER).",
    "- \"full name\" -> FAC_BUILDING.BUILDING_NAME_LONG: unique per row in its own table; the hinted joins multiply FAC_BUILDING rows ~x3.2, so a plain COUNT over the joined result counts duplicates. To count distinct full name, use COUNT(DISTINCT FAC_BUILDING.BUILDING_NAME_LONG).",
    "- \"street address\" -> BUILDINGS.BUILDING_STREET_ADDRESS: already repeats ~1.4 rows per value in its own table; the hinted joins multiply BUILDINGS rows ~x3.2, so a plain COUNT over the joined result counts duplicates. To count distinct street address, use COUNT(DISTINCT BUILDINGS.BUILDING_STREET_ADDRESS).",
    "- \"street address\" -> FAC_BUILDING_ADDRESS.ADDRESS_PURPOSE: already repeats ~60.4 rows per value in its own table; the hinted joins do not multiply FAC_BUILDING_ADDRESS rows.",
    "- \"building type\" -> FAC_BUILDING.BUILDING_TYPE: already repeats ~80.7 rows per value in its own table; the hinted joins multiply FAC_BUILDING rows ~x3.2, so a plain COUNT over the joined result counts duplicates. To count distinct building type, use COUNT(DISTINCT FAC_BUILDING.BUILDING_TYPE).",
    "- \"occupancy date\" -> FAC_BUILDING.DATE_OCCUPIED: already repeats ~1.8 rows per value in its own table; the hinted joins multiply FAC_BUILDING rows ~x3.2, so a plain COUNT over the joined result counts duplicates. To count distinct occupancy date, use COUNT(DISTINCT FAC_BUILDING.DATE_OCCUPIED).",
    "- \"ownership type\" -> FAC_BUILDING.OWNERSHIP_TYPE: already repeats ~121.0 rows per value in its own table; the hinted joins multiply FAC_BUILDING rows ~x3.2, so a plain COUNT over the joined result counts duplicates. To count distinct ownership type, use COUNT(DISTINCT FAC_BUILDING.OWNERSHIP_TYPE).",
    "- \"site location\" -> FAC_BUILDING.SITE: already repeats ~24.2 rows per value in its own table; the hinted joins multiply FAC_BUILDING rows ~x3.2, so a plain COUNT over the joined result counts duplicates. To count distinct site location, use COUNT(DISTINCT FAC_BUILDING.SITE)."
  ],
  "dw_real_81": [
    "- \"subdivision\" -> FAC_BUILDING.PARENT_BUILDING_NUMBER: already repeats ~26.9 rows per value in its own table; the hinted joins do not multiply FAC_BUILDING rows.",
    "- \"construction start year\" -> FAC_BUILDING.DATE_BUILT: already repeats ~2.2 rows per value in its own table; the hinted joins do not multiply FAC_BUILDING rows.",
    "- \"building number\" -> FAC_BUILDING.PARENT_BUILDING_NUMBER: already repeats ~26.9 rows per value in its own table; the hinted joins do not multiply FAC_BUILDING rows.",
    "- \"year of initial occupancy\" -> FAC_BUILDING.DATE_OCCUPIED: already repeats ~1.8 rows per value in its own table; the hinted joins do not multiply FAC_BUILDING rows."
  ],
  "dw_real_82": [
    "- Join FCLT_BUILDING_HIST.FCLT_BUILDING_KEY = FCLT_ROOMS.FCLT_BUILDING_KEY: FCLT_BUILDING_HIST.FCLT_BUILDING_KEY repeats ~39.2 rows per value (10000 rows, 255 values); FCLT_ROOMS.FCLT_BUILDING_KEY repeats ~49.5 rows per value (10000 rows, 202 values).",
    "  -> each FCLT_BUILDING_HIST row appears ~49.5 times after this join.",
    "  -> each FCLT_ROOMS row appears ~39.2 times after this join.",
    "- Join FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY = FCLT_ROOMS.FCLT_ORGANIZATION_KEY: FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY repeats ~1.1 rows per value (180 rows, 168 values); FCLT_ROOMS.FCLT_ORGANIZATION_KEY repeats ~79.4 rows per value (10000 rows, 126 values).",
    "  -> each FCLT_ORGANIZATION row appears ~79.4 times after this join.",
    "  -> each FCLT_ROOMS row appears ~1.1 times after this join.",
    "- \"buildings at MIT\" -> FCLT_BUILDING_HIST.SITE: already repeats ~1111.1 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_HIST rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct buildings at MIT, use COUNT(DISTINCT FCLT_BUILDING_HIST.SITE).",
    "- \"ownership type\" -> FCLT_BUILDING_HIST.OWNERSHIP_TYPE: already repeats ~5000.0 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_HIST rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct ownership type, use COUNT(DISTINCT FCLT_BUILDING_HIST.OWNERSHIP_TYPE).",
    "- \"usage type\" -> FCLT_BUILDING_HIST.BUILDING_USE: already repeats ~1666.7 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_HIST rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct usage type, use COUNT(DISTINCT FCLT_BUILDING_HIST.BUILDING_USE).",
    "- \"buildings\" -> FCLT_BUILDING_HIST.PARENT_BUILDING_NUMBER: already repeats ~1000.0 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_HIST rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct buildings, use COUNT(DISTINCT FCLT_BUILDING_HIST.PARENT_BUILDING_NUMBER).",
    "- \"buildings\" -> FCLT_BUILDING_HIST.FCLT_BUILDING_KEY: already repeats ~39.2 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_HIST rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct buildings, use COUNT(DISTINCT FCLT_BUILDING_HIST.FCLT_BUILDING_KEY).",
    "- \"gross square footage\" -> FCLT_BUILDING_HIST.EXT_GROSS_AREA is a numeric measure of FCLT_BUILDING_HIST; the hinted joins multiply FCLT_BUILDING_HIST rows ~x49.5, so SUM/AVG over the joined result would count each FCLT_BUILDING_HIST row ~49.5 times. Aggregate it at FCLT_BUILDING_HIST's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"rooms\" -> FCLT_ROOMS.FCLT_ROOM_KEY: unique per row in its own table; the hinted joins multiply FCLT_ROOMS rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct rooms, use COUNT(DISTINCT FCLT_ROOMS.FCLT_ROOM_KEY).",
    "- \"organizations\" -> FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct organizations, use COUNT(DISTINCT FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY)."
  ],
  "dw_real_83": [
    "- Join FCLT_BUILDING.(FCLT_BUILDING_KEY,FCLT_BUILDING_KEY) = FCLT_ROOMS.(FCLT_BUILDING_KEY,FCLT_BUILDING_KEY): FCLT_BUILDING.(FCLT_BUILDING_KEY,FCLT_BUILDING_KEY) is unique per row (242 rows); FCLT_ROOMS.(FCLT_BUILDING_KEY,FCLT_BUILDING_KEY) repeats ~49.5 rows per value (10000 rows, 202 values).",
    "  -> each FCLT_BUILDING row appears ~49.5 times after this join.",
    "- Join FCLT_ORGANIZATION.(FCLT_ORGANIZATION_KEY,FCLT_ORGANIZATION_KEY) = FCLT_ROOMS.(FCLT_ORGANIZATION_KEY,FCLT_ORGANIZATION_KEY): FCLT_ORGANIZATION.(FCLT_ORGANIZATION_KEY,FCLT_ORGANIZATION_KEY) repeats ~1.1 rows per value (180 rows, 168 values); FCLT_ROOMS.(FCLT_ORGANIZATION_KEY,FCLT_ORGANIZATION_KEY) repeats ~79.4 rows per value (10000 rows, 126 values).",
    "  -> each FCLT_ORGANIZATION row appears ~79.4 times after this join.",
    "  -> each FCLT_ROOMS row appears ~1.1 times after this join.",
    "- \"building use\" -> FCLT_BUILDING.BUILDING_USE: already repeats ~40.3 rows per value in its own table; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct building use, use COUNT(DISTINCT FCLT_BUILDING.BUILDING_USE).",
    "- \"buildings\" -> FCLT_BUILDING.PARENT_BUILDING_NUMBER: already repeats ~26.9 rows per value in its own table; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct buildings, use COUNT(DISTINCT FCLT_BUILDING.PARENT_BUILDING_NUMBER).",
    "- \"buildings\" -> FCLT_BUILDING.FCLT_BUILDING_KEY: unique per row in its own table; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct buildings, use COUNT(DISTINCT FCLT_BUILDING.FCLT_BUILDING_KEY).",
    "- \"gross square footage\" -> FCLT_BUILDING.EXT_GROSS_AREA is a numeric measure of FCLT_BUILDING; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so SUM/AVG over the joined result would count each FCLT_BUILDING row ~49.5 times. Aggregate it at FCLT_BUILDING's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"organizations\" -> FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct organizations, use COUNT(DISTINCT FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY)."
  ],
  "dw_real_84": [
    "- Join FCLT_ORGANIZATION.(FCLT_ORGANIZATION_KEY,FCLT_ORGANIZATION_KEY,FCLT_ORGANIZATION_KEY) = FCLT_ROOMS.(FCLT_ORGANIZATION_KEY,FCLT_ORGANIZATION_KEY,FCLT_ORGANIZATION_KEY): FCLT_ORGANIZATION.(FCLT_ORGANIZATION_KEY,FCLT_ORGANIZATION_KEY,FCLT_ORGANIZATION_KEY) repeats ~1.1 rows per value (180 rows, 168 values); FCLT_ROOMS.(FCLT_ORGANIZATION_KEY,FCLT_ORGANIZATION_KEY,FCLT_ORGANIZATION_KEY) repeats ~79.4 rows per value (10000 rows, 126 values).",
    "  -> each FCLT_ORGANIZATION row appears ~79.4 times after this join.",
    "  -> each FCLT_ROOMS row appears ~1.1 times after this join.",
    "- Join FCLT_ORGANIZATION.DLC_KEY = MASTER_DEPT_HIERARCHY.DLC_KEY: FCLT_ORGANIZATION.DLC_KEY repeats ~1.3 rows per value (180 rows, 136 values); MASTER_DEPT_HIERARCHY.DLC_KEY is unique per row (310 rows).",
    "  -> each MASTER_DEPT_HIERARCHY row appears ~1.3 times after this join.",
    "- \"Cambridge-MIT Institute\" -> FCLT_ORGANIZATION.ORGANIZATION_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct Cambridge-MIT Institute, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_NAME).",
    "- \"ID\" -> FCLT_ORGANIZATION.ORGANIZATION_ID: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct ID, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_ID).",
    "- \"number\" -> FCLT_ORGANIZATION.ORGANIZATION_NUMBER: already repeats ~1.2 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct number, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_NUMBER).",
    "- \"level\" -> FCLT_ORGANIZATION.ORGANIZATION_LEVEL: already repeats ~30.0 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct level, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_LEVEL).",
    "- \"name\" -> FCLT_ORGANIZATION.ORGANIZATION_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct name, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_NAME).",
    "- \"assignable\" -> FCLT_ORGANIZATION.ASSIGNABLE: already repeats ~90.0 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x79.4, so a plain COUNT over the joined result counts duplicates. To count distinct assignable, use COUNT(DISTINCT FCLT_ORGANIZATION.ASSIGNABLE).",
    "- \"area\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so SUM/AVG over the joined result would count each FCLT_ROOMS row ~1.1 times. Aggregate it at FCLT_ROOMS's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"rooms\" -> FCLT_ROOMS.ROOM: already repeats ~2.8 rows per value in its own table; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct rooms, use COUNT(DISTINCT FCLT_ROOMS.ROOM).",
    "- \"room area\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins multiply FCLT_ROOMS rows ~x1.1, so SUM/AVG over the joined result would count each FCLT_ROOMS row ~1.1 times. Aggregate it at FCLT_ROOMS's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_real_85": [
    "- Join FCLT_MAJOR_USE.FCLT_MAJOR_USE_KEY = FCLT_ROOMS.FCLT_MAJOR_USE_KEY: FCLT_MAJOR_USE.FCLT_MAJOR_USE_KEY is unique per row (14 rows); FCLT_ROOMS.FCLT_MAJOR_USE_KEY repeats ~769.2 rows per value (10000 rows, 13 values).",
    "  -> each FCLT_MAJOR_USE row appears ~769.2 times after this join.",
    "- \"rooms\" -> FCLT_ROOMS.ROOM: already repeats ~2.8 rows per value in its own table; the hinted joins do not multiply FCLT_ROOMS rows.",
    "- \"assignable\" -> FCLT_MAJOR_USE.ASSIGNABLE: already repeats ~7.0 rows per value in its own table; the hinted joins multiply FCLT_MAJOR_USE rows ~x769.2, so a plain COUNT over the joined result counts duplicates. To count distinct assignable, use COUNT(DISTINCT FCLT_MAJOR_USE.ASSIGNABLE).",
    "- \"major use description\" -> FCLT_MAJOR_USE.MAJOR_USE: unique per row in its own table; the hinted joins multiply FCLT_MAJOR_USE rows ~x769.2, so a plain COUNT over the joined result counts duplicates. To count distinct major use description, use COUNT(DISTINCT FCLT_MAJOR_USE.MAJOR_USE).",
    "- \"major use description\" -> FCLT_ROOMS.MAJOR_USE_DESC: already repeats ~769.2 rows per value in its own table; the hinted joins do not multiply FCLT_ROOMS rows.",
    "- \"major use description\" -> FCLT_MAJOR_USE.DESCRIPTION: unique per row in its own table; the hinted joins multiply FCLT_MAJOR_USE rows ~x769.2, so a plain COUNT over the joined result counts duplicates. To count distinct major use description, use COUNT(DISTINCT FCLT_MAJOR_USE.DESCRIPTION).",
    "- \"area\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins do not multiply FCLT_ROOMS rows."
  ],
  "dw_real_86": [
    "- Join FAC_ROOMS.ORGANIZATION_KEY = FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY: FAC_ROOMS.ORGANIZATION_KEY repeats ~1111.1 rows per value (10000 rows, 9 values); FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY repeats ~1.1 rows per value (180 rows, 168 values).",
    "  -> each FAC_ROOMS row appears ~1.1 times after this join.",
    "  -> each FCLT_ORGANIZATION row appears ~1111.1 times after this join.",
    "- Join BUILDINGS.BUILDING_KEY = FAC_ROOMS.BUILDING_KEY: BUILDINGS.BUILDING_KEY is unique per row (211 rows); FAC_ROOMS.BUILDING_KEY repeats ~53.2 rows per value (10000 rows, 188 values).",
    "  -> each BUILDINGS row appears ~53.2 times after this join.",
    "- \"floor key\" -> FAC_FLOOR.FLOOR_KEY: unique per row in its own table; the hinted joins do not multiply FAC_FLOOR rows.",
    "- \"department name\" -> FCLT_ORGANIZATION.ORGANIZATION_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x1111.1, so a plain COUNT over the joined result counts duplicates. To count distinct department name, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_NAME).",
    "- \"Stata building\" -> BUILDINGS.BUILDING_NAME: unique per row in its own table; the hinted joins multiply BUILDINGS rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct Stata building, use COUNT(DISTINCT BUILDINGS.BUILDING_NAME).",
    "- \"rooms\" -> FAC_ROOMS.FAC_ROOM_KEY: unique per row in its own table; the hinted joins multiply FAC_ROOMS rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct rooms, use COUNT(DISTINCT FAC_ROOMS.FAC_ROOM_KEY).",
    "- \"area\" -> FAC_ROOMS.AREA is a numeric measure of FAC_ROOMS; the hinted joins multiply FAC_ROOMS rows ~x1.1, so SUM/AVG over the joined result would count each FAC_ROOMS row ~1.1 times. Aggregate it at FAC_ROOMS's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_real_87": [
    "- Join FAC_ORGANIZATION.ORGANIZATION_KEY = FAC_ROOMS.ORGANIZATION_KEY: FAC_ORGANIZATION.ORGANIZATION_KEY is unique per row (169 rows); FAC_ROOMS.ORGANIZATION_KEY repeats ~1111.1 rows per value (10000 rows, 9 values).",
    "  -> each FAC_ORGANIZATION row appears ~1111.1 times after this join.",
    "- \"building key\" -> FAC_ROOMS.BUILDING_KEY: already repeats ~53.2 rows per value in its own table; the hinted joins do not multiply FAC_ROOMS rows.",
    "- \"floor key\" -> FAC_ROOMS.FLOOR_KEY: already repeats ~12.1 rows per value in its own table; the hinted joins do not multiply FAC_ROOMS rows.",
    "- \"department\" -> FAC_ORGANIZATION.ORGANIZATION_NAME: unique per row in its own table; the hinted joins multiply FAC_ORGANIZATION rows ~x1111.1, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT FAC_ORGANIZATION.ORGANIZATION_NAME).",
    "- \"rooms\" -> FAC_ROOMS.FAC_ROOM_KEY: unique per row in its own table; the hinted joins do not multiply FAC_ROOMS rows.",
    "- \"area\" -> FAC_ROOMS.AREA is a numeric measure of FAC_ROOMS; the hinted joins do not multiply FAC_ROOMS rows.",
    "- \"building name\" -> FAC_BUILDING.BUILDING_NAME_LONG: unique per row in its own table; the hinted joins do not multiply FAC_BUILDING rows.",
    "- \"access level\" -> FAC_BUILDING.ACCESS_LEVEL_NAME: already repeats ~80.7 rows per value in its own table; the hinted joins do not multiply FAC_BUILDING rows.",
    "- \"zip code\" -> FAC_BUILDING_ADDRESS.POSTAL_CODE: already repeats ~56.1 rows per value in its own table; the hinted joins do not multiply FAC_BUILDING_ADDRESS rows.",
    "- \"city\" -> FAC_BUILDING_ADDRESS.CITY: already repeats ~71.4 rows per value in its own table; the hinted joins do not multiply FAC_BUILDING_ADDRESS rows."
  ],
  "dw_real_88": [
    "- Join LIBRARY_RESERVE_MATRL_DETAIL.(LIBRARY_SUBJECT_OFFERED_KEY,LIBRARY_SUBJECT_OFFERED_KEY) = LIBRARY_SUBJECT_OFFERED.(LIBRARY_SUBJECT_OFFERED_KEY,LIBRARY_SUBJECT_OFFERED_KEY): LIBRARY_RESERVE_MATRL_DETAIL.(LIBRARY_SUBJECT_OFFERED_KEY,LIBRARY_SUBJECT_OFFERED_KEY) repeats ~6.5 rows per value (10000 rows, 1550 values); LIBRARY_SUBJECT_OFFERED.(LIBRARY_SUBJECT_OFFERED_KEY,LIBRARY_SUBJECT_OFFERED_KEY) is unique per row (10000 rows).",
    "  -> each LIBRARY_SUBJECT_OFFERED row appears ~6.5 times after this join.",
    "- Join LIBRARY_RESERVE_CATALOG.(LIBRARY_RESERVE_CATALOG_KEY,LIBRARY_RESERVE_CATALOG_KEY) = LIBRARY_RESERVE_MATRL_DETAIL.(LIBRARY_RESERVE_CATALOG_KEY,LIBRARY_RESERVE_CATALOG_KEY): LIBRARY_RESERVE_CATALOG.(LIBRARY_RESERVE_CATALOG_KEY,LIBRARY_RESERVE_CATALOG_KEY) is unique per row (10000 rows); LIBRARY_RESERVE_MATRL_DETAIL.(LIBRARY_RESERVE_CATALOG_KEY,LIBRARY_RESERVE_CATALOG_KEY) repeats ~1.2 rows per value (10000 rows, 8268 values).",
    "  -> each LIBRARY_RESERVE_CATALOG row appears ~1.2 times after this join.",
    "- Join ACADEMIC_TERM_PARAMETER.TERM_CODE = LIBRARY_SUBJECT_OFFERED.TERM_CODE: ACADEMIC_TERM_PARAMETER.TERM_CODE is unique per row (3 rows); LIBRARY_SUBJECT_OFFERED.TERM_CODE repeats ~192.3 rows per value (10000 rows, 52 values).",
    "  -> each ACADEMIC_TERM_PARAMETER row appears ~192.3 times after this join.",
    "- \"department\" -> LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~625.0 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so a plain COUNT over the joined result counts duplicates. To count distinct department, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"school\" -> LIBRARY_SUBJECT_OFFERED.OFFER_SCHOOL_NAME: already repeats ~2000.0 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so a plain COUNT over the joined result counts duplicates. To count distinct school, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.OFFER_SCHOOL_NAME).",
    "- \"course number\" -> LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER: already repeats ~625.0 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so a plain COUNT over the joined result counts duplicates. To count distinct course number, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.COURSE_NUMBER).",
    "- \"subject title\" -> LIBRARY_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~5.6 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so a plain COUNT over the joined result counts duplicates. To count distinct subject title, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.SUBJECT_TITLE).",
    "- \"enrolled students\" -> LIBRARY_SUBJECT_OFFERED.NUM_ENROLLED_STUDENTS is a numeric measure of LIBRARY_SUBJECT_OFFERED; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so SUM/AVG over the joined result would count each LIBRARY_SUBJECT_OFFERED row ~6.5 times. Aggregate it at LIBRARY_SUBJECT_OFFERED's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"term code\" -> LIBRARY_SUBJECT_OFFERED.TERM_CODE: already repeats ~192.3 rows per value in its own table; the hinted joins multiply LIBRARY_SUBJECT_OFFERED rows ~x6.5, so a plain COUNT over the joined result counts duplicates. To count distinct term code, use COUNT(DISTINCT LIBRARY_SUBJECT_OFFERED.TERM_CODE).",
    "- \"catalog ISBNs\" -> LIBRARY_RESERVE_CATALOG.CATALOG_ISBN: already repeats ~2.6 rows per value in its own table; the hinted joins multiply LIBRARY_RESERVE_CATALOG rows ~x1.2, so a plain COUNT over the joined result counts duplicates. To count distinct catalog ISBNs, use COUNT(DISTINCT LIBRARY_RESERVE_CATALOG.CATALOG_ISBN).",
    "- \"current term\" -> ACADEMIC_TERM_PARAMETER.IS_CURRENT_TERM: already repeats ~1.5 rows per value in its own table; the hinted joins multiply ACADEMIC_TERM_PARAMETER rows ~x192.3, so a plain COUNT over the joined result counts duplicates. To count distinct current term, use COUNT(DISTINCT ACADEMIC_TERM_PARAMETER.IS_CURRENT_TERM)."
  ],
  "dw_real_89": [
    "- Join MOIRA_LIST_DETAIL.MOIRA_LIST_OWNER_KEY = MOIRA_LIST_OWNER.MOIRA_LIST_OWNER_KEY: MOIRA_LIST_DETAIL.MOIRA_LIST_OWNER_KEY repeats ~2.5 rows per value (10000 rows, 3966 values); MOIRA_LIST_OWNER.MOIRA_LIST_OWNER_KEY repeats ~1.1 rows per value (10000 rows, 9042 values).",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "  -> each MOIRA_LIST_OWNER row appears ~2.5 times after this join.",
    "- Join MOIRA_LIST.MOIRA_LIST_KEY = MOIRA_LIST_DETAIL.MOIRA_LIST_KEY: MOIRA_LIST.MOIRA_LIST_KEY repeats ~1.1 rows per value (10000 rows, 8815 values); MOIRA_LIST_DETAIL.MOIRA_LIST_KEY repeats ~1.6 rows per value (10000 rows, 6395 values).",
    "  -> each MOIRA_LIST row appears ~1.6 times after this join.",
    "  -> each MOIRA_LIST_DETAIL row appears ~1.1 times after this join.",
    "- Join EMPLOYEE_DIRECTORY.MIT_ID = MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER_MIT_ID: EMPLOYEE_DIRECTORY.MIT_ID is unique per row (10000 rows); MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER_MIT_ID repeats ~2.5 rows per value (10000 rows, 3978 values).",
    "  -> each EMPLOYEE_DIRECTORY row appears ~2.5 times after this join.",
    "- \"departments with names starting with 'Computer Science'\" -> EMPLOYEE_DIRECTORY.DEPARTMENT_NAME: already repeats ~31.1 rows per value in its own table; the hinted joins multiply EMPLOYEE_DIRECTORY rows ~x2.5, so a plain COUNT over the joined result counts duplicates. To count distinct departments with names starting with 'Computer Science', use COUNT(DISTINCT EMPLOYEE_DIRECTORY.DEPARTMENT_NAME).",
    "- \"ownership type\" -> MOIRA_LIST_OWNER.OWNER_TYPE: already repeats ~3333.3 rows per value in its own table; the hinted joins multiply MOIRA_LIST_OWNER rows ~x2.5, so a plain COUNT over the joined result counts duplicates. To count distinct ownership type, use COUNT(DISTINCT MOIRA_LIST_OWNER.OWNER_TYPE).",
    "- \"list name\" -> MOIRA_LIST.MOIRA_LIST_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply MOIRA_LIST rows ~x1.6, so a plain COUNT over the joined result counts duplicates. To count distinct list name, use COUNT(DISTINCT MOIRA_LIST.MOIRA_LIST_NAME).",
    "- \"owners\" -> MOIRA_LIST_OWNER.OWNER: already repeats ~1.2 rows per value in its own table; the hinted joins multiply MOIRA_LIST_OWNER rows ~x2.5, so a plain COUNT over the joined result counts duplicates. To count distinct owners, use COUNT(DISTINCT MOIRA_LIST_OWNER.OWNER).",
    "- \"subscribers\" -> MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER_MIT_ID: already repeats ~2.5 rows per value in its own table; the hinted joins multiply MOIRA_LIST_DETAIL rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct subscribers, use COUNT(DISTINCT MOIRA_LIST_DETAIL.MOIRA_LIST_MEMBER_MIT_ID)."
  ],
  "dw_real_9": [
    "- Join FCLT_ROOMS.(BUILDING_ROOM,BUILDING_ROOM) = MIT_STUDENT_DIRECTORY.(OFFICE_LOCATION,OFFICE_LOCATION): FCLT_ROOMS.(BUILDING_ROOM,BUILDING_ROOM) is unique per row (10000 rows); MIT_STUDENT_DIRECTORY.(OFFICE_LOCATION,OFFICE_LOCATION) repeats ~35.2 rows per value (10000 rows, 284 values).",
    "  -> each FCLT_ROOMS row appears ~35.2 times after this join.",
    "- \"name of the building\" -> FCLT_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins do not multiply FCLT_BUILDING rows.",
    "- \"students\" -> MIT_STUDENT_DIRECTORY.FULL_NAME: already repeats ~1.2 rows per value in its own table; the hinted joins do not multiply MIT_STUDENT_DIRECTORY rows."
  ],
  "dw_real_90": [
    "- Join IAP_SUBJECT_CATEGORY.(IAP_SUBJECT_CATEGORY_KEY,IAP_SUBJECT_CATEGORY_KEY) = IAP_SUBJECT_DETAIL.(IAP_SUBJECT_CATEGORY_KEY,IAP_SUBJECT_CATEGORY_KEY): IAP_SUBJECT_CATEGORY.(IAP_SUBJECT_CATEGORY_KEY,IAP_SUBJECT_CATEGORY_KEY) is unique per row (49 rows); IAP_SUBJECT_DETAIL.(IAP_SUBJECT_CATEGORY_KEY,IAP_SUBJECT_CATEGORY_KEY) repeats ~10.1 rows per value (465 rows, 46 values).",
    "  -> each IAP_SUBJECT_CATEGORY row appears ~10.1 times after this join.",
    "- Join IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY = IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY: IAP_SUBJECT_DETAIL.IAP_SUBJECT_SESSION_KEY repeats ~3.3 rows per value (465 rows, 142 values); IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY repeats ~2.8 rows per value (1199 rows, 421 values).",
    "  -> each IAP_SUBJECT_DETAIL row appears ~2.8 times after this join.",
    "  -> each IAP_SUBJECT_SESSION row appears ~3.3 times after this join.",
    "- Join IAP_SUBJECT_DETAIL.IAP_SUBJECT_SPONSOR_KEY = IAP_SUBJECT_SPONSOR.IAP_SUBJECT_SPONSOR_KEY: IAP_SUBJECT_DETAIL.IAP_SUBJECT_SPONSOR_KEY repeats ~6.8 rows per value (465 rows, 68 values); IAP_SUBJECT_SPONSOR.IAP_SUBJECT_SPONSOR_KEY is unique per row (68 rows).",
    "  -> each IAP_SUBJECT_SPONSOR row appears ~6.8 times after this join.",
    "- \"name\" -> IAP_SUBJECT_CATEGORY.IAP_CATEGORY_NAME: unique per row in its own table; the hinted joins multiply IAP_SUBJECT_CATEGORY rows ~x10.1, so a plain COUNT over the joined result counts duplicates. To count distinct name, use COUNT(DISTINCT IAP_SUBJECT_CATEGORY.IAP_CATEGORY_NAME).",
    "- \"sessions\" -> IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY: already repeats ~2.8 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_SESSION rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct sessions, use COUNT(DISTINCT IAP_SUBJECT_SESSION.IAP_SUBJECT_SESSION_KEY).",
    "- \"attendees\" -> IAP_SUBJECT_DETAIL.MAX_ENROLLMENT is a numeric measure of IAP_SUBJECT_DETAIL; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x2.8, so SUM/AVG over the joined result would count each IAP_SUBJECT_DETAIL row ~2.8 times. Aggregate it at IAP_SUBJECT_DETAIL's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"active period\" -> IAP_SUBJECT_DETAIL.TERM_CODE: already repeats ~465.0 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_DETAIL rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct active period, use COUNT(DISTINCT IAP_SUBJECT_DETAIL.TERM_CODE).",
    "- \"sponsor name\" -> IAP_SUBJECT_SPONSOR.SPONSOR_NAME: unique per row in its own table; the hinted joins multiply IAP_SUBJECT_SPONSOR rows ~x6.8, so a plain COUNT over the joined result counts duplicates. To count distinct sponsor name, use COUNT(DISTINCT IAP_SUBJECT_SPONSOR.SPONSOR_NAME).",
    "- \"session start time\" -> IAP_SUBJECT_SESSION.SESSION_START_TIME: already repeats ~38.7 rows per value in its own table; the hinted joins multiply IAP_SUBJECT_SESSION rows ~x3.3, so a plain COUNT over the joined result counts duplicates. To count distinct session start time, use COUNT(DISTINCT IAP_SUBJECT_SESSION.SESSION_START_TIME)."
  ],
  "dw_real_91": [
    "- Join FAC_BUILDING_ADDRESS.(BUILDING_KEY,BUILDING_KEY) = FCLT_BUILDING_HIST.(FCLT_BUILDING_KEY,FCLT_BUILDING_KEY): FAC_BUILDING_ADDRESS.(BUILDING_KEY,BUILDING_KEY) repeats ~3.2 rows per value (785 rows, 242 values); FCLT_BUILDING_HIST.(FCLT_BUILDING_KEY,FCLT_BUILDING_KEY) repeats ~39.2 rows per value (10000 rows, 255 values).",
    "  -> each FAC_BUILDING_ADDRESS row appears ~39.2 times after this join.",
    "  -> each FCLT_BUILDING_HIST row appears ~3.2 times after this join.",
    "- Join BUILDINGS.(BUILDING_KEY,BUILDING_KEY) = FAC_BUILDING_ADDRESS.(BUILDING_KEY,BUILDING_KEY): BUILDINGS.(BUILDING_KEY,BUILDING_KEY) is unique per row (211 rows); FAC_BUILDING_ADDRESS.(BUILDING_KEY,BUILDING_KEY) repeats ~3.2 rows per value (785 rows, 242 values).",
    "  -> each BUILDINGS row appears ~3.2 times after this join.",
    "- Join BUILDINGS.(BUILDING_KEY,BUILDING_KEY) = FAC_ROOMS.(BUILDING_KEY,BUILDING_KEY): BUILDINGS.(BUILDING_KEY,BUILDING_KEY) is unique per row (211 rows); FAC_ROOMS.(BUILDING_KEY,BUILDING_KEY) repeats ~53.2 rows per value (10000 rows, 188 values).",
    "  -> each BUILDINGS row appears ~53.2 times after this join.",
    "- Join EMPLOYEE_DIRECTORY.(OFFICE_LOCATION,OFFICE_LOCATION) = FAC_ROOMS.(FAC_ROOM_KEY,FAC_ROOM_KEY): EMPLOYEE_DIRECTORY.(OFFICE_LOCATION,OFFICE_LOCATION) repeats ~1.9 rows per value (10000 rows, 5397 values); FAC_ROOMS.(FAC_ROOM_KEY,FAC_ROOM_KEY) is unique per row (10000 rows).",
    "  -> each FAC_ROOMS row appears ~1.9 times after this join.",
    "- \"name of the building type\" -> FCLT_BUILDING_HIST.BUILDING_TYPE: already repeats ~3333.3 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_HIST rows ~x3.2, so a plain COUNT over the joined result counts duplicates. To count distinct name of the building type, use COUNT(DISTINCT FCLT_BUILDING_HIST.BUILDING_TYPE).",
    "- \"buildings that are not subdivisions\" -> FCLT_BUILDING_HIST.FCLT_BUILDING_KEY: already repeats ~39.2 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_HIST rows ~x3.2, so a plain COUNT over the joined result counts duplicates. To count distinct buildings that are not subdivisions, use COUNT(DISTINCT FCLT_BUILDING_HIST.FCLT_BUILDING_KEY).",
    "- \"buildings that are not subdivisions\" -> FCLT_BUILDING_HIST.PARENT_BUILDING_NUMBER: already repeats ~1000.0 rows per value in its own table; the hinted joins multiply FCLT_BUILDING_HIST rows ~x3.2, so a plain COUNT over the joined result counts duplicates. To count distinct buildings that are not subdivisions, use COUNT(DISTINCT FCLT_BUILDING_HIST.PARENT_BUILDING_NUMBER).",
    "- \"employees\" -> EMPLOYEE_DIRECTORY.MIT_ID: unique per row in its own table; the hinted joins do not multiply EMPLOYEE_DIRECTORY rows.",
    "- \"street address\" -> BUILDINGS.BUILDING_STREET_ADDRESS: already repeats ~1.4 rows per value in its own table; the hinted joins multiply BUILDINGS rows ~x53.2, so a plain COUNT over the joined result counts duplicates. To count distinct street address, use COUNT(DISTINCT BUILDINGS.BUILDING_STREET_ADDRESS).",
    "- \"street address\" -> FAC_BUILDING_ADDRESS.ADDRESS_PURPOSE: already repeats ~60.4 rows per value in its own table; the hinted joins multiply FAC_BUILDING_ADDRESS rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct street address, use COUNT(DISTINCT FAC_BUILDING_ADDRESS.ADDRESS_PURPOSE).",
    "- \"city\" -> FAC_BUILDING_ADDRESS.CITY: already repeats ~71.4 rows per value in its own table; the hinted joins multiply FAC_BUILDING_ADDRESS rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct city, use COUNT(DISTINCT FAC_BUILDING_ADDRESS.CITY).",
    "- \"state\" -> FAC_BUILDING_ADDRESS.STATE: already repeats ~392.5 rows per value in its own table; the hinted joins multiply FAC_BUILDING_ADDRESS rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct state, use COUNT(DISTINCT FAC_BUILDING_ADDRESS.STATE).",
    "- \"postal code\" -> FAC_BUILDING_ADDRESS.POSTAL_CODE: already repeats ~56.1 rows per value in its own table; the hinted joins multiply FAC_BUILDING_ADDRESS rows ~x39.2, so a plain COUNT over the joined result counts duplicates. To count distinct postal code, use COUNT(DISTINCT FAC_BUILDING_ADDRESS.POSTAL_CODE).",
    "- \"gross square footage\" -> FCLT_BUILDING_HIST.EXT_GROSS_AREA is a numeric measure of FCLT_BUILDING_HIST; the hinted joins multiply FCLT_BUILDING_HIST rows ~x3.2, so SUM/AVG over the joined result would count each FCLT_BUILDING_HIST row ~3.2 times. Aggregate it at FCLT_BUILDING_HIST's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_real_92": [
    "- Join FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY = SPACE_UNIT.FCLT_ORGANIZATION_KEY: FCLT_ORGANIZATION.FCLT_ORGANIZATION_KEY repeats ~1.1 rows per value (180 rows, 168 values); SPACE_UNIT.FCLT_ORGANIZATION_KEY is unique per row (150 rows).",
    "  -> each SPACE_UNIT row appears ~1.1 times after this join.",
    "- \"Stata building\" -> SPACE_DETAIL.BUILDING_KEY: already repeats ~344.8 rows per value in its own table; the hinted joins do not multiply SPACE_DETAIL rows.",
    "- \"access level\" -> ZPM_ROOMS_LOAD.ACCESS_LEVEL: already repeats ~2500.0 rows per value in its own table; the hinted joins do not multiply ZPM_ROOMS_LOAD rows.",
    "- \"usage type\" -> SPACE_USAGE.SPACE_USAGE: already repeats ~1.8 rows per value in its own table; the hinted joins do not multiply SPACE_USAGE rows.",
    "- \"name of the space\" -> FCLT_ORGANIZATION.DLC_NAME: already repeats ~1.3 rows per value in its own table; the hinted joins do not multiply FCLT_ORGANIZATION rows.",
    "- \"spaces\" -> SPACE_UNIT.SPACE_UNIT: already repeats ~1.1 rows per value in its own table; the hinted joins multiply SPACE_UNIT rows ~x1.1, so a plain COUNT over the joined result counts duplicates. To count distinct spaces, use COUNT(DISTINCT SPACE_UNIT.SPACE_UNIT).",
    "- \"area\" -> SPACE_DETAIL.ROOM_SQUARE_FOOTAGE is a numeric measure of SPACE_DETAIL; the hinted joins do not multiply SPACE_DETAIL rows."
  ],
  "dw_real_93": [
    "- Join FCLT_ORGANIZATION.DLC_KEY = MASTER_DEPT_HIERARCHY.DLC_KEY: FCLT_ORGANIZATION.DLC_KEY repeats ~1.3 rows per value (180 rows, 136 values); MASTER_DEPT_HIERARCHY.DLC_KEY is unique per row (310 rows).",
    "  -> each MASTER_DEPT_HIERARCHY row appears ~1.3 times after this join.",
    "- Join FCLT_ORGANIZATION.DLC_KEY = HR_ORG_UNIT.DLC_KEY: FCLT_ORGANIZATION.DLC_KEY repeats ~1.3 rows per value (180 rows, 136 values); HR_ORG_UNIT.DLC_KEY repeats ~2.8 rows per value (641 rows, 231 values).",
    "  -> each FCLT_ORGANIZATION row appears ~2.8 times after this join.",
    "  -> each HR_ORG_UNIT row appears ~1.3 times after this join.",
    "- Join HR_FACULTY_ROSTER.HR_ORG_UNIT_TITLE = HR_ORG_UNIT.HR_ORG_UNIT_TITLE: HR_FACULTY_ROSTER.HR_ORG_UNIT_TITLE repeats ~22.7 rows per value (681 rows, 30 values); HR_ORG_UNIT.HR_ORG_UNIT_TITLE is unique per row (641 rows).",
    "  -> each HR_ORG_UNIT row appears ~22.7 times after this join.",
    "- \"break group\" -> FCLT_ORGANIZATION.ORGANIZATION_SORT: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct break group, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_SORT).",
    "- \"ID\" -> FCLT_ORGANIZATION.ORGANIZATION_ID: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct ID, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_ID).",
    "- \"name\" -> FCLT_ORGANIZATION.ORGANIZATION_NAME: already repeats ~1.1 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct name, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_NAME).",
    "- \"level\" -> FCLT_ORGANIZATION.ORGANIZATION_LEVEL: already repeats ~30.0 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct level, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_LEVEL).",
    "- \"number\" -> FCLT_ORGANIZATION.ORGANIZATION_NUMBER: already repeats ~1.2 rows per value in its own table; the hinted joins multiply FCLT_ORGANIZATION rows ~x2.8, so a plain COUNT over the joined result counts duplicates. To count distinct number, use COUNT(DISTINCT FCLT_ORGANIZATION.ORGANIZATION_NUMBER).",
    "- \"employer\" -> HR_FACULTY_ROSTER.MIT_ID: unique per row in its own table; the hinted joins do not multiply HR_FACULTY_ROSTER rows.",
    "- \"hierarchy type\" -> MASTER_DEPT_HIERARCHY.HIERARCHY_TYPE: already repeats ~310.0 rows per value in its own table; the hinted joins multiply MASTER_DEPT_HIERARCHY rows ~x1.3, so a plain COUNT over the joined result counts duplicates. To count distinct hierarchy type, use COUNT(DISTINCT MASTER_DEPT_HIERARCHY.HIERARCHY_TYPE)."
  ],
  "dw_real_94": [
    "- Join ACADEMIC_TERMS.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.EFFECTIVE_TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.EFFECTIVE_TERM_CODE repeats ~200.0 rows per value (10000 rows, 50 values).",
    "  -> each ACADEMIC_TERMS row appears ~200.0 times after this join.",
    "- Join FCLT_BUILDING.FCLT_BUILDING_KEY = FCLT_ROOMS.FCLT_BUILDING_KEY: FCLT_BUILDING.FCLT_BUILDING_KEY is unique per row (242 rows); FCLT_ROOMS.FCLT_BUILDING_KEY repeats ~49.5 rows per value (10000 rows, 202 values).",
    "  -> each FCLT_BUILDING row appears ~49.5 times after this join.",
    "- \"title\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"name of the building\" -> FCLT_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct name of the building, use COUNT(DISTINCT FCLT_BUILDING.BUILDING_NAME).",
    "- \"duration\" -> ACADEMIC_TERMS.TERM_END_DATE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x200.0, so a plain COUNT over the joined result counts duplicates. To count distinct duration, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_END_DATE).",
    "- \"duration\" -> ACADEMIC_TERMS.TERM_START_DATE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x200.0, so a plain COUNT over the joined result counts duplicates. To count distinct duration, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_START_DATE)."
  ],
  "dw_real_95": [
    "- Join FCLT_BUILDING.FCLT_BUILDING_KEY = FCLT_ROOMS.FCLT_BUILDING_KEY: FCLT_BUILDING.FCLT_BUILDING_KEY is unique per row (242 rows); FCLT_ROOMS.FCLT_BUILDING_KEY repeats ~49.5 rows per value (10000 rows, 202 values).",
    "  -> each FCLT_BUILDING row appears ~49.5 times after this join.",
    "- \"name\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"name of the building\" -> FCLT_BUILDING.BUILDING_NAME_LONG: unique per row in its own table; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct name of the building, use COUNT(DISTINCT FCLT_BUILDING.BUILDING_NAME_LONG).",
    "- \"start date\" -> ACADEMIC_TERMS.TERM_START_DATE: unique per row in its own table; the hinted joins do not multiply ACADEMIC_TERMS rows.",
    "- \"name of the course\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"access level\" -> FCLT_BUILDING.ACCESS_LEVEL_NAME: already repeats ~80.7 rows per value in its own table; the hinted joins multiply FCLT_BUILDING rows ~x49.5, so a plain COUNT over the joined result counts duplicates. To count distinct access level, use COUNT(DISTINCT FCLT_BUILDING.ACCESS_LEVEL_NAME).",
    "- \"area\" -> FCLT_ROOMS.AREA is a numeric measure of FCLT_ROOMS; the hinted joins do not multiply FCLT_ROOMS rows."
  ],
  "dw_real_96": [
    "- Join ACADEMIC_TERMS.TERM_CODE = COURSE_CATALOG_SUBJECT_OFFERED.EFFECTIVE_TERM_CODE: ACADEMIC_TERMS.TERM_CODE is unique per row (144 rows); COURSE_CATALOG_SUBJECT_OFFERED.EFFECTIVE_TERM_CODE repeats ~200.0 rows per value (10000 rows, 50 values).",
    "  -> each ACADEMIC_TERMS row appears ~200.0 times after this join.",
    "- Join COURSE_CATALOG_SUBJECT_OFFERED.MEET_PLACE = FCLT_ROOMS.FCLT_ROOM_KEY: COURSE_CATALOG_SUBJECT_OFFERED.MEET_PLACE repeats ~18.1 rows per value (10000 rows, 553 values); FCLT_ROOMS.FCLT_ROOM_KEY is unique per row (10000 rows).",
    "  -> each FCLT_ROOMS row appears ~18.1 times after this join.",
    "- \"name\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_TITLE: already repeats ~2.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"name of the building\" -> FCLT_BUILDING.BUILDING_NAME: unique per row in its own table; the hinted joins do not multiply FCLT_BUILDING rows.",
    "- \"courses\" -> COURSE_CATALOG_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"academic year\" -> COURSE_CATALOG_SUBJECT_OFFERED.ACADEMIC_YEAR: already repeats ~416.7 rows per value in its own table; the hinted joins do not multiply COURSE_CATALOG_SUBJECT_OFFERED rows.",
    "- \"course start date\" -> ACADEMIC_TERMS.TERM_START_DATE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS rows ~x200.0, so a plain COUNT over the joined result counts duplicates. To count distinct course start date, use COUNT(DISTINCT ACADEMIC_TERMS.TERM_START_DATE)."
  ],
  "dw_real_97": [
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = SUBJECT_SUMMARY.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); SUBJECT_SUMMARY.TERM_CODE repeats ~83.3 rows per value (10000 rows, 120 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~83.3 times after this join.",
    "- \"biology and chemistry department\" -> SUBJECT_SUMMARY.DEPARTMENT_NAME: already repeats ~181.8 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"current status\" -> ACADEMIC_TERMS_ALL.IS_CURRENT_TERM: already repeats ~150.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x83.3, so a plain COUNT over the joined result counts duplicates. To count distinct current status, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.IS_CURRENT_TERM).",
    "- \"cluster type\" -> SUBJECT_SUMMARY.CLUSTER_TYPE: already repeats ~2500.0 rows per value in its own table; the hinted joins do not multiply SUBJECT_SUMMARY rows.",
    "- \"duration\" -> ACADEMIC_TERMS_ALL.TERM_END_DATE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x83.3, so a plain COUNT over the joined result counts duplicates. To count distinct duration, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_END_DATE).",
    "- \"duration\" -> ACADEMIC_TERMS_ALL.TERM_START_DATE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x83.3, so a plain COUNT over the joined result counts duplicates. To count distinct duration, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_START_DATE).",
    "- \"units\" -> SUBJECT_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_SUMMARY; the hinted joins do not multiply SUBJECT_SUMMARY rows."
  ],
  "dw_real_98": [
    "- Join ACADEMIC_TERMS_ALL.TERM_CODE = SUBJECT_SUMMARY.TERM_CODE: ACADEMIC_TERMS_ALL.TERM_CODE is unique per row (300 rows); SUBJECT_SUMMARY.TERM_CODE repeats ~83.3 rows per value (10000 rows, 120 values).",
    "  -> each ACADEMIC_TERMS_ALL row appears ~83.3 times after this join.",
    "- Join SUBJECT_OFFERED.SUBJECT_ID = SUBJECT_SUMMARY.SUBJECT_ID: SUBJECT_OFFERED.SUBJECT_ID repeats ~4.8 rows per value (10000 rows, 2081 values); SUBJECT_SUMMARY.SUBJECT_ID repeats ~1.9 rows per value (10000 rows, 5251 values).",
    "  -> each SUBJECT_OFFERED row appears ~1.9 times after this join.",
    "  -> each SUBJECT_SUMMARY row appears ~4.8 times after this join.",
    "- \"biology and chemistry department\" -> SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~10000.0 rows per value in its own table; the hinted joins multiply SUBJECT_OFFERED rows ~x1.9, so a plain COUNT over the joined result counts duplicates. To count distinct biology and chemistry department, use COUNT(DISTINCT SUBJECT_OFFERED.OFFER_DEPT_NAME).",
    "- \"current status\" -> ACADEMIC_TERMS_ALL.IS_CURRENT_TERM: already repeats ~150.0 rows per value in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x83.3, so a plain COUNT over the joined result counts duplicates. To count distinct current status, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.IS_CURRENT_TERM).",
    "- \"duration\" -> ACADEMIC_TERMS_ALL.TERM_END_DATE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x83.3, so a plain COUNT over the joined result counts duplicates. To count distinct duration, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_END_DATE).",
    "- \"duration\" -> ACADEMIC_TERMS_ALL.TERM_START_DATE: unique per row in its own table; the hinted joins multiply ACADEMIC_TERMS_ALL rows ~x83.3, so a plain COUNT over the joined result counts duplicates. To count distinct duration, use COUNT(DISTINCT ACADEMIC_TERMS_ALL.TERM_START_DATE).",
    "- \"units\" -> SUBJECT_SUMMARY.TOTAL_UNITS is a numeric measure of SUBJECT_SUMMARY; the hinted joins multiply SUBJECT_SUMMARY rows ~x4.8, so SUM/AVG over the joined result would count each SUBJECT_SUMMARY row ~4.8 times. Aggregate it at SUBJECT_SUMMARY's own grain (aggregate before joining) unless the question wants it weighted."
  ],
  "dw_real_99": [
    "- Join TIP_DETAIL.TIP_MATERIAL_KEY = TIP_MATERIAL.TIP_MATERIAL_KEY: TIP_DETAIL.TIP_MATERIAL_KEY repeats ~1.4 rows per value (10000 rows, 7360 values); TIP_MATERIAL.TIP_MATERIAL_KEY is unique per row (10000 rows).",
    "  -> each TIP_MATERIAL row appears ~1.4 times after this join.",
    "- \"name of the department\" -> TIP_SUBJECT_OFFERED.OFFER_DEPT_NAME: already repeats ~169.5 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"master course code\" -> TIP_SUBJECT_OFFERED.MASTER_COURSE_NUMBER: already repeats ~172.4 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"subjects\" -> TIP_SUBJECT_OFFERED.SUBJECT_ID: already repeats ~2.0 rows per value in its own table; the hinted joins do not multiply TIP_SUBJECT_OFFERED rows.",
    "- \"new shelf price\" -> TIP_MATERIAL.NEW_SHELF_PRICE is a numeric measure of TIP_MATERIAL; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so SUM/AVG over the joined result would count each TIP_MATERIAL row ~1.4 times. Aggregate it at TIP_MATERIAL's own grain (aggregate before joining) unless the question wants it weighted.",
    "- \"TIP materials\" -> TIP_MATERIAL.ISBN: already repeats ~1.5 rows per value in its own table; the hinted joins multiply TIP_MATERIAL rows ~x1.4, so a plain COUNT over the joined result counts duplicates. To count distinct TIP materials, use COUNT(DISTINCT TIP_MATERIAL.ISBN)."
  ]
}